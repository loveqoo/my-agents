# 133 — 전 메뉴 모바일 점검 스윕 + 수정 (승인·빌딩 블록)

## 배경 / 왜
사용자(점검 3탄): "다른 메뉴도 이와 같은 뷰(132 기준 — 온전한 텍스트·화면 내·가로 넘침 없음)로 점검
및 수정." fast-worker가 11개 메뉴+2개 상태(유저 메모리 선택·세션 드로어)를 390×844로 순회 —
스크린샷 13장 + 정량(scrollWidth·뷰포트 밖 요소 수).

## 점검 결과
- **정상 11/13**: 개요·에이전트·프로바이더·컬렉션·세션(+드로어)·메모리(+유저 선택)·유저·배치·허용
  호스트 — 기존 모바일 카드 전환(스펙 095 DataTable)이 커버. 가로 오버플로 0.
- **실버그 1**: 승인 — 체크포인트 안내 행이 `display:flex`(wrap 없음)라 긴 `<code>`(체크포인트 id)가
  폭을 다 잡아먹고 좌우 텍스트가 **한 글자씩 세로로 짜부라져 판독 불가**. 정량 지표(scrollWidth·밖
  요소)로는 안 잡히고 육안으로만 확인 — flex 항목화된 텍스트 조각의 내부 압착이라 뷰포트 밖 요소가
  아니었음.
- **경미 1**: 빌딩 블록 — 탭이 좁아 셋째 탭 라벨 잘림(antd 더보기 폴백은 동작).

## 수정
1. 승인: flex 행 → **일반 인라인 텍스트 흐름**(code만 overflowWrap anywhere) — 어떤 폭에서도 자연 감김.
2. 빌딩 블록: 모바일(!md)에서 Tabs `size=small`+`tabBarGutter=14` — 두 탭 온전 표시, 나머지는 antd
   표준 더보기(…) 수용.

## 검증
- 390px 재촬영: 승인 안내문 정상 감김(판독 가능)·블록 탭 개선, scrollWidth 둘 다 390. tsc 0.
- 점검 산출물: /tmp/audit390-*.png 13장(스크래치 — 저장소 미보관).

## 비목표 (OUT)
- 세션 뷰의 "밖 요소 1"(빈 div, 시각 무영향 — 측정 노이즈 판정).
- 커스텀 Drawer의 Escape 닫기·모바일 마스크 접근성(점검 중 관찰 — 후속 후보로 백로그).
- 각 뷰 드로어 내부 폼의 모바일 최적화(항목별 후속).
