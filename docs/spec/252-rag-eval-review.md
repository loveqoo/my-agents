# 스펙 252 — RAG 컬렉션·평가 화면 점검·수정

## 배경 (사용자 지시, 2026-07-09)

두 화면 모두 복잡도 표면 수치는 정상(0.9x)이나, 245 진단이 명시한 사각 = **오버레이 속 복잡도**
(평가: 문제집 드로어에 실행 폼·추이·케이스 편집·비교 겹겹 / RAG: 문서·검색 드로어). 250에서 중복은
기정독 — 이번엔 **문법 전반**(3층 위계·식별자 강등·값=상태·탭 규칙·오버플로 상태 축) + 오버레이.

## 방법 (3중 — 자가검증 회피)
1. deep-reasoner: CollectionsView·EvalView(+EvalMatrix·EvalTrend·RetrievalTest류) 코드 정독 —
   문법 위반 후보 열거(file:line·확신도·처방).
2. 메인: 런타임 육안 — 화면 + **주요 오버레이 열린 상태**(문제집 드로어·문서/검색 패널)를
   데탑·모바일 캡처, 오버플로 실측(빈 상태 아닌 데이터 상태).
3. 교차 판정 → 명백 위반은 수정, 회색지대는 근거와 함께 보고.

## 완료 조건
- 두 화면과 그 오버레이 전부 육안 판정 완료, 확정 위반 수정 + e2e 무회귀.

## 결과 1차 (2026-07-09) — 소수술 5건 수정
- [실측] 문제집 드로어 judge 판정문 Tag nowrap 관통(over=1 양 뷰포트) → 줄바꿈 허용. 재실측 0.
- [카피] 평가 런 상태 raw enum(ok/error) 1급 노출 3곳(성적표 헤더·이력 행·격자) → 완료/오류/실행 중
  한글 매핑(컬렉션 상태 태그와 일관).
- [컨테이너] 성적표의 1행짜리 Descriptions(에이전트) → 라벨+값 한 줄(과잉 컨테이너 해체).
- [내부어] 검색 시험 top_k → "상위 몇 개", 회상 시험 limit → "최대 몇 개"(평이한 언어).
- [예방] 컬렉션 임베딩 모델 Tag maxWidth+ellipsis(긴 HF 경로 대비).

## 건전 판정 (deep-reasoner 반증 근거 기록)
- 탭 규칙·식별자 강등·값=상태·죽은 코드: 위반 없음(212·194·197 등 기존 스펙이 이미 처방).
- RetrievalTestPanel의 meta JSON 상시 노출: 검색 시험=정밀 디버그 통로(플그 세션 칩 선례) — 의도.
- 성적표 단일 Collapse(env·관측): 긴 JSON 점진공개 목적 — 유지.
- 드로어 구조 요약: 성적표·비교·문서·검색은 관심사 1~2개로 건전. **문제집 드로어만 3겹**(실행 폼+
  추이·최근런+케이스 CRUD) — 유일한 구조 이슈, 사용자 결정 대기.

## 결과 2차 (사용자 결정 — 문제집 드로어 탭 분리)
- 관심사 3겹(실행 폼+추이·최근런+문제 CRUD)을 드로어 내부 Tabs 두 개로: **문제(N)** / **실행 · 성적**.
  기본=문제(문제집의 정체성), 재열기 시 리셋. 실행 탭에 실행 기록 없음 안내 추가.
- e2e 3/3(기본 탭·전환·리셋)·tsc 0·육안 확인. 245가 지목한 "드로어 속 복잡도" 마지막 건 해소.
