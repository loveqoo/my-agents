# Spec Index  (docs/spec/)

> 각 줄: `NNN 제목 — 언제 열까(후크) [키워드] [archived]`. Context 단계에서 이 인덱스를 먼저 읽고,
> 후크로 불충분할 때만 해당 full 파일을 연다. `[archived]`는 완료된 로드맵 스펙으로 `archive/`에 있다.

- 001 시스템 전체 설계 — 제품의 큰 그림(비전·아키텍처·모듈 경계·도메인 모델·핵심 흐름)을 다시 확인할 때 [overview,architecture,vision]
- 002 페르소나 등록 + chat 노출 — 페르소나 REST 등록/조회와 `POST /agents/{id}/chat` SSE 스트리밍의 최초 배선을 볼 때 [persona,registry,chat,sse]
- 003 Admin Web UI 채팅 — React+TS+antd SPA로 에이전트 골라 브라우저 스트리밍 채팅하는 최초 테스트 환경을 볼 때 [admin,spa,chat,sse]
- 004 Web UI 프롬프트 확인 — 선택 에이전트의 페르소나와 모델 전송 메시지를 읽기전용으로 보는 인스펙트 기능을 볼 때 [inspect,prompt,readonly]
- 005 Ant Design X 채팅 리팩터 — 손제작 채팅 UI를 @ant-design/x(Bubble/Sender/ThoughtChain)로 교체한 UI 토대를 볼 때 [antdx,chat,ui,refactor]
- 006 Admin 콘솔 + Playground 이식 — claude.ai/design handoff 번들을 antd 6/antd-x로 재현한 5뷰 콘솔+Playground 이식을 볼 때 [admin,console,handoff,playground]
- 007 실 에이전트 서비스 — mock UI가 기대하는 도메인을 실제 백엔드(FastAPI/SQLAlchemy/LangGraph/Mem0/MLX) 런타임으로 구현한 핵심 토대를 볼 때 [backend,runtime,langgraph,mem0]
- 008 모델 레지스트리 — LLM/임베딩 모델 설정 CRUD와 런타임이 등록 모델로 ChatOpenAI 구성하는 방식을 볼 때 [model,registry,llm,runtime]
- 009 코드 에이전트 원격 실행 — source=code 에이전트를 원격 엔드포인트로 프록시하는 SSE 재전송 계약을 볼 때 [code-agent,remote,proxy,sse]
- 010 비밀값 암호화 저장 — 모델 api_key·코드 에이전트 token을 Fernet at-rest 암호화하고 런타임에서만 복호화하는 방식을 볼 때 [secret,fernet,encryption]
- 011 API 인증 — 모든 도메인 API에 단일 공유 Bearer 토큰(소유자=전체 접근)을 거는 인증을 볼 때 [auth,bearer,token]
- 012 대화 히스토리 윈도우+저장정책 — historyDepth(최근 N개 절단)와 persistHistory(DB 저장 여부) 에이전트별 설정을 볼 때 [history,window,persist,session]
- 013 모델 연결 테스트 — 등록 전/후 모델을 `{base_url}/models`로 도달성·인증·가용성 검증하는 연결 테스트를 볼 때 [model,test,connection,health]
- 014 페르소나 등록/수정 UI — 백엔드 CRUD는 있었으나 빠져 있던 페르소나 작성/편집 UI를 채운 작업을 볼 때 [persona,ui,authoring,form]
- 015 빌딩 블록 작성/편집 폼 — memory/embedding/permission 블록의 작성·편집 폼이 통째로 없던 빈틈을 메운 작업을 볼 때 [blocks,form,authoring,ui]
- 016 메모리 읽기전용 + kind별 모델 테스트 — 메모리=시스템 enum이라 UI 읽기전용으로 잠그고, embedding 모델은 임베딩으로 테스트하도록 정정한 작업을 볼 때 [memory,readonly,enum,embedding-test]
- 017 어드민 콘솔 모바일 반응형 — Tailscale 노출 후 모바일(~390px)에서 깨진 Sider/검색창/패딩/테이블/그리드 반응형 풀패스를 볼 때 [mobile,responsive,admin,layout]
- 018 메모리 유저 스코핑 — mem0를 에이전트 1버킷에서 user_id(유저 장기)·세션 단기로 나눈 스코핑 도입을 볼 때 [memory,mem0,user-scope,session]
- 019 mem0 pgvector 공유 백엔드 — 파일 기반 qdrant를 제거하고 mem0 벡터 스토어를 공유 Postgres/pgvector로 이전한 작업을 볼 때 [mem0,pgvector,postgres,vector-store]
- 020 mem0 다층 스코프 + 카탈로그 재정렬 — memory_type N종 카탈로그가 mem0 설계의도와 어긋남을 확인하고 user/session 다층 스코프로 재정렬한 작업을 볼 때 [mem0,scope,catalog,realign]
- 021 Playground userId UX — Playground 헤더 userId 입력 레이아웃 정리와 과거 userId 선택(세션 영속 출처)을 볼 때 [playground,userid,ux,session]
- 022 모바일 인스펙터 콘텐츠 오버플로 픽스 — 턴 인스펙터가 모바일 전체화면에서 긴 콘텐츠로 오버플로하는 줄바꿈/flexWrap 픽스와 전 페이지 회귀 검증을 볼 때 [mobile,inspector,overflow,regression]
- 023 에이전트 파운데이션 모델 정정 — 등록 에이전트들이 레지스트리에 없는 가상 모델명을 가리켜 화면-실제 불일치를 정정한 작업을 볼 때 [agent,model,realign,foundation]
- 024 Mock LLM 레지스트리 1급 모델 — 라이브 MLX 없이 결정적 실행을 위해 mock chat completions를 레지스트리 ModelConfig로 1급 등록한 작업을 볼 때 [mock,llm,registry,deterministic]
- 025 Playground Proxy 세션 한정 오버라이드 — 에이전트를 실제로 수정·저장하지 않고 세션 한정으로 모델/프롬프트/도구를 덮어 실행해 보는 오버라이드를 볼 때 [playground,proxy,override,session]
- 026 외부 에이전트 A2A 카드 등록 — 남이 만든 A2A 에이전트를 Agent Card(/.well-known/agent.json)로 내 생태계에 편입(등록·표시, 1차)하는 작업을 볼 때 [a2a,external,agent-card,registration]
- 027 어드민 same-origin /api 프록시 — Tailscale 접근 시 localhost 무의미·mixed-content·CORS 연쇄를 same-origin `/api` 프록시로 끊은 작업을 볼 때 [admin,proxy,same-origin,tailscale]
- 028 Playground 모델 배지 정직화 — source별로(ui=진짜 qwen / code=mock 원격) 모델 배지를 정직하게 표시하도록 고친 작업을 볼 때 [playground,badge,model,source]
- 029 에이전트 전용 메모리(agent_id) — agent_id 스코프 메모리의 에이전트 자가 기록 쓰기 채널과 관리자 큐레이션을 볼 때 [memory,agent-id,self-write,curation]
- 030 메모리 관리 화면 — 에이전트(agent_id)·유저(user_id) 메모리를 검색·수정·삭제하는 통합 큐레이션 콘솔을 볼 때 [memory,console,curation,crud]
- 031 멀티유저 인증 + 오픈형 권한 — 단일 토큰에서 fastapi-users+Casbin 차용으로 사용자 등록·로그인·RBAC(pluggable provider seam)로 확장한 작업을 볼 때 [auth,multi-user,fastapi-users,casbin,rbac]
- 032 Playground userId를 인증 주체에서 도출 — 수동 userId 입력을 제거하고 mem0 user_id 축을 로그인 인증 주체에서 고정 도출하는 작업을 볼 때 [playground,userid,auth-principal,mem0]
- 033 기능 로드맵 — 사용자 제기 12개 문제를 코드 전수조사 위에서 의존순(P0~P5)으로 배열한 로드맵·현황 한 줄 요약을 볼 때 [roadmap,planning,phases]
- 034 세션 페이징 — `GET /sessions` 전량 반환을 offset/limit+total 서버 페이징과 서버측 status 필터·배지 카운트로 바꾼 작업을 볼 때 [session,paging,offset-limit] [archived]
- 035 Provider 엔티티 — provider/base_url/api_key가 모델마다 인라인 중복되던 것을 1급 Provider 엔티티(1:N, 모델은 FK)로 승급한 토대를 볼 때 [provider,entity,model,fk] [archived]
- 036 RAG 인제스트 토대 — 죽은 vectorTables를 Collection/Document/Chunk로 재생하고 PDF 청킹·임베딩·pgvector 적재(쓰기 경로)를 볼 때 [rag,ingest,pgvector,chunk] [archived]
- 037 RAG Retrieval — 죽은 vectorTables 필드를 search_documents 도구로 부활시켜 pgvector HNSW cosine 검색을 에이전트에 배선한 읽기 경로를 볼 때 [rag,retrieval,search,tool] [archived]
- 038 배치 토대 + 세션 보존정리 — 시스템과 격리된 별도 배치 서비스(자체 스케줄러)와 첫 작업(오래된 세션 정리)을 볼 때 [batch,scheduler,session-cleanup,retention] [archived]
- 039 유저 메모리 통합·재적재 — 단조 증가하는 mem0 user_id 기억을 임계치 초과 시 LLM으로 통합·스냅샷 백업하며 교체하는 배치 작업을 볼 때 [memory,consolidation,batch,mem0] [archived]
- 040 메모리 백엔드 추상화 — memory.py의 mem0 호출을 Protocol/ABC 뒤로 들어내 그래프DB 등 백엔드 drop-in을 가능케 한 인터페이스 추출을 볼 때 [memory,abstraction,protocol,backend] [archived]
- 041 HIL 승인 게이팅 — 픽션이던 승인을 langgraph checkpoint/interrupt로 위험 도구를 진짜 멈추고 admin 승인/거부로 그래프를 재개/중단하는 게이트로 바꾼 작업을 볼 때 [hil,approval,langgraph,interrupt] [archived]
- 042 A2A 실호출 — 등록된 외부 에이전트를 JSON-RPC 2.0(message/stream) SSE로 실제 호출해 응답을 받는 A2A 런타임 전송 계층(026 2차)을 볼 때 [a2a,external,runtime,jsonrpc,ssrf] [archived]
- 043 컨텍스트 경량화 자산 인덱스 — Context 단계 읽기량이 compaction을 당기는 문제를 인덱스 층+아카이브로 줄인 워크플로 정비를 볼 때 [context,index,archive,workflow]
- 044 UI 피드백 정리 마스터 — UI 테스트 14건을 메뉴별 6배치(045~050)로 트리아지·의존순 배열한 지도를 볼 때 [feedback,cleanup,master,triage]
- 045 어드민 표시 정직화 — 승인 배지를 mock 상수에서 실 pending 수로, A2A 무조건 online을 도달성 probe로 정직화한 작업을 볼 때 [admin,honesty,badge,a2a,liveness,ssrf,approvals]
- 046 빌딩블록 재료 정리 — 미사용 권한/MCP 재료와 데모 에이전트를 cascade 삭제하고 dangling 참조를 정리한 작업을 볼 때 [blocks,cleanup,permission,mcp,seed,dry-run,cascade]
- 047 프로바이더·모델 통합+models.dev — 두 메뉴를 마스터-디테일 한 뷰로 합치고 models.dev 스냅샷으로 모델 메타를 자동채우는 작업을 볼 때 [provider,model,catalog,models.dev,merge,migration,ssrf]
- 048 RAG 임베딩 게이팅+조건부 샘플 적재 — 임베딩 모델 존재를 RAG 전제조건으로 게이팅하고 샘플 컬렉션을 mock-embed로 적재하는 작업을 볼 때 [rag,collection,embedding,gate,sample,ingest,mock-embed]
- 049 세션 정리 정책 — 0턴 세션 미영속(lazy-create)과 턴 기준 배치 정리(min_session_turns+idle-guard)를 볼 때 [session,retention,batch,turns,lazy-create,idle-guard,cleanup]
- 050 파괴적 데이터 정리(dry-run) — A2A·세션·유저 정크를 보수적으로 식별해 dry-run→검토→실행으로 정리하고 재생성 뿌리를 self-fixture로 막은 작업을 볼 때 [cleanup,destructive,dry-run,a2a,user,casbin,self-fixture,floor]
- 051 채팅 자가기록 차단·agent_id 메모리는 어드민 저작 전용 — 채팅 LLM이 유저 개인사실을 agent_id 교차사용자 스코프에 자가기록하는 누출버그를 도구 제거로 닫을 때 [memory,agent-scope,leak,write-channel,self-write,admin-curation,spec051]
- 052 유저 메모리 큐레이션 신원 식별 — 큐레이션 UI가 user_id raw UUID로만 라벨링돼 유저 식별 불가인 것을 email·display_name JOIN으로 보강하는 작업을 볼 때 [memory,curation,identity,scoped-endpoint,authz-decouple]
- 054 MCP 실연결 런타임(HTTP) — runtime이 canned 문자열만 반환하던 가짜 MCP를 langchain-mcp-adapters 실연결로 바꾼 작업을 볼 때 [mcp,runtime,langchain-mcp-adapters,streamable-http,self-host,discovery,hil,ssrf]
- 053 유저 메모리 역할 기반 스코핑 — 052가 신원 라벨만 붙이고 접근은 무방비였던 프라이버시 구멍을 본인 기본+어드민 타인 접근의 2축 가드로 닫을 때 [memory,authz,role-scope,privacy,two-axis-guard,capability]
- 055 Playground 세션 이어가기(과거 세션 선택·재개) — 승인 이탈 후 복귀 시 Playground가 초기화되는 것을 세션 피커+대화 복원으로 이어가게 한 작업을 볼 때 [playground,session,resume,agent-filter,preview-label,optimistic-async,adversarial]
- 056 세션 정리: 카운터를 진실로 유지하고 거짓말의 출처(seed)를 고친다 — 시드 빈 데모세션이 turns를 부풀려 저턴 정리에서 고턴 오인 보존되던 것을 seed turns=0 진실화로 고칠 때 [session,cleanup,counter,seed,windowed-mode,adversarial,reversal]
- 057 원격/외부 에이전트 등록을 A2A로 단일화(provenance만 구분) — 자체 SSE 원격 경로를 폐기하고 모든 원격을 표준 A2A로 통일하며 source를 출처(code/external)로만 구분할 때 [a2a,unification,provenance,protocol-consolidation,connect-endpoint,redirect-ssrf,contextId,migration,adversarial]
- 058 첫 실행(first-run) 견고성: 부팅 전제와 사람 진입의 갭 메우기 — 외부 환경서 처음 받아 실행할 때의 부팅 전제(DB 프리플라이트·pgvector)와 사람 진입(첫 관리자·런북) 갭을 메울 때 [first-run,db-preflight,bootstrap-admin,pgvector,graceful-degradation,fail-closed,adversarial]
- 059 MLX를 env에서 떼고 Mock LLM을 기본 채팅/임베딩 모델로 — MLX env 블록 제거가 두 부팅 경로 seed drift 발견으로 번져 마이그레이션+seed로 기본 모델을 수렴시킨 작업을 볼 때 [default-establishment,boot-path-convergence,seed-drift,mock-llm,no-clobber,migration,adversarial]
- 060 A2A 에이전트를 플레이그라운드에서 테스트 가능하게(endpoint 정규화+로컬테스트 UX) — A2A 등록 에이전트 채팅 시 URL 절대성 오류와 SSRF 차단을 관대 정규화+로컬테스트 UX로 푸는 작업을 볼 때 [a2a,playground,url-normalization,ssrf-invariant,lenient-parse,local-test-ux]
- 061 로컬(ui) 에이전트를 실제 A2A로 노출(exposed.a2a 실동작화) — 무력하던 exposed.a2a 플래그를 실제 A2A 서버 라우터(카드 공개+JSON-RPC 인증)로 동작시켜 로컬 에이전트를 A2A로 여는 작업을 볼 때 [a2a,exposure,inert-flag,inbound-surface,unauthed-router,self-dogfood,reuse-runtime,adversarial]
- 062 Admin API 에러 detail 가시화 + RAG 차원불일치 조치안내 — POST가 status만 보여 원인불명이던 것을 프런트 중앙헬퍼가 detail을 추출하게 하고 차원불일치 조치문구를 더한 작업을 볼 때 [error-surfacing,detail,central-boundary,cap-raw-source,curated-message,rag-dimension,adversarial]
- 063 A2A endpoint 호출 경계 정규화 + 빌더 하드닝 + stale 데이터 마이그레이션 — 060이 등록시점만 정규화해 stale endpoint가 호출경계서 거부되던 것을 호출경계 정규화+마이그레이션으로 닫을 때 [a2a,call-boundary,url-normalization,ssrf-invariant,userinfo-disguise,stale-data-migration,self-healing,adversarial]
- 064 SSRF allowlist를 env에서 DB로(무재시작 관리·Admin UI/API) — allowlist를 env에서 DB 테이블로 옮겨 재기동 없이 Admin UI/API로 관리하게 한 작업을 볼 때 [allowlist,env-to-db,no-restart,ssrf,bootstrap-seed,cache-snapshot,admin-crud,adversarial]
- 065 어드민(슈퍼유저) 메뉴를 그룹 헤더로 구분 — 슈퍼유저 항목이 일반 메뉴 뒤 평면 append라 경계가 없던 것을 group 헤더로 시각 분리한 작업을 볼 때 [admin,menu,group,superuser,antd,presentational]
- 066 유저 self-승인: permission 기반 RBAC 3-way 승인 권한 — admin 전용이던 HIL 승인 resolve를 Casbin RBAC로 admin/owner-self/그외 3분할한 작업을 볼 때 [self-approval,approval,hil,rbac,permission,casbin,self-approve,fail-closed,user-id,enumeration-oracle,adversarial]
- 067 세션 라우터 유저별 스코핑(대화 전사 교차열람 차단) — sessions.py가 principal을 안 받아 member가 타인 대화를 교차열람하던 것을 본인 스코핑+가시성404로 닫을 때 [session,rbac,per-user-scoping,enumeration-oracle,list-vs-item,visibility-404,personal-data,chat-resume-bypass,residual-gap,adversarial]
- 068 chat resume 소유권 경계(067 게이트 우회로 봉인) — 067이 막은 읽기 게이트를 chat resume가 소유권 덮어쓰기로 우회하던 P1을 own 스코핑+무덮어쓰기 불변식+엔트로피로 닫을 때 [session,rbac,chat-resume,ownership-boundary,enumeration-oracle,no-overwrite-invariant,entropy,approval-session-stamp,no-regression,adversarial]
- 069 RBAC/소유권 경계 스펙 체크리스트(회고→재발방지 훅) — 소유권/테넌시 경계 스펙 쓸 때 모든 입구(list/item/create/resume/lazy-create)×소유권·404비노출·검증사다리3런·자가잠금핀을 강제하려 할 때; 폴더 CLAUDE.md에 박음, 근거 learning 068·069·070 [rbac,ownership-boundary,multi-tenant,spec-checklist,process-hook,entry-enumeration,verification-ladder,self-lockout,enumeration-oracle,folder-context]
- 070 세션 읽기 owner 스코프를 쿼리에 융합(067 P3-1 타이밍 봉합) — fetch-then-check 타이밍 오라클을 닫을 때(SELECT-WHERE 융합으로 거부행 미로드·체크리스트 069 첫 적용·저장계층 잔존은 073) [session,timing-side-channel,fetch-then-check,select-where,visibility,no-action-refactor,checklist-application,storage-layer-residual,adversarial]
- 071 A2A 카드 prefix-상대 endpoint 구제 + probe 404 정직화 — 프록시 path-prefix 뒤 카드가 루트상대 `/a2a` 발행 시 채팅 404일 때(mount prefix 상대로 urljoin resolve·probe 404/410→dead 정직화·꼬리중복은 082) [a2a,agent-card,url-resolution,proxy-path-prefix,root-relative,urljoin,ssrf-boundary,liveness-probe,route-vs-host,rfc-vs-deployment,adversarial]
- 072 RAG retrieval 시험 수단: 공유 코어 + `/search` 엔드포인트 + 관리 UI 패널 — "rag는 등록만 있고 테스트 길 없음" 공백을 닫을 때(retrieval 본체를 도구·엔드포인트 공유 `search_collections`로·CollectionsView 검색 드로어·승격 시 입력 신뢰경계 리셋) [rag,retrieval,shared-core,drift-prevention,search-endpoint,admin-ui,trust-boundary,input-validation,verification-ladder,adversarial]
- 073 세션 읽기 저장계층 타이밍 봉합: owner-선두 복합 인덱스 + EXPLAIN 측정(070 P2) — 070 뒤 남은 저장계층 타이밍 잔존(session_id 단독 unique heap-fetch)을 닫을 때(owner-선두 복합 인덱스·EXPLAIN buffer 실측+반증·"선택≠강제" 정직기록) [session,timing-side-channel,storage-layer,composite-index,owner-leading,explain-buffers,counterfactual,idempotent-migration,planner-selection,honest-residual,adversarial]
- 074 RAG 컬렉션 액션 가시성: 검색 버튼 승격 + 테이블 오버플로우 해소(072 후속) — 072 검색 버튼이 "안 보임" 보고를 닫을 때(max-content 테이블 오버플로를 scrollWidth로 측정해 범인=이름 컬럼 특정·검색 primary 승격·순수 표현변경 RBAC 무관) [rag,admin-ui,table-overflow,max-content,measure-not-guess,button-prominence,responsive,verify-ui-browser]
- 075 에이전트 생성/수정 폼 체크박스 행 레이아웃 깨짐 — "생성 드로어 레이아웃 깨짐"(메모리 라벨 글자단위 세로 줄바꿈) 보고를 닫을 때(flex-shrink·중첩 label invalid HTML→이름·설명을 Checkbox children 세로 스택·측정 검증·순수 표현 RBAC 무관) [admin-ui,antd6,checkbox,flex-shrink,nested-label,layout,verify-ui-browser,measure-not-guess]
- 076 create_react_agent → langchain.agents.create_agent 마이그레이션 — deprecated 런타임 빌더를 후속 API로 갈 때(prompt→system_prompt 개명·유일 breaking은 콜러블 prompt·계약 4종은 verify_041 회귀로 보존 증명·deprecation 0) [langgraph,langchain,create-agent,deprecation,migration,call-contract,system-prompt,regression,verify_041,adversarial-not-needed]
- 077 온도 에이전트 필드 승격 + 페르소나 대칭 (이슈2) — 온도·페르소나가 한 곳만 있는 비대칭 보고를 닫을 때(런타임은 이미 cfg.temperature 읽음→입구만 추가·model_dump round-trip·폼 Slider+OverridePanel 페르소나 Select로 대칭·RBAC 무관) [agent-config,temperature,persona-symmetry,override-panel,jsonb-roundtrip,model-dump,antd6-select] → learning 080
- 078 미활성 초안 배지로 "편집 미반영" 안내 (이슈1) — "수정 후 플레이그라운드에서 안 바뀜" 보고를 닫을 때(버그 아닌 설계대로=서빙은 활성 config만·증상 화면 Playground에 초안 신호 0이 갭→표시층만 "미반영 초안" 배지·백엔드 0줄·RBAC 무관) [playground,draft-badge,staging-surface,version-status,truth-source,no-backend-change,antd6-tag,verify-ui-browser] → learning 081
- 079 인스펙터에 RAG 문서검색·메모리 조회 이력 노출 (이슈5) — "tag 조회·메모리 조회 이력 안 보임" 보고를 닫을 때(갭=결과 없음 아닌 *조회 행위 비가시*→회상 쿼리 에코 한 키 추가·Inspector에 조회 블록+RAG 섹션·0건도 쿼리 노출·RBAC 표시전용) [inspector,playground,rag,memory-recall,observability,trace,instrument-the-act,0-result-still-happened,antd6,verify-ui-browser] → learning 082
- 080 Playground 에이전트 변경 전파(BroadcastChannel + 포커스 백스톱) (078 후속버그) — "초안 활성화해도 미반영 배지 남음" 보고를 닫을 때(진짜 조건=*별도 탭* Playground가 마운트 스냅샷 들고 외부 activate 모름→BroadcastChannel+focus 재페치 백스톱·백엔드 0줄·RBAC 무관) [playground,draft-badge,stale-snapshot,cross-tab,broadcastchannel,focus-backstop,consumer-surface,verify-ui-browser] → learning 083
- 081 A2A 런타임 호출 경로 견고화 (071 후속) — 외부 에이전트 채팅 "응답 오류 404"를 닫을 때(message/stream·send 동일 endpoint라 404=endpoint 틀림으로 재분별·cardUrl 저장+resync가 fetch_card→probe로 in-place 자가치유·stream 404→send 1회 폴백·RBAC 무관) [a2a,stale-endpoint,self-heal,resync,card-url,re-derive,message-send-fallback,offline-honesty,single-endpoint,probe-deeper] → learning 084
- 082 A2A endpoint resolution 꼬리 중복(/a2a/a2a) collapse (071 누락) — endpoint 뒤 /a2a/a2a 중복으로 Playground 404일 때(prefix 꼬리==카드 상대경로면 collapse·정확 꼬리매치만·fetch_card 단일지점이라 connect/register/resync 일괄·SSRF 새입구 0·RBAC 무관) [a2a,endpoint-resolution,duplicate-tail,collapse,idempotent,prefix-relative,071-followup,probe-deeper] → learning 085
- 083 A2A 노출은 source=ui 에이전트에만 (원격/외부 노출 토글 제거) (이슈2) — "원격 에이전트를 A2A로 오픈은 이상함" 보고를 닫을 때(보안은 이미 닫혔으나 code/external에 exposed.a2a=true 박힌 "죽은 상태"→불변식을 모든 쓰기/렌더 입구에 정렬·마이그레이션 stale 청소·JSONB 형제키 하드닝·RBAC 무관) [a2a,expose-gate,source-ui-only,fail-closed-consumer,dead-state,align-predicate-every-writer,jsonb-sibling-key,no-frontend-only-guard,adversarial-codex,verify-ui-browser] → learning 086
- 084 메모리 회상 시험 도구 (조회 시험 드로어) (이슈1) — "인스펙터는 메모리 검색했다는데 실제 시험 불가"를 닫을 때(메모리 콘솔 양 탭에 RAG 072 /search 미러링한 조회 드로어·enabled를 프록시 아닌 백엔드 가용성에 묶는 recall_probe·None≠[] 구분·유저검색 본인만·RBAC 미러) [memory,recall-test,enabled-honesty,proxy-vs-actual,recall-probe,none-vs-empty,mirror-sibling-endpoint,rbac-mirror,adversarial-codex,verify-ui-browser] → learning 087
- 085 커스텀 에이전트 런타임 인터페이스(in-process SDK) — source 문자열로만 갈리던 에이전트에 공통 런타임 인터페이스를 들일 때(`CustomAgent` Protocol+`AgentBuildContext`로 적합 그래프 astream 루프 소유·미구현은 None→_a2a_stream fallback·분기축은 config["impl"] 레지스트리 키·레퍼런스 2구현으로 과적합 측정·RBAC 미적용) [custom-agent,in-process-sdk,protocol,agentbuildcontext,graceful-fallback,trusted-registry,real-node-trace,full-replace-silent-drop,dead-contract,adversarial-codex] → learning 088
- 086 턴 인스펙터 노드별 세부정보(상태 델타 미리보기 + 실측 시간) — 인스펙터 "LangGraph 경로"가 노드 이름+가짜 균등분할 ms만 보일 때(`updates` 청크 값·도착시각을 살려 노드별 실측 ms+안전 요약·키기반 redaction+plan allowlist·3경로 우선순위·비밀누출0 불변식·MCP args 누출은 087로 분리) [inspector,node-detail,state-delta,real-per-node-ms,redaction,value-secret-allowlist,budgeted-cap,parallel-superstep-honesty,failclosed,adversarial-codex,verify-ui-browser] → learning 089
- 087 MCP 호출 인자·결과 redaction(형제 trace 표면) — 086 codex F1 분리; "MCP args/result 원문 trace 노출 P0"를 액면 수용 않고 위협 모델 실측으로 재분별할 때(시스템 비밀은 헤더/설정에 있지 kwargs에 안 옴+표면 admin전용→defense-in-depth·runtime.py 한 곳 sink 정화·민감키 blocklist는 086 allowlist와 반대 polarity·RBAC 무관) [redaction,sibling-surface,inherited-severity,reframe-threat-model,defense-in-depth,key-blocklist,purpose-sets-polarity,central-boundary,jsonb-nan,failclosed-serialization,adversarial-codex,probe-deeper] → learning 090
- 088 플레이그라운드 응답 markdown 렌더 + raw-JSON 휴리스틱 — 응답을 평문으로만 그려 markdown/JSON이 안 풀릴 때(프런트만·렌더시점 휴리스틱·스트림완료 게이트·접이식 JSON트리·codex 4건 수정) [playground,markdown-render,react-markdown,json-tree,format-inference,streaming-gate,render-time-heuristic,covering-guard,tracking-pixel,bigint-precision,contentRender,adversarial-codex] → learning 091
- 089 커스텀 에이전트 공통 인터페이스 준수 분류 3상태 (제안#1, 초안) — "커스텀 에이전트가 공통 인터페이스를 지키나"를 1급으로 가를 때(준수/비준수/설정실패 3상태·isinstance(CustomAgent)+resolve 게이트, 선언 impl 미해결이면 폴백 마스킹 대신 서빙거부) [custom-agent,conformance,three-state,config-error,protocol-isinstance,fallback-masking-fix,gap-closure,single-helper-derive,no-store,agentout-field,ui-badge,precursor-to-4,085-followup]
- 090 플레이그라운드 메시지 역방향 페이징 (파킹 — #2 오독 산물) — 제안 #2를 "긴 대화 위로 스크롤 페이징"으로 오독해 쓴 스펙; 사용자 정정으로 *진짜* 의도는 091(입력 히스토리 재호출)로 분리, 본건은 삭제 않고 좋은 후속 후보로 파킹 [playground,reverse-pagination,parked,intent-misread,split-to-091]
- 091 플레이그라운드 입력 히스토리 재호출(터미널 콘솔식 ↑/↓) (제안#2) — 입력창 커서 맨앞서 ↑=과거 입력 로드(셸 히스토리)를 넣을 때(순수 reducer로 정책 분리, caret 맨앞 진입조건·IME 가드; codex가 값의존 effect 미발화·stale-idx 2건 잡아 클램프) [playground,input-history,terminal-recall,shell-history,pure-reducer,dedupe-consecutive,caret-at-front-gate,ime-guard,in-memory,frontend-only,value-dependent-effect,monotonic-counter,stale-index-race,verification-ladder-3rung,adversarial-codex] → learning 093
- 092 채팅 본문서 도구(블록) 원본 응답 숨김 (제안#3) — 메모리/MCP/RAG 호출의 raw 응답(ToolMessage.content)이 본문에 새던 걸 닫을 때(백엔드 스트림 게이트 isinstance(ToolMessage) 단일술어를 두 sink 공유, codex가 content-block 리스트 TypeError 선재버그도 함께봉합) [playground,hide-tool-response,toolmessage,stream-gate,isinstance-not-type-string,messagechunk-instability,content-block-normalization,_content_text,single-predicate-two-sink,covering-guard,blocklist-polarity,fail-open-narrow-surface,observability-overclaim-fix,verification-ladder-3rung,adversarial-codex,frontend-zero] → learning 094
- 093 연결된 에이전트 있으면 MCP서버·RAG컬렉션 삭제 차단 (제안#5) — name 문자열 참조(FK아님)를 삭제/rename하면 config에 dangling name이 남아 런타임 능력소실될 때 409로 차단(참조스캔=활성∪archived 전버전, codex가 "archived는 dead" 가정을 반증해 교정) [reference-integrity,name-not-fk,dangling-name,delete-rename-guard,operation-symmetry,archived-rollbackable-live,covering-guard,config-names-normalizer,guard-runtime-drift,string-detail-062,frontend-zero,toctou-deferred,verification-ladder-3rung,adversarial-codex] → learning 095
- 095 DataTable 가로 overflow: 액션 컬럼 고정 + 반응형 숨김 (제안#6) — 커스텀 DataTable이 넘쳐 편집·삭제 액션버튼이 화면밖 밀리던 걸 "둘 다"로 닫을 때(Column에 hideBelow·fixed:'right' 추가, 무제목 마지막컬럼 자동 sticky-right; codex가 짚은 hover 미반영은 data-sticky 재배선으로 해소) [datatable,overflow,sticky-right,responsive-hide,hideBelow,covering-guard-frontend,measure-then-tune,opaque-overlay-rehover,browser-verify,adversarial-codex] → learning 096
- 097 검색시험 드로어 통합 + 메모리 뷰 검색 UX 정합 (제안#7) — RecallDrawer(084)가 SearchDrawer(072)를 수동미러링해 갈린 드로어 UI를 공유 제네릭 셸 RetrievalTestDrawer<H>로 통합할 때(어댑터로 도메인차 흡수, 정직성계약 None≠[] 보존; codex가 원본 두 드로어에 잠복하던 stale-async 스코프유출을 reqSeq 가드로 양쪽 봉합) [retrieval-drawer,unify-collection-memory,shared-generic-shell,covering-guard-frontend,drift-zero,honesty-contract-none-not-empty,render-prop-meta,enabled-normalization,preAlert-not-ready,rbac-untriggered-pure-ui,scope-binding-preserved,stale-async-scope-leak,reqSeq-guard,browser-verify,adversarial-codex] → learning 097
- 098 세션 목록 서버측 메타데이터 검색 (제안#8) — 세션목록에 검색 q를 추가할 때(session_id·user_id·agent_name 3컬럼 OR ilike, 메타문자 이스케이프; 서버페이징이라 로컬필터는 부정직→서버측 필수; predicate를 own-scope WHERE 뒤 AND로 얹어 스코프 못 넓힘) [session-search,server-side,3col-or-ilike,ilike-metachar-escape,scope-where-then-and,server-paged-local-filter-dishonest,honesty-contract-opposite-verdict,rbac-triggered,existence-nondisclosure,debounce-page-reset,verification-ladder-3rung,adversarial-codex] → learning 098
- 099 에이전트 플로우 스캐폴드 스킬 (그래프 빌더 #4 폐기 대체) — 시각 그래프빌더(#4) 폐기하고 목적을 스킬 코드젠으로 대체할 때(agent-flow 스킬이 CustomAgent Protocol 적합 LangGraph 플로우를 flows/에 저작→신뢰등록 2줄 eval없음→089/085 재검증, 데모 route로 실증) [agent-flow-skill,codegen-scaffold,discard-graph-builder,custom-agent-protocol,trusted-registry-no-eval,flows-folder,reuse-089-085-gate,conditional-branch-route,pure-function-branch,api-restart-authoring-time,verification-ladder-3rung,adversarial-codex] → learning 099
- 100 능력 브로커 시임 + A2A 오케스트레이션(서브스텝) Phase1 — 능력(agent·mcp·rag·memory)을 preload 아닌 discovery로 오케스트레이션할 때(discover/describe/invoke 단일 시임, 정책=allowlist∩RBAC deny-by-default, 첫 provider A2A+데모 orchestrate 서브스텝) [capability-broker,discovery-over-preload,discover-describe-invoke,one-seam,ctx-broker-injection,policy-scoped-broker,allowlist-intersect-rbac,deny-by-default,existence-nondisclosure,invoke-recheck-toctou,untrusted-output,broker-invoke-trace,substep-orchestration,agent-flow-demo,rbac-boundary-checklist,verification-ladder-3rung]
- 101 능력 브로커 kind 확장: MCP provider (Phase 2-a) — 능력 브로커에 두번째 provider(MCP)를 더할 때(_CapabilityProvider 시임 도입, 툴단위 입도+네임스페이싱, 위임 MCP툴 승인요구 시 broker.invoke가 전송이전 interrupt로 기존 HIL 재사용) [capability-broker,kind-extension,mcp-provider,provider-seam,behavior-preserving-refactor,tool-level-granularity,allowlist-namespacing,reuse-mcp-transport,hil-substep,interrupt-before-sideeffect,approval-policy-single-source,supports-hil-honest,per-provider-approval,verification-ladder-3rung,rbac-boundary-checklist,config-capabilities-persistence,seed-bypasses-write-schema,stale-reload-split-brain,verify-the-reviewer] → retrospect 082 · learning 101
- 102 discovery 오케스트레이션: 전략 교체형 골격 (공통 조상 + 첫 출하 2전략) — 브로커 위 오케스트레이션 방식을 소유자가 고르는 전략으로 열 때(전략=impl로 갈라 config["impl"] 재사용, 공통조상 ABC 템플릿메서드가 골격·채널격리·HIL·정책 소유, select만 자식 구멍, 첫출하 2전략으로 무누수 측정) [discovery-orchestration,pluggable-strategy,strategy-as-impl,common-ancestor-abc,template-method,select-override-only-hole,pure-function-rank,two-impls-measure-abstraction,behavior-preserving-refactor,sequential-fold,channel-isolation-inherited,drift-zero,conformance-089,reuse-099-registry,skill-orchestration-branch,authoring-time-enforcement,final-static-enforcement,chosen-subset-guard,complement-attack-honest-boundary,verification-ladder-3rung] → learning 102
- 103 능력 브로커 kind 확장: RAG provider (Phase 2, 셋째 provider·첫 읽기전용) — 세번째 provider(RAG)를 더해 시임을 재측정할 때(RagProvider가 McpProvider 거울로 6메서드 구현, search_collections 코어+format_rag_hits 포맷 세입구 공유 drift0, 읽기전용이라 approval_for 항상 None) [capability-broker,kind-extension,rag-provider,third-provider-measures-seam,read-only-provider,two-gates-policy-vs-approval,cap-id-one-level,policy-unchanged,shared-core-search-collections,format-rag-hits-extract,three-entrances-one-core,drift-zero-measured,untrusted-data-channel,approval-for-none,rbac-checklist-by-inheritance,owner-less-shared-catalog,complement-attack-honest-boundary,document-boundary-not-silent,core-holds-cross-entrance-invariant,empty-resource-id-guard,verification-ladder-3rung,adversarial-codex] → retrospect 084 · learning 103
- 104 능력 브로커 kind 확장: Memory provider (Phase 2-c, 첫 per-user 소유 능력) — 네번째 provider(memory, 첫 per-user 소유)로 인가 입도 빚을 갚을 때(cap_id에 대상 안 박고 소유자를 principal서 도출→memory:user 하나, invoke 스코프는 user_id뿐이라 교차유출 구조적 불가) [capability-broker,kind-extension,memory-provider,fourth-provider,per-user-ownership,bind-to-principal-not-cap-id,structural-anti-leak,no-admin-escalation,policy-unchanged,recall-probe-core,format-memory-hits-extract,two-gates-read-only,approval-for-none,rbac-checklist,fakemem-deterministic-isolation,real-mem0-integration,limit-coerce-clamp,resume-wiring-symmetry,new-state-axis-all-factories,complement-attack-honest-boundary,verification-ladder-3rung,adversarial-codex] → retrospect 085 · learning 104
- 105 능력 브로커 kind 확장: Memory write provider (첫 부수효과·승인 게이트) — 다섯번째 provider(memwrite, 첫 부수효과)로 자동쓰기 누출을 구조로 봉인할 때(principal 바인딩 자기만 + 승인 게이트 항상 non-None, 승인돼야 저장·소유자 self-승인 기본) [capability-broker,kind-extension,memory-write-provider,first-side-effect,approval-gate-is-structure,prompt-not-boundary,two-structural-defenses,principal-bound-self-scope,agent-id-forbidden,read-write-separate-permission,owner-self-approve-default,sensitivity-grade,infer-false,minimal-harness-gate-test,shared-helper-approved-equals-stored,privacy-finding-permission-delta,admin-already-053,complement-attack-honest-boundary,verification-ladder-3rung,adversarial-codex] → retrospect 086 · learning 105
- 106 에이전트 편집 폼에 impl + capabilities 노출 (능력 브로커 UI) — 능력 브로커(100–105)를 UI에서 엮게 편집 폼에 실행방식(impl)+능력(capabilities) 노출할 때(cap id `<kind>:<name>` 규약이라 새 카탈로그 엔드포인트 불요, config 필드 왕복 4지점 교집합 확인 필요) [capability-broker,admin-ui,impl-select,capabilities-picker,agent-impls-endpoint,registry-single-source,kind-namespace-formatting-layer,config-roundtrip-4-points,asymmetric-serialize-gap,client-side-assembly,browser-verification,rbac-checklist-not-triggered,adversarial-codex-skipped] → retrospect 087 · learning 106
- 107 능력 피커 UX: 오케스트레이터 조건부 + 종류별 접이식 — 106 능력 피커를 사용자 지적(RAG 늘면 폼 복잡)으로 조건부+접이식으로 바꿀 때(오케스트레이터 impl일 때만 렌더, kind별 Collapse 기본접힘+카운트 배지) [capabilities-picker-ux,user-feedback-driven,conditional-display,accordion-by-kind,count-badge,form-height-independent-of-catalog,pure-presentation-swap,askuserquestion-preview,antd-checkbox-check-input,browser-verification] → retrospect 088 · learning 107
- 108 에이전트 폼 재구성: 유저 언어 + 종류가 설정을 가른다 — 106·108 폼을 사용자 세번째 지적(내부어·MCP 중복·전략 어려움)으로 뿌리수술할 때(내부 아키텍처를 유저언어로, 종류(직접응답/조율형)가 설정면을 갈라 MCP 중복 소멸) [agent-form-plain-language,type-decides-fields,no-internal-jargon-in-ui,system-decomposition-vs-user-model,duplicate-resource-two-exclusive-paths,mode-splits-config-surface,measure-jargon-absence-innertext,askuserquestion-preview-naming,pure-presentation-swap,repeated-feedback-is-framing-signal] → retrospect 089 · learning 108
- 109 폼 3단계 재편 + 재사용 효율 피커(등록 폼 + 플레이그라운드 오버라이드) — 108 후 필수/선택 미분리·오버라이드에도 같은 개선을 요구받아 폼을 3단계+재사용 효율피커로 바꿀 때(PickerGroups 컴포넌트 추출해 등록폼·오버라이드 공유, id prefix 어댑터로 이질저장 흡수) [form-tiers,reusable-component,cross-surface-consistency,pickergroups,adapter-prefix-routing,storage-agnostic-component,pure-presentation-swap,required-vs-optional,type-specific-vs-common,browser-verification-two-surfaces] → retrospect 090 · learning 109
- 110 엔드투엔드 오케스트레이션 시연(브로커 스모크·106 잔여) — 브로커(100–105)+UI(106–109) 각 고리는 증명됐으나 채팅 1턴이 전 체인 관통하는지 미실측일 때(결정적 E2E+브라우저 시연, 위임은 유저세션만·머신토큰 deny-by-default 발견) [e2e-smoke,links-vs-chain,integration-rung,auth-context-is-contract,machine-token-deny-by-default,dependency-overrides-principal,mock-llm-deterministic-delegate,broker-invoke-trace-node,lexical-discover-substring,orchestrator-playground-demo] → retrospect 091 · learning 110
- 111 능력 브로커 메모리 수정/삭제(memedit) — 브로커 메모리 수정/삭제(memedit)를 더할 때(기존 mem_id 대상이라 소유권 선행: principal 고정+user_owns 소유권선행+승인게이트 항상, 소유권 술어 단일화) [memedit,memory-update-delete,ownership-precheck,user-owns-single-source,404-fold,anti-leak-principal-scope,approval-gate,delete-irreversible-fail-closed,check-then-act-load-bearing-assumption,honest-boundary,codex-adversarial-rung3,three-rung-verification] → retrospect 092 · learning 111
- 112 공유 카탈로그 소유권 + per-cap 인가(인가강화) — 브로커 인가를 kind→per-cap으로 좁히고 카탈로그(Agent·Mcp·Collection)에 owner_id 붙여 관리 게이트를 걸 때(소유권=관리만·사용은 RBAC, 게이트 추가가 기존 열린 문을 드러냄) [percap-authz,capability-per-name,ownership-management-not-invoke,gate-reveals-open-door,assert-may-manage,404-fold-body,fail-closed-null-owned,lazy-enforcer,machine-token-context-dependent,codex-rung3,honest-boundary-p0] → retrospect 093 · learning 112
- 113 런타임 tool 배선 인가(112 P0 봉합) — 112 P0 봉합: 채팅 런타임이 config mcps/vectorTables 이름으로 무인가 크레덴셜 tool 배선하던 경로에 인가할 때(주체=에이전트 작성자, codex가 override 주입 confused-deputy P0 발견해 봉합) [runtime-tool-wiring,confused-deputy,authorize-by-who-supplied-ref,author-not-caller,override-injection,caller-authority-for-injected,uuid-validate-before-authz-key,rbac-lever-covers-both-paths,agent-may-wire,null-owner-no-regression,codex-rung3] → retrospect 094 · learning 113
- 114 화면에 주인 표시 + 비소유 관리 버튼 숨김 — 112 백엔드 게이트를 UI에 반영할 때(owner_id·can_manage를 Out에 실어 프론트는 필드만 읽음, 브라우저 검증이 목록행 삭제아이콘 놓친 렌더지점 포착) [owner-display,can-manage-flag,backend-owns-predicate,may-manage-boolean-sibling,ownertag,guard-every-render-site,reenumerate-ui-render-sites,route-intercept-for-unreachable-state,browser-catches-missed-site,blocks-endpoint-not-mcp-servers] → retrospect 095 · learning 114
- 115 위임 결과 attribution 견고화(nonce 펜스, codex 102 설계한계 봉합) — 다중 위임 fold의 라벨 스푸핑(codex 102 설계한계)을 봉합할 때(요청별 nonce 펜스로 위조불가 경계, codex가 라벨도 자원명이라 위조가능 짚어 _label_safe 정규화 추가) [attribution-fence,unforgeable-boundary-secret-nonce,per-request-nonce-not-exposed,label-outside-fence,sanitize-trust-side-too,label-safe-normalize,defense-instruction-matches-data-shape,fence-single-too,random-injected-not-in-pure-fn,codex-rung3] → retrospect 096 · learning 115
- 116 재개 시 선행 위임 결과 보존(멱등 재개, codex 102 [P1] 봉합) — 다중 순차 위임 중 뒤 cap interrupt 시 재개서 delegate가 처음부터 재실행돼 앞선 결과 소실되던 걸 봉합할 때(cap 하나=노드 하나 self-loop, codex가 재-discover 갭도 plan 노드 분리로 봉합) [resume-idempotent,node-boundary-is-commit-unit,self-loop-per-cap,pending-done-reducer,plan-node-before-invoke,select-committed-before-sideeffect,fail-closed-not-completeness,codex-rung3] → retrospect 097 · learning 116
- 117 A2A 협업 실증 + 위임 승인 게이트 — A2A 위임이 배선됐으나 성공 협업 관통 미실증일 때(a2a_stream 결정적 패치로 성공 경로 재현+정확히1회 호출 실측, opt-in 승인게이트 추가, codex가 write-schema 드롭 P1 발견해 봉합) [a2a-collaboration,invocation-vs-success,deterministic-success-path,delegation-approval-optin,requires-approval-flag,write-schema-roundtrip,seed-bypasses-write-schema,signature-unify-pass-row,approved-equals-sent,interrupt-before-sideeffect,codex-rung3] → retrospect 098 · learning 117
- 118 관측·측정 계층(Langfuse, inert-until-configured) — 기술스택엔 있으나 코드 0줄이던 Langfuse 관측을 배선할 때(inert-until-configured+graceful+비파괴 병합 3겹, codex가 with_trace 타입계약 구멍 P2 발견) [observability,langfuse,inert-until-configured,graceful-degradation,keys-are-the-switch,secret-never-logged,non-destructive-config-merge,callback-delivery-passthrough,guarantee-covers-type-contract,codex-rung3] → retrospect 099 · learning 118
- 119 에이전트 평가 하네스(결정적 수치, 개발·CI) — 수치 자율(Ralph) 전제인 결정적 에이전트 평가 하네스를 만들 때(데이터셋×run_fn→통과율, codex가 조용한초록 4구멍 잡아 전 경로 fail-closed로 봉합) [eval-harness,deterministic-numeric-score,signal-tool-fail-closed,silent-green-is-cardinal-sin,discrimination-measured,behavior-signal-not-llm-text,dataset-times-runfn,adapter-must-not-hide-failure,codex-rung3] → retrospect 100 · learning 119
- 120 에이전트 복제(저마찰 재사용) — 저마찰 재사용을 위해 에이전트 복제를 넣을 때(복제=읽기+새생성이라 원본 관리권한 불요, 복제자 소유 스탬프, card/비밀은 미복사) [agent-clone,low-friction-reuse,new-verb-on-use-manage-axis,clone-needs-visibility-not-manage,clone-button-outside-can-manage-gate,creator-owns-clone-069,copy-behavior-strip-registration,wire-all-render-variants,browser-verify] → retrospect 101 · learning 120
- 121 삭제/이름변경 참조 가드 활성-only 완화(버그1) — RAG/MCP 삭제 시 과거 버전 참조까지 검사하던 093 과엄격을 활성 config만으로 완화할 때(usedBy 배지와 재정렬, verify_093 조용한 red 발견해 복구) [delete-guard,active-only-not-all-versions,relax-realign-sibling-guard,route-gate-breaks-direct-call-test,silent-red,principal-stub,codex-clean] → retrospect 102 · learning 121
- 122 조율형 플레이그라운드 오버라이드에 capabilities 반영(버그2) — 조율형 MCP가 오버라이드에서 누락되던 버그2를 닫을 때(108이 편집폼만 kind로 가르고 109가 직접형만 이식한 틈, OverridePanel을 kind-aware로) [orchestrator-override,capabilities-not-mcps,fork-surface-by-kind-propagate,kind-aware-override,single-source-helper,broker-caller-scoped-no-confused-deputy,browser-verify,nul-byte-binary-diff] → retrospect 103 · learning 122
- 123 오버라이드 드로어 그룹 헤더 오토글(label이 그룹 감쌈) — 오버라이드 드로어 그룹 헤더 클릭이 첫 체크박스를 오토글하던 버그3을 닫을 때(Field가 label로 다중 컨트롤을 감싸 클릭이 전달, group prop으로 div role=group 전환) [label-wraps-one-control,click-forwards-to-first-descendant,field-group-role,not-htmlFor-but-wrapping,failed-fix-points-to-cause,forceRender-dom-reorder-test,event-capture-trace,adjacent-click-crosstalk] → retrospect 104 · learning 123
- 124 조율형이 도구/문서/기억 못 쓰던 버그(discover 어휘 필터→랭킹) — 조율형이 도구/문서/기억을 못 쓰던 버그를 닫을 때(broker.discover의 어휘 하드필터가 자연어 쿼리서 허가 능력 전부 탈락시킴→필터를 랭킹으로 전환) [discover-rank-not-filter,hard-filter-after-gate-nullifies,query-subset-cap-inverted,gate-preserved-above,green-test-encodes-bug,fix-test-to-invariant,layer-isolation-debug,codex-clean] → retrospect 105 · learning 124
- 125 메모리 검색 진단: "왜 0건/실패인지" 표면화(디버깅 우선) — 메모리 검색 "왜 0건/실패인지" 표면화할 때(recall_probe가 미가용 사유를 None으로 뭉개고 검색예외 500이던 것을 recall_diag 구조화+지속 진단패널로) [memory-search-diagnostics,recall-diag,why-not-zero,persistent-diag-panel,secret-redaction-exact-first,failure-not-zero,single-authority,codex-2-fixed] → retrospect 106 · learning 125
- 126 메모리 조회(검색)를 드로어→상세 페이지 인라인 — 메모리 조회(검색)를 드로어에서 상세 페이지로 인라인화할 때(RetrievalTestDrawer 본문을 RetrievalTestPanel로 추출, 컬렉션 무변경·타입 재노출) [memory-search-inline-page,extract-body-thin-wrapper,zero-drift,type-reexport,card-on-top-list-below,verification-follows-ui,codex-clean] → retrospect 107 · learning 126
- 127 메모리 서버 페이지네이션+일치/유사도 통합 검색(안 B) — 메모리 목록 20건 잘림 버그(mem0 get_all 기본 top_k=20)를 서버 페이지네이션+일치/유사도 통합 검색으로 닫을 때(MemoryBackend.list_page Protocol 계약 추가) [paged-memory,list-page-contract,mem0-get-all-default-20-cap,raw-sql-isolated,ilike-escape,failure-not-zero,segmented-exact-similar,sessionsview-pattern,fake-mirror,codex-3-fixed] → retrospect 108 · learning 127
- 128 PagedListShell: 페이지 목록 패턴 일반화(세션·컬렉션 문서·메모리) — 127 페이지 패턴을 세션·컬렉션 문서에도 일반화할 때(PagedListShell<T,X> 엔진 추출, fetchPage ref 고정으로 재조회 루프 방지, codex가 과도기 요청 경합 잡음) [paged-list-shell,engine-extraction,per-screen-prescription,ref-pinned-fetch,page-reset-key,transient-request-eliminated,drawer-fixed-viewport,docs-page-api,codex-2-fixed] → retrospect 109 · learning 128
- 129 세션 종료 버튼 배선(+백엔드 500 수정) — 세션 종료 버튼을 배선할 때(백엔드 완성 전제를 브라우저 검증이 반증—commit 후 onupdate 접근 MissingGreenlet 500, refresh 1줄로 수정) [session-end-wiring,mockup-first-e2e,missing-greenlet-fix,refresh-before-serialize,false-failure-ux,popconfirm-irreversible] → retrospect 110 · learning 129
- 130 조율형 RAG 검색을 인스펙터에 표면화 — 조율형 RAG 검색이 안 된다는 신고를 진단으로 반증할 때(진짜 근인=인스펙터가 브로커 경유 검색을 표시 안 함, hits/topScore 구조화+brokerCalls 3경로 전수 표면화) [broker-rag-visibility,perceived-bug-diagnosed,hits-topscore-structured,broker-calls-3paths,rag-chip,low-relevance-tag,codex-3-fixed] → retrospect 111 · learning 130
- 131 인스펙터 상세화: 전송 프롬프트 전문·도구 결과 본문·노드 값 — 인스펙터에 전송 프롬프트 전문·도구 결과 본문·노드 값을 노출할 때(가림 계보 추적해 관문대로 개방, codex가 delegated·페르소나 무마스킹 등 4건 잡아 마스킹+캡+개수상한 셋 다 적용) [inspector-full-detail,sent-messages,result-preview,value-safe-keys,mask-cap-count,redaction-lineage,reload-persistence,codex-4-fixed] → retrospect 112 · learning 131
- 132 플레이그라운드 모바일 헤더: 여러 줄 스택+온전 텍스트(v2) — **done**(retrospect 113): 점검 2탄; v1(아이콘만)을 실기기 피드백("뜻 안 보임")이 뒤집어 v2=모바일 헤더 세로 스택 3줄+fullWidth prop으로 식별정보 복원 → retrospect 113
- 133 전 메뉴 모바일 점검 스윕+수정 — **done**(회고 생략·소규모): 점검 3탄, 전 메뉴 390px 순회로 실버그 1(승인 행 checkpoint code 세로압착, 정량지표 안 잡히고 육안만)+경미 1(블록 탭 잘림) 발견해 수정 [mobile-audit-sweep,flex-crush-not-caught-by-metrics,inline-flow-fix,tabs-small-mobile,quant-plus-visual] → (회고 생략)
- 134 턴 트레이스에 적용 오버라이드 기록 — 오버라이드한 세션 재진입 시 그 설정 유지되는지 질문에 답할 때(오버라이드는 세션 아닌 UI 적용 상태임을 진단, trace.overrides로 턴별 기록+인스펙터 섹션, codex가 적용 안 된 값 기록 드리프트 잡음) [override-in-trace,ui-state-not-session-property,record-mirrors-apply-guard,allowed-keys-masked-capped,toast-success-only,codex-2-fixed] → retrospect 114
- 135 드로어/인스펙터 Escape 닫기 통일 — **done**(회고 생략·소규모): 드로어 모바일 점검 결과 가로넘침 0·진짜 갭=닫기수단 불일치(antd만 Escape)→커스텀 Drawer+Inspector에 Escape keydown 통일 [drawer-escape-unify,close-affordance-gap,audit-first,no-width-issues] → (회고 생략)
- 136 INDEX 재압축(Scaffolding 정비) — 세 인덱스 줄이 서사 수록으로 비대(평균 440~481자·최대 3.6k)해져 후크 220자 이하로 124줄 재작성; fast-worker 위임+보존 4종(항목수·NNN·키워드·참조꼬리)을 조율자가 독립 재측정(첫 FAIL은 측정기 오탐 — 후크 속 화살표) [index-recompression,one-line-hook,delegate-bulk,measure-preservation,verify-the-measurer] → (회고 생략)
- 137 평가 하네스 제품화 1탄(문제집 DB·실행 API·평가 메뉴) — **done**(retrospect 115·learning 137): 119 하네스를 화면으로 — Eval 4테이블(kind=agent|rag 확장축)+하네스 승격(build_asserts 닫힌 집합)+CRUD(admin)+오염 제로 러너(stream_local_reply 골격+관측 union+브로커 주입, 세션/mem0 카운트 불변 실측)+백그라운드 실행(순차·폴링)+평가 메뉴 UI(문제집·assert 편집기·성적표); codex 4건(좀비 sweep·중복 409·크기 상한·성적표 이름)+e2e가 성적표 500(RunDetailOut results×ORM lazy 관계 — MissingGreenlet 3변종) 포착→수정+C4d 핀; 검증=CRUD 12/12·러너 14/14·e2e 8/8(100% 성적표까지)·119 무회귀; OUT=이력 비교(2탄)·LLM-judge(3탄)·RAG 러너(kind 축만)·member 개방 [eval-productization,pollution-zero-runner,observation-union,declarative-asserts,background-run,missing-greenlet-3rd,route-called-verify] → retrospect 115 · learning 137
- 138 평가 2탄: 성적 추이+런 비교(회귀 가시화) — **done**(retrospect 116): 문제집 드로어에 추이 차트(dataviz 절차 — 단일 계열 SVG·프라이머리 블루 검증 PASS·점 툴팁·마지막 점 라벨·최근 N회 명시)+최근 런 목록(클릭→성적표), 실행 이력에 문제집 필터+런 2개 체크 비교 드로어(Δ%p·회귀 Alert·회귀 우선 정렬·assert 단위 diff 3라벨: 새로 실패 red/신규 기준·실패 orange/무변화); 백엔드=dataset_id 필터 한 줄(비교는 프론트 계산); codex 3건(50캡 명시·공통 문제 0 경고·stale 비교 선택 prune)+e2e가 assert diff 상태 누락 드러내 3라벨 완성; 검증=결정적 회귀 시나리오(불가능 기준 추가→100%→0%) T1~T8 PASS·멱등 재실행; OUT=안정 case id·장기 추이·LLM-judge(3탄) [eval-trend,run-compare,regression-first-sort,assert-diff-three-states,deterministic-regression-scenario,dataviz-procedure] → retrospect 116
- 139 평가 3탄: LLM-judge(AI 판정 — 비결정 축) — **done**(retrospect 117): assert type llm_judge(기준 문장)·심판=기본 chat 모델 직접 해석(embedding 인질 해소)·OpenAI 호환 temp 0·첫 줄 PASS/FAIL 강제 파싱(순수 함수, 형식 이탈=실패)·fail-closed 전 지점·러너가 obs["judge"] 주입/scorer는 읽기만·성적표 관측에 "AI 판정"+이유 표시; codex 5건(구분자 위조 치환 봉인+실공격 핀 J4d·기준 평탄화(저작자=평가자라 자기조작 비위협 경계 명시)·chat만 해석·judge 5개/케이스+30s 캡·이름 해시 접미); 검증=단위/통합 17/17(실 모델 PASS/FAIL/위조 핀)+e2e 5/5×2; OUT=심판 모델 선택 UI·다중 심판 합의 [llm-judge,nondeterministic-axis,fail-closed,prompt-injection-seal,judge-time-cap] → retrospect 117
- 140 평가 4탄: RAG 컬렉션 평가 러너(kind='rag') — **done**(retrospect 118): eval_run_rag(공유 resolve_search_collection+search_collections — 평행 구현 0)·obs["rag"]{hits,top_score}·rag assert 3종(hits_gte/score_gte/source_contains, 부재=False fail-closed, NaN/범위 거부)·실행 분기(collection_id, 교차 대상 400)·UI(종류 선택 모달·컬렉션 대상 Select·kind별 기준 필터·성적표에 유사도+근거 파일)·"기준 추가" 기본값=필터 후 첫 옵션(e2e가 잡은 표시 버그); 검증=단위/실 컬렉션 11/11·e2e 7/7×2·codex 4건 반영; OUT=다중 컬렉션 동시·인제스트 진단·골든셋 생성 [rag-collection-eval,shared-resolver,rag-asserts,kind-branch,default-option-coherence] → retrospect 118
- 141 평가 5탄: 모델별 비교+격자 — **done**(retrospect 119): RunStartIn.models(≤6, 레지스트리 사전 검증 — 폴백 만회 차단)·EvalRun model_name/group_id(마이그레이션)·그룹 순차 실행·1개=단순 오버라이드 런·격자 탭(행=문제·열=모델·Different 필터·합계·그룹 전량 재조회)·조율형은 메인 모델만 교체(UI 힌트로 정직 경계); e2e가 "도구 흔적은 모델 축과 독립"(위임 선정=결정 로직) 반증→출력 기준으로 결정적 분기; 검증=단위/실행 8/8·e2e 7/7·codex 3건; OUT=프롬프트 축·병렬·그룹 간 비교 [model-matrix,promptfoo-grid,fallback-pre-validation,axis-sensitive-asserts] → retrospect 119
- 142 평가 6탄: 골든셋 자동 생성(컬렉션→RAG 문제집) — **done**(retrospect 120): eval_golden(청크 표본→기본 chat 출제→자기일관 기준 3종 자동 부여)·POST /eval/generate-dataset(즉시 문제집+백그라운드 생성·description에 진행/완료/실패 박제)·"컬렉션에서 생성" 모달·목록에 상태 부제; e2e가 실 모델 물음표 생략 습관 반증→의문형 어미 정규화(평서문 거부 유지)로 수율 5/5·self-consistency 100%; codex 4수정(빈 생성 실패 박제·좀비 생성 sweep·생성 중 실행 409·인젝션 방어)+3정직화; 검증=단위/통합 17/17·e2e 7/7; OUT=멀티홉·진화·expected 텍스트 생성 [golden-set,self-consistency,question-yield,zombie-generation-sweep] → retrospect 120
- 143 평가 7탄: 에이전트 문제집 AI 출제(도우미 1탄, 버튼식) — **done**(retrospect 121): suggest-cases API(기존 문제 보존·order_idx 이어붙임·메모리 락 게이트·description 상태+sweep 확장)·출제기(RAG형=컬렉션 골든+trace_has, 조율형 capabilities 포함/페르소나형=한 호출 N줄+llm_judge)·helper-status(기본 chat 실모델일 때만 — mock 정직 비활성)·"AI로 문제 채우기" 버튼(비활성 사유 툴팁); 검증=단위/통합 15/15(혼합·보존·자기일관 5/5·락·sweep)+e2e 6/6×3(엄격 K1 포함)+codex 2건; OUT=MCP 능력 출제·대화형·타 메뉴 도우미(백로그 방향) [eval-helper,ai-suggest,capability-dual-wiring,memory-lock,mock-gating] → retrospect 121
- 144 에이전트 메뉴 UX 보완 1 — **done**(retrospect 122, 실사용 피드백 3건): 편집 저장/취소→그 에이전트 드로워 복귀(활성화 동선)·"테스트"=플레이그라운드 이동+자동 선택(AdminShell prop→Playground initialAgentId)·리스트 hideBelow 제거+검색(이름/모델/소스)+정렬 Select(이름순 기본)+카운터; 검증=e2e 5/5×2(tsc 0, codex 생략 합의 — 순수 프론트 UX) [agents-ux,drawer-return,playground-handoff,list-search-sort] → retrospect 122
- 145 반응형 테이블(전 메뉴 공유 DataTable) — **done**(retrospect 123, 144 재보고 교정): tableLayout:'fixed'+셀 overflowWrap(내용이 표를 못 늘림)·카드 스택 전환점 md→lg(992, 태블릿 구간은 카드)·AgentsView 고정폭 5개 제거; e2e 실측 7/7×2(1400/1024=표 diff 0·820/600/390=카드·세션/평가 회귀 0) — 1차 overflowWrap만으론 intrinsic 890px(고정폭 합+min-content)을 e2e가 반증 [responsive-table,table-layout-fixed,card-breakpoint-lg,measured-diff] → retrospect 123
- 146 에이전트 리스트 2줄 행+라벨 교정 — **done**: '공유'→'공용'(소유권)·'공개'→'A2A 공개'(라벨 혼동 질문이 신호), DataTable subRow(행=tbody 2tr 한 몸·액션 rowSpan 2·모바일 카드 하단 합류)·1줄=이름/페르소나/버전/A2A공개/상태/액션·2줄=공용/소스/준수/MCP/RAG 태그; 실측 행당 2tr·스크롤 0·세션 표 무회귀 [two-line-rows,subrow,label-clarity] → (회고는 다음 피드백 배치와 묶음)
- 147 에이전트 최소 어휘+private 정책 — **done**(retrospect 124): 태그 public/private/external+A2A 스위치(켬/끔), may_use_agent 술어(external=항상·public=모두·private=소유자/특권), 게이트 6입구(목록·채팅·단건 조회·복제·메모리 읽기 3종 — codex High가 뒤 3개 우회 적발)·A2A 켜기 400·타인 private는 admin에게도 기본 숨김(필터 opt-in)·'타인' 판정 meId 비교(e2e 적발); 검증=verify_147 16/16·e2e 16/16×2·codex(비이슈: 브로커/A2A서버/평가/재개) [minimal-vocabulary,use-gate,404-fold,admin-default-hide] → retrospect 124
- 148 네이밍 규칙+식별 이름/별명 분리 — **done**(retrospect 126): 5종 name=식별(규칙 `^[가-힣a-z0-9.\-]+$`·참조 키)+alias=별명(자유) 분리(사용자 설계), 마이그레이션 자동 변환+config/casbin 참조 재작성(준수 행 우선 dedupe), 원격 유래=slugify+원문 별명, 페르소나·권한 rename/삭제 참조 가드(093 확장, capabilities 축 포함), 폼=식별(힌트+즉시검사)+별명·표시=별명 우선; 검증=verify 45/45·e2e 6/6·codex 6수정/2OUT; 이후 데이터 초기화(재시드·docs-kb 재적재·qwen 재등록) [naming-rule,alias,reference-integrity,rename-guard,slugify] → retrospect 126
- 149 엔티티 RAG — **done**(retrospect 127): 컬렉션 kind=document|entity 축, 엔티티=JSONL({metadata,data}) 업로드 1행=1청크·data 임베딩·meta JSONB·검색 hit meta 동반(엔드포인트+인-챗 도구), 선택 JSON Schema 행 전수 검증(fail-closed 행 번호 400, 정규식 키워드 미지원=ReDoS 경계, 명시적 null=제거), 캡 3종(행5000/텍스트8000/meta2000); 검증=verify 31/31·e2e 7/7·codex 5수정/2경계; OUT=DB직결·메타필터·upsert·엔티티 평가 assert [entity-rag,kind-axis,jsonl,metadata-search,json-schema] → retrospect 127
- 150 기본 모델 전환 UI/API — **done**(retrospect 128, 실사용 버그 #1): PUT /models/{id}/default(배타 이양)+기본 요약 배너+행별 지정 버튼(임베딩은 차원 경고 확인), kind당 기본 1개 부분 유니크 인덱스(DB 불변식, 해제 선-flush)+경합 409, models/providers 변이 전체 관리 게이트(member 403), kind 가드 400; 검증=verify 12/12·e2e 4/4(원복 실측)·codex 6수정 [default-model,unique-index,manage-gate,banner] → retrospect 128
- 151 MCP 도구 상세 정보 — **done**(retrospect 129, 실사용 버그 #2): discover가 toolsDetail(설명+args 파생 params) 동반, McpServer.tools_meta 스냅샷, 상세 드로어=도구 카드(설명·파라미터 표·grandfather)+'도구 정보 새로 탐색'(백엔드 복호 자격증명·enabled 교집합·참조 시 409), 직접 저장 경계 validator(100/500/30 캡), local-tools 메타 시드; 검증=verify 21/21·e2e 8/8×2·codex 3수정 [mcp-tools-meta,tool-cards,rediscover,validator-cap] → retrospect 129
- 152 외부 유래 재공개 금지 — **done**(실사용 #5): MCP 3입구(publish/create/update)서 external+published 400(끄기 허용)·source 생성 후 불변(세탁 봉인)·배선 술어 fail-closed(external이면 published 무효 — 오염 행도 런타임 차단), 에이전트는 기존 봉인(083) 핀; 검증=verify 11/11·codex 2수정·e2e 생략(UI 무변화 사유 기록); 경계=생성 시 provenance 자기 신고는 등록자 책임 [no-reexport,provenance,fail-closed] → retrospect 130(INDEX 줄 수록)
- 153 A2A organization 설정 — **done**(retrospect 131, 실사용 #6): app_settings 키-값 저장소 신설(닫힌 키 집합·키별 검증기·특권 게이트·무재시작 반영)+관리자 '설정' 메뉴, 카드 organization=저장값→env→기본(저장 여부로 분기·읽기 재검증 fail-safe); 검증=verify 17/17(카드 ASGI 실호출·폴백 3종)·e2e 9/9(PUT 미호출 감시 포함)·codex 2수정 [app-settings,a2a-org,found-vs-value] → retrospect 131
- 154 커스텀 에이전트 A2A 공개 — **done**(retrospect 132, 실사용 #3+백로그③): private↔public 전환(승격=소유 해제/강등=재스탬프+A2A 자동 off·no-takeover) + code(제1자 SDK) 1홉 중계 공개(a2a_client 재사용·x-my-agents-relay 루프 가드·external은 152 봉인), 드로어 공개 범위 박스+code A2A 스위치; 검증=verify 12/12(impl 루프백·실중계)·e2e 16/16(원복 실측)·codex High0/Low1 수정+경계 3건(셀프 승격=의도·1홉 표식=실수 방지·중계 Bearer=등록 토큰) [visibility,relay,promote-demote,loop-guard] → retrospect 132
- 155 플레이그라운드 A2A 루프백 테스트 — **done**(retrospect 133, 실사용 #8): 노출 에이전트를 플레이그라운드에서 직접(/chat)·A2A 경유(/agents/{id}/a2a message/stream) 두 방식으로 테스트(헤더 Segmented 토글)+클라 A2A 전송(streamChatA2A·status-update 프레임 파서·평문 JSON-RPC 에러 바디 폴백)+154 후속 배지 게이트 정직화(source ui→ui|code); A2A는 단발·비영속(세션/trace 미전달) 인라인 배너로 정직화; 검증=verify 6/6(ui·code stream·루프가드-32000·404 게이트)·e2e 4/4·codex High0/경계2(세션혼입·trace stale 정직화) [playground,a2a-loopback,stream-parser,honest-boundary] → retrospect 133
- 156 내부(커스텀) MCP 외부 공개 — **done**(retrospect 134, 실사용 #4): 우리가 외부 MCP 등록해 쓰는 것의 반대편 — LangChain @tool→to_fastmcp→FastMCP를 /_served/mcp/{name}에 서빙(외부가 MultiServerMCPClient로 붙음, mock_mcp 동형), source=custom 신설·공개 가드(요청마다 published+source=custom+레지스트리, 아니면 404)·external 서빙 불가(152); UI 커스텀 배지·서빙 URL·publish 스위치; 검증=verify 9/9(루프백 add(2,3)=5 실호출)·e2e 4/4·codex High1(source=custom 사용자 선점→시스템 전용+예약이름 400)/Med1(빈테이블만 시드→멱등 reconcile)/Low1(무인증 서빙→도구 allowlist 부팅강제) [custom-mcp,served-mcp,to-fastmcp,provenance-system-only,idempotent-reconcile,unauthenticated-serving-invariant] → retrospect 134
- 157 A2A skills 확장 — **done**(retrospect 135, 실사용 #7, B 시리즈 마지막): A2A 카드가 chat 하나만 광고하던 걸 에이전트의 실제 능력(MCP 도구·서브에이전트 위임·RAG)으로 확장(_agent_a2a_skills, config.mcps+capabilities broker 파서 재사용, 살아있는 참조만·민감정보 미포함·50캡)+플레이그라운드 A2A 모드 광고 스킬 칩; 검증=verify 9/9·e2e 6/6·codex High1(ui 미노출 서브에이전트 이름 누출+거짓 위임→remote+endpoint만 광고)/Med1(stdio 광고→http만 runtime일치)/Med2(빈 enabled=전체→tools 스냅샷)/Low1(칩 stale→deps 시그니처) [a2a-skills,card-advertise,real-capability-not-configured,existence-nondisclosure-public-surface,runtime-semantics-replication] → retrospect 135
- 158 회상 진단이 실패를 "정상·0건"으로 위장 (버그) — **done**(retrospect 136, 실사용 버그: "기억 있는데 유사도 검색 안 됨"): 근인=mem0 2.0.7 search의 숨은 기본 threshold=0.1을 우리가 안 넘겨 상속→저유사도(arctic query-prefix 미주입 등) 전부 컷→0건·예외없음(진단 "정상" 위장); +M1 예외삼킴이 같은 화면. 수정=recall_diag를 threshold=0으로 top-k 표시(UI 약속대로 낮은점수까지→자가진단)+저장건수(list_page.total) 진단+Mem0Backend.search 전축실패 raise/챗파사드 흡수[]/브로커 InvokeResult.error/로그 타입명만(비밀 마스킹); 검증=verify 12/12(M1표면화·M2재현·로그누출 V3b/V5c)·e2e PASS(results3·stored3)·deep-reasoner 근인확정(M2 유력)·codex High1(로그누출)/Med1(agents.py stored) [recall-diag,hidden-library-default,threshold-cut,two-failure-modes-same-screen,honest-diagnostic-shows-promise,mask-logs] → retrospect 136
- 159 mem0가 임베더에 차원을 강제해 self-hosted 임베더가 400 (버그) — **done**(retrospect 137, 스펙 158 후속): 158이 위장 걷어내자 사용자 배포본이 진짜 에러 표면화(Error 400 snowflake only supports [256] matryoshka). 근인=`MEM0_EMBED_DIMS`가 컬럼차원+임베더요청차원 두 축 겹쳐 씀→mem0가 dimensions=1024 강제전송→snowflake(256만 명시허용) 거부. 수정=네이티브 차원 probe(RAG 방식)해 네이티브==컬럼이면 미전송(snowflake 안전)·≠면 컬럼길이 전송(text-embedding-3 회귀 방지); 정적 기본값은 어느쪽이든 한 케이스 깸→probe가 답. 비파괴(11건 네이티브 1024=컬럼, 재인덱싱 불요). 검증=verify 15/15·codex 2R(정적미전송 회귀→probe전환, 캐시키 api_key·probe 타임아웃·env검증 반영, 불가조합=경계문서화) [mem0,embed-dims,forced-dimensions-param,column-vs-request-axis,probe-not-static-default,honest-diagnostic-breeds-next-fix,user-correction-halts-destructive] → retrospect 137
- 160 mem0 임베딩 query/passage 접두어 주입 — **done**(retrospect 138, 158·159 OUT 후속): 비대칭 모델(e5·arctic)이 검색어=query·문서=passage 접두어 요구하나 mem0가 memory_action 무시하고 원문 전송. 측정 우선(로컬 e5 실측: 접두어 효과 미미 +0.062→+0.063, 검색만 접두어 무해 +0.069)→하드코딩 대신 **설정형**(env MEM0_QUERY_PREFIX/MEM0_PASSAGE_PREFIX, 기본 no-op). embed/embed_batch 래핑해 action별 접두어(래핑시점 캡처). arctic=query만·passage raw라 기존 11건 재인덱싱 없이 검색만 접두어(비파괴). 접두어는 embed 입력만·payload 원문 유지(누출 없음 측정). 검증=verify 9/9·codex 결함0(payload분리·이중래핑없음)/경계1(passage 기존DB 혼재=arctic query-only 비파괴 문서화) [embed-prefix,asymmetric-model,measure-before-assume,non-destructive-via-measurement,transform-input-not-stored-data,configurable-default-noop] → retrospect 138
- 161 페르소나 수정 반영: 스냅샷 유지 + 명시적 동기화 — **done**(retrospect 139, 실사용 피드백): 에이전트가 페르소나를 저장시점 스냅샷(agent.persona)으로 복사·런타임 서빙→편집이 반영 안 됨·오래됨 표시 없음. 사용자 반론(복사=영향도격리 좋음, 반영흐름이 문제)로 라이브참조 기각→템플릿→인스턴스 동기화: personaStale 가시화+refresh(에이전트쪽)/apply(페르소나쪽 선택반영), 전부 can_manage 게이트. 검증=verify 22/22·codex High(usage 라우트 may_use_agent 누락→private 에이전트 누출, 테스트도 누출 고정중이었음→필터+교정)/Med(TOCTOU 요청시점반영 경계)·프론트 tsc0·브라우저스모크. [persona-snapshot,template-instance-sync,user-tradeoff-reshapes-design,derived-route-inherits-visibility-gate,self-test-locks-in-leak,per-resource-write-gate] → retrospect 139
- 162 mem0 테이블 미생성 시 목록 502 — **done**(retrospect 140, 실사용 버그 초기화 직후 `relation "mem0_memories" does not exist`): mem0가 첫 저장 시 lazy 생성하는 테이블을 list_page(직접 SQL, 스펙 127 페이지네이션)가 우회 조회→UndefinedTable→502. 검색은 mem0 초기화가 CREATE IF NOT EXISTS로 되고 목록만 깨지는 불일치. 수정=UndefinedTable만 잡아 빈 상태(진짜 0건, 실패 아님)로—"실패≠0건"(158)의 대칭 함정(빈 상태를 실패로 위장), catch 좁게해 158 안 되돌림. 읽기 부수효과0(테이블 생성 안 함). 검증=verify 3/3 비파괴(별도 스키마)·codex 결함0/Low경계1(42P01 미초기화vs drop 미구분=의도된 tradeoff 문서명시). [mem0-lazy-table,failure-vs-empty-symmetric,bypass-loses-invariant,read-path-no-side-effect,honest-boundary-doc] → retrospect 140
- 163 RAG 컬렉션 사용 공개(publish) — **done**(retrospect 141, 실사용 "공개 설정 없어 공유 못 함"): 컬렉션 사용은 agent_may_wire(113) 게이트인데 chat.py가 published를 항상 False 하드코딩→소유자만 사용. MCP처럼 published 플래그+`PUT /collections/{id}/publish`(소유자만)+chat.py c.published 전달. "사용만 공개"(수정·삭제는 소유자, owner⟂published). codex High=직접 `POST /search`가 게이트없이 청크 유출(배선만 막고 직접입구 뚫림, installed≠covering)→may_use_collection 신설해 search+documents 404-fold. 세 축 분리(가시성 전역·관리 owner·사용 published). 검증=verify 16/16·프론트 tsc0·스모크. [collection-publish,gate-coverage-consistency,use-axis-orthogonal,mirror-precedent] → retrospect 141  ⟨※ 스펙 172로 되돌림 — published 제거⟩
- 164 프로바이더 연결 테스트가 항상 "모델 미발견" — **done**(retrospect 142, 실사용 버그): 프로바이더 레벨 테스트는 `_probe(...,"","chat")`로 빈 model_id를 넘기는데 `available = model_id in ids if model_id else False`가 빈 model_id를 무조건 False→항상 "미발견"(등록 화면엔 모델 주르륵). 수정=빈 model_id면 /models 목록 개수로 판정("모델 N개 발견"/"목록 비어있음"), 지정 시만 정확일치 유지. 스펙 158 계열(정상/미지정을 실패로 위장). 프론트 무변경(ProbeRow가 detail 표시). 검증=verify 4/4 stub 서버. [diagnostic-honesty,name-the-state,reuse-empty-arg-footgun] → retrospect 142
- 165 다디바이스 UI 오버플로 감사 하네스(검증 축2 파일럿) — **done**(retrospect 143, 사용자 "자가검증 관대·꼼꼼히 검증 방법?"): 자가검증 관대함=구조(같은저자 사각+합격선없음→재량). 처방=측정+적대타자. `tests/browser/ui-audit.mjs`=14화면×3뷰포트(360/768/1280) 전수 순회, 판정=`rect.right>vw`(우측경계 초과)+pageScroll, 오탐컷(off-screen `left>=vw`·antd탭 `.ant-tabs-nav` ⋯). 도구가 좁히고 육안이 소수 확정(42→진짜1건: mobile/batch 버튼행 잘림→`<Space wrap>`→0 FAIL). 교훈=과잉억제 필터가 진짜 버그(batch) 삼켜 뺌(회고133). 축1·3도 같은 골격. [ui-overflow-audit,tool-narrows-eye-confirms,over-suppression-is-new-leniency,measure-what-user-feels] → retrospect 143
- 166 사용 시나리오 감사(검증 축1) — **done**(retrospect 144, 축2 골격 적용): 여정 아티팩트(`tests/browser/scenarios.md`: 페르소나+단계+기대결과)+러너(`scenario-audit.mjs` Playwright 실행+단계assert+스크린샷+scn- 접두 엔티티 API 정리). J1 저마찰 생성→대화·J2 지식 붙이기. 결과=7단계 전부 ok·막힘0(J1 초안 대화 감시점=오히려 잘 처리: 즉시 테스트+"초안/미반영 초안" 배지). 교훈=clean pass도 정직한 산출물(적대+기대결과 박제라 믿을 수 있는 통과)·clean이어도 육안 검증·소프트 flag는 코드로 추적(모달잔류=setFormOpen(false) 확인→페이드 아티팩트). fast-worker 기계구동+메인 판단 분업. [scenario-audit,clean-pass-is-honest-output,eyeball-even-when-green,trace-soft-flag-to-code] → retrospect 144
- 167 문구·평이함 감사(검증 축3) — **done**(retrospect 145, 축2·1 골격 적용): 루브릭(`tests/copy-rubric.md`)+적대적 리뷰어(deep-reasoner "IT 모르는 일반인" 연기)로 사용자 노출 문자열 전수 채점. 결과=오탈자 0(카피 깨끗)·진짜 문제는 **내부 산출물 누출**(오류창 `(스펙 089)`·`impl`, 버튼 `GET /models`, `2차 스펙`, `(kind=embedding)`, `ready` enum, `Fernet`)=자가검증이 못 잡는 종류. 12건 수정(막힘8+거슬림4). 도메인어는 없애기보다 코드부스러기만 제거. 검증=tsc0·회귀grep(누출어 0)·ui-audit 재실행 0 FAIL(감사끼리 상호검증). [copy-clarity,internal-artifact-leak-worst-not-typos,gloss-dont-delete-domain-terms,audits-cross-verify] → retrospect 145
- 168 드로어·모달 오버플로 감사(축2 확장) — **done**(retrospect 146): ui-audit(165)가 top-level만 봐서 오버레이로 확장. `ui-audit-overlays.mjs`=MEASURE 재사용, 7 오버레이(생성모달·상세드로어·페르소나편집·오버라이드/인스펙터)×2뷰포트(360/1280) 열고 측정, 생성모달은 열기만(엔티티0). 결과=14/14·FAIL0(오버레이 반응형 건전, 육안 재확인). 부산물=learning 142(antd 아이콘버튼 접근성이름 "edit 편집"→exact:true 매칭 깨짐·모바일 DataTable=카드렌더). 소프트 UX(모바일 현재메뉴 재탭 백드롭)는 백로그. [overlay-overflow,clean-pass-maps-strength,test-infra-trap-as-asset,soft-ux-report-not-fix] → retrospect 146
- 169 감사 커맨드화 + 상시 루프(검증 아크 캡스톤) — **done**(retrospect 147): 축1·2 감사(165·166·168)를 `tests/browser/audit-all.mjs`로 묶어 한 명령 전수 실행+종합 종료코드+프리플라이트(서버 down이면 exit2 중단=빈 화면 오판 방지). `.claude/skills/ui-audit/SKILL.md`=`/ui-audit`+트리아지 규율(도구좁힘·육안확정·과잉억제금지·soft flag 코드추적·clean pass도 정직) 문서화. 카피 축(축3)은 판단형이라 별도 안내(deep-reasoner+copy-rubric.md). 검증=3 harness 구동 exit0. [audit-command,rerunnable-compounds,preflight-is-honesty-gate,numeric-unlocks-autonomy,skill-packages-discipline] → retrospect 147
- 170 평가 도구 정직성(평가 통과 ≠ 도구 호출) — **done**(retrospect 148): 구남님 관찰 "평가 통과인데 도구 안 불림"의 근인=거짓 초록이 출제 시점에 태어남(trace_has는 이미 있었으나 기본 assert=no_error+output_nonempty뿐·아무도 도구검사 안 넣음). admin EvalView.tsx만(백엔드 무변경, 성적표가 이미 obs.trace_nodes 보유): ①성적표 "도구 미사용" 배지+집계(firedTool=rag:/mcp:/memory: 접두, 통과인데 흔적0→표시, 기존 결과 소급) ②출제 편집기 안내(trace_* 없으면 비차단 경고). 검증=shot-eval-tool-honesty-170.mjs PASS(iff 일관성·mock 도구미호출 재현). [eval-tool-honesty,false-green-born-at-authoring,nudge-defaults-toward-honesty,retroactive-signal,neutral-not-bug-verdict] → retrospect 148
- 171 승인 재개 impl-drift 명시 가드 — **done**(retrospect 149): 승인 로직 적대 검토(deep-reasoner) 결과 인가/이중실행/오라클 다 견고, 유일 Low=config-drift 가드 주석 "현 출하엔 HIL 커스텀 없어 미발생"이 stale(orchestrate 2종+DefaultUiAgent 다 supports_hil=True로 출하→impl-A→B 교체 재개 도달 가능, 미정의 동작 의존). Approval.impl 스냅샷 컬럼+생성 시 `ctx.get("impl") or ""` 스탬프+재개 시 `_impl_drifted` 대조해 다르면 graceful 거부(부수효과 이전). 마이그레이션 e5f6a7b8c9d1(적용). 검증=verify_171(단위10/10)·verify_116 회귀·deep-reasoner 재검토("새 구멍 없음"). [approval-hil,impl-drift-guard,conditional-safety-claim-is-timebomb,replace-undefined-behavior-with-explicit-guard] → retrospect 149
- 172 RAG 공개/비공개(published) 제거 → 전부 공용 — **done**(retrospect 150, 163 되돌림): 사용자 교정=public/private를 *수정 가능*하게 원했는데 공개/비공개 *상태 추가*로 어긋남→개념 전면 제거. 백엔드=Collection.published 컬럼·may_use_collection·/publish 엔드포인트·게이트 제거(마이그레이션 f6a7b8c9d1e2 drop), chat.py rag 배선 게이트 제거(전부 공용). 접근 모델=사용(검색·문서목록) 로그인 누구나(익명 401)·관리(수정/삭제/인제스트) 소유자만 유지. 프론트=publish 토글+OwnerTag(public/private 라벨) 제거(육안이 잡음). 검증=verify_172 7/7(비소유자 사용 200·관리 404·익명 401)·deep-reasoner 적대(안전). [collection-visibility,user-intent-editable-not-new-state,dont-mirror-pattern-blindly,eyeball-catches-what-check-misses,removal-must-retire-its-tests] → retrospect 150
- 173 RAG 컬렉션 별명(alias) 편집 노출 — **done**(retrospect 151): 스펙 172 교정서 드러난 실제 의도(수정 가능한 표시 속성)를 최소 충족. 백엔드(update_collection이 alias 세팅·소유자만)·API 이미 있었고 갭은 UI뿐 — DocsDrawer "컬렉션 설정"에 별명 Input 추가+savePolicy에 `alias: alias.trim()`(비우기=""전송, null은 미변경). 배선은 name(슬러그)이라 별명 편집 무영향(안전). 검증=브라우저 e2e 4/4(노출·프리필·저장·비우기 원상복구)·tsc. [collection-alias,not-missing-just-unexposed,identity-vs-display-separation,clear-via-empty-not-null] → retrospect 151
- 174 프로바이더·모델 메뉴→"관리자" 그룹 이동 — **done**(retrospect 152): 메뉴 감사(구남님 "관리자 하위가 관리자 전용 맞나?"). 관리자 그룹 5개는 서버 require(...,manage/run) 강제 확인(함정 없음); 역방향 불일치=프로바이더·모델이 상단인데 변이 전부 _manage(비관리자 403·보기만)→관리자 그룹으로 이동(사용자 결정). GET /models는 _auth라 에이전트/RAG 폼 모델선택 무영향. AdminShell.tsx menuItems만 변경. 검증=사이드바 순서 4/4·tsc. [menu-audit-both-directions,menu-visibility-not-a-gate,read-write-gate-split] → retrospect 152
- 175 RAG 생성 폼: 임베딩 상위+기본 프리셀렉트 — **done**(retrospect 153): 구남님 요청(임베딩 불변이니 모델설정 상위로+기본모델 선택). CreateModal 재배치=불변 3인방(이름→종류→임베딩) 위·편집가능(별명·설명) 아래, 임베딩 라벨 빨강 "(생성 후 변경 불가)". useEffect가 `models.find(m=>m.is_default)` 프리셀렉트(없으면 단일모델 fallback)·옵션 "· 기본" 표기. 프론트만. 검증=e2e 5/5(순서·불변힌트·기본프리셀렉트)·스샷. [form-order-reflects-reversibility,prefill-server-default-cuts-mistakes,antd6-modal-class] → retrospect 153
- 176 컬렉션 편집 버튼(별명·설명·청크) 발견성 — **done**(retrospect 154): 구남님 "별명 수정 어디 있나요?"(173이 문서관리 드로어 맨 아래에 묻혀 못 찾음). 사용자 결정=행에 편집(연필) 버튼. EditModal 신설(별명·설명·청크, 식별이름·모델·차원 불변이라 미노출)·행 액션에 연필(can_manage 게이트)·문서 드로어의 "컬렉션 설정" 패널 제거(편집 일원화). 백엔드 무변경. 검증=e2e 7/7·edit 아이콘 MAP 확인. [where-is-it-signals-discoverability,single-edit-entry-point,verify-icon-key-in-map] → retrospect 154
- 177 권한/승인 목표 모델: 도구 기본 정책+에이전트 오버라이드 — **완료**(P1/P2/P3, retrospect 155/156/157): 진단=권한이 6곳 흩어짐+죽은 설정 2개(config.permissions[]·Permission.approver 런타임 미강제)+관리자 기본정책 단일지점 부재+도구 두 경로 게이팅 상이. 모델=UI에 3개념(능력/역할/승인)만; 도구 기본 승인정책(tools_meta[tool].approval={required,approver}, admin)→에이전트 오버라이드(신규 config.toolPolicy)→단일 리졸버(runtime+broker 공유, _APPROVAL_ACTIONS 하드코딩 제거). 결정=D1 toolPolicy 신규·D2 강화+완화 둘다(완화는 특권게이트 후보)·D3 approver 도구정책 일원화(Permission.approver 제거). 단계=P1 도구기본+리졸버+admin UI→P2 오버라이드+승인자연결→P3 능력부여 UI+죽은설정 정리. Cedar 폐기·ABAC 추후 [permission-model,default-plus-override,dead-settings,single-resolver] → deep-reasoner 매핑
- 178 멤버 자율 평가(본인 에이전트 한정, 소유 기반) — **완료**(P1~P4, retrospect 158): 평가를 admin 전용서 에이전트/컬렉션과 동일 소유 기반으로 개방. 데이터 모델 준비됨(EvalDataset·EvalRun owner_id 존재, 마이그레이션 불필요). require("eval")→current_principal+ownership.py 술어(assert_may_manage 404-fold·may_use_agent 구멍#1 봉합·can_manage 필드), 비용가드, 프론트 eval "관리자"→"도구"(일반접근)+can_manage 게이트+"결과 공개" 안내. 결정=D1 열람공개·관리만 소유자(컬렉션 패턴)·D2 동시성+작업량 상한. **P4 codex 하드닝**: 적대검증이 비용경계 결함 4건 포착→봉합=①generate-dataset 무가드(동시 배경작업 상한 신설)②suggest-cases 락이 배경태스크 안=TOCTOU(엔드포인트 동기획득)③run 동시성 check-then-insert TOCTOU(pg_advisory_xact_lock 직렬화)⑤work가 judge 누락(models×(cases+judges)로 정직화). ⑥RAG컬렉션 사용권·⑦obs공개는 by-design(공개읽기 정책, 교차private 누출 없음)로 문서화. 검증 verify_178 24/24+브라우저 [self-serve-eval,read-public-manage-owner,may-use-agent-gate,cost-guard,guard-all-sibling-entries,lock-must-be-synchronous,advisory-lock-for-check-then-insert] → codex 적대검증
- 179 mock 모델 도구 트리거(HIL 승인 대화 실습) — **완료**(retrospect 159): mock 챗이 툴콜 미지원(평문만)이라 승인 게이트 도구(local-tools.delete_record)를 플레이그라운드 대화로 못 띄우던 것. mock_remote.py에 개발용 트리거 추가=바인딩된 도구 있고 마지막 user 메시지가 트리거 키워드(삭제/지워/제거/delete) 매치면 그 도구의 결정적 tool_call emit(비스트림+스트림), 그 외엔 기존 평문(무회귀). 재개 후 턴(messages에 role=tool) 평문화로 무한 tool_call 루프 차단. 실제 바인딩명은 네임스페이스 붙어(local-tools__delete_record) suffix 매칭+실명 emit. 검증 verify_179 13/13+e2e(채팅→승인대기→승인페이지 pending→승인 및 재개→실행). 어드민 승인=delete_record 기본 approver=admin, 본인 승인=approver=self 오버라이드(단 슈퍼유저는 self 분기 안 탐→member 계정 필요). **P3(실사용 제보, retrospect 160)**: 2브라우저서 승인 후 요청자 채팅이 "승인 대기"에 멈추던 버그(백엔드는 재개·영속하나 라이브 push 없음 §7)→onApproval 콜백+승인 폴링(listApprovals 2.5s)+세션 재로드로 자동 갱신, 레이스(status 먼저 커밋 후 persist)는 메시지수 증가로 게이트 [mock-tool-trigger,hil-approval-demo,namespaced-tool-name,guard-preserve-plaintext-contract,requester-live-update-polling] → 직접 구현+e2e
- 180 대화 내 인라인 승인(본인 승인은 그 자리서) — **완료**(retrospect 161): 사용자 질문서 출발("본인 승인=요청자=승인자인데 왜 승인 메뉴로? 대화서 바로 하면 되지"). 처리 가능한 사용자면 대화의 승인 대기 자리서 바로 승인/거부(self=요청자 본인 항상·admin=슈퍼유저), 승인 메뉴는 남의 것 검토용 유지. 5파일 배선=chat.py(프레임에 approver+wait_msg "관리자 승인" 하드코딩→approver 반영)→api.ts onApproval(approver)→Playground(canResolve=self‖superuser, resolvePending: approve는 폴링 반영·reject는 대기버블 "거부됨"+즉시해제)→DebugChat(인라인 승인/거부 바+입력 잠금 안내)→AdminShell(is_superuser 전달). **발견=폴링 baseline 레이스**: 179 P3 완료감지가 "메시지수>baseline"인데 baseline을 첫 틱(2.5s 뒤) 포착→인라인은 즉시 승인이라 첫 틱 때 이미 완료→baseline이 완료-후 카운트 잡아 n>n=false 영영(2브라우저는 승인 느려 우연히 초록). 수정=첫 틱 즉시(0ms) 발화로 대기시점 포착, 곁효과로 179 단일브라우저 한계(승인 이동→언마운트)도 인라인엔 무관 해소. 인가는 서버 _may_resolve 그대로(버튼은 표시용·권한없으면 403). 검증 verify-inline-approval 6/6 [inline-approval,action-location-by-actor,baseline-capture-before-event,hardcoded-label-siblings] → 직접 구현+e2e
- 181 승인 내역(감사) 화면 — **완료**(retrospect 162): 실사용 제보 "승인한 내역 볼 화면 없다"(승인 화면이 pending만). 승인 화면에 대기 중/처리됨 탭(Segmented), 처리됨=결과(승인/거부)·처리 시각·처리자(본인/관리자) 읽기전용 카드. resolve가 status만 갱신하던 걸 resolved_at·resolved_by도 같은 원자 UPDATE에 스탬프(마이그레이션 a181c2d3e4f5, nullable=레거시 하위호환). ApprovalOut에 resolvedAt·resolvedBySelf(resolved_by==user_id면 본인, 원 UUID 미노출). 소유 스코프는 list_approvals 이미 반영(자기 것만)→이력 자동 상속. RBAC: 읽기전용(새 write 입구 0)·resolve 입구 그대로. 검증 e2e 4/4+마이그레이션 그래프(단일 head·중복0·체인) [approval-audit,record-who-when-at-transition,nullable-backfill-legacy-blanks-ok] → 직접 구현+e2e
- 182 코드 정리 훑기(죽은 코드·오너십 통합·중복·재발방지) — **완료**(retrospect 163): "코드 정리 좀"→deep-reasoner 2개(프론트/백엔드 병렬)+기계스캔(tsc noUnusedLocals·pyflakes). 소스 매우 깨끗, 정리감 소수. 적용=①프론트 죽은 API 8(getAgent·listAgentImpls·listBatchJobs·testSavedModel·updateModel·listUserIds·listAgentMemory·listUserMemory)+interface AgentMemory+DemoBanner+AgentsView import 2, 백엔드 죽은 함수 3(catalog.stats·eval_harness.failures·ownership.owner_scope_filter) ②오너십 단일규칙 통합(chat._next_owner→ownership.next_owner, ""빈문자열 경계서 fail-closed 정본 채택+verify_068/112 경계테스트) ③ApprovalsView 카드 중복 추출(CardHeaderRow·PermActionTags, 픽셀보존+시각회귀0) ④tsconfig noUnusedLocals 켜 재발장치화. **실행 정정**: 에이전트가 "죽음"이라던 3건(may_use·_reset_cache·_set_allowed_hosts_for_test)이 실제 테스트 사용→삭제 직전 재-grep으로 보존, _next_owner 삭제가 verify_068 import 깸→재배선. 검증 tsc0·verify_068/112·시각캡처 [dead-code-sweep,ownership-consolidation,truth-table-diff-on-merge,reverify-before-delete] → deep-reasoner 위임+메인 재검증
- 183 Agent.source 제1자/제3자 축 술어(OCP 완성) — **완료**(retrospect 164): 스펙 182 리뷰가 짚은 유일 OCP 위반 봉합. source의 remote/local 축은 is_remote_source로 통일됐으나 자매 축(제1자 ui/code vs 제3자 external)은 술어 없어 리터럴 ~8곳 산재(a2a 노출·재공개 등 보안 게이트 걸림→새 source 시 shotgun surgery·조용한 오동작). runtime.py에 is_first_party/is_third_party 추가 후 Agent.source 리터럴 7곳 치환+라우팅 1곳(remote 축이라 is_remote_source 재사용). **chat.py:274 제외**=McpServer.source(local/custom/external, 다른 어휘)라 사이트별로 읽어 오분류 거름. 행동보존(is_third_party≡==external, 3-source 진리값 동일)—stash 대조로 verify_083(5)·152(1) 실패가 pre-183에도 동일=내 무관 증명. 검증 술어단위·147(16/16)·161·154(12/0)·112. 발견=152 V4 stale(154가 code노출 허용으로 바꿈)·083 인프라의존(백로그) [first-party-predicate,apply-existing-pattern-to-sibling-axis,same-literal-different-vocabulary,prove-refactor-changed-nothing] → 직접 구현+회귀
- 184 useAsyncData/runWithToast 공용 훅 — **완료**(retrospect 165): 스펙 182 리뷰가 짚은 프론트 복붙(단발페치+로딩+에러+alive 가드 .catch34곳·message.error83곳 / mutation try-catch-토스트 51곳)을 훅 2개로 흡수. `admin/src/hooks.ts`=useAsyncData<T>(fetcher,deps,opts)→{data,loading,error,reload}(stale-race 가드 흡수, 에러표면은 onError로 소비자 소유=토스트vs지속Alert 다양성 보존)+runWithToast(fn,{success,error})→bool(모달닫기·reload 분기, 버튼로딩은 호출자). **HoC 불필요 결론**(공유대상=트리아닌 로직→훅, HoC니치는 AuthGate render-prop·PagedListShell 제네릭이 이미 덮음). 소비자 3곳 변환 검증(AllowedHostsView=mutation+reload, MemoryView 2탭=표시페치+파생필터). 배운것=폼시드 패턴(SettingsView)은 훅 부적합(페치→가변폼초기값은 되레 코드증가, 데이터 수명 봐라)·자가하네스 과엄격하면 코드아닌 하네스 보정(401·antd경고=사전노이즈 명시제외). tsc0·browser verify-hooks-183 ALL PASS·스샷2 [useAsyncData,hook-not-hoc,form-seed-pattern-unfit,relax-overstrict-harness] → 직접 구현+3소비자+브라우저
- 185 AgentsView.tsx(2128줄) 분해 — **Phase A 완료**(retrospect 166): god component서 서브컴포넌트 8개를 views/agents/(+detail/)로 파일분리→AgentsView 2128→747줄. deep-reasoner 정밀매핑(대부분 로직수술 아닌 이동—prop-driven 자기완결)후 fast-worker 위임, 메인이 tsc+브라우저 검증. 생성=types/primitives/PersonaStaleNote(순환방지 독립)/AgentForm/ConnectAgentModal/AgentDetail/detail{Code,External}. 검증=tsc0(noUnusedLocals)·순환 정적스캔(역참조0)·browser verify-agentsview-decomp-185 ALL PASS(리스트·3드로어 ui/code/external·생성폼·연결모달·pageerror0)·스샷. 함정=shared.Drawer는 antd아닌 커스텀(className/role 없음, 닫힘=translateX off-screen)→.ant-drawer-* 셀렉터 실패, 고유라벨 Playwright 가시성으로 판정. **Phase B(useAgents 훅) 완료**(retrospect 167, 사용자 진행선택): 데이터 오케스트레이션(목록·레퍼런스·12뮤테이션)을 useAgents.ts(120줄)로, 747→684줄. runWithToast 미채택(성공=커스텀 플로팅Alert이라 적용시 행동변경)·notify비대칭 보존·폼시드 잔류. 검증=실 생성→삭제 왕복(verify-useagents-185b, 커스텀토스트 보존·list반영·pageerror0)+Phase A 무회귀 [decomposition-is-mostly-file-moves,tsc-green-not-done-browser-confirms-circular,custom-drawer-selector-trap] → 매핑+위임+tsc+브라우저
- 186 antd 일관성: 토스트 표준화+비-antd 감사 — **완료**(retrospect 168): 사용자 발견=AgentsView 성공토스트가 커스텀 플로팅Alert(antd6포팅 c9848df 잔재)인데 다른 ~10뷰는 표준 message.success=의도없는 불일치. ①토스트 표준화(AgentsView 13 setToast→message.success·684→658줄, 감사중 ApprovalsView 동일패턴 발견해 message.success/warning 통일, state/타이머/Alert렌더 제거, 전앱 잔여0) ②비-antd 감사 분류: 정당(shared.DataTable↔Table/Drawer↔Drawer/Desc↔Descriptions=자체 미니 디자인시스템층 사유있음[2줄subRow·모바일드로어·컴팩트]·EvalMatrix table=매트릭스·AdminShell fixed=모바일셸·OverviewView button=a11y통계카드·PagedMemoryList divonClick=전파가드 falsepos)/사고(커스텀토스트 2건=수정). 검증 tsc0·verify-useagents-185b ALL PASS(표준 .ant-message)·ApprovalsView 렌더 pageerror0·스샷 [toast-to-standard-message,preserve-isnt-always-right,grep-siblings-when-fixing-one,audit-classifies-justified-vs-accidental] → 직접+전수grep+브라우저
- 187 커스텀 컴포넌트를 antd로 통일(단계별) — **Phase 1(Drawer) 완료**(retrospect 169): 스펙186 감사가 짚은 shared.tsx DataTable/Drawer/Desc from-scratch 재구현을 antd로 통일(사용자 결정=antd 스타일·a11y·키보드·반응형을 재구현않고 취함). 전략=shared 구현만 antd로+prop API 유지→70곳 호출부 무변경. Phase1: shared.Drawer(커스텀95줄)→antd Drawer 래퍼(open/title/width/onClose/footer 유지), 포커스트랩·Escape·스크롤락·애니메이션은 antd, shared.tsx 575→480줄, CloseOutlined 제거, 모바일100%·footer우측·destroyOnHidden 보존, 닫기X 우측→좌측(antd기본). 검증 tsc0·드로어6사이트 전수(에이전트3 decomp-185 + 컬렉션/세션/블록 antd-drawer-187)·pageerror0·스샷. 166 커스텀Drawer 셀렉터함정도 해소. **Phase2(DataTable) 완료**(retrospect 170): 데스크톱 표 분기만 antd Table(모바일 카드분기=스펙145 의도설계라 유지, 통일단위는 컴포넌트 아닌 분기), subRow→expandable(controlled expandedRowKeys=전행 — defaultExpandAllRows는 최초렌더만이라 생성행 누락 함정, showExpandColumn:false)+CSS 두 규칙으로 2줄 한몸 룩 보존(구조 재조립 대신 스킨), hideBelow 필터 유지, shared.tsx 누적 575→409줄, 검증=tsc0·브라우저3종 전수·스샷(2줄행·컬렉션·모바일카드). **Phase3(Desc) 완료**(retrospect 171, 사용자 전환선택): 50곳 전수조사서 용도 2종 발견—표시 42곳→Descriptions(column1·items, 조건부=스프레드, width커스텀은 표기반 자동정렬로 불필요화, 비-Desc 낀 지점서 그룹분할)/폼행 8곳(허용호스트2·배치6, 라벨+Input)→Form 레이아웃전용(component=div·name미지정=상태 controlled 유지, Descriptions에 입력은 의미론위반). shared.Desc 삭제, shared.tsx 누적 575→384(−191줄). 검증 tsc0·브라우저3종·스샷. **187 아크 완주**—잔여 커스텀은 전부 정당(모바일카드·도메인·토큰래퍼) [swap-shared-internals-keep-api,custom-mimicking-antd-is-swap-signal,antd-adoption-has-minor-visual-shifts] → 래퍼교체+전수브라우저+스샷
- 188 산출물형 에이전트 뼈대(ArtifactAgentBase) — **완료**(retrospect 173·174): "산출물을 만들기 위해 노력하는 에이전트"를 구체 로직 아닌 **뼈대**로(사용자 교정 — produce(로직)→Artifact(산출물)→sink(처리), 알맹이는 유저 정의). 스펙 102 패턴=조상이 골격·불변식 소유, 유일 구멍 produce(ctx), 프리미티브 닫힌집합(ask·form·rag·tool·extract, rag/tool은 브로커 경유라 새 권한 0). **이중 입력 일급**(채팅 텍스트/폼 union 재개봉투, 병합은 뼈대 소유). 핵심 기계=interrupt 재실행 시맨틱→스텝캐시(리플레이 결정성, produce 3회 실행에 tool 1회)+결정성 계약 문서화. P1=flows/artifact.py(ABC+ProduceContext+slot-fill)+chat.py ask kind게이트+세션 pending 포인터. P2=`ctx.form`(interrupt kind=form, 제출/텍스트 둘 다 재개, `validate_form_values` 이중게이트)+chat.py form 분기+`ChatRequest.form`(formId 불일치 409 fail-closed). P3=TargetingDemoAgent(둘째 구현 **누수0**=뼈대 무변경, rag∪동의어→동적 폼 합성)+served MCP targeting-catalog. P4=artifact 프레임→admin ArtifactCard/InlineFormPanel(입력 안 잠금=이중입력). **적대(codex challenge) 5P1·2P2→2고침(P1-3 pending 크래시원복·P2-2 텍스트병합 무확인확정봉인=confirm이면 재제시)·1하드닝(P2-1 candidates 리스트형만 enum게이트)·3경계정직화(broker 호출자RBAC로 더제약·스텝캐시 멱등성계약·produce 신뢰코드라 닫힌집합=규율≠샌드박스)**. 검증 사다리 3단=verify_188 단위 ALL PASS·e2e 2종(slot-fill+targeting A/B 재제시)·적대codex·무회귀(041 HIL·117 a2a승인·tsc0) [artifact-agent-skeleton,produce-hole-primitives,step-cache-replay-determinism,session-pending-thread-pointer,dual-input-commit-timing,adversarial-verdict-boundary-vs-defect] → 직접+단위+e2e+적대+무회귀
- 189 가이드 문서 2종+어드민 "가이드" 메뉴 — **완료**(retrospect 175): 노코드(일반사용자, 메뉴 표·권한 세 층=사람역할/에이전트붙이기/도구승인±오버라이드·"조이는 건 누구나 푸는 건 관리자만"·평가·승인·플그, 스크린샷 9장)+개발자(produce 저작, 프리미티브 5종·규칙 3) 가이드. shot-guide-menus.mjs(UI 변경 시 재캡처)+build-html.py(md→admin/public/guide 정적, charset 필수—누락 시 한글깨짐 실발생)+AdminShell 가이드 메뉴(onSelect 가로채 새 탭, ViewKey 불포함=라우팅 무접촉). 정확성=실코드 grep+실행 화면 캡처 2중 근거(스샷이 초안 오류 2건 잡음: 빌딩블록 3탭·평가=문제집). 후속: 개발자 가이드도 HTML(펜스 코드블록 변환·상호 내비·가이드 간 .md→html 재작성·저장소 전용 .md는 텍스트 강등=깨진 href 0, 깨진 상호참조 1건 발견수정). 검증=verify-guide-menu e2e 11항목 ALL PASS·tsc0·참조무결성 14/14 [nocode-guide,permission-three-layers,guide-menu-static-newtab,screenshots-as-doc-verification] → e2e+측정
- 190 노코드 산출물형 에이전트 빌더(P1) — **완료**(retrospect 176): 스펙 188 코드 뼈대의 **노코드 버전**. 범용 ConfigDrivenArtifactAgent(NAME=artifact_form)가 config.artifactSpec 읽어 폼 돌림(하드코딩 대신 설정주도)=셋째 구현·**뼈대 무변경 누수0**. 통로=AgentBuildContext.impl_config→ProduceContext.config(뼈대 배선), chat.py가 cfg서 뽑아 주입. 스키마 왕복 3지점(AgentConfig+AgentOut+serializer, 얕은 검증기). 프런트=ArtifactSpecEditor(항목 라벨·결과키·유형[자유/선택]·후보태그·필수, 스펙185 분해) + AgentForm "종류=산출물형" 분기 + AgentsView 왕복 3곳. sink=브라우저 콜백만(기존 프레임, P2=외부API SSRF검토). 검증 사다리=verify_190 단위(normalize·config폼·빈명세·이중입력)·verify-nocode-190 e2e(폼저작→API왕복보존→플그실행→폼→제출→artifact)·무회귀(188·041·tsc0). RBAC=새 쓰기입구0(기존 에이전트CRUD owner스코프). 경계=평면필드만·동적후보는 코드버전 [nocode-artifact-builder,config-driven-produce,impl-config-channel,schema-roundtrip-three-points] → 단위+e2e+무회귀
- 191 RAG 인스펙터 가독성 + **컬렉션별** 유사도 임계값(필터) — **완료**(retrospect 177): 실사용 문의 3건(문서검색 결과=벽텍스트/유사도 "낮음" 기준 불명/검색어 안 보임). 인스펙터를 히트별 **카드**(컬렉션 태그+파일명+점수대 색 배지+본문 접기)+**척도 범례**("유사도 0~1, 0.5미만=낮음")+**검색어**로 개선. **컬렉션별 최소 유사도 필터**(search_collections min_scores 맵, 히트 collection 태깅→_apply_min_scores 컬렉션별 판정·경계포함·미설정=무필터·top_k 후필터 0건가능). v1 에이전트당 1값→v2 **컬렉션마다**(사용자 추가요구, 폼에 컬렉션당 슬라이더). 배선=build_rag_tool/RagProvider(row.name 조회)/build_broker/chat.py 4곳. 왕복 3지점(AgentConfig.ragMinScores dict+검증기·AgentOut·serializer). **누출 봉합(codex)**: hitsDetail.textPreview·직접 result 모두 _sanitize_preview(비밀 마스킹) 단일경로=브로커 resultPreview와 대칭(codex가 직접 result _cap만인 비대칭 지적). mock에 search_documents 트리거 추가(179 확장, 결정적 e2e). 검증=verify_191 단위(컬렉션별 필터·마스킹 D5/W5)·e2e 15/15·codex 적대검토·무회귀. 기존실패(072/103/130 컬렉션400·Obsidian전제)=내변경 무관(stash 격리확인). RBAC 비트리거(config owner스코프). 커스텀 에이전트 RAG도 ctx.broker 경유면 인스펙터 표시(같은 경로) [rag-inspector,per-collection-threshold,similarity-filter,hit-cards,sanitize-preview-symmetry,mock-search-trigger] → 단위+e2e+codex+무회귀
- 192 인스펙터 개편: 단계(노드)별 실행 타임라인 — **완료**(retrospect 178): 자원종류별 섹션(메모리/문서검색/MCP/브로커)→**노드 축**으로 완전 대체. 사용자 요구="어느 단계서 뭘 어떻게 호출했고 결과 어땠나". antd `Timeline` 스파인(시작→모델 호출→도구·문서 검색 실행→…→종료), 각 노드에 그 단계 이벤트 귀속: 브로커=`brokerCalls[].node`(스펙130 기존)·직접=선형그래프 타입귀속(회상→retrieve_memory·rag/mcp→tools·토큰→model). 프런트 순수함수 `nodeEventContent`. RAG=**커트라인 미달로 못 쓴 문서**까지 회색 표시(HitCard belowCutoff 분기). **백엔드 유일변경**: 커트라인을 "드롭"→"표시"(`_apply_min_scores`→`_annotate_cutoffs` belowCutoff/cutoff 부착+`used_hits` 통과분; 에이전트는 통과분만 봄=필터의미 유지, trace hitsDetail은 전부). 공간=탭(실행 흐름/프롬프트·설정)+**전체화면 Modal 90vw**(드로워 384 비좁음 해소). 노드명 `model`(스펙 문구 call_model 아님, 스샷이 잡음). 검증=verify_191 확장 단위(F1-F10·D6·W6 annotate/used)·verify-node-timeline-192 e2e 15/15(타임라인 노드·미달문서·프롬프트탭·확대Modal 1260px)·무회귀(191 e2e 12/12·tsc0·130 실패는 stash격리로 기존 인프라 확인). RBAC 비트리거(표시 재구성+마스킹 규율 191 유지, 새 쓰기입구0). 경계=직접경로 타입귀속 휴리스틱(ReAct 다중 tools 한 노드로 접힘, per-call은 calls_sink에 node 실어야=후속빚)·전체화면=Modal 확대(별 라우트 아님) **후속(execute 프리뷰)**: messages 델타를 건수만→role+본문 프리뷰(_msg_role·캡 160·마스킹, 086 불변식 유지)—인스펙터·채팅은 다른 패널이라 '중복'이 아닌 부재였음; verify_086 계약 재작성 [inspector-node-timeline,axis-flip-reassembly,annotate-dont-drop,node-names-from-trace,elide-only-if-visible-together] → 단위+실통합+스샷
- 193 평가 UX 개선 3건(RAG 컬렉션 고정·성적추이 구분·생성 로딩) — **완료**(retrospect 179): 실사용 문의 3건. **P1** RAG 문제집에 `EvalDataset.collection_id` 고정(마이그레이션 c193a1b2c3d4=고유ID·단일head a181, SET NULL FK)·생성 시 저장(수동 모달 컬렉션Select+컬렉션생성 경로)·`DatasetOut.collection_id` 왕복(헬퍼 `_dataset_out` 5곳 드리프트0)·실행UI 고정 칩(재선택 Select 제거)·구버전 첫 실행 lazy 저장·start_run `ds.collection_id` 우선. RAG엔 챗모델 축 없음(임베딩=컬렉션 고정, 사용자 확인)이라 모델 선택 미도입. **P2** 성적추이 박스 옅은 배경(geekblue-1)+강조바+아이콘(문제 카드 흰색과 구분). **P3** `DatasetOut.generating`(description "생성 중" 마커 구조 승격)+목록 스피너 배지+드로어 antd `Skeleton` 3카드+"만드는 중" 안내+2.5s 폴링(다 끝나면 정지)+열린 드로어 재바인딩 동기화. 마이그레이션 적용=앱 부팅 임베디드 upgrade(alembic CLI 순환import 불능→손수+파싱 헤더검증). 검증=verify_193 단위 9/9(왕복·agent무시·update보존·lazy·generating)·verify-eval-ux-193 e2e 15/15(고정칩·실행·성적추이 배경 rgb(240,245,255)·배지·Skeleton)·무회귀(137 A2·140 Obsidian은 pre-existing, stash격리 확인). RBAC 비트리거(collection_id는 dataset owner 스코프 내·실행 게이트 기존·새 쓰기입구0). [eval-collection-pin,migration-at-boot,generating-skeleton,poll-rebind-detail,dataset-out-helper-drift-zero] → 단위+e2e+무회귀
- 194 채점 기준(assert) 편집 UX: 문장형 인라인 폼 + AND 명확화 + lte 확장 — **완료**(retrospect 180): 실사용 문의(채점 기준 유형마다 입력 UI 달라 어려움·AND/OR 헷갈림). 채점은 전부 AND(`all(ok…)`). **문장형 인라인 폼**(사용자 제안): 각 기준을 읽으면 자연어("검색 결과가 [3]건 [이상▾] 여야 통과"), 유형 select=카테고리 그룹(답변/도구/RAG/AI), 숫자 비교형은 연산자(이상/이하) select. **AND 배지**("모든 기준을 만족해야 통과")+**"그리고" 커넥터**로 조건 명시. 저장된 케이스 카드도 `assertLabel` 순수함수로 편집과 같은 문장 어휘 공유(드리프트0, raw type 노출 제거). **백엔드 lte 확장**: `rag_hits_lte`·`rag_score_lte`(연산자 select "이하" 실동작). **함정**: lte는 gte 단순 반전 아님—`len([])<=N`은 부재 관측서 vacuously true(fail-open)라 명시 부재체크(`"hits" not in rag→False`)로 대칭 맞춤(gte는 `>=`라 자연 fail-closed). 검증=verify_194 단위 15/15(경계·부재 fail-closed·범위거부·gte무회귀)·verify-assert-ux-194 e2e 18/18(카드 문장·AND배지·그리고·유형그룹·rag_hits전환·연산자 select·raw type 미노출)·tsc0·스샷. RBAC 비트리거(표시/입력 UI+scorer 종류확장, 소유권·저장입구·all판정 무변경, lte=gte 순수반전). 경계=OR 미도입(사용자 결정, 필요시 문제 분리) [assert-sentence-form,and-badge-connector,assertlabel-shared-pure-fn,lte-not-simple-inverse-fail-closed] → 단위+e2e+스샷
- 195 평가 케이스 등록 UX 4건(배지제거·파일명선택·이름제거·출제버튼) — **완료**(retrospect 181): 실사용 문의 4건. **1** 카드 'N개 기준' 배지 제거(아래 assert 태그와 중복). **2** rag_source_contains 근거 파일명을 Input→**AutoComplete**(컬렉션 문서 파일명 옵션, DatasetDrawer가 collection_id로 listDocuments→CaseForm→AssertEditor 전달, contains라 자유입력 허용). **3** '문제 이름' 입력 **제거**→내부 해시 관리(유저 비노출): CaseIn.name optional→없으면 `case-{token_hex}`, update 미전송 시 기존 보존(덮어쓰기 금지), 실행 시 `HarnessCase.name=c.input`으로 성적표 case_name=질문(DB name=해시), 카드 제목=input. **4** AI 출제를 드로어 상단 '시험 실행' 옆 버튼으로(하단→상단, agent·rag 공통): suggest_cases에 rag 분기(`_execute_generation_append`=골든 생성을 기존 문제집에 order_idx 이어붙임, 소유게이트·비용가드·락 재사용). '컬렉션에서 생성'(자동 채움) UI 제거→빈 문제집+상단 AI출제로 일원화(generate_dataset 엔드포인트는 유지). 검증=195 e2e 12/12(name 해시 왕복 case-xxxxxxxx·update 보존·배지없음·질문제목·상단 AI출제·파일명 옵션4)·194 e2e 무회귀·tsc0·스샷. RBAC 경계(케이스 소유): name optional/해시=표시용 무관, rag 출제는 기존 suggest_cases 게이트(assert_may_manage·404fold·비용가드) 뒤 분기=새 입구0, ds.collection_id는 소유 dataset 고정값(body 미수신). 경계=자동생성 강제 제거(빈 문제집+상단 버튼 경로 추가) [remove-field-follow-consumers,branch-existing-endpoint-not-new-route,remove-over-automate] → e2e+무회귀+스샷
- 196 평가 문제집 목록: 최근순 정렬+검색+페이징 — **완료**(retrospect 182): 실사용 요청(이름순·검색없음·전량렌더). 백엔드 list_datasets 페이징화(created_at DESC, q=name·설명 ilike+`_ilike_literal` 이스케이프, limit/offset, DatasetPageOut{items,total,any_generating})+get_dataset 단건(드로어 rebind용). 프론트 목록을 공용 PagedListShell(스펙128: 디바운스 서버검색·페이지네이션)로 교체—전체 datasets 상태 제거→파생 4곳 재배선: generating폴링→any_generating 신호, detail rebind→getEvalDataset 단건폴링, runFilter옵션→runs유도, 생성/삭제→refreshKey++. 검증=196 e2e 14/14(최근순 items[0]·q검색·페이징 2+1경계·이스케이프·get 단건·404·UI 검색필터)·195/194 무회귀·tsc0·스샷. RBAC 비트리거(목록=읽기 전원공개 178 D1, q/페이징은 표시범위만·소유판정 무변경, get_dataset도 읽기공개, q는 _ilike_literal로 와일드카드 오라클 차단, 새 쓰기입구0). 경계=최근순 고정(컬럼정렬 토글 밖)·검색은 이름·설명만(케이스 전문검색 밖) [state-handoff-rewire-all-consumers,paging-breaks-whole-list-assumers,verify-order-boundary-via-api] → e2e+무회귀+스샷
- 197 RAG 컬렉션에서 '평가하기' 단축 진입 — **완료**(retrospect 183): 실사용 제안(컬렉션 상세서 평가로 바로). 컬렉션 문서 드로어(DocsDrawer)에 '이 컬렉션 평가하기' 버튼→평가 화면 이동+이 컬렉션 프리필된 '새 문제집' 창(이름만, 문제 자동생성 없음=스펙195 정책 일관, 사용자 결정 A). 배선=Playground initialAgentId 패턴 재사용(AdminShell evalCollection 상태+EvalView initialCollectionId/onConsumedInitial 1회소비+CollectionsView onEvaluate). 순수 프론트. 검증=197 e2e 9/9(드로어→버튼→평가전환→모달 rag·컬렉션 프리필→생성 collection_id 고정)·tsc0·스샷·무회귀. RBAC 비트리거(프론트 네비+기존 모달 프리필, 생성은 create_dataset owner스탬프 그대로, 새 쓰기입구0). 경계=모든 컬렉션 종류 노출(rag=search_collections 동일)·즉시생성(B) 미채택. **후속**(스샷 피드백): 버튼을 콘텐츠상단→헤더 extra(위계), 문서0=비활성(근거없음), 목록 '문서'버튼 중복제거(행클릭과 동일) [view-param-transfer-is-pattern-clone,fast-vs-policy-tradeoff,screenshot-catches-placement] → e2e+스샷
- 198 청크 정책 수정 기능 제거(재청킹 폐기) — **완료**(retrospect 184): "재청킹·재임베딩 만들어줘"→코드에서 **원본 미저장** 발견(Document에 raw 컬럼 없음, Chunk.text만; 청크는 겹침 있어 이어붙여도 원본 복원 불가)→사용자와 방향 재합의: 재청킹 안 만들고 **혼란원인 청크 정책 수정 제거**(청크=생성 후 불변, 모델·차원과 동일). 참고=AI 출제는 Chunk.text 그대로 읽어 질문생성(원본 불필요)—"조각 그대로 쓰기(출제)"O·"조각 붙여 다시 자르기(재청킹)"만 원본필요. 구현=프론트 EditModal 청크 입력필드 제거(값은 참고표시)+백엔드 CollectionUpdate 스키마서 chunk_size/overlap 필드 제거(API 우회 차단)+update_collection 대입 제거+api.ts body 제거. 검증=198 e2e 10/10(A: PUT에 chunk_size+1234 보내도 무시·값불변·별명은 수정됨=구클라 우회 재현 / B: 편집모달 InputNumber 0·불변문구·별명입력 유지)·tsc0·스샷. RBAC 비트리거(소유자 게이트 assert_may_manage 무변경, 수정 범위축소=필드제거로 표면↓, 새 쓰기입구0). 경계=기존 컬렉션 청크 고정(마이그레이션 없음)·바꾸려면 컬렉션 새로 만들어 재업로드 [verify-hidden-premise-in-code,remove-root-not-add-feature,immutability-is-backend-schema-contract] → e2e+스샷
- 199 '능력 부여' UI 진단(구현 보류) — **진단만**(사용자 결정 "먼저 진단만, 구현 나중"): 유저 메뉴 `능력 부여(정책)` 카드(177 P3)가 설계자도 헷갈릴 만큼 어려움=발견성 실패, permission-redesign 목표 "쉽게 쓰기"의 정반대. 진단(스샷 근거)=P1 **무엇을 줄 수 있는지 안 보임**(종류 mcp/rag/agent만 고르고 이름은 자유 타이핑→오타=무효정책; 195 파일명 Select·106/107 능력피커 원칙이 이 관리자 화면엔 미적용)·P2 `capability:…` 개발자 코드 UI 노출·P3 대상에 역할+유저 혼재(정신모델 안 섬)·P4 맥락/결과 안내 부족. 개선후보(보류)=A 자유입력→카탈로그 체크박스·B 코드→문장화·C 역할/유저 분리·D 도입 문장. RBAC=구현 시 트리거(grantPolicy 인가 write; 카탈로그 선택이 화이트리스트 되는지 확인), 진단 단계라 경계설계는 구현 스펙서. 참고=UsersView.tsx:133-350·스펙106/107/195/112/177 [discoverability-failure-designer-cant-use,free-input-vs-catalog-select,dev-code-leak-to-ui,diagnosis-only-defer-impl] → 진단 스펙(구현 보류)
- 200 '능력 부여' UI 개편(199 진단의 구현) — **완료**(retrospect 185): 유저 메뉴 능력 부여 카드를 사람 중심으로 — A 자유입력→**카탈로그 Select**(value=브로커 판정 키: mcp=서버name·rag=컬렉션name·agent=**agentId(agt_…)**, `_cap_resource` 역산; 이름을 value로 썼으면 무효부여)·B `capability:…`→**문장 미리보기**("'member' 역할에게 도구 'calc' 사용을 허용합니다", 코드는 회색 라인+목록 툴팁)·C 역할/유저 **Segmented 분리**+효과 한 줄·D 도입 문장(member=기본 잠김)·P5 **종류 6종 완성**(기억 3종이 UI에 없어 부여 자체 불가였음; 기억류는 리소스 내부고정 user라 이름 Select 숨김). 백엔드 무변경(grantPolicy admin 전용+capability: 경계 그대로, 새 쓰기입구 0)+api.ts listMcpServers 추가. 검증=200 e2e 18/18(도입·축·6종 사람말·**옵션수=실자원수 측정**(agent 6+1)·문장/코드 정확·부여→사람말 목록→회수 왕복·기억=이름Select 없음·agent 코드=agt_ 판정키)·tsc0·스샷. 경계=백엔드 존재검증·rename 정책참조 갱신 OUT·승인정책과 별개 축. **후속**: 유저 목록/능력 부여 탭 분리(유저 늘면 세로 나열 불편, e2e 21/21) [select-value-is-enforcement-key,ui-closed-list-vs-code-full-set,diagnosis-spec-then-impl-spec] → e2e 측정+스샷
- 201 web-fetch 커스텀 MCP(위키 검색·본문)+능력 부여 도그푸딩 — **완료**(retrospect 186): 사용자 설계 "주소 조립→패치"(검색엔진 파싱 대신 위키 공식 API). wiki_search(검색 목록)+wiki_page(요약 본문)를 156 레지스트리(_DEFS)에 추가 → 재기동 reconcile 자동 등록 → 공개 → **200 UI로 member 실부여(유지)** → 플그에서 qwen 실모델이 wiki_page 277ms 호출·라이브 데이터 답변(전 루프 실증). 안전=lang{ko,en} allowlist(호스트 고정·SSRF 0)·8s 타임아웃·512KB raw 캡·리다이렉트 후 호스트 재검증·limit 1~10 클램프·순수도구 allowlist(부팅 불변식). **발견: 오염 행 500** — 적대 테스트 DB 직삽 잔재(source='user')가 /mcp-servers 전체 500, UI와 e2e의 이중 catch(()=>[])가 삼켜 "mcp=0" 공허 일치(200 e2e 옵션수=자원수 0=0 초록) → 잔재 삭제+e2e에 500 회귀 감시. 오버라이드 체크≠적용('적용(새 대화)' 커밋)·mock web_search 이름 경합(제거 후 결정적). 검증=단위 15/15(mock transport: allowlist·바이트캡·오프호스트 리다이렉트·클램프·태그제거+live 스모크)·e2e 7/7·플그 스샷. OUT=타 사이트·범용 URL·레이트리밋 [url-assemble-fetch-not-scrape,poisoned-row-plus-double-catch-vacuous-green,drawer-check-is-not-apply,tool-name-contention] → 단위+e2e+실모델 실증
- 202 플랜 에이전트 도구-인지(노코드 계약 "설정=동작") — **완료**(retrospect 189): 사용자 문제제기("공통 클래스 구현이면 도구 연결이 동작해야, 플랜이 도구로 더 영리해야")→진단: 계약은 이미 주입(ctx.tools, chat.py가 mcps→빌드→주입)·**PlanExecuteAgent가 버림**(Protocol은 시그니처만 검사—버려도 조용히 합격). 구현=plan 결정적 유지+계획에 도구 단계 반영(086 계약 plan<execute·핵심/근거 문구 보존)·execute bind_tools("필요할 때만"=사용자 결정)·tools(ToolNode) 조건 루프·도구 없으면 2노드 무회귀. 적합성 089에 T1(주입→tools 노드 배선=**가용성**, 강제사용 아님)/T2(무도구=2노드) — 커스텀 impl이 도구 버리면 걸림(201 후속2 재발방지). 검증=089 전체+T·086 무회귀(plan 4ms≤execute)·사용자 재현 경로(plan-execute-demo에 web-fetch 연결→plan에 도구 등장→wiki_search 550ms 실호출→위키 근거 답변, 연결 유지). OUT=모델 기반 동적 plan(086 계약 변경 별도)·승인게이트 도구 HIL [contract-injection-needs-consumption-rung,capability-not-behavior-verification,existing-tests-as-design-guardrail] → 단위+무회귀+실모델 재현
- 203 도구 다수 시 디스커버 전환(하이브리드) — **완료**(retrospect 190): 202 논의→사용자 "바로 구현". toolbox.py 신설 effective_tools(임계 10, env TOOLS_DISCOVER_THRESHOLD): 이하=직접 바인딩(코드 경로 동일 무회귀)·초과=**메타 도구 2개**(search_tools 어휘 랭킹(124 사상: 필터 아닌 랭킹)·call_tool 이름 디스패치+graceful 오류/후보 제안). **브로커 미경유**(per-user RBAC라 직접형 권한 의미 바꿈—주입 목록 위 래퍼로 HIL/트레이스/권한 보존; "디스커버=부품, 조율형=형태" 사용자 문답 확정). 적용=직접형 build_agent(+프롬프트 안내 1줄)·플랜형(계획 문구 분기)+AgentForm 안내 문구(임계 FE 미러). **live가 잡은 P1**: 인자명 `args`→LangChain 예약어 충돌로 스키마 v__args 둔갑→모델 호출 전량 실패(단위는 .coroutine 위치인자라 초록=스키마 경로 미경유)→`arguments`로 개명+테스트를 ainvoke 스키마 경로로+스키마 인자명 검사. 검증=단위 16/16·089(T)/086 무회귀·tsc0·live(임계=1: search_tools 2ms→call_tool→wiki 672ms→근거답변 5.79s 전 체인, 스트림 드랍 1회=로컬 모델 flake 재시도 판별). OUT=벡터 검색(백로그)·승인게이트 도구 HIL·오버라이드 드로어 문구 [meta-tools-over-injected-list,langchain-args-reserved-name,schema-path-testing,discovery-is-part-not-shape] → 단위+무회귀+실모델 전 체인
- 204 admin 전면 antd 전환 — **교체 18건 완료**(retrospect 192, 논의 5건 보류=사용자 결정): 지시 "모두 antd로, 일괄 조사·교체"→규칙 admin/CLAUDE.md 명문화→Explore 전수조사(교체18/래퍼무변경/대응물없음/애매)→3배치. 하11=StatusPill→Badge·Panel→Card·VersionHistory/Overview목록2/EvalView행→List·Inspector Section→Collapse·유사도바→Progress·ModelBadge→Tag·IdRow→Typography copyable·EvalTrend아코디언→Collapse·StatTile→Card+Statistic. 중4=플그 좁은폭 오버레이→Drawer·AdminShell 모바일 백드롭+fixed Sider→Drawer·EvalMatrix 생table→Table(동적컬럼+Summary합계)·TraceChips→Tag. 상3=**인스펙터 도킹→Splitter**(드래그 리사이즈 덤)·AgentCombo/SessionCombo 수제 드롭다운→Dropdown(popupRender, 바깥클릭/포지셔닝/z-index 이양). 함정 실측=문자열 수술 잔재(이른 `})}` 매칭·상수 딸려 삭제—tsc가 잡음, 앵커+잔재grep 필요)·JSX주석은 삼항 자리 불가·포털 전환 후 무스코프 getByText 오탐(.ant-dropdown 스코프로 해결). 검증=배치마다 tsc0+ui-audit 3종 FAIL 0(모바일 내비 Drawer 순회 포함)+도킹/피커 기능 e2e+스샷. 보류 5=차트SVG·마크다운/JsonTree·DataTable 모바일카드·트레이스카드 겉면·죽은 Chat.tsx [antd-everywhere,rule-then-survey-then-classify,string-surgery-anchor-residue,portal-breaks-selector-contract] → 조사+3배치+감사
- 205 인스펙터 정직성 3건 — **완료**(retrospect 193, 사용자 "순서대로" ①): ①전송 프롬프트 재구성(131)→**모델 콜백 실측**(trace_capture.TraceCaptureHandler — on_chat_model_start 호출별 메시지·on_llm_end usage 합산, config.callbacks에 Langfuse와 병행, 마지막 호출 표시+modelCalls 병기+sentMessagesSource로 실측/재구성 정직 라벨, 마스킹·캡 재사용) ②토큰 글자수÷4 추정→usage 실측(부재 시 estimated=true·"(추정)≈" 표기) ③턴 배지 메시지 인덱스→질문 순번(user 메시지 수). 검증=실모델 7/7(플랜 위키 턴: system 401자에 계획·도구 안내 실측 노출·도구 왕복 6메시지·호출 3회·토큰 2,978/131·"턴 1")·핸들러 단독 단위·tsc0. 교훈=재구성은 소스 다양화 순간 거짓(관측=소비 지점)·usage는 안 읽고 있었을 뿐·접이식 검증 오탐 2연(defaultOpen 클릭닫음·행 미펼침) [observe-at-consumption-point,usage-was-free,collapse-assertion-pitfall] → 단위+실모델 7/7
- 206 매니페스트 소비 선언+폼 게이트 — **완료**(retrospect 194, "순서대로" ②): AgentManifest.consumes(impl이 읽는 설정 표면 자기선언: mcps/vectorTables/memories/capabilities/artifactSpec, None=미선언→전부 노출 무회귀)+전 impl 선언(default·plan_execute=전부/route=memories만/orchestrate=capabilities·memories/artifact=artifactSpec)→/agent-impls 메타 확장(key+consumes)→AgentForm 게이트(안 읽는 그룹 숨김+저장 연결 "무시됩니다 N개" 경고, 데이터 보존—impl 되돌리면 부활). e2e=route 편집: 기억만+경고 스샷/plan_execute 전부(무회귀). **부수 발견·수리 2**: ①모바일 내비 잠김(기본 뷰 agents 재클릭에 antd Menu onSelect 미발화→드로어 안 닫힘→mask 연쇄 4건—옛 하네스 백드롭 클릭이 가려온 결함, onClick으로)+②overlays 감사 NAV 실패가 종료코드 미반영(공허 초록—navFails 포함). 게이트 기본값=노출(선언 깜빡=거짓 소멸 방지). OUT=런타임 저장 거부·089 rung [manifest-self-declaration,reclick-onselect-blindspot,audit-exitcode-nav,gate-default-open] → 단위+API+e2e+overlays 20/20·NAV0
- 207 ant-design-x 채택(x-markdown)+204 보류 정리 — **완료**(retrospect 195): 사용자 "ant-design/x 레포 기능 소개"→"@ant-design/x-markdown 안 쓰나요?"(내 "x에 마크다운 없음" 오판 교정). 채팅은 이미 Bubble/Sender. MessageContent의 react-markdown→**XMarkdown**(x 네이티브 AI md: GFM·katex·DOMPurify·스트리밍 tail▋/fade-in) 교체, 구조(형식추론·예산캡·JSON트리) 보존. **보안 관문 3방어 전부 이식**: escapeRawHtml(raw HTML 평문)·components.img=SafeImgX(domNode.attribs로 src/alt→링크 치환, <img> 미렌더=추적픽셀 fetch 0)·예산캡 앞단 유지. 204 보류 5정리=②MessageContent→x-markdown·JsonTree/①TrendChart=대응물없어 유지(CLAUDE.md 예외)·③모바일카드=**antd List v6 deprecated**라 Flex+Card 유지·④InlineForm/ArtifactCard raw div→Card·⑤죽은 Chat.tsx 삭제. **codex 적대검증**: G1(원격fetch: video/svg/style/link/picture/css-url·raw img 전부 escapeRawHtml 평문화)·G2(XSS: js URL DOMPurify 살균) 방어 유지 확인, **G3 우회 1건(High)→수리**: 바이트캡(1MB) 아래 GFM 표/목록이 DOM 폭증(`|x`*240k→<th>24만·2.6MB 프리즈)→messageFormat renderCost(파이프·줄바꿈 가중 구조증폭 대리값) 추가. 검증=shot-markdown-088 하네스 24/24(E1<img>미렌더·E2 href보존·E3 alt)·단위 B5~B8(표/목록캡·코드블록 과캡방지)·육안(tail▋·이미지 링크치환)·tsc0. react-markdown/remark-gfm 제거(유일 소비처). [placeholder-npm-desc-can-hide-real-package,x-markdown-preserves-guards-via-components-and-dompurify,byte-cap-misses-structural-amplification,antd-list-deprecated-v6] → 하네스+단위+codex 적대
- 208 antd v6 deprecation 마이그레이션 — **완료**(retrospect 196): 사용자 "antd 마이그레이션 가시죠". 관측 3종 전수(완료기준=콘솔 deprecation 경고 0건, 수치검증). Alert `message`→`title` 30건(perl \b 치환, 전-src bare message=는 전부 Alert 검증)·Drawer `width`→`size` 13건(**같은-줄 grep이 4로 오측→멀티라인 놓침**, `<Drawer` 태그범위 스캔으로 전수; **두 종류**=antd직접5는 size 직접·공용래퍼 소비자8은 래퍼 내부 1곳만 size로 고쳐 커버(공개 API width 유지), tsc가 래퍼 오교체 8건 잡음)·List→Flex 조립 4건(drop-in 없음; Overview|Meta 2개는 MetaRow 헬퍼, VersionHistory bordered→테두리div, EvalView 클릭행→div; 미사용 import 2 제거). 검증=tsc0·ui-audit 3종(화면42·오버레이20 FAIL0, audit-all NAV1은 overlays 단독 2회 clean=플레이크)·**deprecation 경고 0건 grep**·육안(Overview MetaRow·Alert title·Drawer 480 개폐). admin/CLAUDE.md 규칙 갱신(목록=Table/Flex, List v6 deprecated). [same-line-grep-misses-multiline-jsx,bsd-sed-no-wordboundary-use-perl,fix-wrapper-boundary-not-consumers,tsc-points-to-boundary,completion-as-zero-warnings] → 전후카운트+tsc+감사+경고0
- 209 응답 피드백(👍/👎) 수집→평가 수확 — **Phase 1 완료**(retrospect 197): 사용자 "좋아요/싫어요로 개선(DPO 같은 것)". **DPO 정직 재정의**=우리는 모델 학습 안 함(등록 모델 base_url)→피드백을 평가 케이스로 수확(수치 검증 강점), 👍도 수확(변경 회귀 지표=사용자 통찰), 실제 세션까지. **Phase 1=수집**: MessageFeedback 모델+마이그레이션 f209a1b2c3d4(재기동 적용)·PUT upsert/DELETE·GET messages에 id+feedback·소유권 게이트(_get_session_or_404 스코프 융합). UI=SessionsView assistant 응답에 재사용 FeedbackButtons(👍/👎+이유 Popover, antd). **소유권 경계 스펙**(docs/spec/CLAUDE.md 체크리스트): 세션 소유자만·assistant-only·머신 403·타인 404 은폐. **codex 적대검증** 핵심 보장 유지+4건 발견 수리3: F1(High 설정의존)`sessions:read`가 쓰기 넓힘→**_own_scope_write**(진짜 superuser만) 분리·F2 경합500→PG on_conflict upsert·F3 reason무제한→max_length2000·F4 malformed UUID 422=수용(존재오라클 아님). 검증=verify_209 통합 18/18(소유권·upsert·머신403·assistant-only·reason422·DELETE own-only)·_own_scope_write 단위·브라우저(필드👍+이유 영속렌더, DB/GET 실측)·tsc0·ui-audit 오버레이 FAIL0. **Phase 1.5 분리**=플그 피드백(_persist message_id 배관 5곳=민감 스트리밍 경로). **Phase 2**=수확(피드백→초안 케이스 LLM assert·문제집·검토편입). [dpo-honest-redefine-no-training,read-scope-must-not-widen-write,phase-split-by-sensitive-plumbing,malformed-not-existence-oracle] → 통합18+단위+codex적대+브라우저
- 209 Phase 2 수확 — **완료**(retrospect 198): 미수확 피드백(👍/👎)→초안 평가 케이스. 마이그레이션 f210a1b2c3d4(MessageFeedback.harvested_case_pk·EvalDataset.source_agent_pk). eval_harvest.py(gather_unharvested=Session.agent_pk 조인 격리·직전 user 질문·미수확만·상한50, harvest_agent_feedback=👍/👎→llm_judge LLM 합성·llm_cfg None이면 폴백 우아저하). 엔드포인트 GET /eval/harvest-count·POST /eval/datasets/harvest(find-or-create by source_agent_pk·advisory락 직렬화·_active_jobs·배경 _execute_harvest가 harvested_case_pk 스탬프). UI=AgentDetail "피드백 수확" 패널(배지=미수확수, can_manage). 소유권=assert_may_manage(agent). **codex 5건 수리**: F1 수확케이스 eval공개읽기 노출→_gate_harvest_read(source_agent_pk≠NULL 소유자/admin만·목록필터)·F2 동시경합→pg_advisory_xact_lock·F3 상한50·F4 미존재→404선행·F5 문제집소유=agent.owner_id. 검증 통합24/24(소유권404·격리·idempotent·스탬프·F1읽기게이트·F4미존재)·브라우저e2e(실데이터 파이프라인, LLM 합성 기준 실증)·tsc0·ui-audit FAIL0. **Phase D(지표 재실행)=수동 MVP**(별도 자동트리거 OUT). [feedback-to-eval-pipeline,harvest-graceful-degrade,enumerate-public-surfaces,reload-reruns-migrations] → 통합+codex적대+브라우저e2e
- 209 Phase 1.5 플그 피드백 — **완료**(retrospect 199): 플레이그라운드 응답에도 👍/👎. _persist가 flush 후 assistant Message.id 반환·_mid_frame으로 event:message_id SSE 프레임 방출(4곳: A2A remote·ask·form·direct; 5번째 resume_approval=비제너레이터→세션 리로드가 커버). 프론트: ChatMsg+=id/feedback·streamChat onMessageId·Playground onMessageId→appendToLastAi·loadSession 매핑 id+feedback·DebugChat이 id 있는 완료응답에 재사용 FeedbackButtons. 검증 브라우저 e2e(렌더→클릭→DB영속 실측)·209스위트18/24회귀·tsc0·ui-audit42 FAIL0. 플그 피드백도 Phase2 수확 대상→세 Phase 한 파이프라인 종결. codex 생략(가산·소유권경계 Phase1 재사용). [reuse-feedbackbuttons,message-id-frame,resume-via-reload] → 브라우저e2e+DB
- 210 별명 폐지: name 단독 표시+alias→description 개명 — **done**(retrospect 202): DB 개명(f211a1b2c3d4 — 3테이블 rename+Collection 병합 drop, 소실 0), 백44+프론트64 rename(fast-worker 2기 병렬)·displayName=name 단일출처·리스트 툴팁화·세션 박제 사본 수선(시드+UPDATE 4행)·구계약 verify 3개 갱신(148 40/40)·stale 샷 7개 기록. 검증=DB실측+브라우저 6/6(가짜PASS 함정은 스샷 육안이 잡음) [alias-removal,name-only-display,tooltip-description,stamped-copy-data-fix] → retrospect 202
- 211 MCP 리스트·정책 정리 — **done**(retrospect 203): 사용=공용(agent_may_wire·_wiring_owner_privileged·stored_mcps 제거, 172 동형)+published=커스텀 외부서빙 전용(3입구 non-custom=400, 152 서빙봉인 불변), 컬럼 분리(이름+🔒/유형/소유/외부서빙, 상태 제거)·드로어 이름중복·status 제거. codex P2(크레덴셜 유출)→직접 MCP result도 _sanitize_preview(사용자 결정 전부공용+마스킹). 검증 verify_211 15/15·verify_152 11/11(계약갱신+부채2 청소)·156 9/9·브라우저 10/10 [mcp-shared-use,published-narrowed,lock-emoji,seal-two-axes] → retrospect 203
- 212 탭 통일+kind 분리 — **done**(retrospect 204): 기준 명문화(데이터집합전환=Tabs/목록필터=Segmented/Radio금지), 승인 Segmented→Tabs·세션 Radio→Segmented, RAG 문서/엔티티 Tabs(클라필터), 평가 에이전트/RAG Tabs(사용자 확정 "탭 통일"—첫 구현 Segmented→스샷확인→Tabs, 상위Tabs속 자식Tabs·백엔드 kind파라미터 서버필터). 검증 브라우저7/7·tsc0. UI-only라 codex 생략 [tab-vs-segmented-by-role,server-param-filter,screenshot-checkpoint] → retrospect 204
- 213 페이지 제목 중복 제거+죽은 검색 제거 — **done**(retrospect 205): 헤더(TITLES)+Page 이중 제목→Page 제목 제거(헤더 단독 진실원)·부제 유지, 미배선 전역 검색창 제거(각 목록 자체검색은 유지). 전역 컴포넌트(shared.Page·AdminShell) 단일수정 전 메뉴 전파. 검증 브라우저 9메뉴(제목 h3 화면당1·검색껍데기0)+tsc0 [single-source-title,remove-dead-widget,global-component-fix] → retrospect 205
- 214 태그 안내 팝업 레이아웃 정돈 — **done**: 태그+설명 혼류 flex→2열 그리드(태그열 128px 고정+설명 정렬), 상태 노트 구분선 아래 분리. 레이아웃만(의미·문구 불변). 브라우저 스샷 확인·tsc0 [legend-two-column-grid,align-descriptions] → 스샷
- 215 에이전트 필터 드롭다운 라벨 잘림 — **done**: antd Select 팝업이 트리거폭에 맞춰(기본 popupMatchSelectWidth) 긴 옵션 라벨 말줄임. 4개 필터(정렬·소유·소스·상태)에 popupMatchSelectWidth={false}→내용폭. 브라우저 옵션 말줄임0·tsc0 [antd-select-popup-content-width,dropdown-label-truncation] → 스샷
- 216 빌딩 블록 '수정일' 실배선 — **done**(retrospect 206): '수정' 열이 편집해도 불변(백엔드 "updated":"—" 하드코딩). 편집가능성으로 범위분기—페르소나(updated_at 기존)·MCP(컬럼추가 마이그f212) 실배선, 메모리(읽기전용 enum)는 정직한 "—" 유지. 열명 "수정"→"수정일". fmtTime 친화표기. 검증 편집→updated_at 상승·DB컬럼실존·head단일 [dead-value-is-unwired,honest-na-for-readonly,verify-value-changes-on-action] → retrospect 206
- 217 식별 이름 규칙 영문화(한글·마침표 제거) — **done**(retrospect 207): 사용자 지시 "영소문자·숫자·대시만". NAME_RE=^[a-z0-9-]+$(전 리소스 공용). 숨은 생산자 수정: slugify fallback "이름-미상"→"unnamed", 에이전트 복제 접미 "-복사본"→"-copy"(한글 이름 생성 버그). 기존 데이터 0건 위반(V6). 검증 42/42·브라우저 힌트·tsc0 [name-rule-english-only,audit-producers-not-just-validators,fallback-must-obey-rule] → retrospect 207
- 218 컬렉션 생성 모달 임베딩필터·라벨·청크툴팁 — **done**(retrospect 208): 사용자 3건(같은 모달). mock 임베딩 필터(실제 모델 있으면 제외)=provider.kind 신호(ModelOut.provider_kind 추가, 이름 하드코딩 아님), 청크 겹침 (?) 툴팁 추가, 모델 라벨 name==model_id면 중복표기 생략+popup 내용폭. selectable을 프리셀렉트·옵션·게이트 일관 적용. 브라우저 검증·tsc0 [robust-signal-over-literal,batch-same-surface,filter-all-derivations] → retrospect 208
- 219 메모리 일치/유사도 Segmented→Tabs 통일 — **done**(retrospect 209): 상위 기억 Tabs와 시각 불일치 지적. 일치=편집목록·유사도=회상시험이라 서로 다른 도구→Tabs가 규칙(내용전환)에도 부합(원 Segmented 분류 미스핏). Agent/User 패널 둘 다. admin/CLAUDE.md 규칙 예시 재분류. 브라우저 role=tab·Segmented잔재0·tsc0 [mode-switch-swaps-tools-is-tabs,update-rule-doc-on-reclassify] → retrospect 209
- 220 메모리 스코프 이름 위주+중복제거 — **done**(retrospect 210): 스코프 UUID→이름(친숙), 유저 패널 안내문이 이미 이메일 표기라 이메일 2회 노출→스코프 줄을 신분 단일 표기처로·안내문서 이메일 제거(사용자 선택). UUID는 툴팁 강등(진단 보존). PagedMemoryList/Agent·UserMemoryPanel/MemoryView. 브라우저 유저 이메일 1회·tsc0 [one-canonical-place-for-identity,scan-nearby-before-surfacing,demote-not-delete] → retrospect 210
- 221 유사도 검색 입력 레이아웃 정돈 — **done**: TextArea|세로스택 top-align이라 바닥·상단 어긋나 들쭉날쭉→세로 스택(질의 전체폭 위+아래 한줄 컨트롤 행: limit라벨·숫자·조회 우측정렬 수직중앙). 공용 RetrievalTestPanel이라 컬렉션 검색 드로어도 개선. 브라우저 스샷·tsc0 [stack-plus-toolbar-row,shared-panel-fix] → 스샷
- 222 승인 시각 원본ISO→날짜+시각 친화 — **done**(retrospect 211): 원본 ISO 마이크로초+TZ "너무 길다"→fmtTime 적용했더니 옛날은 날짜만("시간까지 보여줘야")→초까지("초까지 보여주세요"). format.ts에 fmtDateTime 신설(항상 날짜+HH:MM:SS 24h), ApprovalsView 3곳 적용. fmtTime은 목록용 유지. 브라우저 "요청 7월 5일 20:29:50"·ISO0·tsc0 [audit-needs-datetime-not-friendly-list-fmt,cut-noise-not-info] → retrospect 211
- 223 승인 처리됨 카드 요청→처리 흐름 순서+라벨 공백 — **done**: 처리→요청(역순)이던 메타를 요청→처리자→처리 시각 순으로(실제 시간 흐름), 요청 줄 clock아이콘+흐린색 제거, 라벨 뒤 {' '} 공백 명시. 브라우저 순서·공백·tsc0 [display-in-causal-time-order,explicit-label-space] → 스샷
- 224 능력 부여 대상 Segmented→Tabs 통일 — **done**: [역할에게/특정 유저에게]가 Segmented라 상위 Tabs와 불일치. 대상(역할 vs 유저) 전환이 폼·목록 바꿈→Tabs 부합(스펙 219 동형). UsersView Segmented→Tabs·미사용 import 제거. 브라우저 role=tab·Segmented잔재0·안내문 갱신·tsc0 [mode-switch-swaps-tools-is-tabs] → 스샷
- 225 유저 권한 부여 어휘 정리(능력→권한) — **done**: 사용자 지적. UsersView 표시용 '능력'→'권한' 전량(탭·도입문·컬럼·토스트), '역할에게'→'역할 기준'(권한기준 요청은 첫박스가 역할이라 되돌림)·'특정 유저에게'→'유저 기준'. 표시 문자열만(백엔드 capability 개념 불변). 브라우저 '능력'0·tsc0 [ui-label-vs-backend-term,permission-wording] → 스샷
- 226 배치 폼 레이아웃 정돈 — **done**: 사용자 "스타일/컴포넌트 배치 조절 필요". 좁은 입력 오른쪽에 떠 있던 힌트 span 6개 → Form.Item extra(필드 아래 정렬), 세션 정리 두 숫자필드(보존일수·최소 턴 수) 나란히. 레이아웃만·로직 불변. tsc0 [batch-form,extra-hint-below,related-fields-side-by-side]
- 227 배치 4개 잡 탭 구성 — **done**: 사용자 제안 "배치를 탭으로". 세로 나열 네 잡(세션 정리·메모리 통합·A2A 정크·테스트 유저)→언더라인 Tabs 스위처, 활성 잡만 조건 렌더. 실행 이력(공용)은 탭 아래 공통 유지, 입력 상태 탭전환 보존. 스크롤 대폭 감소·앱 전역 탭과 일치. tsc0 [batch-tabs,jobs-as-tabs,shared-history-below]
- 228 허용 호스트 SSRF 설명+레이아웃 — **done**: 사용자 "SSRF 경고만 있고 설명 없다"+"스타일/배치 수정". 경고 배너에 SSRF(서버측 요청위조) 평이 정의·왜 기본차단하는지 2단락 추가. 추가 시각 raw toLocaleString→fmtDateTime(앱 통일·감사정밀), 호스트/메모 두 필드 나란히. tsc0 [ssrf-explain,threat-model-in-warning,fmtDateTime,side-by-side-form]
- 229 SSRF 설명 접이식 — **done**: 사용자 "자세한 건 알겠는데 매일 보고 싶진 않아". 228 상세 정의를 Collapse(ghost·기본 접힘) "SSRF가 무엇인가요?"로 분리, 경고(보안 주의)는 상시 유지. 교훈: 경고=상시·정의=접이식(충분히 자세히≠매일 보이게). tsc0 [collapsible-explanation,always-vs-on-demand,antd-collapse]
- 230 호스트 경고 한 문장화 — **done**: 사용자 스샷 "한 문장만 노출, 나머지 숨김". 경고 Alert를 3문장→핵심 한 문장("신뢰하는 대상만 추가"), 나머지 상세(통과경로·정확host 규칙)는 SSRF Collapse로 합침. 교훈: 상시=행동 좌우 한 줄, 상세=접이식(노출 계층 분리). 228→229→230 축약. tsc0 [one-sentence-banner,disclosure-layers,collapse-details]
- 231 호스트 부제 제거 — **done**: 사용자 "이 문장도 숨기자". Page subtitle(SSRF 가드…allowlist) 제거·제목만, 무재시작 운영정보는 SSRF Collapse 끝에 보존(토스트에도 안내). 228~231 4단 축약(노출 계층 접기). tsc0 [remove-subtitle,disclosure-layers,minimal-chrome]
- 232 호스트 새로고침 목록 옆으로 — **done**: 사용자 "새로고침도 아래로". Page actions(상단) 제거→"등록된 허용 호스트" 제목 옆(우측)으로. 액션은 갱신 대상 곁에. tsc0 [action-near-target,move-refresh]
- 233 능력×타입 조합 전수 매트릭스 — **done**: 사용자 "도구·기능·타입 경우의 수 계산+모든 조합 실발동 테스트"(계기 201 배선부재). impl 8×기능 6=48칸, verify_110 패턴(실저장→채팅관통→trace, 오버라이드 금지). 예측 19/29→실측 22/26(발견: memory 회상은 플랫폼레벨·consumes 무관 전impl 발동). codex 2R로 false-green 3건(노드존재≠성공·표면우회·공유상태 vacuous) 봉합. PASS30+IGNORE26, 제품결함0 [capability-matrix,fire-not-just-wired,broker-success-not-node,causal-control,memory-platform-level,codex-adversarial] → 회고212
- 234 메모리 회상 타입별 게이트(233 봉합) — **done**: 사용자 "메모리 설정 거짓?" 233 발견(회상=플랫폼선처리라 consumes 무관 발동, 폼 "무시됩니다"가 기억엔 거짓)을 런타임을 폼에 맞춰 봉합. chat.py 신규+재개 두 경로에 consumes 게이트(memories 선언 impl만 회상·자동기록), consumes=None 면제(206 정합). verify_233 artifact memory 발동→IGNORE 뒤집힘. codex 재개경로 잔여회귀 지적 봉합 [memory-gate-by-consumes,dual-wiring-both-paths,form-claim-made-true] → 회고212
- 235 비영속(ephemeral) 1회성 추론 — **done**: 사용자 "기능 다 끄고 1회성 추론 제공=DB 적재 안 함"(고트래픽·기록 무의미). config.ephemeral(bool)—_persist 즉시 return None(세션·메시지·카운터·commit 스킵)+메모리 off. **핵심 봉합**: ckpt=None(체크포인터 미부착→checkpoints/writes/blobs 미기록+HIL interrupt 구조불가→_create_approval 미도달)+langfuse 스킵(신규·재개 대칭). persistHistory=false는 반쪽(메시지만·세션 남음)이라 그 상위 플래그. 다배선 미러링 8점(스키마×2·직렬화·타입·매핑×3·mock×2). UI: SectionHeader로 모델동작/저장·영속 경계+토글(ON시 하위 비활성). verify_235=실 checkpointer 초기화 후 ephemeral 전테이블 Δ0·대조 checkpoints Δ3(인과증명). codex 2R(R1 _persist밖 3갭 적발→봉합,R2 인과확인). verify_233 무회귀 [ephemeral-no-persist,zero-write-count-all-tables,structural-seal-checkpointer,causal-control-activate-signal,boundary-sectioning] → 회고213
- 236 도구 무발동 진단성+mock 한계 표면화 — **done**: 실사용(2026-07-07 위키 도구 조용한 무발동—사용자가 엉뚱한 층 추적) 처방 3부. P1 trace.toolDiag(bound/called, consumes 게이트로 route/artifact 오진 방지)→인스펙터 기본탭 무발동 Alert+설정탭 "연결된 도구" 섹션. P2 폼 mock+도구 사전 경고(provider_kind 레지스트리 감지). P3 mock 일반 트리거(문장에 도구 base 이름 언급→발동, 키워드 트리거 우선)—**원 버그 시나리오(wiki_search)가 mock서 실발동**. codex 5건: 단어경계(search⊂research)·비-string required 포기·브로커턴 억제 수정, 승인/ask 턴 부재는 문서화 경계. verify_236 4/4·233 무회귀·브라우저 대조 포함 [tool-silence-diag,why-not-surface,mock-limit-declare-and-narrow,word-boundary-trigger,consumes-gated-diag] → 회고214
- 237 비영속 도구 경계(DB 쓰기만 금지) — **done**: 사용자 결정 "직접 DB에 쓰는 도구만 금지(MCP는 내부DB 안쓴다 가정·읽기 무관, RAG 조회 가능)". memwrite/memedit만 금지: 저장 게이트 422(create/clone/update+**activate/revert** codex#1)+런타임 ctx 필터(레거시·오버라이드 방어)+승인 명시 에러. **실측 발견=235 가정 반증**: 체크포인터 None이어도 interrupt 발생→_create_approval이 세션+Approval 씀(E6은 승인도구 미트리거 vacuous)→명시 게이트가 실봉합. 폼: 비영속시 저장항목 숨김(단 기선택시 노출=해제가능)+경고. verify_237 8/8(RAG조회 실발동+전테이블 Δ0·레거시 Δ0)·235/233 무회귀 [ephemeral-write-tool-ban,structurally-impossible-must-be-tested,vacuous-zero-again,promotion-paths-need-gate,hide-but-keep-escape] → 회고215
- 238 에이전트 폼 UX 재정비(사용자 리뷰 5건) — **done**: ①영속/비영속 상단 Segmented 1급 승격(기본 영속, 세부설정 토글 제거) ②금지=경고 아닌 disabled(**미선택-잠금** `disabled=조건&&!선택됨` — 새 진입 잠그되 기선택 해제 가능, 비영속 전환시 memwrite 자동해제, mock Alert도 보조문구 강등) ③종류 Select optionRender rich화 ④오버라이드 폼 정합(비영속 read-only 태그+기억/memwrite 동일 잠금 — 122 자매표면 누락 재발 방지) ⑤config.suggestedPrompts(캡 8×200 서버 validator)→플그 카드(미등록시 기본 3종 폴백). codex 5건: 편집폼 기억그룹 누락·stale disabled+checked 수정, 자동해제 미복원=의도(취소시 무손실 확인). e2e 11/11×2·237/235/233 무회귀 [form-ux,first-class-mode-placement,disabled-not-warn,unselected-lock-pattern,sibling-form-sync,suggested-prompts] → 회고216
- 239 에이전트 폼 위저드 개편 — **done**: 사용자 "지금도 어렵다, 계획 논의 열심히"→AskUserQuestion 3라운드 합의(4단계 위저드: 정체성→하는 일→세부→요약·확인, 생성/수정 통합, 요약까지 가야 생성/저장, 760px). 순수 표현 개편(데이터 모델 무변경). codex 5건: **조율형 ragMinScores 저장 유실(191 이래 잠복 실버그 — 표시는 vectorTables∪rag:* 저장은 vectorTables만)** 수정·요약 payload 축소 2건(보존 연결 표기+산출물 필드 나열) 수정·2건 경계. e2e 13/13·scenario-audit 위저드 갱신 7/7·237/235/233 무회귀 [form-wizard,structure-discussion-first,summary-must-cover-full-payload,display-vs-save-condition-mismatch,scenario-audit-same-turn] → 회고217
- 240 평가의 버전 귀속+경량 환경 기록 — **done**(AgentOps A): EvalRun.agent_version(시작 시점 활성 버전, 초안만이면 None=정직)+env JSONB(모델 params 마스킹·MCP enabled_tools·컬렉션 docs/chunks/임베딩 조인). 외부 제안 "완전 재현 Snapshot"은 진단 단서로 경량화. 마이그레이션 f240a1b2c3d4(단일 head 검증). codex: params 비밀노출(P1)→_env_redact·오버라이드 name/params 불일치→_model_env·ORM 파생필드 착각→조인 수정, 박제시점 드리프트=경계. verify_240 5/5(버전 전환 추적)·브라우저 3/3(칩·헤더·환경 접이식) [eval-version-attribution,env-snapshot-lightweight,redact-diagnostic-records,derived-field-not-orm,null-is-honest] → 회고218
- 241 버전 활성화 시 자동 회귀 — **done**(AgentOps B): activate·revert(승격만, promoted 플래그) 후 fire-and-forget으로 회귀 자산(실행 이력∪수확 문제집, may_manage 필터, 최근 3개) 자동 재실행. env.trigger=activate+240 버전 박제→이력 "자동 회귀·vN" 태그. **문제집×버전 dedupe**(재활성화 연타 비용 차단). 정직 경계: 서빙 실측이라 "배포 전" 게이트 아님(회귀 시 revert가 대응)·동시 경합/task 보관은 문서화. codex: 소유권 우회·반복 비용 수정. verify_241 7/7·240 무회귀·브라우저 태그 [auto-regression,inherit-manual-gates,idempotency-key-per-version,fire-and-forget-contract] → 회고219
- 242 버전 지정 실행+버전별 평가 — **done**(AgentOps, 사용자 방향 "내부 버전별 서빙·외부 활성 하나"): "버전별 서빙"=배포 아닌 **config 소스 전환**(AgentVersion 스냅샷 기보유 — _load_context version 파라미터·persona 재해석). trace.agentVersion(+versionPinned) 전 턴 기록, RunStartIn.agent_version→초안 평가=**배포 전 게이트 성립**(241 경계 해소), EvalView 버전 Select. codex: ask/form 재개 drift(pending 버전 승계)·초안 권한(may_manage 403)·trace 누락 수정, A2A 경계 확인. verify_242 9/9(인과·멤버 403)·240/241/235 무회귀 [version-pinned-exec,config-source-not-deployment,resume-paths-inherit-axis,draft-needs-manage-perm,pre-deploy-gate] → 회고220
- 243 플레이그라운드 버전 선택 — **done**(242 UI): 헤더 AgentCombo 옆(세션 선택 **전** — 사용자 지시 순서) 버전 Select(활성 기본·초안/보관, ui+버전존재+can_manage만)+"미리보기 vN" 배지, 변경=새 대화(혼재 방지), streamChat version 전달, 인스펙터 메트릭에 "버전 vN (미리보기)". 경계: A2A 토글=버전 미적용(설계)·미리보기+과거세션 혼재는 턴별 trace로 식별. 브라우저 5/5·242 무회귀 [playground-version-preview,version-before-session,reset-on-change,inspector-version-metric] → 회고220(242와 합본)
- 244 버전 운영 화면 — **done**(AgentOps D — 로드맵 A~D 마감): GET /agents/{id}/ops(버전별 평가·자동회귀·피드백 집계, trace.agentVersion 귀속)+상세 드로어 버전 행 칩("평가 N% (최근 성공)·M회"+자동회귀+실패+👍👎)+버전 미상 분리 표기. codex 4건 전부 수정: 조인 불일치 방어·**관리자 전용 403**(집계=전 유저 요약)·**limit500 조용한 캡→SQL 전수(GROUP BY+DISTINCT ON)**·최근성공 라벨+실패 칩. verify_244 8/8·브라우저·241/242 무회귀 [version-ops-view,aggregate-four-traps,join-consistency,summary-needs-own-perm,sql-full-not-capped,label-carries-meaning] → 회고221
- 245 에이전트 상세 풀페이지+복잡도 전수 진단 — **done**: 사용자 "기능 쌓여 어려워짐" → 드로어(640px·4관심사 12블록)를 5섹션 페이지(개요/구성/버전·배포/공개·연동/운영+좌측 네비)로 승격(ui만, code/external 드로어 유지·레거시 source 구멍 codex 수정). **complexity-audit.mjs 신설**: 전 화면 블록·컨트롤·세로배수 실측 → **승인 화면 압도적 1위(117블록·22x — 무페이지네이션이 데이터 비례 성장)=다음 개편 후보**. 한계 정직: 드로어/모달 속은 지표 밖. e2e 9/9×2·tsc0 [detail-full-page,concern-based-sections,complexity-measured-not-felt,rearchitect-periodically,self-contained-guards] → 회고222
- 246 정보 3층 위계(상세 1차 적용) — **done**: 사용자 "복잡한 데이터 인정, 잘 보여줄 고민 — 구분+필요한 것만" → 서비스 문법 합의(1층 상태배지 항상/2층 관심사 탭 하나만 렌더/3층 빈값 접기·식별자 복사 강등). 개요=**요약 대시보드(지도)**: 관심사별 한 줄+클릭 점프, 행동 필요한 알림만 입장. 245는 분류만·노출량 그대로였음(구분≠필요한 것만 — 탭·접기·요약층 3처방 별도). e2e 7/7(비활성 탭 미렌더·빈값 접기·활성화 동작·360px 오버플로 0) [three-tier-hierarchy,overview-is-a-map,classification-then-exposure-control,grammar-not-one-screen] → 회고223
- 247 플레이그라운드 집중 모델 — **done**: 사용자 설계 수업("레이아웃은 마지막 — ①무엇에 집중 ②무엇을 가림") 후 자료 학습(NN/g·HIG)→합의: 대화=주 과업(개선 루프 작업대)·**계측 상시 유지**(숨김 추천 기각)·헤더=에이전트만 노출(런처안 기각). 설정 6종(버전·세션·A2A경유·시스템프롬프트·오버라이드·인스펙터)→"대화 설정" 팝오버, 신호 배지(미리보기·오버라이드✓·A2A)만 헤더, 추천카드 중복·LangGraph 내부어 제거. e2e 7/7·모바일 0 [focus-then-hide-then-layout,conversation-is-primary,signal-badges,settings-behind-one-entry] → 회고224
- 248 플레이그라운드 에이전트 조회·헤더 공동 설계 — **done**(후속15까지): 콤보 검색·최근 사용 정렬·시작 캐스케이드(선택지 있을 때만 노출)·2줄 칩(버전 ●상태/vN 우측·세션 요약/전체ID 좌측·상태별 3문구)·중복 5건 제거(초안 태그/라벨·+새대화·인스펙터·시스템프롬프트 버튼)·U 손잡이+top 드로어·모바일 3줄 확정. e2e 스위트+overflow-both 게이트 [agent-lookup,two-line-chips,u-handle-top-drawer,dedupe-canonical,mobile-3rows] → 회고224·225
- 249 오버라이드 서랍 단계형 — **done**: 생성폼 기조 이식(Steps 2단계 ①모델·프롬프트 ②도구·지식·세부, 요약 생략=사용자 결정, 하단 고정 푸터에 적용—새 대화 상시), 데탑 2단계 2열·쓸 것 내부 스크롤 상한(도구 14 실측 봉합)·destroyOnHidden·autoFocus off. U 손잡이 서랍(248 후속15~31)과 한 벌: ∨/∧ 손잡이·패널 부착·영역 한정·무헤더. **모바일 2단계 실기기 확인 잔여** [override-steps,u-handle-drawer,inline-drawer-traps,capacity-cap] → 회고226
- 250 UI 중복 표현 전수 스캔 — **done**: 3중 검증(deep-reasoner 14뷰 정독+기계 텍스트 스캔 14화면+육안 확정)으로 확정 중복 3(플그 헤더 오버라이드✓ 이중·세션 드로어 채널 이중·유저 활성/상태 컬럼 이중), 회색지대 1(상세 배지↔개요 지도=의도된 위계, 유지), 소견(승인 116x=무페이지네이션 재확인·개요 세션 UUID 1급) [dedupe-audit,three-way-verification,natural-repeat-heuristics] → 수정은 사용자 선별 대기
- 251 승인 화면 개편 — **done**: 복잡도 1위(117블록·컨트롤 233·세로 22x)→3블록·45·2.4x. GET /approvals/page({items,total}·status·q)+PagedListShell 재사용, 행=판정 요약·인자 JSON=드로어(3층), resolve→total 재조회·배지 동기화. e2e 9/9·복잡도 재측정 [approvals-pagination,paged-shell,three-tier,badge-sync] → 회고228
- 252 RAG·평가 점검·수정 — **done**: 3중 점검(deep-reasoner 6파일+런타임 실측+육안)으로 소수술 5(judge 태그 관통·raw enum 한글화·과잉 컨테이너·내부어 top_k/limit·Tag 폭 상한)+문제집 드로어 탭 분리(문제/실행·성적 — 관심사 3겹 해소, 사용자 결정). 건전 판정 근거 기록(meta JSON=디버그 통로 의도 등) [rag-eval-grammar,dataset-drawer-tabs,plain-language] 
- 253 칩 시맨틱·헤더 줄바꿈 — **done**: rag/mcp 칩=성공만 카운트+실패는 빨간 별도 칩(시도 +1=거짓 신호, 실패는 지우지 않고 승격), 데탑 헤더 minHeight+wrap(인스펙터 병행 짜부 해소). 실패 칩 실검증=사용자 실기기 장애 상황 [chip-success-only,fail-chip-promoted,header-wrap] 
- 254 엔티티 히트 카드 정돈 — **done**: 직렬화 원문의 빈 키 라인 나열(사용자 불편)→값 필드만 2열 표+빈 필드는 이름만 한 줄, meta raw JSON→null 제외 key=value 칩(149 원본 특정 축 유지). 사용자 케이스 시드 재현 e2e 8/8 [entity-hit-card,empty-fields-folded,meta-chips]
- 255 엔티티 meta JsonTree·인스펙터 히트 구조화 — **done**: meta=JsonTree(사용자 제안, null 포함 원본), EntityFields 공용화(검색 시험+인스펙터 드리프트 0), 백엔드 프리뷰 개행 보존(구조화 렌더 전제). e2e 8/8+2/2. **후속**: 개행 보존이 노출한 버그 — parseEntityText가 멀티라인 필드 값(description 등)의 연속 줄을 key: 아님으로 보고 통째 null→원시 폴백("예전처럼"). 수정=연속 줄을 직전 필드 값에 이어붙임+문서청크는 여전히 원문(필드<2 가드)+값 pre-wrap. 단위 4케이스+브라우저 e2e 4/4 [jsontree-meta,entityfields-shared,preview-newline,multiline-value-continuation,preserve-more-breaks-consumer]
- 256 조율형 로컬 에이전트 위임 — **done**: 인프로세스 직접 호출(I/O 최소, 사용자 방향), 평가 러너 재사용(무오염·승인=정직 실패), 깊이 N(v2 — 방문 집합 체인: 재방문만 차단·다단 하강 가능·캡 8, 3단 체인 실증), 하위 트레이스 관통(subTraceNodes→인스펙터 "하위 실행 흐름"), 폼 "· 로컬" 노출. 보너스: A2A 위임 카드 미표시 잠복 버그(node↔cap_id 매칭) 수정. **codex 후속**: 브로커 로컬 위임에 may_use_agent 게이트 누락(P1 — 타인 private 실행·존재 오라클, 본경로만 정합 깨짐)+재개 경로 principal/chain 유실(P2 — build_broker(None) 크래시+순환 약화)+위임 총량 예산 부재(P2 — breadth 폭주, DELEGATION_MAX_TOTAL=32 공유카운터) 봉합, resultPreview 누출=P1 종속 소멸. verify_256 9/9 [local-delegation,in-process,depth-n,subtrace,latent-a2a-card-bug,broker-may-use-gate,resume-principal-chain,delegation-budget] 
- 257 위임 구조(DAG) 시각화 — **done**: DelegationGraph 공용(들여쓰기 트리·조율형/로컬·A2A/미서빙 태그·비-agent 능력 요약·순환=빨간 "실행 시 자동 차단" 마커·깊이 캡 8), 상세 구성 탭(저장 config)+오버라이드 서랍 2단계(draft 미리보기 — 체크 즉시 반영). 구성 표 raw id 행 제거(그래프 canonical) [delegation-graph,dag-view,cycle-marker,draft-preview]
- 258 agent-flow 스킬 두 저작 모드 — **done**: 사용자 보고("source=code로, doc-translator처럼")가 개념 충돌 노출 — agent-flow=인프로세스 flow(source=ui+impl, 우리 런타임 실행)인데 doc-translator=source=code=원격 A2A 서비스(endpoint 필수, _a2a_stream 릴레이). 사용자 결정=두 모드 다 지원+의도 물어 분기. SKILL.md에 0단계(모드 가르기)+모드A(인프로세스, 기존 보존)+모드B(SDK code, 신규: x-my-agents 카드 계약·register/connect·mock_remote 데모·verify_057/154/117). 유령참조 버그 교정(classify_conformance→classify_runtime, 실존 안 함=따라하면 실패). API/런타임 코드변경 0(경로 기존). deep-reasoner 검토 코드일치 확인 P1 0 [agent-flow,two-modes,in-process-vs-remote,source-ui-impl,source-code-a2a,mode-split-ask-intent,ghost-reference-fix,doc-code-parity] → deep-reasoner 문서검증
- 259 노드형 일렬 파이프라인 에이전트 — **done**: 노코드로 노드마다 프롬프트·모델·도구를 직접 정해 순서대로 잇는 impl=pipeline(LinearPipelineAgent, 신뢰 impl). 백엔드=노드별 모델 플랫폼 사전해석(model 이름→cfg, ctx에만·AgentOut엔 이름만=비밀무유출)+2 impl_config 주입+스키마 왕복. 프론트=NodeListEditor(190 관용구 계승)+위저드(239) 통합(정체성서 모델/페르소나 숨김·하는 일서 노드편집기·요약 처리단계행)+finalizeForm이 도구 풀을 노드 합집합서 파생(에이전트-레벨 도구 UI 없음)+왕복 3지점. 검증=verify_259 15/15+브라우저 왕복 11체크 ALL GREEN(GET /api/agents로 nodes보존·conformance·도구파생·페르소나불러오기 실측)+tsc0. 잔여=execute e2e(활성+chat)·memories 미사용·RAG 단일도구 스코핑 한계. codex 커밋후 권장 [pipeline,no-code,per-node-model-tool-prompt,linear,platform-resolves-model,derive-pool-from-union,wizard-variant,browser-roundtrip] → codex 완료(회고 236: A2A 3번째 입구·스키마 하드닝 봉합)
- 260 노드별 맥락 모드(이어받기/깨끗이 받기) — **done**(259 확장): 노드마다 context=carry(기본, 누적 대화 이어받기)/clean(앞 결과만 격리 입력·이전 대화 제거) 토글. 형식 고정(JSON) 노드가 쌓인 대화에 오염되던 문제(사용자 지적)를 노드별로 해결(전역 아님). 구현=pipeline.py normalize+_make_step(clean 첫 진입 RemoveMessage로 격리 buffer, 도구 루프 재진입 재-오염0)+프론트 Segmented 토글; _resolve_node_models {**n}·노드 통째 저장이라 자동 왕복. 검증=verify_260 10/10(U4 clean+도구 재진입 격리 유지, 가짜 모델로 받은 메시지 계측)+259 무회귀 15/15+tsc0+브라우저 왕복 context 보존. 템플릿 표현식 미도입(토글이 실수요 덮음)·형식 강제는 후속 [node-context-mode,carry-vs-clean,isolation-buffer,removemessage-reset,tool-loop-reentry,per-node-not-global] → codex 완료(회고 236)
- 261 노드 출력 형식 강제(자유 텍스트/JSON) — **done**(259·260 확장): 노드마다 format=text(기본)/json + fields(필수 키, 선택). JSON이면 프롬프트 지시+파싱검증(_coerce_json 관대 추출)+1회 보정+실패 시 원래 응답 통과(거짓 JSON 조작 안 함=정직). 강제는 최종 응답(tool_calls 없음)에만·정규화로 하류에 깨끗한 JSON. 구현=pipeline.py(_coerce_json 순수·normalize·_make_step _finalize)+프론트 Segmented+필수키 태그. 검증=verify_261 14/14(U5 보정실패→원문·가짜 모델 스크립트)+259/260 무회귀+tsc0+브라우저 왕복 format 보존. 경계=best-effort(보장 아님)·키 존재만(타입·중첩 후속)·폴백 은폐 신호는 후속 [node-output-format,json-enforcement,coerce-json,repair-once,honest-fallback,intervene-final-only] → codex 완료(회고 236: JSON 실패 관측 로그)
- 262 인스펙터 노드 요약: clean 노드 remove를 사람 말로 — **done**(260 표시 다듬기): "깨끗이 받기" 노드가 인스펙터에 "remove / remove"(RemoveMessage 내부어)로 날것 노출되던 걸 "이전 맥락 N개 정리 (격리)"로 접음. 고치기 전 계층 판별=remove는 모델 프롬프트에 없고(ainvoke [sys,human]뿐) 반환 델타에만=순수 표시(동작 무변). api/runtime.py _summarize_node_update messages 블록서 role=="remove" 카운트→노트, 나머지 발화만 프리뷰(086 불변식 유지). 검증=verify_262 6/6+브라우저 인스펙터 실측 "이전 맥락 N개 정리" 표시·"remove" 미노출. 표시만—격리 동작 불변 [inspector,node-summary,clean-mode,humanize-internal-term,display-only,keep-debug-signal]
- 263 노드형 폼 UI/UX 재검토 — **done**(259 다듬기): 상태 트리 5상태(정체성/빈/채움/요약/모바일360) 캡처 검토. 결함 2건 수정=①요약 거짓 확인(노드형인데 숨은 기본 모델·페르소나가 1급 표시→"모델·프롬프트: 노드마다 설정"+처리 단계에 노드별 모델 병기) ②안내 문구가 원인 필드(종류) 위에 떠 있고 typeDesc와 중복→종류 아래 합류. 나머지 양호 판정(근거: 스크린샷·모바일 오버플로 0 수치). 무회귀 왕복 그린+tsc0 [node-form,ux-review,summary-honesty,hidden-default-false-confirm,note-below-cause-field,state-tree-capture]
- 264 플랜 실행 데모를 노드형으로 전환+실모델 테스트 — **done**(259-263 도그푸딩): 하드코딩 plan_execute impl로 돌던 plan-execute-demo(v5)를 노드형 2노드(계획=모델이 진짜 계획 수립으로 승격·실행=페르소나 내장+wiki 도구)로 전환(v6, 가역). 실모델 qwen3.6-35b 4.7~8s ALL GREEN: 트레이스 계획→실행 순서·인스펙터 노드별 시간/출력·실토큰·도구 미호출 진단(125) 합작동. 정직 기록=중간 노드 출력이 최종 답 앞에 섞임(원 plan은 무토큰)→"마지막 노드만 응답" 옵션 백로그 [plan-demo,pipeline-conversion,real-model,deterministic-to-real-plan,reversible-version,intermediate-output-in-response]
- 265 노드 도구 이름 해석 버그(조용한 미바인딩→도구 환각) — **done**(264 후속, 사용자 지적): 노드 tools가 민이름("wiki_search")인데 런타임 도구는 접두명("web-fetch__wiki_search", _safe_name)이라 by_name 매칭 0→도구 조용히 미바인딩→모델이 "검색한 결과…" 호출한 척 환각(크래시 아님). 수정=엔진 _resolve_tool(정확 일치→무모호 접미 폴백·모호 스킵, 권한 영향 0)+프론트 safeToolName 미러 저장(동명 도구 value 충돌도 해소). 검증=verify_265 5/5+실모델·실MCP 필수 단언(도구 루프 2회·wiki_search/wiki_page ok·위키 근거 답변)+무회귀+RAG 축 실발화(search_documents 호출 기록·docs-kb 원문 근거 답변, 무접두=정확 일치 경로). 교훈=핵심 행동 0회 발화한 그린은 그린 아님·미바인딩 증상은 에러가 아니라 환각 [tool-name-mismatch,silent-unbind,tool-hallucination,suffix-fallback,safe-name-mirror,mandatory-assertion]
- 266 인스펙터가 노드형 도구 노드(X__tools) 조회 카드를 안 붙이던 버그 — **done**(사용자 보고 "RAG 조회했는데 인스펙터에 안 나와"): 카드 귀속 술어 3곳이 node==='tools' 정확 일치(ReAct 관례)라 노드형의 `<노드명>__tools` 전부 탈락(백엔드 기록·칩은 정상, 표면만 누락). 수정=isToolNode 술어(endsWith '__tools')로 nodeMeta 라벨·카드 귀속·다회 순번 일반화(202/203 도시락 방지 승계). 검증=플그 UI e2e 필수 단언(칩 1 rag·도구 노드 라벨·RagCall 3건/검색어/유사도/히트카드)+tsc0. 교훈=축 검증 완결은 기록+표면, 새 그래프 위상은 노드명 소비 표면(귀속 술어)까지 정렬 [inspector,tool-node-attribution,name-predicate-closed-set,record-vs-surface,pipeline-topology-consumer]
- 267 노드 카드 문구·컴포넌트 간결화 — **done**(사용자 정의 문구): "맥락"→"이전 결과 받기"(대화 전체와 함께/이전 결과만), "출력 형식"→"응답 형식"; 두 설정 한 줄 그리드+한 줄 설명(세로 부피 절감); 첫 노드에도 받기 유지(A2A 연계 시 앞 에이전트 결과 수신, 사용자 확인). 저장값 불변(표시만). 왕복 17체크 그린+tsc0+모바일 오버플로0 [node-card,copy-clarity,user-defined-labels,compact-row,first-node-context-kept]
- 268 노드별 RAG(컬렉션)·메모리: 캐싱 회상 프록시 — **done**(P2=사용자 설계): P1=컬렉션별 검색 도구(search_documents__<컬렉션>, 세 입구 정합·비노드형 무회귀)로 노드마다 컬렉션 스코프, P2=턴 스코프 키워드 캐시 프록시(같은 키워드=캐시 공유→비용 1회 수렴, input 모드=노드 입력 키워드; 스코프 플랫폼 고정=브로커 선례·조회 계측 공짜)+노드 memories/memoryQuery+엔진 첫 진입 회상 블록(graceful). 검증=verify_268 20/20+실모델·실mem0 e2e(docs-kb 스코프 히트·cached:true 실측)+전 무회귀+tsc0. 주의=회상 게이트는 '장기 기억 (mem0)' 블록만(단기는 미회상, 직접형 동일) [per-node-rag,per-collection-tool,memory-recall-proxy,keyword-cache,cache-shared-live,broker-style-injection] → codex 완료(회고 243: 예약 접미·재개 스코프/트레이스·기록 마스킹·회상 안내 봉합)
- 269 기억 어휘 정리: 단기(히스토리)/장기(mem0) — **done**(3부작 1/3, 사용자 관찰: "단기(세션)이 뭐냐"=죽은 라벨이 선택 표면에 잉여). 단기(세션)은 백엔드 memory_enabled 무시하는 죽은 카탈로그 라벨(단기는 historyDepth가 소유)→선택 3표면(폼 피커·노드 옵션·오버라이드)서 제거+표시 4표면(인스펙터 태그·개요/요약 카운트) 필터, "채팅 히스토리"→"단기 기억" 4라벨, NodeListEditor 죽은 회상 경고(243 P3) 소멸, SHORT_TERM_MEMORY 단일 출처(mockData). 데이터 모델·백엔드 무변경(저장값 원문 보존). 검증=verify-269 e2e 8/8+tsc0+codex(Medium 인스펙터 표시누락·Low 카운트 수정/High 노드형 유도 드롭=죽은값이라 OUT). 교훈=무동작 선택지는 경고 말고 제거·필터는 옵션원천+저장값원본 두 축 전수 [dead-label-remove-not-warn,filter-both-axes,derived-field-dead-value-out,vocab-first-brick] → 회고 244
- 270 단기 기억 프록시: 노드별 대화 히스토리 창(HistoryWindowProxy) — **done**(3부작 2/3, 사용자 설계: 프록시가 원본 대화 쥐고 노드별 슬라이스). 근인=파이프라인 시드가 대화(턴 가로지름)+노드출력(턴 내)을 한 messages에 섞어 노드별 제어 불가. 처방=대화를 누적서 분리(시드=현재 턴만)해 _HistoryWindowProxy(mem0 프록시 형제)가 쥐고 노드별 depth 슬라이스=_window(전체,d)[:-1] 주입(carry만·clean 생략·재진입엔 매번 주입/기록 1회=inject-but-don't-persist). 노드 historyDepth facet(미지정=에이전트 상속·무회귀 기본)·NodeListEditor "단기 기억" Select(clean 비활성). 3주입 미러(메인·재개=DB대화재구성·A2A=None by-design). 검증=verify_270 20/20+실모델 e2e(3노드 depth[0,2,상속]→count[0,1,2] 실측)+268 무회귀(단위·실모델 도구루프)+codex(High 재개 [:-1] 오류=drop_last 플래그 봉합·Low2 pending트레이스/캡 미러). 교훈=한 자료구조가 두 축 겸하면 독립제어 불가·같은 슬라이스 불변식이 경로마다 다른 데이터에 깨짐(재개 DB엔 현재 턴 미영속)·안 만든 경로는 내 초록에 안 걸림(codex가 재개 잡음) [per-node-history,axis-separation,proxy-shared-slice,seed-reduction,inject-not-persist,drop-last-per-path,resume-db-reconstruct,untested-path-caught-by-adversary] → 회고 245
- 271 공용 역량 단위(기억 facet) — 에이전트·노드가 같은 단기/장기 컨트롤 — **done**(3부작 3/3, 안 B=facet별 공용 컨트롤). 노드=미니 에이전트라 6 facet 공유 중 기억부터: MemoryFields.tsx(ShortTerm/LongTermMemoryField 공용)를 NodeListEditor·AgentForm이 조립(라벨·옵션·도움말 1곳=drift 0). 에이전트 단기를 세부 ③→"하는 일" 기억 구획(장기 옆, 269 과도기 해소), 직접형 한정 이동(조율/노드/산출물은 세부 유지—노드형은 상속 원천). 269가 장기를 단일 옵션으로 줄여 접이식 PickerGroup→간결 multi-Select 무손실 하강(복리). 검증=verify-271 11/11(구획 이동·쓰기 배선·저장 왕복)+269 e2e 갱신+270/268 무회귀+codex Low2(단기가 기억게이트에 결합→단기 항상·장기만 게이트 해소/죽은 단기세션값 유실=무해 OUT). 데이터·백엔드 무변경. 교훈=공용 추출이 잠복 결합 드러냄("같이 보인다"≠"같은 조건에 보인다")·앞 정리스펙이 뒤 설계여지 넓힘·UI리팩터는 형제 테스트 노후화(불변식 유지·셀렉터 갱신) [shared-control,facet-extraction,coupling-revealed-by-extraction,gate-condition-differs,prior-cleanup-compounds,sibling-test-staleness] → 회고 246
- 272 노드 문서 facet 분리 — 도구에서 문서 떼어 에이전트와 개념 정렬 — **done**(271 뒤 facet 정렬, 공용화 아님). 노드가 도구+문서를 한 셀렉트에 접던 것을 도구(MCP)/문서(컬렉션) 2컨트롤로 분리(isDocTool=`search_documents`|`search_documents__*` 술어), onChange가 상대 부분 명시 보존해 합쳐 저장(병합 무손실). 저장 n.tools·백엔드·265/268 파생 무변경—UI 표현만. NodeListEditor(2컨트롤+불변식 주석)·AgentForm(nodeToolOptions→nodeMcp+nodeDoc). 공용 컴포넌트 통일은 카탈로그 스케일 패러다임 충돌로 반려(개념만 정렬·렌더 각자). 검증=verify-272 9/9(분리·병합보존·왕복)+268 실모델·270 무회귀+tsc0+codex(High/Medium 서버명 search-documents 오분류=측정상 미도달 오탐: safeToolName 대시보존+NAME_RULE 밑줄금지 이중차단·Low 레거시 민이름 드롭 OUT). 교훈=정렬≠공용화(스케일 다르면 개념만)·쓰기 병합보존이 핵심위험·적대 High도 측정 반증하고 불변식 못박음 [node-document-facet,alignment-not-sharing,merge-preserve,adversary-false-positive,pin-invariant] → 회고 247
- 273 오버라이드 드로어 기억 컨트롤 공용화 — **done**(사용자 검증 요청 "정말 통합됐나"서 측정으로 사본 발견, 지시=최소수선 말고 전체 교체). OverridePanel의 DEPTH_OPTS 사본+자체 단기 Select 제거→공용 ShortTerm/LongTermMemoryField 조립(장기를 좌측 피커에서 우측 세부로 이동=AgentForm 271 동구조, 비영속 미선택-잠금 중복 소멸), 스텝 제목 kind별("도구 · 기억 · 세부"/"맡길 것 · 세부"). 드래프트·payload 스키마 무변경. 검증=grep(DEPTH_OPTS 1곳·소비처 3곳)+verify-273 9/9(구조+기능왕복: 요청 캡처로 overrides={memories,historyDepth:0} 배선 실증)+109/123 형제 스크립트 갱신(249부터 이미 낡아 있던 것 포함, 시드명→자가 픽스처)+오버레이 감사 FAIL 0+tsc0, codex 스킵(UI 전용·payload 무변경 사유 명시). 교훈=공용화 완료조건은 개념 편집 표면 전수 grep [override-shared-memory,concept-surface-grep,request-capture,sibling-test-refresh] → 회고 248
- 274 모델·프롬프트 facet 공용화(+chat 필터 버그 수정) — **done**(273 기준 전수 점검서 발견, 사용자 승인+실행 중 노드 카드 배치 지시 합류). ModelFields(chatModelOptions+ModelField: chat 필터·미등록 보존 단일 출처—에이전트 폼만 무필터라 임베딩 노출→저장 통과→런타임 kind=="chat" 미해석 실패하던 버그 소멸)·PromptFields(라벨 줄 페르소나 로더+TextArea, 사본 페어=노드·오버라이드만—에이전트 폼 참조 저장은 시맨틱 달라 유지) 신설, 3+2 표면 교체. 노드 카드 배치=프롬프트→모델→단기·장기→도구·문서→받기·형식(사용자 지시, 단기가 받기/형식 사이 끼임 해소). 검증=verify-274 8/8(임베딩 제외 실증·DOM 순서·로더 기능 자가픽스처·ghost 보존)+269/271/272/273/109/123 회귀+tsc0+육안, codex 스킵(UI 전용 사유 명시). 발견=antd v6 단일 Select는 .ant-select-content 렌더(관례 셀렉터 거짓 음성→DOM 프로브) [model-prompt-shared,chat-filter-bug,node-card-layout,semantic-boundary-sharing,antd-v6-select-dom] → 회고 249
- 275 노드 카드 접기/펼치기+접힘 헤더 요약 — **done**(사용자 요청: 아코디언 접기·접으면 헤더에 설정 요약). 노드당 antd Collapse 1아이템(collapsible="icon"=123 오토글 함정 회피, ↓ 연결 유지), 접힘 헤더=이름+요약 한 줄(모델·도구/문서/장기/단기 수·이전결과만/JSON 비기본값·프롬프트 앞 60자 ellipsis)+미완성 "작성 필요" 빨간 태그(접혀도 저장 게이트 원인 노출), 기본 전부 접힘·추가 노드만 펼침(복수 펼침 허용), 펼침 상태=UI 로컬 인덱스 키(이동/삭제 재매핑). 저장 무변경. 검증=verify-275 11/11+269/272/274 회귀+tsc0+데탑/모바일 육안·오버플로 0, codex 스킵(UI 전용). 교훈=선례가 설계를 선결·접힘은 오류 신호를 헤더로 승격·인덱스 키는 배열 연산마다 재매핑 [node-collapse,summary-header,collapsible-icon,error-promotion,index-key-remap] → 회고 250
- 276 직접형 에이전트 도구 단위 배선 — 서버 단위→도구 단위 — **done**(사용자 근본질문서 발견: 직접형은 서버 고르면 하위 도구 전부 노출, 노드형만 도구 단위). AgentConfig.tools(런타임명 server__tool) 신설, mcps=서버 합집합 파생 유지(진실원 무변경·소비처 전수 안 건드림). runtime.selected_for_server(서버별: 항목 있으면 그것만·없으면 전체=구저장 무회귀)로 build_mcp_tools 도구별 필터. chat.py impl 게이트(pipeline/조율형은 tools 미적용=노드/capabilities 소유). 프론트=도구 단위 피커·mcps 파생 토글·구저장 하이드레이션(카탈로그 확장)·승인 목록 실배선 도구만·오버라이드 도구 단위. 검증=단위 10/10+e2e 15/15(positive/negative 노출차단/control 대조/pipeline 게이트/하이드레이션)+272~275·268 회귀+tsc0+codex(pipeline 조용한 미바인딩 1건 수정, High 오버라이드확대·Medium safe_name충돌·다중서버폴백 3건은 정직경계/의도). 교훈=사용자 근본질문은 감사 트리거·틀린 전제는 코드로 반증·진실원 위 필터층=무회귀 [tool-level-wiring,mcps-truth-source,selected-for-server,impl-gate,honest-boundary] → 회고 251
- 277 도구 선택을 서버→도구 계층 트리(antd Tree)로 — **done**(사용자 요청: 리치 컴포넌트 [x]server/[x]tool). 공용 ToolTree(antd Tree checkable·검색·선택배지, 서버=부모·도구=자식 리프 key=런타임명·부모체크=전체·부분=indeterminate 비-strict 자동, onCheck서 srv: 걸러 리프만 onChange) 신설. 직접형 폼(도구 PickerGroup→트리·setTools mcps 파생)·노드 카드(도구 Select→트리·문서 Select 유지)·오버라이드(도구 PickerGroup→트리) 조립. 값=런타임명(276 그대로)·저장/백엔드 무변경. 검증=verify-277 9/9(계층·자식체크 저장왕복·부모체크 전체·노드 병합보존·오버라이드 트리)+272·273·274·276·109 셀렉터 갱신 GREEN+275·268 무영향+tsc0+모바일 오버플로0, codex 스킵(위젯 표현만). 교훈=데이터가 트리면 트리 위젯·앞 스펙 값축통일이 위젯교체 값싸게·위젯교체는 형제 셀렉터 무더기 노후 [tool-tree,antd-tree-checkable,data-hierarchy-widget,widget-swap-staleness] → 회고 252
- 278 도구 트리 점진 공개 — 아코디언 복원+1 depth 초기화+블릿 수리+도구 설명 — **done**(사용자 피드백 4건: 아코디언 소실·블릿 불능·전부 펼침 인지부담·설명 없이 못 고름). 근인=277이 expandedKeys 상수 고정+빈 onExpand(스위처 죽고 만개). ToolTree v2=Collapse 래퍼(도구+선택배지·선택 있으면 기본 펼침)·expandedKeys 상태화(초기 []=서버만·스위처 동작·검색 시 매칭 자동확장)·리프에 toolsMeta 설명(151부터 카탈로그에 있던 데이터, 표면만 누락)·서버 "도구 n개". 세 표면 자동 적용(공용 위젯)·값/백엔드 무변경. 검증=verify-278 8/8(접힘·1depth·토글 왕복·설명·검색확장·배지)+형제 277/276/272/273/109/275 갱신 GREEN(275=중첩 Collapse가 nth 인덱싱 깨서 collapsible-icon 스코프)+tsc0+오버플로0+육안, codex 스킵. 교훈=controlled 상수=상호작용 사망·리치=점진 공개·중첩 컨테이너는 형제 nth 셀렉터 의심 [progressive-disclosure,expanded-keys-state,tools-meta-description,nested-collapse-scope] → 회고 253
- 279 에이전트 폼 흐름 3종 — 종류 선행·비영속 기억 게이트·도구/승인/문서 형제 아이템 — **done**(사용자 UI 집중수정 3건+실행 중 정정 2건). ①이름·설명→종류→모델·페르소나(종류가 설정 위치 결정); ②최초 "단기/장기 모두 잠금" 지시를 사용자 확인 질문("비영속에도 최근 N개 되나?")→측정(단기=body.messages 실행 창, chat.py:1114, DB 무관)으로 정정—장기만 memories 제거+잠금·단기 활성+안내 hint(238 원결정 유지); ③1차 "한 패널에 3구획" 반려→Collapse(도구 Item/승인 Item(도구 선택 시 사이 출현)/문서 Item) 형제 아이템, ToolTree bare prop·승인 렌더러 추출(조율·노드형은 기존 위치). 검증=verify-279 16/16(순서·비영속 소거 저장 실증·아이템 출현 순서·전 필드 왕복)+형제 8종 GREEN(276·277 .last() 갱신)+tsc0, codex 스킵. 함정=v6 패널 클래스 .ant-collapse-panel·순서 변경이 div-has .first() 깸 [form-order,ephemeral-memory-gate,collapse-sibling-items,order-breaks-selectors] → 회고 254
- 280 대화 저장 토글을 저장 방식 아래로+관계 설명 — **done**(사용자 질문 "영속과 대화 저장 차이?"가 신호: 포함 관계인데 step ①/③ 분산). step ③ '저장·영속' 구획 소멸→step ① 저장 방식 바로 아래(비영속=실효값 off+disabled 유지), 설명 구체화(ON="세션 재개·인스펙터 소급 열람 가능"/OFF="세션·기억·통계는 유지, 재개·소급 열람만 불가"). payload 무변경. 검증=verify-280 6/6(순서·문구 전환·disabled·persistHistory=false 왕복)+279 무회귀+tsc0, codex 스킵. 교훈="차이가 뭐냐" 질문=배치 결함 신호·부모-자식 설정은 인접 배치 [persist-toggle-relocation,adjacent-parent-child,difference-question-signal] → 회고 255
- 281 저장 항목 리스트(확장 가능)+비영속 승인 제약 명시 — **done**(280 대체: 사용자 "대화는 저장 대상 중 하나, 상태 늘어도 확장 가능해야"+"비영속인데 승인 오버라이드 나오는 게 맞나?"). step ①=저장 방식 아래 '저장 항목' 리스트(행=토글·이름·설명, 현재 대화 내용 1행·확장=행 추가), 비영속=리스트 소멸+"저장 항목 없음"+승인 제약 안내. 측정=비영속이 승인 필요 도구 호출 시 실행 거부(chat.py:1290 fail-closed)→오버라이드 의미 반전(완화 '승인 없음'=유일 레버)이라 숨김 아닌 명시(step ② 승인 본문에도 경고). payload 무변경. 검증=verify-281 12/12+280 갱신·279 무회귀+tsc0+육안, codex 스킵. 교훈=상태 공간을 위젯에 굳히지 말고 자랄 축은 리스트로·의미 반전은 라벨링 [storage-items-list,extensible-axis,ephemeral-approval-inversion] → 회고 256
- 282 비영속 승인 상태 고정+승인 목록 폰트 정합 — **done**(사용자: "문구가 약속했으면 각 도구를 '승인 없음'으로 고정해야"+폰트 상이+헤더 '선택' 거짓 어포던스). 비영속=승인 행 Select 값 '승인 없음'+disabled·헤더 "(n개 · '승인 없음' 고정)"(두 곳), 저장은 finalize가 배선 도구 전부 required:false 파생(폼 상태 불변→영속 복귀 복원). <code> 서버명→평문 13px. 경계=완화 자동 기록이나 서버 완화 게이트(177) 무변경 강제→비영속+도구 저장=관리자 전용(403 fail-closed), 새 우회 0. 검증=verify-282 9/9+281 GREEN+279 갱신(승인 설정은 영속 복귀 후)+tsc0, codex 스킵(게이트 서버 강제 유지). 교훈=문구는 UI가 이행할 계약·어포던스 라벨은 상태 따라 [ephemeral-approval-pinning,copy-contract,affordance-label,relax-gate-boundary] → 회고 257
- 283 에이전트 목록 검색에 종류 축 — **done**(사용자 지시). typeLabel(impl) 단일 출처 export(AGENT_TYPES 옆, orchestrate 계열=조율형 접음)→검색 필터 배열 추가, placeholder "이름·종류·모델 검색". 검증=verify-283 7/7(노드형/조율형 필터·이름 무회귀)+tsc0. 교훈=검색 축은 내부 키 아닌 사용자 어휘·placeholder 동시 갱신 [type-search,user-vocabulary,type-label-single-source] → 회고 258
- 284 에이전트 목록 재설계 — 출처 탭·내 것 tint·신호등(태그 계층 해소) — **done**(사용자 6지시). 출처=Tabs(Internal UI/Code/External, 소유·소스 Select 소멸)·내 것=행 배경 연초록(DataTable rowTint prop, 본행+보조행 클래스—기존 expanded 배경 규칙과 특이성 충돌 수리)·UI 탭 '종류' 컬럼(typeLabel)·페르소나 컬럼 삭제·탭별 검색 placeholder(+커밋 축)·상태=색 점(파랑/노랑/빨강)+툴팁·표시 안내 범례 갱신. "기본 public"은 285 분리(공개=owner_id 소멸이라 tint와 양립 불가+147 보안 경계—생성자 축 분리 필요). 검증=verify-284 16/16+283/276/274 회귀+tsc0+오버플로0+육안(tint 한 몸), codex 스킵. 교훈=축마다 표현 수단 배분(태그는 예외만)·모순 지시는 분리 선언·클래스 단언≠칠해짐 [agents-list-tabs,row-tint,traffic-light,axis-widget-mapping] → 회고 259
- 285 resync 배포 메타 재보고 — commit·repo·runtime — **done**(사용자 결정: 상태는 실측인데 버전은 등록시점 신고라 낡던 비대칭 해소). resync(081)가 카드 재fetch 후 code(x-my-agents 확장)면 deploy 재반영: repo/runtime은 값 있을 때만(확장 소실=보존, merge-preserve), commit 변경 시 버전 행 전이(057 F4: 구 active→archived·기존 행 승격 or 신규 active·active_version=commit·재왕복 중복 0). manifest(config) 재반영은 범위 밖. 검증=verify_285 11/11(실DB+스레드 카드서버, ⑤판별자=스냅샷 갱신 여부로 도달실패 경로와 구분)+081 수리(112 이후 principal 노후) 8/8, codex 스킵(자기신고 모델 057 그대로·SSRF/게이트 무변경). 교훈=보존 단언엔 경로 판별자·몽키패치는 원본 선캡처 [resync-deploy-meta,version-transition-057f4,path-discriminator] → 회고 260
- 286 에이전트 상세 화면 정비 — 카운트 정확화·종류 라벨·헤더 배지 284 계승 — 버그2(구성 "도구 N"=서버 수→tools 진실원·헤더 typeLabel 로컬 사본이 pipeline 키 노출→283 단일 출처)+정리3(헤더 배지=신호등+종류+예외만: private/초안/A2A/서빙 태그 제거·노드형 구성 노드 요약·어휘 메모리→기억/벡터 테이블→문서/지표 없음→피드백 아직 없음) +후속(개요 압축 5행: 실행=모델·세션만/위임 이름/vN·미서빙/비공개·A2A점/피드백 없음 + private→비공개 어휘 전수·OwnerTag 삭제·이모지 제거, 노후 e2e 154/156 수리) +후속2(각주→상설 행 없음값·아이콘 미등록 5건=조용한 폴백 차집합 측정·A2A 사유 자리 이동+간결화·범례 기호=뜻 정렬, copy-rubric 유형 6) [agent-detail,badge-slim,tools-count,type-label,overview-compress,icon-fallback,legend]
- 287 노드형 오버라이드 — 구조 불변·필드만(사용자: 노드 추가/삭제=테스트 범위 밖, 서버 강제) — 백엔드=_merge_node_overrides(길이 일치 시 인덱스별 필드 화이트리스트 merge·name 보존·불일치=mismatch 트레이스 표면화)+allowed에 vectorTables+트레이스 nodes 요약 / 프론트=derivePipelinePool 단일 출처·Overrides.nodes·NodeListEditor fixedStructure·드로어 노드형 분기(노드/세부, 직접형 표면 숨김) [pipeline-override,structure-invariant,node-editor]
- 288 기능 조합 시나리오 스위트(실모델 전제) — **done**(retrospect 263, 최종 31시나리오·연속 2회 전판 초록 322s/292s·codex High4 Med5 수용): 기억×도구×세션×오버라이드×impl 6종 조합 32시나리오를 한 명령(tests/suite/run.py)으로, 단언=기록만(trace mcp/toolDiag/brokerCalls/ragCollections·recall_diag·DB 행 실측, 문장일치 0)·오버라이드=1급 검증 수단(+저장 설정 대조군)·픽스처=suite- 접두 멱등 부트스트랩(seed 비결합)·프리플라이트=실모델 없으면 exit 2. 다음 작업(파이썬 대정리)의 안전망 [combination-suite,real-model-gate,record-based-assert,override-as-fixture,idempotent-bootstrap]
- 289 연동 계약 정비(서버가 기억·파생, 상태는 미리) — **done**(retrospect 264, 스위트 36시나리오 연속 2회 초록·codex Med5 수용/High0/보안 NoFinding)(288 실측 3건의 승격, 사용자 "이대로면 잘 만든 쓰레기" 우려): P1 히스토리 서버 재구성(sessionId+새 메시지만 → DB 대화 이어붙임, assistant 포함=클라 모드 무회귀·소유권 게이트 뒤에서만)·P2 노드형 풀 서버 파생(생성/초안/오버라이드 입구 전수, 파생=denormalization이라 대체)·P3 위임 상태 표면화(그래프 미서빙 마커+위임 0건 사유 trace). 검증=조합 스위트 신규 4++전판 무회귀+codex 경계 [integration-contract,server-side-history,pool-derivation,delegation-visibility]
- 290 조합 스위트 엣지 티어(실패의 품질) — **done**(retrospect 265, core+edge 51개 초록·수리 2건): 한계·경계·오류·적대·동시성·자원 6축 15시나리오(--tier), 단언="성공"이 아니라 실패의 품질(4xx/error/폴백 흔적/fold 증거); 수리=NUL sessionId 500→새 세션 fold(입구 위생)·무효 모델 오버라이드 조용한 폴백→400; retries 기본 2(실모델 도구 miss 흡수, flaky 정직 표기) [edge-tier,failure-quality,null-byte-fold,refuse-loud]
- 291 파이썬 대정리(PEP8·복잡도·구조·타입) — **done**(retrospect 266, make metrics 전판 통과: CC D+ 20→0·MI A미만 4→0·ruff 191→0·mypy 44→0·스위트 51/51 8연속; mypy 도입 첫 판이 Phase 2 keyword-only 개명 회귀 적발=사다리 비겹침 실증, 실버그 1건 404 정직 거절 수리) — 5 Phase: ⓪기준 수립(ruff/mypy/make metrics 게이트, 의도 예외=문서화 ignore) ①자동 안전 수정 ②잔여 수동(ruff 0) ③god-module 4개 분할(chat 1891·eval 1393·broker 1296·agents 1237 — Extract Method·guard clause·타입 컨텍스트) ④mypy(코어 strict 우선); 수치 게이트=CC D+ 블록 20→0·MI A미만 4→0·ruff ~250→0·mypy 37→0·스위트 51/51, 네이밍·주석은 리뷰 게이트(WHAT 삭제·WHY 압축·스펙 ID 링크); 전권 위임=분석 메인·작업 서브에이전트·랄프 루프 [python-cleanup,metrics-gate,god-module-split,ralph-loop]
- 292 네이밍 컨벤션 정밀화 — **done**(retrospect 267, R1 23→0·R3 66→0·ANN 423→0·mypy 0·스위트 51/51; 룰이 오탐에서 진화=S-V-O 술어 인정, 타입 주석 작업이 approvals 머신 토큰 500 적발) — 룰 6개(R1 bool=질문형 술어/동작 2부류·판별자=부수효과, R2 타입주석 전수=ruff ANN, R3 루프 전체단어, R4 축약 화이트리스트, R5 상수/배치, R6 도메인 동사 사전) + **기계 게이트 승격**(scripts/naming_audit.py→make metrics — 291처럼 리뷰로만 남기면 재발); 실태=bool 60 중 29 규칙밖·파라미터 16% 미주석·한글자 루프 69 [naming-rules,machine-gate,predicate-naming,ann-rules]
- 293 메모리 provider 공통 조상 — **done**(retrospect 268, memory.py 436→396줄·verify_104/105/111+스위트 51/51·codex 5축 No Finding+Low1 수리): 사용자 지적("공통 조상=반복 감소") 실측=6개 아닌 **메모리 3형제만**(Agent/Mcp/Rag는 계약만 공유·내부 상이=억지 상속 OUT): MemoryAxisProvider(ABC)가 골격 4메서드(__init__·describe·_cap·candidates·load·node_label) `@final` 소유·자식은 유일 구멍 invoke/approval_for만, 파서 3갈래→_cap_resource 단일화; 선례=OrchestrationAgentBase 템플릿 메서드, 자식 이름 불변(재수출 계약), 행위 보존(verify_104/105/111+스위트+codex anti-leak) [provider-base,template-method,dedup,anti-leak-preserved]
- 294 브로커 provider 조립 분리(순수 주입) — **done**(retrospect 269, verify_294 주입seam 8/8·104 anti-leak/105/111/101/112/117+스위트 51/51·codex 6축 SAFE·재수출 여집합0): PolicyScopedBroker가 6 provider를 __init__서 직접 생성=조립+소비 겸직(broker가 저장만·안 읽는 session_factory=누출 증거), agent(_REGISTRY)·backend(_BACKENDS)는 이미 은닉하는데 provider만 예외 → composition.py 신설(BrokerContext frozen dataclass=provider 생성 규약+build_providers 구체 아는 유일 함수), broker는 providers: list[_CapabilityProvider] **주입받고 구체 0개 앎**; DI 축≠상속 축(293 본문공유), 개방-폐쇄=조립함수 한 줄(런타임 가변 레지스트리는 OUT—provider=내부 고정집합, driver 없어 카고컬트), 신뢰경계 파싱(_kind_of·if source·외부 JSON)은 명시 유지(억지 추상 금지) [provider-composition,inject-abstraction,brokercontext-spec,open-closed-function] → retrospect 269
- 295 에이전트 flow 중복 제거(모델빌더·사용자텍스트) — **done**(retrospect 270, ChatOpenAI 구성 model.py 단일·로컬 def 0·verify 041/076/085/099/102/259/260/261/268/270+스위트 51/51·codex 4축 SAFE): 사용자가 "전수=0" 반증(복붙 적발)→정의 넓혀 B 8건 중 agent 2종 우선. 6복제 ChatOpenAI 구성→`agent/model.py:build_chat_openai`(차이=cfg출처·on_missing raise/none·default_temp·error_label 파라미터화·@overload 반환타입), 3복제 `_last_user_text`→`toolbox.last_user_text(roles=None)`; pipeline `_model_from_node`는 얇은 어댑터로 존치(몽키패치 시임 보존, 구성블록만 위임)·그래프 위상 통합은 OUT(억지) [flow-dedup,build-chat-openai,parameterize,seam-preserve] → retrospect 270
- 296 api 공통 헬퍼 정본화(A2A 프레이밍·카드·이름·공백) — **done**(retrospect 271, 로컬 def/클로저 0·verify_042/117/148+스위트 51/51·codex 4 SAFE+1 정직화): 스펙295 후속 api 4종 dedup. a2a_wire.py 신설(a2a_user_text/a2a_result/a2a_error/a2a_status_event — mock_remote·a2a_server 클로저의 rpc_id/task_id 캡처를 인자로 승격=wire 정본 단일화)·card_streaming→a2a_client·assert_valid_name→naming·_require_non_blank→schemas 공유 validator; codex 재수출 지적은 심볼 이전(importer 0 측정)이라 정직화. get-or-404(36곳)=스펙 297 분리(소유권 경계) [api-helpers,a2a-wire,closure-to-arg,dedup] → retrospect 271
- 297 get-or-404 정본화(순수 PK-존재 체크) — **done**(retrospect 272, 순수패턴 잔존 0·get_or_404 37호출·verify_112/147/148/104+스위트 51/51·codex 여집합0): `session.get(M,id)`+`if None:404` 관용구 **37곳**→`db.get_or_404[T](session,model,ident,detail)`(PEP695 제네릭). **census-lens 재발**=1차 분류 33건은 단일줄만 매칭, 멀티라인 `get(\n)`·`if(\n None)`·꼬리주석 형태의 동일 순수패턴 4건(eval_cases2·eval_authoring2) near-miss 방치→견고 스캐너(or/and 없을 때만 순수) 재전수로 37 편입. 안전규칙=session.get은 항상 PK순수조회(스코프 fetch는 select-where)→위험은 결합게이트뿐(제외), 존재-404→assert_may_manage 순서=superuser 특권도 방벽(codex P2 F4), dedup이 미사용 import(HTTPException F401) 드러냄 [get-or-404,census-lens-not-fact,pk-get-is-safe,existence-before-ownership] → retrospect 272
- 298 authz 스코프 + LIKE-escape 정본화(저위험 dedup) — **done**(retrospect 273, 로컬 정의0·크로스모듈 private import0·verify_066/067/112/147/177+스위트51/51·codex): census 최저위험 2건. (A) LIKE-escape 3중 바이트동일(sessions `_like_escape`·eval_common `_ilike_literal`·mem0 인라인)→신설 `sqlutil.like_escape`(의존0). (B) authz 트리오 2중: `_agent_id_map`(동일)→`serializers.agent_id_map`·`_is_admin`((obj,act)튜플만 차이)→`authz.is_admin_for(p,obj,act)`·`_own_scope`(동일)→`authz.own_scope(p,obj,act)`, 호출부가 자기 튜플 명시=라우터 독립 보존·골격 단일화. 크로스모듈 private import 제거(rag→sessions·chat→sessions). **놓친 시임=단위 verifier `S._is_admin` 직접참조**(몽키패치 아닌 모듈속성)→정본 갱신. **존치**: `_own_scope_write`(superuser-only 209 F1)·`_may_resolve`. codex 인접발견=`end_session` mutating인데 read-scope(회귀 아님, 스펙299 이월) [authz-scope,like-escape,body-not-signature,cross-module-private-import-smell,seam-is-direct-ref] → retrospect 273
- 299 end_session 쓰기 스코프 정합 — **done**(retrospect 274, verify_067_scope M3+067_live+스위트51/51·codex 7축 결함0): 298 codex 인접발견 정정. `POST /sessions/{id}/end`(mutating)가 read-scope(`own_scope(·,"sessions","read")`) 유일 인가라 sessions:read 운영자가 타인 세션 종료 가능(209 F1이 feedback엔 넣은 write-scope를 end엔 누락). `authz.own_scope_write(principal)` 신설(옛 sessions._own_scope_write 승격+**machine 분기**—end는 machine 허용, 단순 스왑 시 str "machine".id AttributeError), 3 write 라우트(end+feedback PUT/DELETE) 정본. 읽기 라우트는 read-scope 유지. **인접발견도 census부터**(write 라우트 전수=버그 end 하나로 격리, approvals resolve는 own_scope=404-fold/게이트=_may_resolve라 정상)·**정본화(298) 기계치환이 잠복버그 동결→codex 발견 도움**. 행위변경=운영자만 타인 종료 차단(machine/superuser/owner/member 불변) [write-scope,read-not-widen-write,census-adjacent,machine-sentinel] → retrospect 274
- 300 llm_cfg 빌더 정본화(ModelConfig→연결 cfg) — **done**(retrospect 275, 인라인 조립0·llm_cfg_of 8call·스위트51/51·codex): census 후보 C. `{base_url, api_key: crypto.decrypt, model_id}` 조립을 `mem_config.model_usable(cm)->TypeGuard[ModelConfig]`+`llm_cfg_of(cm)->dict` 원자로 8곳 단일화(eval_runs·eval_authoring·eval_guards 3-키·chat_context _chat_model_cfg/기본해석 4-키(+params)·chat_context _resolve_mem_cfg embedder·mem_config _build_mem_cfg 중첩). **census-lens**=서브에이전트 C 스코프(eval 3곳)를 전역 grep으로 8곳(핫패스 포함) 확장, 사용자 AskUser 전체원자. **dedup vs mypy narrowing**=인라인 술어 빼면 narrow 잃음→TypeGuard(TypeIs는 값-유효성이라 불건전), early-return은 assert 보강. **비밀 홈**=llm_cfg_of는 평문 복호화라 serializers(마스킹) 아닌 mem_config. **술어는 메시지 같을 때만 공유**(chat_context 2메시지 존치). 무효분기 호출자별 존치 [llm-cfg,typeguard-not-typeis,secret-home,predicate-share-same-message] → retrospect 275
- 301 eval 배경 작업 스캐폴딩 정본화 — **done**(retrospect 276, 인라인 스캐폴드0·verify_209_harvest 10/10·스위트51/51·codex 7축): census 후보 D. eval_authoring 4개 배경 작업(생성·출제·append·수확) 반복 스캐폴딩 원자화. `_job_lock`(asynccontextmanager, discard만—add는 엔드포인트 flood가드 codex#1/#2)·`_next_order_idx`·`_apply_completion_marker(ds,mark,tail)`·`_stamp_failure(ds_id,msg)`·`_execute_append_job(*,generate,build_case)`(suggestion+generation_append ~90% 통합, 콜러블 주입). generation/harvest는 원자만 차용(본문 존치=억지 배제). 사용자 노출 문자열 바이트 보존(codex 142/143/195 계약). **팬텀 회귀 교훈**: verify_209_harvest "0건" 실패를 회귀 오판·추격→실제는 `--reload`가 배경 harvest 태스크 kill + 동시 스위트가 이벤트루프 점유해 10초 폴 초과, 계측 타임라인으로 코드무결 확정(정상 1.5초·cases정확) [bg-job-scaffold,reload-kills-bg-task,instrument-dont-guess,inject-callable] → retrospect 276
- 302 stale verifier 정리 + artifact config 주입 회귀 수정 — **done**(retrospect 277, 6개 verifier 전부 PASS·metrics-fast0·스위트·codex): backlog 방치 6개(084/100/103/130/131/190) 전수 진단. **190은 stale 아닌 실 프로덕션 회귀**=`artifact.py` produce_node의 config 애노테이션 `RunnableConfig | None`(from __future__ 문자열화)이 langgraph 1.2.5 주입 판정(문자열 매칭·PEP604 유니언 미허용) 못 통과→artifact 계열 첫 노드 크래시(스위트 미검출), `RunnableConfig`로 수정해 주입 복원(`=None` 기본값은 눈속임=thread_id 리플레이 무력화). 픽스처 5: 100 `_FakeAgent.active_version=None`(스펙256 게이트)·084 몽키패치 lookup지점 이동+FakeMem threshold kwargs 흡수·103 컬렉션명 밑줄→대시(명명규칙)·130/131 Obsidian→docs-kb(표준 seed)+131 연쇄드리프트 4단(approval_for/fake_search 인자·V3 화이트리스트 node 통과). **교훈: 실패 verifier "낡았겠지" 방치가 실 회귀 은폐, 연쇄드리프트는 재실행-수선 반복** [stale-verifier,langgraph-config-annotation,none-default-sham,cascading-drift] → retrospect 277
- 303 첫 설치 시드 예제 트림(최소 정예) — **done**(retrospect 278, verify_303 11/11·smoke_303 실DB 2/3/0/5/0·metrics-fast0): "처음 설치 예제 정돈 안 됨"을 두 층 분리(테스트잔해 53에이전트=신선설치 무관 OUT / 시드예제 IN). `seed.py` 고아·빈 데모 제거: 페르소나 4→2(strict-senior-engineer·calm-sre=스펙046 Reviewer/Ops 제거로 참조 끊긴 고아)·컬렉션 4→3(support-tickets 참조0)·세션 4→0(turns=0 빈 껍데기 전부·정직한 빈 상태로 시작)·시드 도크 "mockData 미러" 주석 정정(mockData 데이터배열=死코드, seed=단일출처). **층 먼저 갈라야 스코프**(seed∪test-debris 중 무엇이 괴롭히나 확정 후 손댐)·**고아=실 참조로 판정**(scorecard.json 언급=산출물이지 참조 아님)·**상수→행 1:1+제거만이라 단위가 라이브 대리**(+rung2 일회용DB에 전체 alembic+seed_if_empty 실측으로 확증, 라이브 무접촉)·**"인프라 없음" 결론 전 계층 분리**(VM·타 컨테이너·내 컨테이너 3겹, 사용자 보고와 어긋나면 내 측정 의심)·**RBAC/codex는 성격 트리거—데이터 큐레이션 미해당** [seed-trim,layer-split-first,orphan-by-real-ref,unit-proxies-live-on-pure-removal,probe-infra-layers] → retrospect 278
- 304 테스트 잔해 스윕(프리픽스 화이트리스트) + 기존 정리 — **done**(retrospect 279, dry-run 53매칭·--apply 58→5·codex 여집합실패·DB실측): 스펙 303이 갈라낸 2번째 층. dev DB 58에이전트 중 53이 브라우저/verify 테스트 산물(teardown 부재 누적, `_fixture.mjs`는 임시 유저만 삭제·에이전트 방치). `tests/sweep_debris.py`(프리픽스 화이트리스트 10 + 고정명 exact 1 + seed 5 keeplist **이중 가드**, dry-run 기본·`--apply` 실삭제, DB단 ondelete=CASCADE 위임)로 지금 정리(58→5)+make 타깃(`sweep-debris`/`-apply`) 재사용. **재발원까지 봐야 마감**(삭제=증상, teardown 부재=병)·**화이트리스트 vs keeplist=미래 실 데이터 보호로 갈림**(랜덤 hex agent_id는 실/테스트 구분 못해 이름 프리픽스만 신호)·**비가역=dry-run기본+명시apply+삭제전 codex**(codex가 결함0 판정하며 `approval-demo` 하이픈없는 프리픽스 오매칭 운영위험 덤 지적→exact 매칭 조임)·**cascade는 DB단 위임+삭제후 카운트 실측**(versions70→17·sessions1380→133·messages6048→276·시드데이터 불변) [test-debris-sweep,scope-to-recurrence,whitelist-protects-future,dry-run-then-apply,adversarial-catches-adjacent] → retrospect 279
- 305 mockData.ts 死데이터 배열 제거(타입·상수 보존) — **done**(retrospect 280, tsc0·vite build✓·외부참조0): 스펙 303이 "seed=단일출처, mockData 배열=死코드"로 정정한 것을 실제 제거. 死배열 3종(BLOCKS·ADMIN_AGENTS·ADMIN_SESSIONS)+변형 IIFE+死상수 MCP_STATUS 제거, 타입 14·live 상수 8 원문 보존. **참조0≠死**(AgentCard/ToolApprovalOverride는 직접 importer 0이나 live 타입 Agent.card/ToolPolicy에 박혀 보존; 死/live는 심볼카운트 아닌 참조 성격=값소비 vs 타입골격 vs 어노테이션)·**死배열+변형 IIFE는 한 단위**(배열만 지우고 IIFE 남기면 ReferenceError, HARD 블로커 H1—조사가 선식별)·**다중영역 삭제는 조각편집보다 死부분 뺀 전체 재작성**·**죽은 흔적은 주석까지**(ADMIN_SESSIONS 마지막 외부참조=AgentsView 주석 한 줄, 청소 후 진짜 grep 0). 순수 프론트 死코드라 codex 트리거 미해당, tsc/build 정본 [mockdata-dead-arrays,zero-ref-not-dead,dead-array-with-iife-one-unit,rewrite-over-fragment] → retrospect 280
- 306 브로커 kind 파싱 OCP 흡수(레지스트리 구동) — **done**(retrospect 281, verify_306 105/105·브로커066/104/105/111/112 PASS·metrics-fast0·codex): 구조 리뷰(182/183) "중간 우선". `common._kind_of`(접두사 if-체인)·`_cap_resource`(kind if-체인) 하드코딩을 단일 `_PREFIXED_KINDS` 레지스트리+`_strip_kind` 프리미티브 구동으로 흡수. 함정=새 kind 시 `_cap_resource` 분기 누락→per-cap RBAC 리소스(`capability:{kind}:{resource}`) 조용한 오추출(스펙112 경계). **핵심 관찰=5 kind 리소스 추출 전부 균일(`{kind}:` 스트립), mcp만 2레벨 유지**. **균일이면 per-provider resource_of는 5중복 과추상→데이터 레지스트리가 옳음**(구조 지시 YAGNI, 파싱 context-free라 provider 인스턴스 못 매담)·**순수 리팩터 완료조건=옛 if-체인과 바이트 동일→옛 코드 오라클 박제 대조**(표본×전 kind, agent fallback은 스트립 안 함=`kind in _PREFIXED_KINDS` 게이트로 봉합)·**드리프트 핀은 introspection이라야 진짜**(codex: 수동 ALL_KINDS면 새 상수 누락 못 잡음→`vars(common)` CAP_KIND_* 순회)·**적대가 결함0이어도 테스트의 구멍을 덤 적발** [broker-kind-registry,uniform-dup-registry-over-abstraction,refactor-byte-identical-oracle,drift-pin-introspection] → retrospect 281
- 307 테스트 격리 마감(stale verifier 정리 + 상태 격리) — **done**(retrospect 282, 036·061·056·100·101·103 전부 exit=0·codex): backlog "stale verifier" 6건을 **서버 띄우고 전수 진단** → 3부류: 100·101·103=**인프라**(dev 서버·mock MCP·mock 임베딩 필요, stale 아님·오분류)·036·061=**드리프트**·056=**상태 의존**. 036=네이밍 밑줄→대시(`col-v036-`, 스펙148)·061=seam 개명 추종(`_load_exposed_ui_agent`→`_load_exposed_agent`+`_agent_a2a_skills` 패치+`exposed_agent_a2a` request/`_principal` 시그니처)·056=**일회용 DB 러너**(`tests/_throwaway_db.py`: temp DB 생성·alembic+seed·subprocess 격리·drop, `_THROWAWAY_DB` 가드로 자동 재실행)로 virgin DB서 결정화. **"stale" 전 실행 환경 재측정**(red=코드 아닌 전제 미충족일 수 있음)·**전역 집계 테스트는 소유-픽스처론 부족→virgin DB**(dry-run sample 전역 캡을 ambient가 채워 거짓초록)·**격리는 subprocess+DATABASE_URL이 in-process 재바인딩보다 깨끗**(SessionLocal import-바인딩)·**DB 러너 URL은 make_url로**(codex: rpartition 쿼리스트링서 라이브 유출, `_create` try 안). 전149 이관=OUT(러너 채택 점진) [test-isolation,stale-vs-infra,seam-follows-rename,global-test-needs-virgin-db,url-parse-not-string-split] → retrospect 282
- 308 useAsyncData 뷰 이관(단발 페치 관용구 정본화) — **done**(retrospect 283, tsc0·vite build✓·브라우저 8/11 네트워크 실증·콘솔치명0): 스펙 184 훅을 남은 11 사이트/8파일에 이관, 복붙 `useState+useEffect+alive+.then(set).catch` 단일화. **10 이관·1 정당 skip**(SessionsView `messages`=`applyFeedback` 로컬 낙관 뮤테이션→setter 없는 useAsyncData 부적합, 선례 useAgents). fast-worker 위임+메인 diff 전수 리뷰·기능 왕복. **공유 훅 이관≠균일 기계작업**(로컬 뮤테이션 상태는 훅 계약 밖, 위임자가 오버레이 발명 말고 선례 근거 skip이 옳음)·**훅 이관 검증은 네가 바꾼 코드경로=네트워크 페치를 단언**(`.ant-drawer-content` DOM 오탐 vs `listEvalCases` 페치=권위)·**keyed-fetch는 마운트도 발화**(providers[0] 자동선택→"N클릭=N페치" 오탐, deps 반응은 고유 fetch 키로 증명)·**기본값을 effect deps에 넣으면 매렌더 재실행**(raw undefined-until-resolved를 dep, 기본은 렌더만)·**clear-on-key-change=`loading?null:data`**. **잔여**: runWithToast ~26사이트(309 후보)·#3/#4/#6 브라우저 미실증(리뷰+정적 커버) [useasyncdata-migration,shared-hook-not-uniform,verify-fetch-not-dom,keyed-fetch-fires-on-mount,default-in-deps-trap] → retrospect 283
- 309 runWithToast 뷰 이관(mutation 토스트 관용구 정본화) — **done**(retrospect 284, tsc0·vite build✓·errorPrefix 4/4·브라우저 왕복): 스펙 308 후속. 전수 인벤토리(11파일 40 try/catch=FIT11·CAUTION19·SKIP10)→**27 이관·10 skip**. 공유 훅 `errorPrefix?` 1옵션 확장(사용자 승인)으로 프리픽스 에러 6곳 무손실. **헬퍼가 실사용보다 좁으면 사이트 왜곡 말고 계약 넓힘**(프리픽스+detail 동시 불가→errorPrefix, 단 계약변경이라 확인 선행)·**에러규칙 3분(P/C/X)이 dedup의 미묘한 핵심**(passthrough를 error:'STR'로 옮기면 e.message를 고정문자열이 덮어 원인 소실=조용한 UX저하; passthrough→opts없음·custom→error·prefix→errorPrefix)·**결과소비는 fn 안·UI정리는 if(ok)**(boolean 헬퍼로도 "성공+결과" 시맨틱 보존, 결과파생 success도 fn 내부 message.success)·**결과소비 부수효과가 토스트보다 강한 성공 증거**(Settings 필드=서버echo가 durable 증거, transient 토스트보다 지속상태 겨눔). SKIP=읽기6·결과분기2결말3(doUpload·trigger·testProvider)·409분기1 [runwithtoast-migration,widen-contract-not-distort-site,error-rule-taxonomy,result-consume-in-fn,durable-effect-over-toast] → retrospect 284
- 311 영화 엔티티 데모 로더(임베딩 모델 선택형 스킬) — **done**(retrospect 285, dev mock-embed 실행·멱등·시각확인): 엔티티 RAG(149) 첫 구동 예제 부재를 온디맨드 로더로. `movies.jsonl`(행=1영화·metadata 숫자 id 여럿·data=줄거리)+`ingest_movie_entities.py`(모델 선택·kind=entity 생성·인제스트·검색+meta 자가확인)+`.claude/skills/movie-demo`. **모델 선택이 핵심**(mock-embed=1024결정적 픽스처/CI·310평가, 실모델=의미 데모; 컬렉션 모델 불변149). **seed 무변경**(온디맨드·최소주의 보존). movies-demo 생성·10행·검색 hit meta에 movie_id 노출·멱등. 스펙310 픽스처 공급원. **미리 real 벡터 주입은 무의미**(질의도 같은 모델이라야 벡터공간 비교, 첫 구동 mock뿐). ingest_rag_samples 계보(게이트 밖). [entity-rag-demo,model-choosable-loader,seed-clean-ondemand,vector-space-same-model,confirm-existing-first] → retrospect 285
