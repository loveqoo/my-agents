# Spec Index  (docs/spec/)

> 각 줄: `NNN 제목 — 언제 열까(후크) [키워드] [archived]`. Context 단계에서 이 인덱스를 먼저 읽고,
> 후크로 불충분할 때만 해당 full 파일을 연다. `[archived]`는 완료된 로드맵 스펙으로 `archive/`에 있다.

- 001 시스템 전체 설계 — 제품의 큰 그림(비전·아키텍처·모듈 경계·도메인 모델·핵심 흐름)을 다시 확인할 때 [overview,architecture,vision]
- 002 페르소나 등록 + chat 노출 — 페르소나 REST 등록/조회와 `POST /agents/{id}/chat` SSE 스트리밍의 최초 배선을 볼 때 [persona,registry,chat,sse]
- 003 Admin Web UI 채팅 — React+TS+antd SPA로 에이전트 골라 브라우저 스트리밍 채팅하는 최초 테스트 환경을 볼 때 [admin,spa,chat,sse]
- 004 Web UI 프롬프트 확인 — 선택 에이전트의 페르소나와 모델 전송 메시지를 읽기전용으로 보는 인스펙트 기능을 볼 때 [inspect,prompt,readonly]
- 005 Ant Design X 채팅 리팩터 — 손제작 채팅 UI를 @ant-design/x(Bubble/Sender/ThoughtChain)로 교체한 UI 토대를 볼 때 [antdx,chat,ui,refactor]
- 006 Admin 콘솔 + Playground 이식 — claude.ai/design handoff 번들을 antd 6/antd-x로 재현한 5뷰 콘솔+Playground 이식을 볼 때 [admin,console,handoff,playground]
- 007 실 에이전트 서비스 — mock UI가 기대하는 도메인을 실제 백엔드(FastAPI/SQLAlchemy/LangGraph/Mem0/MLX) 런타임으로 구현한 핵심 토대를 볼 때 [backend,runtime,langgraph,mem0]
- 008 모델 레지스트리 — LLM/임베딩 모델 설정 CRUD와 런타임이 등록 모델로 ChatOpenAI 구성하는 방식을 볼 때 [model,registry,llm,runtime]
- 009 코드 에이전트 원격 실행 — source=code 에이전트를 원격 엔드포인트로 프록시하는 SSE 재전송 계약을 볼 때 [code-agent,remote,proxy,sse]
- 010 비밀값 암호화 저장 — 모델 api_key·코드 에이전트 token을 Fernet at-rest 암호화하고 런타임에서만 복호화하는 방식을 볼 때 [secret,fernet,encryption]
- 011 API 인증 — 모든 도메인 API에 단일 공유 Bearer 토큰(소유자=전체 접근)을 거는 인증을 볼 때 [auth,bearer,token]
- 012 대화 히스토리 윈도우+저장정책 — historyDepth(최근 N개 절단)와 persistHistory(DB 저장 여부) 에이전트별 설정을 볼 때 [history,window,persist,session]
- 013 모델 연결 테스트 — 등록 전/후 모델을 `{base_url}/models`로 도달성·인증·가용성 검증하는 연결 테스트를 볼 때 [model,test,connection,health]
- 014 페르소나 등록/수정 UI — 백엔드 CRUD는 있었으나 빠져 있던 페르소나 작성/편집 UI를 채운 작업을 볼 때 [persona,ui,authoring,form]
- 015 빌딩 블록 작성/편집 폼 — memory/embedding/permission 블록의 작성·편집 폼이 통째로 없던 빈틈을 메운 작업을 볼 때 [blocks,form,authoring,ui]
- 016 메모리 읽기전용 + kind별 모델 테스트 — 메모리=시스템 enum이라 UI 읽기전용으로 잠그고, embedding 모델은 임베딩으로 테스트하도록 정정한 작업을 볼 때 [memory,readonly,enum,embedding-test]
- 017 어드민 콘솔 모바일 반응형 — Tailscale 노출 후 모바일(~390px)에서 깨진 Sider/검색창/패딩/테이블/그리드 반응형 풀패스를 볼 때 [mobile,responsive,admin,layout]
- 018 메모리 유저 스코핑 — mem0를 에이전트 1버킷에서 user_id(유저 장기)·세션 단기로 나눈 스코핑 도입을 볼 때 [memory,mem0,user-scope,session]
- 019 mem0 pgvector 공유 백엔드 — 파일 기반 qdrant를 제거하고 mem0 벡터 스토어를 공유 Postgres/pgvector로 이전한 작업을 볼 때 [mem0,pgvector,postgres,vector-store]
- 020 mem0 다층 스코프 + 카탈로그 재정렬 — memory_type N종 카탈로그가 mem0 설계의도와 어긋남을 확인하고 user/session 다층 스코프로 재정렬한 작업을 볼 때 [mem0,scope,catalog,realign]
- 021 Playground userId UX — Playground 헤더 userId 입력 레이아웃 정리와 과거 userId 선택(세션 영속 출처)을 볼 때 [playground,userid,ux,session]
- 022 모바일 인스펙터 콘텐츠 오버플로 픽스 — 턴 인스펙터가 모바일 전체화면에서 긴 콘텐츠로 오버플로하는 줄바꿈/flexWrap 픽스와 전 페이지 회귀 검증을 볼 때 [mobile,inspector,overflow,regression]
- 023 에이전트 파운데이션 모델 정정 — 등록 에이전트들이 레지스트리에 없는 가상 모델명을 가리켜 화면-실제 불일치를 정정한 작업을 볼 때 [agent,model,realign,foundation]
- 024 Mock LLM 레지스트리 1급 모델 — 라이브 MLX 없이 결정적 실행을 위해 mock chat completions를 레지스트리 ModelConfig로 1급 등록한 작업을 볼 때 [mock,llm,registry,deterministic]
- 025 Playground Proxy 세션 한정 오버라이드 — 에이전트를 실제로 수정·저장하지 않고 세션 한정으로 모델/프롬프트/도구를 덮어 실행해 보는 오버라이드를 볼 때 [playground,proxy,override,session]
- 026 외부 에이전트 A2A 카드 등록 — 남이 만든 A2A 에이전트를 Agent Card(/.well-known/agent.json)로 내 생태계에 편입(등록·표시, 1차)하는 작업을 볼 때 [a2a,external,agent-card,registration]
- 027 어드민 same-origin /api 프록시 — Tailscale 접근 시 localhost 무의미·mixed-content·CORS 연쇄를 same-origin `/api` 프록시로 끊은 작업을 볼 때 [admin,proxy,same-origin,tailscale]
- 028 Playground 모델 배지 정직화 — source별로(ui=진짜 qwen / code=mock 원격) 모델 배지를 정직하게 표시하도록 고친 작업을 볼 때 [playground,badge,model,source]
- 029 에이전트 전용 메모리(agent_id) — agent_id 스코프 메모리의 에이전트 자가 기록 쓰기 채널과 관리자 큐레이션을 볼 때 [memory,agent-id,self-write,curation]
- 030 메모리 관리 화면 — 에이전트(agent_id)·유저(user_id) 메모리를 검색·수정·삭제하는 통합 큐레이션 콘솔을 볼 때 [memory,console,curation,crud]
- 031 멀티유저 인증 + 오픈형 권한 — 단일 토큰에서 fastapi-users+Casbin 차용으로 사용자 등록·로그인·RBAC(pluggable provider seam)로 확장한 작업을 볼 때 [auth,multi-user,fastapi-users,casbin,rbac]
- 032 Playground userId를 인증 주체에서 도출 — 수동 userId 입력을 제거하고 mem0 user_id 축을 로그인 인증 주체에서 고정 도출하는 작업을 볼 때 [playground,userid,auth-principal,mem0]
- 033 기능 로드맵 — 사용자 제기 12개 문제를 코드 전수조사 위에서 의존순(P0~P5)으로 배열한 로드맵·현황 한 줄 요약을 볼 때 [roadmap,planning,phases]
- 034 세션 페이징 — `GET /sessions` 전량 반환을 offset/limit+total 서버 페이징과 서버측 status 필터·배지 카운트로 바꾼 작업을 볼 때 [session,paging,offset-limit] [archived]
- 035 Provider 엔티티 — provider/base_url/api_key가 모델마다 인라인 중복되던 것을 1급 Provider 엔티티(1:N, 모델은 FK)로 승급한 토대를 볼 때 [provider,entity,model,fk] [archived]
- 036 RAG 인제스트 토대 — 죽은 vectorTables를 Collection/Document/Chunk로 재생하고 PDF 청킹·임베딩·pgvector 적재(쓰기 경로)를 볼 때 [rag,ingest,pgvector,chunk] [archived]
- 037 RAG Retrieval — 죽은 vectorTables 필드를 search_documents 도구로 부활시켜 pgvector HNSW cosine 검색을 에이전트에 배선한 읽기 경로를 볼 때 [rag,retrieval,search,tool] [archived]
- 038 배치 토대 + 세션 보존정리 — 시스템과 격리된 별도 배치 서비스(자체 스케줄러)와 첫 작업(오래된 세션 정리)을 볼 때 [batch,scheduler,session-cleanup,retention] [archived]
- 039 유저 메모리 통합·재적재 — 단조 증가하는 mem0 user_id 기억을 임계치 초과 시 LLM으로 통합·스냅샷 백업하며 교체하는 배치 작업을 볼 때 [memory,consolidation,batch,mem0] [archived]
- 040 메모리 백엔드 추상화 — memory.py의 mem0 호출을 Protocol/ABC 뒤로 들어내 그래프DB 등 백엔드 drop-in을 가능케 한 인터페이스 추출을 볼 때 [memory,abstraction,protocol,backend] [archived]
- 041 HIL 승인 게이팅 — 픽션이던 승인을 langgraph checkpoint/interrupt로 위험 도구를 진짜 멈추고 admin 승인/거부로 그래프를 재개/중단하는 게이트로 바꾼 작업을 볼 때 [hil,approval,langgraph,interrupt] [archived]
- 042 A2A 실호출 — 등록된 외부 에이전트를 JSON-RPC 2.0(message/stream) SSE로 실제 호출해 응답을 받는 A2A 런타임 전송 계층(026 2차)을 볼 때 [a2a,external,runtime,jsonrpc,ssrf] [archived]
- 043 컨텍스트 경량화 자산 인덱스 — Context 단계 읽기량이 compaction을 당기는 문제를 인덱스 층+아카이브로 줄인 워크플로 정비를 볼 때 [context,index,archive,workflow]
- 044 UI 피드백 정리 마스터 — UI 테스트 14건을 메뉴별 6배치(045~050)로 트리아지·의존순 배열한 지도를 볼 때 [feedback,cleanup,master,triage]
- 045 어드민 표시 정직화 — 승인 배지를 mock 상수에서 실 pending 수로, A2A 무조건 online을 도달성 probe로 정직화한 작업을 볼 때 [admin,honesty,badge,a2a,liveness,ssrf,approvals]
- 046 빌딩블록 재료 정리 — 미사용 권한/MCP 재료와 데모 에이전트를 cascade 삭제하고 dangling 참조를 정리한 작업을 볼 때 [blocks,cleanup,permission,mcp,seed,dry-run,cascade]
- 047 프로바이더·모델 통합+models.dev — 두 메뉴를 마스터-디테일 한 뷰로 합치고 models.dev 스냅샷으로 모델 메타를 자동채우는 작업을 볼 때 [provider,model,catalog,models.dev,merge,migration,ssrf]
- 048 RAG 임베딩 게이팅+조건부 샘플 적재 — 임베딩 모델 존재를 RAG 전제조건으로 게이팅하고 샘플 컬렉션을 mock-embed로 적재하는 작업을 볼 때 [rag,collection,embedding,gate,sample,ingest,mock-embed]
- 049 세션 정리 정책 — 0턴 세션 미영속(lazy-create)과 턴 기준 배치 정리(min_session_turns+idle-guard)를 볼 때 [session,retention,batch,turns,lazy-create,idle-guard,cleanup]
- 050 파괴적 데이터 정리(dry-run) — A2A·세션·유저 정크를 보수적으로 식별해 dry-run→검토→실행으로 정리하고 재생성 뿌리를 self-fixture로 막은 작업을 볼 때 [cleanup,destructive,dry-run,a2a,user,casbin,self-fixture,floor]
- 051 채팅 자가기록 차단·agent_id 메모리는 어드민 저작 전용 — 채팅 LLM이 유저 개인사실을 agent_id 교차사용자 스코프에 자가기록하는 누출버그를 도구 제거로 닫을 때 [memory,agent-scope,leak,write-channel,self-write,admin-curation,spec051]
- 052 유저 메모리 큐레이션 신원 식별 — 큐레이션 UI가 user_id raw UUID로만 라벨링돼 유저 식별 불가인 것을 email·display_name JOIN으로 보강하는 작업을 볼 때 [memory,curation,identity,scoped-endpoint,authz-decouple]
- 054 MCP 실연결 런타임(HTTP) — runtime이 canned 문자열만 반환하던 가짜 MCP를 langchain-mcp-adapters 실연결로 바꾼 작업을 볼 때 [mcp,runtime,langchain-mcp-adapters,streamable-http,self-host,discovery,hil,ssrf]
- 053 유저 메모리 역할 기반 스코핑 — 052가 신원 라벨만 붙이고 접근은 무방비였던 프라이버시 구멍을 본인 기본+어드민 타인 접근의 2축 가드로 닫을 때 [memory,authz,role-scope,privacy,two-axis-guard,capability]
- 055 Playground 세션 이어가기(과거 세션 선택·재개) — 승인 이탈 후 복귀 시 Playground가 초기화되는 것을 세션 피커+대화 복원으로 이어가게 한 작업을 볼 때 [playground,session,resume,agent-filter,preview-label,optimistic-async,adversarial]
- 056 세션 정리: 카운터를 진실로 유지하고 거짓말의 출처(seed)를 고친다 — 시드 빈 데모세션이 turns를 부풀려 저턴 정리에서 고턴 오인 보존되던 것을 seed turns=0 진실화로 고칠 때 [session,cleanup,counter,seed,windowed-mode,adversarial,reversal]
- 057 원격/외부 에이전트 등록을 A2A로 단일화(provenance만 구분) — 자체 SSE 원격 경로를 폐기하고 모든 원격을 표준 A2A로 통일하며 source를 출처(code/external)로만 구분할 때 [a2a,unification,provenance,protocol-consolidation,connect-endpoint,redirect-ssrf,contextId,migration,adversarial]
- 058 첫 실행(first-run) 견고성: 부팅 전제와 사람 진입의 갭 메우기 — 외부 환경서 처음 받아 실행할 때의 부팅 전제(DB 프리플라이트·pgvector)와 사람 진입(첫 관리자·런북) 갭을 메울 때 [first-run,db-preflight,bootstrap-admin,pgvector,graceful-degradation,fail-closed,adversarial]
- 059 MLX를 env에서 떼고 Mock LLM을 기본 채팅/임베딩 모델로 — MLX env 블록 제거가 두 부팅 경로 seed drift 발견으로 번져 마이그레이션+seed로 기본 모델을 수렴시킨 작업을 볼 때 [default-establishment,boot-path-convergence,seed-drift,mock-llm,no-clobber,migration,adversarial]
- 060 A2A 에이전트를 플레이그라운드에서 테스트 가능하게(endpoint 정규화+로컬테스트 UX) — A2A 등록 에이전트 채팅 시 URL 절대성 오류와 SSRF 차단을 관대 정규화+로컬테스트 UX로 푸는 작업을 볼 때 [a2a,playground,url-normalization,ssrf-invariant,lenient-parse,local-test-ux]
- 061 로컬(ui) 에이전트를 실제 A2A로 노출(exposed.a2a 실동작화) — 무력하던 exposed.a2a 플래그를 실제 A2A 서버 라우터(카드 공개+JSON-RPC 인증)로 동작시켜 로컬 에이전트를 A2A로 여는 작업을 볼 때 [a2a,exposure,inert-flag,inbound-surface,unauthed-router,self-dogfood,reuse-runtime,adversarial]
- 062 Admin API 에러 detail 가시화 + RAG 차원불일치 조치안내 — POST가 status만 보여 원인불명이던 것을 프런트 중앙헬퍼가 detail을 추출하게 하고 차원불일치 조치문구를 더한 작업을 볼 때 [error-surfacing,detail,central-boundary,cap-raw-source,curated-message,rag-dimension,adversarial]
- 063 A2A endpoint 호출 경계 정규화 + 빌더 하드닝 + stale 데이터 마이그레이션 — 060이 등록시점만 정규화해 stale endpoint가 호출경계서 거부되던 것을 호출경계 정규화+마이그레이션으로 닫을 때 [a2a,call-boundary,url-normalization,ssrf-invariant,userinfo-disguise,stale-data-migration,self-healing,adversarial]
- 064 SSRF allowlist를 env에서 DB로(무재시작 관리·Admin UI/API) — allowlist를 env에서 DB 테이블로 옮겨 재기동 없이 Admin UI/API로 관리하게 한 작업을 볼 때 [allowlist,env-to-db,no-restart,ssrf,bootstrap-seed,cache-snapshot,admin-crud,adversarial]
- 065 어드민(슈퍼유저) 메뉴를 그룹 헤더로 구분 — 슈퍼유저 항목이 일반 메뉴 뒤 평면 append라 경계가 없던 것을 group 헤더로 시각 분리한 작업을 볼 때 [admin,menu,group,superuser,antd,presentational]
- 066 유저 self-승인: permission 기반 RBAC 3-way 승인 권한 — admin 전용이던 HIL 승인 resolve를 Casbin RBAC로 admin/owner-self/그외 3분할한 작업을 볼 때 [self-approval,approval,hil,rbac,permission,casbin,self-approve,fail-closed,user-id,enumeration-oracle,adversarial]
- 067 세션 라우터 유저별 스코핑(대화 전사 교차열람 차단) — sessions.py가 principal을 안 받아 member가 타인 대화를 교차열람하던 것을 본인 스코핑+가시성404로 닫을 때 [session,rbac,per-user-scoping,enumeration-oracle,list-vs-item,visibility-404,personal-data,chat-resume-bypass,residual-gap,adversarial]
- 068 chat resume 소유권 경계(067 게이트 우회로 봉인) — 067이 막은 읽기 게이트를 chat resume가 소유권 덮어쓰기로 우회하던 P1을 own 스코핑+무덮어쓰기 불변식+엔트로피로 닫을 때 [session,rbac,chat-resume,ownership-boundary,enumeration-oracle,no-overwrite-invariant,entropy,approval-session-stamp,no-regression,adversarial]
- 069 RBAC/소유권 경계 스펙 체크리스트(회고→재발방지 훅) — 소유권/테넌시 경계 스펙 쓸 때 모든 입구(list/item/create/resume/lazy-create)×소유권·404비노출·검증사다리3런·자가잠금핀을 강제하려 할 때; 폴더 CLAUDE.md에 박음, 근거 learning 068·069·070 [rbac,ownership-boundary,multi-tenant,spec-checklist,process-hook,entry-enumeration,verification-ladder,self-lockout,enumeration-oracle,folder-context]
- 070 세션 읽기 owner 스코프를 쿼리에 융합(067 P3-1 타이밍 봉합) — fetch-then-check 타이밍 오라클을 닫을 때(SELECT-WHERE 융합으로 거부행 미로드·체크리스트 069 첫 적용·저장계층 잔존은 073) [session,timing-side-channel,fetch-then-check,select-where,visibility,no-action-refactor,checklist-application,storage-layer-residual,adversarial]
- 071 A2A 카드 prefix-상대 endpoint 구제 + probe 404 정직화 — 프록시 path-prefix 뒤 카드가 루트상대 `/a2a` 발행 시 채팅 404일 때(mount prefix 상대로 urljoin resolve·probe 404/410→dead 정직화·꼬리중복은 082) [a2a,agent-card,url-resolution,proxy-path-prefix,root-relative,urljoin,ssrf-boundary,liveness-probe,route-vs-host,rfc-vs-deployment,adversarial]
- 072 RAG retrieval 시험 수단: 공유 코어 + `/search` 엔드포인트 + 관리 UI 패널 — "rag는 등록만 있고 테스트 길 없음" 공백을 닫을 때(retrieval 본체를 도구·엔드포인트 공유 `search_collections`로·CollectionsView 검색 드로어·승격 시 입력 신뢰경계 리셋) [rag,retrieval,shared-core,drift-prevention,search-endpoint,admin-ui,trust-boundary,input-validation,verification-ladder,adversarial]
- 073 세션 읽기 저장계층 타이밍 봉합: owner-선두 복합 인덱스 + EXPLAIN 측정(070 P2) — 070 뒤 남은 저장계층 타이밍 잔존(session_id 단독 unique heap-fetch)을 닫을 때(owner-선두 복합 인덱스·EXPLAIN buffer 실측+반증·"선택≠강제" 정직기록) [session,timing-side-channel,storage-layer,composite-index,owner-leading,explain-buffers,counterfactual,idempotent-migration,planner-selection,honest-residual,adversarial]
- 074 RAG 컬렉션 액션 가시성: 검색 버튼 승격 + 테이블 오버플로우 해소(072 후속) — 072 검색 버튼이 "안 보임" 보고를 닫을 때(max-content 테이블 오버플로를 scrollWidth로 측정해 범인=이름 컬럼 특정·검색 primary 승격·순수 표현변경 RBAC 무관) [rag,admin-ui,table-overflow,max-content,measure-not-guess,button-prominence,responsive,verify-ui-browser]
- 075 에이전트 생성/수정 폼 체크박스 행 레이아웃 깨짐 — "생성 드로어 레이아웃 깨짐"(메모리 라벨 글자단위 세로 줄바꿈) 보고를 닫을 때(flex-shrink·중첩 label invalid HTML→이름·설명을 Checkbox children 세로 스택·측정 검증·순수 표현 RBAC 무관) [admin-ui,antd6,checkbox,flex-shrink,nested-label,layout,verify-ui-browser,measure-not-guess]
- 076 create_react_agent → langchain.agents.create_agent 마이그레이션 — deprecated 런타임 빌더를 후속 API로 갈 때(prompt→system_prompt 개명·유일 breaking은 콜러블 prompt·계약 4종은 verify_041 회귀로 보존 증명·deprecation 0) [langgraph,langchain,create-agent,deprecation,migration,call-contract,system-prompt,regression,verify_041,adversarial-not-needed]
- 077 온도 에이전트 필드 승격 + 페르소나 대칭 (이슈2) — 온도·페르소나가 한 곳만 있는 비대칭 보고를 닫을 때(런타임은 이미 cfg.temperature 읽음→입구만 추가·model_dump round-trip·폼 Slider+OverridePanel 페르소나 Select로 대칭·RBAC 무관) [agent-config,temperature,persona-symmetry,override-panel,jsonb-roundtrip,model-dump,antd6-select] → learning 080
- 078 미활성 초안 배지로 "편집 미반영" 안내 (이슈1) — "수정 후 플레이그라운드에서 안 바뀜" 보고를 닫을 때(버그 아닌 설계대로=서빙은 활성 config만·증상 화면 Playground에 초안 신호 0이 갭→표시층만 "미반영 초안" 배지·백엔드 0줄·RBAC 무관) [playground,draft-badge,staging-surface,version-status,truth-source,no-backend-change,antd6-tag,verify-ui-browser] → learning 081
- 079 인스펙터에 RAG 문서검색·메모리 조회 이력 노출 (이슈5) — "tag 조회·메모리 조회 이력 안 보임" 보고를 닫을 때(갭=결과 없음 아닌 *조회 행위 비가시*→회상 쿼리 에코 한 키 추가·Inspector에 조회 블록+RAG 섹션·0건도 쿼리 노출·RBAC 표시전용) [inspector,playground,rag,memory-recall,observability,trace,instrument-the-act,0-result-still-happened,antd6,verify-ui-browser] → learning 082
- 080 Playground 에이전트 변경 전파(BroadcastChannel + 포커스 백스톱) (078 후속버그) — "초안 활성화해도 미반영 배지 남음" 보고를 닫을 때(진짜 조건=*별도 탭* Playground가 마운트 스냅샷 들고 외부 activate 모름→BroadcastChannel+focus 재페치 백스톱·백엔드 0줄·RBAC 무관) [playground,draft-badge,stale-snapshot,cross-tab,broadcastchannel,focus-backstop,consumer-surface,verify-ui-browser] → learning 083
- 081 A2A 런타임 호출 경로 견고화 (071 후속) — 외부 에이전트 채팅 "응답 오류 404"를 닫을 때(message/stream·send 동일 endpoint라 404=endpoint 틀림으로 재분별·cardUrl 저장+resync가 fetch_card→probe로 in-place 자가치유·stream 404→send 1회 폴백·RBAC 무관) [a2a,stale-endpoint,self-heal,resync,card-url,re-derive,message-send-fallback,offline-honesty,single-endpoint,probe-deeper] → learning 084
- 082 A2A endpoint resolution 꼬리 중복(/a2a/a2a) collapse (071 누락) — endpoint 뒤 /a2a/a2a 중복으로 Playground 404일 때(prefix 꼬리==카드 상대경로면 collapse·정확 꼬리매치만·fetch_card 단일지점이라 connect/register/resync 일괄·SSRF 새입구 0·RBAC 무관) [a2a,endpoint-resolution,duplicate-tail,collapse,idempotent,prefix-relative,071-followup,probe-deeper] → learning 085
- 083 A2A 노출은 source=ui 에이전트에만 (원격/외부 노출 토글 제거) (이슈2) — "원격 에이전트를 A2A로 오픈은 이상함" 보고를 닫을 때(보안은 이미 닫혔으나 code/external에 exposed.a2a=true 박힌 "죽은 상태"→불변식을 모든 쓰기/렌더 입구에 정렬·마이그레이션 stale 청소·JSONB 형제키 하드닝·RBAC 무관) [a2a,expose-gate,source-ui-only,fail-closed-consumer,dead-state,align-predicate-every-writer,jsonb-sibling-key,no-frontend-only-guard,adversarial-codex,verify-ui-browser] → learning 086
- 084 메모리 회상 시험 도구 (조회 시험 드로어) (이슈1) — "인스펙터는 메모리 검색했다는데 실제 시험 불가"를 닫을 때(메모리 콘솔 양 탭에 RAG 072 /search 미러링한 조회 드로어·enabled를 프록시 아닌 백엔드 가용성에 묶는 recall_probe·None≠[] 구분·유저검색 본인만·RBAC 미러) [memory,recall-test,enabled-honesty,proxy-vs-actual,recall-probe,none-vs-empty,mirror-sibling-endpoint,rbac-mirror,adversarial-codex,verify-ui-browser] → learning 087
- 085 커스텀 에이전트 런타임 인터페이스(in-process SDK) — source 문자열로만 갈리던 에이전트에 공통 런타임 인터페이스를 들일 때(`CustomAgent` Protocol+`AgentBuildContext`로 적합 그래프 astream 루프 소유·미구현은 None→_a2a_stream fallback·분기축은 config["impl"] 레지스트리 키·레퍼런스 2구현으로 과적합 측정·RBAC 미적용) [custom-agent,in-process-sdk,protocol,agentbuildcontext,graceful-fallback,trusted-registry,real-node-trace,full-replace-silent-drop,dead-contract,adversarial-codex] → learning 088
- 086 턴 인스펙터 노드별 세부정보(상태 델타 미리보기 + 실측 시간) — 인스펙터 "LangGraph 경로"가 노드 이름+가짜 균등분할 ms만 보일 때(`updates` 청크 값·도착시각을 살려 노드별 실측 ms+안전 요약·키기반 redaction+plan allowlist·3경로 우선순위·비밀누출0 불변식·MCP args 누출은 087로 분리) [inspector,node-detail,state-delta,real-per-node-ms,redaction,value-secret-allowlist,budgeted-cap,parallel-superstep-honesty,failclosed,adversarial-codex,verify-ui-browser] → learning 089
- 087 MCP 호출 인자·결과 redaction(형제 trace 표면) — 086 codex F1 분리; "MCP args/result 원문 trace 노출 P0"를 액면 수용 않고 위협 모델 실측으로 재분별할 때(시스템 비밀은 헤더/설정에 있지 kwargs에 안 옴+표면 admin전용→defense-in-depth·runtime.py 한 곳 sink 정화·민감키 blocklist는 086 allowlist와 반대 polarity·RBAC 무관) [redaction,sibling-surface,inherited-severity,reframe-threat-model,defense-in-depth,key-blocklist,purpose-sets-polarity,central-boundary,jsonb-nan,failclosed-serialization,adversarial-codex,probe-deeper] → learning 090
- 088 플레이그라운드 응답 markdown 렌더 + raw-JSON 휴리스틱 — 응답을 평문으로만 그려 markdown/JSON이 안 풀릴 때(프런트만·렌더시점 휴리스틱·스트림완료 게이트·접이식 JSON트리·codex 4건 수정) [playground,markdown-render,react-markdown,json-tree,format-inference,streaming-gate,render-time-heuristic,covering-guard,tracking-pixel,bigint-precision,contentRender,adversarial-codex] → learning 091
- 089 커스텀 에이전트 공통 인터페이스 준수 분류 3상태 (제안#1, 초안) — "커스텀 에이전트가 우리 공통 인터페이스를 지키나"를 1급으로 가를 때(준수/비준수(A2A,정당한종류)/설정실패 3상태·source 라벨 아닌 `isinstance(CustomAgent)`+resolve 게이트·교정3 폴백 마스킹 금지=선언한 impl 미해결이면 DefaultUiAgent로 만회 말고 서빙거부+설정실패 표면·impl미선언만 default유지·저장은 허용(합의B)·get_agent_impl isinstance 갭 봉합·단일 헬퍼 파생 비저장·AgentOut.conformance+UI 배지·#4 선행·RBAC 미적용) [custom-agent,conformance,three-state,config-error,protocol-isinstance,fallback-masking-fix,gap-closure,single-helper-derive,no-store,agentout-field,ui-badge,precursor-to-4,085-followup]
- 090 플레이그라운드 메시지 역방향 페이징 (파킹 — #2 오독 산물) — 제안 #2를 "긴 대화 위로 스크롤 페이징"으로 오독해 쓴 스펙; 사용자 정정으로 *진짜* 의도는 091(입력 히스토리 재호출)로 분리, 본건은 삭제 않고 좋은 후속 후보로 파킹 [playground,reverse-pagination,parked,intent-misread,split-to-091]
- 091 플레이그라운드 입력 히스토리 재호출(터미널 콘솔식 ↑/↓) (제안#2) — "입력창 커서 맨앞서 ↑=과거 입력 로드"(셸 히스토리)를 닫을 때(순수 reducer inputHistory.ts로 정책 분리=컴포넌트/단위 드리프트0·history=messages의 role'me'+연속중복접기·caret 절대맨앞 진입조건+탐색진입후 caret무관·편집/전송 탐색종료·IME 가드·in-memory 세션로컬·순수프런트 백엔드0; codex P2 2건: 값의존 effect no-op 미발화→단조카운터 recallSeq, 탐색중 history축소 stale-idx→recallNewer 클램프) [playground,input-history,terminal-recall,shell-history,pure-reducer,dedupe-consecutive,caret-at-front-gate,ime-guard,in-memory,frontend-only,value-dependent-effect,monotonic-counter,stale-index-race,verification-ladder-3rung,adversarial-codex] → learning 093
- 092 채팅 본문서 도구(블록) 원본 응답 숨김 (제안#3) — 메모리/MCP/RAG 호출의 raw 응답(ToolMessage.content)이 본문에 새고 추론이 이어붙던 걸 닫을 때(백엔드 스트림 게이트 `runtime.is_tool_message=isinstance(ToolMessage)` 단일술어를 event_stream·stream_local_reply 두 sink 공유=드리프트0·프런트0줄; `.type` 문자열은 청크불안정이라 isinstance로=verify 단위런이 codex전에 잡음; codex P1=content가 content-block 리스트일 때 `"".join(acc)` TypeError 선재잠복을 `_content_text` 정규화로 함께봉합; P2 polarity blocklist fail-open 유지+현실표면 협소 측정, 인스펙터"손실0" 과장정정=MCP 2000자캡·RAG건수만, 빈 어시스턴트 영속은 raw누수보다 개선이라 수용; calls_sink 독립보존) [playground,hide-tool-response,toolmessage,stream-gate,isinstance-not-type-string,messagechunk-instability,content-block-normalization,_content_text,single-predicate-two-sink,covering-guard,blocklist-polarity,fail-open-narrow-surface,observability-overclaim-fix,verification-ladder-3rung,adversarial-codex,frontend-zero] → learning 094
- 093 연결된 에이전트 있으면 MCP서버·RAG컬렉션 삭제 차단 (제안#5) — name 문자열 참조(FK아님)를 삭제/rename하면 config에 dangling name→런타임 조용히 능력소실, 이를 409로 차단할 때(참조 스캔=활성 config ∪ **archived 포함 전 버전**=롤백가능 live·codex가 "archived는 dead" 가정 반증→교정; 입구=삭제2+rename1 동일헬퍼 operation-symmetry; 가드/런타임 `config_names` 단일 normalizer drift0; detail은 **string**이라 httpError 062 그대로 노출·프런트 0줄; TOCTOU는 서빙불변식 논증으로 spec094 이연) [reference-integrity,name-not-fk,dangling-name,delete-rename-guard,operation-symmetry,archived-rollbackable-live,covering-guard,config-names-normalizer,guard-runtime-drift,string-detail-062,frontend-zero,toctou-deferred,verification-ladder-3rung,adversarial-codex] → learning 095
- 095 DataTable 가로 overflow: 액션 컬럼 고정 + 반응형 숨김 (제안#6) — 커스텀 DataTable(shared.tsx, minWidth:max-content)이 넘쳐 편집·삭제 액션버튼이 화면밖 밀리던 걸 사용자승인 "둘 다"로 닫을 때(Column에 hideBelow?:Breakpoint·fixed?:'right' 추가, isStickyRight=무제목 마지막컬럼 자동 sticky-right, ResizeObserver로 overflow감지 훅은 모바일 early-return 앞 무조건호출, 4뷰 hideBelow 주석·모바일 카드경로는 전컬럼유지; 완료조건 픽셀수치 GREEN=@1280·1440 0/10·@1024 Agents0/Sessions16px·Batch21px 사소, STICKY_OK; codex rung③ P0/P1 0, P2 hover미반영은 data-sticky재배선으로 HOVER_OK) [datatable,overflow,sticky-right,responsive-hide,hideBelow,covering-guard-frontend,measure-then-tune,opaque-overlay-rehover,browser-verify,adversarial-codex] → learning 096
- 097 검색시험 드로어 통합 + 메모리 뷰 검색 UX 정합 (제안#7) — RecallDrawer(메모리 084)가 SearchDrawer(컬렉션 072)를 수동미러링해 갈린 드로어 UI(top_k/limit·검색/조회·유사도/관련도·결과/회상·filename vs scope·type)를 공유 제네릭 셸 RetrievalTestDrawer<H>로 통합할 때(도메인차=props/renderMeta 주입, 어댑터 얇게 -147줄, 072 백엔드 shared-core를 UI로 확장=covering-guard 프런트판·drift0; 정직성계약 084 보존=onSearch 정규화 {results,enabled}로 enabled false→disabledAlert / enabled&&빈→인라인 회상없음 구분 None≠[]; 컬렉션 어댑터 enabled 상수true→비활성안내는 !ready preAlert; RBAC 미트리거=순수UI, onSearch가 searchAgentMemory/searchUserMemory로 스코프 이미 바인딩); 브라우저 검증(docs_kb ready·Personal Secretary)로 도메인라벨·결과카드·카운트·스코프리셋·0건인라인 실증, codex rung③ P1=원본 두 드로어 잠복 stale-async 스코프유출→공유셸 reqSeq 가드로 양쪽봉합 [retrieval-drawer,unify-collection-memory,shared-generic-shell,covering-guard-frontend,drift-zero,honesty-contract-none-not-empty,render-prop-meta,enabled-normalization,preAlert-not-ready,rbac-untriggered-pure-ui,scope-binding-preserved,stale-async-scope-leak,reqSeq-guard,browser-verify,adversarial-codex] → learning 097
- 098 세션 목록 서버측 메타데이터 검색 (제안#8) — 세션목록 GET /sessions에 검색 q 추가(session_id·user_id·agent_name 3컬럼 OR ilike 부분일치, 기존 own-scope(067)·status버킷·agent_id(055)와 AND, `%_\`는 리터럴 이스케이프 escape="\\"); 서버측 필수=세션은 서버페이징(034)이라 로컬필터는 현재페이지만 걸러 부정직(097 로컬필터 정당판정의 반대, 데이터지역성이 가름); 프런트 디바운스(~300ms)→q변경 page=1리셋→서버재조회, 검색 Input placeholder "세션ID·유저·에이전트 검색"; RBAC 트리거됨=predicate를 own-scope WHERE 뒤 AND로(AND는 좁히기만→스코프 못넓힘)·존재 비노출(스코프밖 세션은 base서 제외돼 검색누설0)·counts도 067스코프 유지; 검증 3런=verify_098(14단언: 3컬럼·OR·대소문자·빈/공백q·status AND·이스케이프·RBAC own-scope 통합[타인 agent_name 공유검색→누출0/본인검색→조회됨]) + 브라우저E2E(마커→2건·clear복귀·무매칭empty) + codex no P0/P1 [session-search,server-side,3col-or-ilike,ilike-metachar-escape,scope-where-then-and,server-paged-local-filter-dishonest,honesty-contract-opposite-verdict,rbac-triggered,existence-nondisclosure,debounce-page-reset,verification-ladder-3rung,adversarial-codex] → learning 098
- 099 에이전트 플로우 스캐폴드 스킬 (그래프 빌더 #4 폐기 대체) — 시각 그래프빌더 폐기하고 목적을 Claude Code 스킬 코드젠으로 대체: agent-flow 스킬이 CustomAgent Protocol(085) 적합 LangGraph 플로우를 packages/agent/src/agent/flows/<key>.py에 저작→runtime _bootstrap_builtins에 신뢰등록(late-import+register_agent 2줄, 런타임 eval 없음 085 U5 보존)→089 conformance·085 구조검증까지; 결정 A=flows/ 신규폴더(examples/는 참조 default·plan_execute 2종 유지) B=신뢰등록만·반영엔 API재기동1회 C=.claude/skills/agent-flow/SKILL.md 4단계(의도수집→모듈생성 describe/build_graph ctx만읽음·매니페스트정직→배선→검증스크립트) D=수용게이트 발명안하고 089/085 재사용; 데모 route(조건분기 classify→answer_a/answer_b)로 엔드투엔드 실증, 분기로직 classify_route는 모듈 순수함수; 검증 3런=verify_099_route(단위17: 적합·정직supports_hil=False·노드{classify,answer_a,answer_b}·분기결정성·드리프트0 + 통합 질문→answer_a/평서문→answer_b 반대분기미발화 실노드타임라인) + 085/089 무회귀 + codex no P0/P1; 비목표=admin UI impl선택노출(별도)·원격code/external생성·런타임동적로딩 [agent-flow-skill,codegen-scaffold,discard-graph-builder,custom-agent-protocol,trusted-registry-no-eval,flows-folder,reuse-089-085-gate,conditional-branch-route,pure-function-branch,api-restart-authoring-time,verification-ladder-3rung,adversarial-codex] → learning 099
- 100 능력 브로커 시임 + A2A 오케스트레이션(서브스텝) Phase1 — **done**(retrospect 081·learning 100): A2A·MCP·RAG·memory를 1개 시임(discover/describe/invoke)으로, preload 대신 discovery(컨텍스트절약+모델이식성); Phase1=인터페이스(packages/agent, ctx.broker 주입 085 U2보존)+정책게이트(allowlist∩RBAC casbin deny-by-default, chat.py ctx빌드경계, 발견·호출 둘다 게이트=존재비노출)+첫provider A2A(a2a_client 재사용)+데모flow orchestrate(agent-flow규약099: analyze→delegate→synthesize, untrusted결과=데이터)+트레이스 broker_invoke노드(통째프록시 a2a_call 단일노드 대비); 완료조건 데모주도(deny실증·서브스텝트레이스·인젝션방어·RBAC교집합·자가잠금·085/089무회귀); OUT=MCP/RAG/memory kind·벡터하이브리드·admin UI정책·런타임동적로딩 [capability-broker,discovery-over-preload,discover-describe-invoke,one-seam,ctx-broker-injection,policy-scoped-broker,allowlist-intersect-rbac,deny-by-default,existence-nondisclosure,invoke-recheck-toctou,untrusted-output,broker-invoke-trace,substep-orchestration,agent-flow-demo,rbac-boundary-checklist,verification-ladder-3rung]
- 101 능력 브로커 kind 확장: MCP provider (Phase 2-a) — **done**(retrospect 082·learning 101): spec100 "1시임 여러kind" 주장을 두번째 provider(MCP)로 실증; broker.py 안에 내부 _CapabilityProvider 시임 도입(정책=브로커 단일지점 유지·kind별 메커닉=provider로: AgentProvider 행위보존 이관+McpProvider 신규), MCP 능력입도=**툴단위**(툴1개=cap1개, hook=툴description·input_schema=툴스키마), allowlist 네임스페이싱 `<kind>:<id>`(bare=agent 하위호환·`mcp:<server>`=서버전체), 전송=기존 MultiServerMCPClient+net_guard 재사용·데모 orchestrate 코드0 재사용; **HIL 서브스텝 지원**(사용자 요청): 위임 MCP툴이 승인요구시 broker.invoke가 전송이전 interrupt()→기존 Approval/resume 파이프라인 재사용(새배선0, orchestrate 이미 checkpointer컴파일), 승인정책=`_APPROVAL_ACTIONS` 단일소스 재사용(그래프-tools와 드리프트0)·interrupt는 부수효과이전(멱등)·supports_hil→True정직화; per-provider approval_for(agent=None 후속); 완료조건=verify_100무회귀(행위보존)·MCP discover/서브스텝트레이스·서버전체허용이 타서버툴로 안샘·untrusted격리·HIL왕복(approve→전송1회+재개·reject→부수효과0·승인이전_execute0 멱등)·085/089/100무회귀·codex no P0/P1; OUT=RAG/memory kind·벡터·per-cap/user인가·A2A위임승인·다중interrupt·재개라이브스트림·admin UI [capability-broker,kind-extension,mcp-provider,provider-seam,behavior-preserving-refactor,tool-level-granularity,allowlist-namespacing,reuse-mcp-transport,hil-substep,interrupt-before-sideeffect,approval-policy-single-source,supports-hil-honest,per-provider-approval,verification-ladder-3rung,rbac-boundary-checklist,config-capabilities-persistence,seed-bypasses-write-schema,stale-reload-split-brain,verify-the-reviewer] → retrospect 082 · learning 101
- 102 discovery 오케스트레이션: 전략 교체형 골격 (공통 조상 + 첫 출하 2전략) — **done**: 브로커(100·101) 위에 오케스트레이션 방식을 **에이전트 소유자가 고르는 전략**으로 노출(플랫폼이 하나로 못박지 않음); 현 데모 orchestrate=discover(limit=1) 첫매치 단일위임=오케스트레이션 아님; 결정=**전략=각각의 impl**(플로우 안 if 아님·새 config필드 없이 config["impl"] 재사용→088 함정회피)·**공통조상=ABC+템플릿메서드**(OrchestrationAgentBase가 build_graph 골격[analyze→delegate→synthesize]·채널격리[100]·HIL[101]·정책 소유=드리프트0, 자식은 `@abstractmethod select(query,candidates)`만 override=유일한 구멍, describe/build_graph 구현으로 CustomAgent Protocol 자동적합→089 conformance 무회귀)·**select=모듈 순수함수**(rank_candidates 겹침점수·id tie-break=실LLM없이 결정성단언 099규약)·**첫출하 2전략**(039/085 무누수는 둘째구현으로 *측정*: ①현 OrchestrateAgent→FirstMatchOrchestrateAgent 행위보존리팩터[impl키 orchestrate 그대로], ②RankedOrchestrateAgent=impl orchestrate-ranked 신규)·**다중위임=순차 fold**(병렬+interrupt 멱등은 별개난제로 후속·순차는 101 HIL 시맨틱 보존)·신뢰등록 099 재사용·**agent-flow 스킬에 전략 분기**(오케스트레이션 전략이면 조상 상속+select만 생성·골격/HIL/채널격리 작성금지·수용게이트에 override홀 검사); RBAC체크리스트=상속으로 만족(새 유저데이터 입구0·invoke가 정책소유·자식은 정책 못건드림); 완료조건 측정가능 3런=verify_102(40 ok: 단위 rank결정성·select·골격드리프트0[get_graph().nodes 실측]·채널격리k개·conformance + 통합 Ranked가 더관련후보선택·다중broker_invoke타임라인·HIL보존·다중위임+중간interrupt 경계[H10 gated exactly-once]·FirstMatch=현동작 + 100/101/085/089무회귀); **codex 5건 정직분류**=[P1]다중위임+중간interrupt 재실행=여집합공격성공이나 안전위반 아님(gated 부수효과 exactly-once 유지)→주석경계명시+H10실측(고침 아닌 정직화)·[P2]override홀→`@final` 정적강제·[P2]select계약→조상 delegate가 chosen⊆candidates 교집합 구조강제·[설계한계]라벨스푸핑=데이터채널내부 표식일뿐 G1견고→docstring명시·[FALSE-POSITIVE]ABC강제=이미 fail-closed; OUT=진짜병렬·다중interrupt·플래너(B)·반복(C)·k config노출·admin UI드롭다운(085 H5)·벡터검색 [discovery-orchestration,pluggable-strategy,strategy-as-impl,common-ancestor-abc,template-method,select-override-only-hole,pure-function-rank,two-impls-measure-abstraction,behavior-preserving-refactor,sequential-fold,channel-isolation-inherited,drift-zero,conformance-089,reuse-099-registry,skill-orchestration-branch,authoring-time-enforcement,final-static-enforcement,chosen-subset-guard,complement-attack-honest-boundary,verification-ladder-3rung] → learning 102
- 103 능력 브로커 kind 확장: RAG provider (Phase 2, 셋째 provider·첫 읽기전용) — **done**(retrospect 084·learning 103): spec100 "1시임 여러kind"를 셋째 provider(rag)로 재측정; RagProvider(kind=rag, cap_id `rag:<collection_name>` 1레벨·이름콜론슬래시허용)를 McpProvider 거울로 6메서드계약 구현, **정책 무변경**(rag는 1레벨이라 mcp 서버전체특례 불요·기본 `cap_id in allow`가 정답·`_permitted` rag분기 0줄·`build_broker` enforce `capability:rag` kind만 바뀜), invoke=`runtime.search_collections`(037본체·072추출) 코어재사용+결과포맷 `build_rag_tool`에서 `format_rag_hits` 추출공유(A안)=엔드포인트·인챗도구·브로커 **세입구 한코어** drift0(H7 provider invoke==코어+포맷 실측), trust=untrusted(문서=데이터 100), **approval_for 항상None**(읽기전용=비가역부수효과0=HIL불요); RBAC체크리스트=상속으로 만족(새 유저데이터입구0·읽기전용=부수효과입구0), Collection=owner없는 공유카탈로그(per-user인가는 memory provider가 강제→3안 분리); 3런=verify_103_broker_rag(46 ok: 단위순수 네임스페이스파싱·_permitted rag·approval_for None·describe스키마·col=None graceful·_by_kind 3종·시임6메서드·빈이름`rag:`가드·deny-by-default DB미접촉 + 실mock임베딩·실DB통합 discover/describe/invoke왕복[untrusted·broker_invoke:rag프레임·복호화키미노출]·정책격리[RBAC거부/allow밖 존재비노출]·공유포맷 drift0·과대질의상한) + 072/100/101/102무회귀(verify_101 U6 provider집합 하드코딩 {agent,mcp}→⊇완화) + codex; **codex 3판정**=[P1]인챗도구 `vectorTables`가 브로커정책밖=**정직한경계**(저작자 정의시점바인딩[페르소나·모델과 동급신뢰] vs 런타임 RBAC위임 다른신뢰모델·103은 위임능력 추가일뿐 기존도구 브로커뒤로 안옮김·complement공격성공이나 안전위반아님→스펙 비목표 OUT기록+H4/H5 안전불변식)·[P2]질의무제한→공유코어 4000자상한1회(엔드포인트 스키마≤4000 무영향·도구/브로커 함께경계)·[P2]빈이름`rag:`승격→브로커 파싱층(candidates/load) 방어(근본=037 CRUD경계); OUT=per-cap/user인가+소유권(memory가 강제)·memory provider·벡터하이브리드 discover·admin UI(Phase2-d)·인챗도구 브로커통합 [capability-broker,kind-extension,rag-provider,third-provider-measures-seam,read-only-provider,two-gates-policy-vs-approval,cap-id-one-level,policy-unchanged,shared-core-search-collections,format-rag-hits-extract,three-entrances-one-core,drift-zero-measured,untrusted-data-channel,approval-for-none,rbac-checklist-by-inheritance,owner-less-shared-catalog,complement-attack-honest-boundary,document-boundary-not-silent,core-holds-cross-entrance-invariant,empty-resource-id-guard,verification-ladder-3rung,adversarial-codex] → retrospect 084 · learning 103
- 104 능력 브로커 kind 확장: Memory provider (Phase 2-c, 첫 per-user 소유 능력) — **done**(retrospect 085·learning 104): 네번째 provider(memory)로 시임 재측정+100/081이 미룬 **인가 입도 빚 상환**; 메모리=per-user 개인데이터라 공유카탈로그(agent·mcp·rag) 방식(cap_id에 대상 박기) 치명적(`memory:앨리스`를 밥이 실행=유출)→**정공법=능력이름에 대상 안박고 소유자를 런타임 principal서 도출**: 능력 `memory:user` 하나, user_id=build_broker가 `str(principal.id)` 뽑아 MemoryProvider 주입, invoke스코프 **오직** `{"user_id":self._user_id}`(cap_id·args 불가)→이름으로 남 가리킬 방법없어 교차유출 *구조적* 불가+어드민 에스컬레이션 자동차단(superuser도 자기것만·타인큐레이션은 URL받는 별도경로 053); 정책 무변경(`_permitted` memory분기0=1레벨정확매치), invoke=`memory.recall_probe`(챗회상·시험084 공유)+`format_memory_hits`(챗 회상포맷 추출)공유 drift0, trust=untrusted(기억=데이터 100), 읽기전용→approval_for None(두게이트분리 103); RBAC체크리스트 발동(유저별데이터)=입구{discover,describe,invoke}뿐 읽기전용·소유owner=principal도출(cap_id·args불가 §2d 비-SQL은 호출에 owner묶음)·단일헬퍼·존재비노출; 3런=verify_104(FakeMem 결정적격리[같은cap·주체만 bob↔alice→분리]·머신deny·args anti-leak·build_broker배선 + 실mem0·실DB통합 H3 실backend필터격리·H5 RBAC deny + 무회귀 084/100/101/102/103)+codex; **codex 3판정 P0/P1없음**=[P2실결함]limit타입미검증(`{"limit":"boom"}`→TypeError, 103 P2동형)→recall_probe 정수강제+clamp(공유지점)·[P2실결함]승인재개 브로커 user_id누락(`_build_resume_broker` 미주입→재개시 memory:user 사라짐)→주입[새상태축=모든팩토리 세라]·[P2미문서경계]format_memory_hits=격리아님(브로커경로=flow 데이터채널·챗직접회상=persona주입 104이전설계·자기기억이라 교차인젝션아님)→docstring+비목표명시; OUT=memory 쓰기(add, 승인게이트 처음필요+051누출 이력→별도분석)·agent_id메모리 공유읽기·run_id단기·벡터discover·admin UI·챗 직접회상 채널모델 [capability-broker,kind-extension,memory-provider,fourth-provider,per-user-ownership,bind-to-principal-not-cap-id,structural-anti-leak,no-admin-escalation,policy-unchanged,recall-probe-core,format-memory-hits-extract,two-gates-read-only,approval-for-none,rbac-checklist,fakemem-deterministic-isolation,real-mem0-integration,limit-coerce-clamp,resume-wiring-symmetry,new-state-axis-all-factories,complement-attack-honest-boundary,verification-ladder-3rung,adversarial-codex] → retrospect 085 · learning 104
- 105 능력 브로커 kind 확장: Memory write provider (첫 부수효과·승인 게이트) — **done**(retrospect 086·learning 105): 다섯번째 provider(memwrite, `memwrite:user`)—**항상 부수효과(쓰기)**라 두 게이트 중 **승인 게이트 첫 상시 발화**+자동쓰기 누출(051·031) 구조 봉인; **두 방어 겹침**=①쓰기축=user_id(자기)만·principal 바인딩(104, agent_id 금지·cap_id·args 불가→남에 쓸 방법 구조부재) ②승인게이트(031 처방)=`approval_for` **항상 non-None**(읽기와 정반대), memory.add 이전 interrupt→승인돼야 저장(reject 무저장/approve 정확히1회 멱등, 101 HIL 재사용 새배선0); 읽기≠쓰기 별도권한(`capability:memwrite`≠`capability:memory`), **소유자 self-승인 기본**(사용자 결정—`_DEFAULT_POLICIES`에 member memory.write self_approve, 자기기억 쓰기는 소유자승인 옳은 민감도, data.delete는 admin 유지), 저장=승인원문(infer=False)·payload 마스킹없이(승인가시성); RBAC체크리스트 발동=입구{discover,describe,invoke} invoke만 부수효과·승인뒤·소유owner=principal도출; 3런=verify_105(단위 FakeMemAdd+**최소1노드 graph 승인왕복 LLM불요**+실 mem0 쓰기→읽기 왕복·교차유저격리+self-approve 시드·거대text 상한)+무회귀 066/084/100-104+codex; **codex 3판정 P0/P1없음**=[P2실결함]승인·저장 길이무제한(103/104 동형)→`_memwrite_text` 공유헬퍼 4000자(승인한것==저장되는것)·[P2미문서경계]admin 타인 memory-write 승인 열람/결정=admin은 이미 053으로 임의유저 기억접근→새노출아님(권한델타0)→명시화·오탐기각=args scope주입/None경로/머신차단 공격실패; OUT=다중interrupt 멱등(101/102 기존경계)·infer=True·agent_id/run_id 쓰기·memory 수정/삭제(053 _assert_user_owns 선행)·admin owner-only resolve 후속 [capability-broker,kind-extension,memory-write-provider,first-side-effect,approval-gate-is-structure,prompt-not-boundary,two-structural-defenses,principal-bound-self-scope,agent-id-forbidden,read-write-separate-permission,owner-self-approve-default,sensitivity-grade,infer-false,minimal-harness-gate-test,shared-helper-approved-equals-stored,privacy-finding-permission-delta,admin-already-053,complement-attack-honest-boundary,verification-ladder-3rung,adversarial-codex] → retrospect 086 · learning 105
- 106 에이전트 편집 폼에 impl + capabilities 노출 (능력 브로커 UI) — **done**(retrospect 087·learning 106): 브로커(100–105) 6 provider를 UI에서 엮게 편집 폼에 두 축 추가 — 실행 방식(impl) Select(에이전트를 오케스트레이터로)+능력(capabilities) 피커(kind별 위임 cap id); 백엔드 최소=`GET /agent-impls`(신뢰 레지스트리 `list_agent_impls` 노출, `/agents/{uuid}` 충돌피해 top-level meta_router)+AgentOut에 capabilities 필드·직렬화 보강, 프론트=AgentConfig·Agent·AgentFormData에 impl/caps+blankForm·configOf·initial·save 4곳 배선+impl Select(키→친절라벨 매핑·미지 raw폴백)+kind별 능력 피커; **능력 피커는 새 카탈로그 엔드포인트 불요**=cap id `<kind>:<name>` 규약이라 폼이 이미 로드한 collections·mcps·agents+고정 memory:user/memwrite:user에서 순수 포매팅 조립(100–105 ID네임스페이스가 저작계층서 배당); impl은 레지스트리서 끌어 drift0(agent-flow 099 새flow 자동반영); 검증=브라우저 실Chrome 10/10(impl선택·능력피커·토글·오케스트레이터힌트)+백엔드왕복(impl+caps 저장→재조회 보존)+엔드포인트 인증(무인증401); RBAC체크리스트 비발동(순수 admin 저작UI·강제=백엔드 broker 104/105, 권한없는 cap저작해도 런타임 deny-by-default→거짓부여없음)→적대codex 스킵 명시; **함정**=capabilities 직렬화 비대칭누락(write-schema 101서 수용·serializer impl만 방출 caps빠짐→저장되나 재열기때 사라짐, config왕복=4지점 교집합 learning 106)·vite IPv6-only(localhost만)·antd Tag중첩 getByText; OUT=엔드투엔드 스모크(UI→config 지속 증명·config→build_broker 소비 100–105 증명이나 한화면 미실행)·mcp툴단위·impl별 조건부표시 [capability-broker,admin-ui,impl-select,capabilities-picker,agent-impls-endpoint,registry-single-source,kind-namespace-formatting-layer,config-roundtrip-4-points,asymmetric-serialize-gap,client-side-assembly,browser-verification,rbac-checklist-not-triggered,adversarial-codex-skipped] → retrospect 087 · learning 106
- 107 능력 피커 UX: 오케스트레이터 조건부 + 종류별 접이식 — **done**(retrospect 088·learning 107): 106 능력 피커를 사용자가 화면 보고 지적("설정화면 편의성 해침·RAG 늘면 폼 계속 복잡")→종류별 체크박스 벌판이 카탈로그 크기에 비례해 세로로 무한정 길어짐; 두 축 수정(사용자 확정=AskUserQuestion 미리보기로 "종류별 접이식" 선택)=①능력 칸은 오케스트레이터 impl일 때만 렌더(일반 에이전트엔 0픽셀=최강 단순화, 106 OUT 조건부표시를 지적으로 당김) ②종류별 접이식(antd Collapse) 기본접힘+헤더 `선택/전체` 카운트 배지+선택있는 패널만 기본펼침+늘수있는 종류(RAG/MCP/agent) 패널내 검색(>6); 데이터모델·저장배선 무변경(form.capabilities·toggleCap 그대로=순수 표현계층 교체, 회귀위험 표현계층에 갇힘); 검증=브라우저 실Chrome 7/7(기본impl 능력칸없음·orchestrate 등장·패널4 기본접힘·카운트배지·펼침·체크→1/2 반영)+실화면; 함정=antd Checkbox 토글은 라벨span 클릭 불안정→input `.check()`(테스트셀렉터 함정, UI정상); OUT=편집 default펼침 코드검증만·mcp툴단위(106 유지)·타 폼섹션 접이식화(지적범위=능력칸만) [capabilities-picker-ux,user-feedback-driven,conditional-display,accordion-by-kind,count-badge,form-height-independent-of-catalog,pure-presentation-swap,askuserquestion-preview,antd-checkbox-check-input,browser-verification] → retrospect 088 · learning 107
