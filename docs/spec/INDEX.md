# Spec Index  (docs/spec/)

> 각 줄: `NNN 제목 — 언제 열까(후크) [키워드] [archived]`. Context 단계에서 이 인덱스를 먼저 읽고,
> 후크로 불충분할 때만 해당 full 파일을 연다. `[archived]`는 완료된 로드맵 스펙으로 `archive/`에 있다.

- 001 시스템 전체 설계 — 제품의 큰 그림(비전·아키텍처·모듈 경계·도메인 모델·핵심 흐름)을 다시 확인할 때 [overview,architecture,vision]
- 002 페르소나 등록 + chat 노출 — 페르소나 REST 등록/조회와 `POST /agents/{id}/chat` SSE 스트리밍의 최초 배선을 볼 때 [persona,registry,chat,sse]
- 003 Admin Web UI 채팅 — React+TS+antd SPA로 에이전트 골라 브라우저 스트리밍 채팅하는 최초 테스트 환경을 볼 때 [admin,spa,chat,sse]
- 004 Web UI 프롬프트 확인 — 선택 에이전트의 페르소나와 모델 전송 메시지를 읽기전용으로 보는 인스펙트 기능을 볼 때 [inspect,prompt,readonly]
- 005 Ant Design X 채팅 리팩터 — 손제작 채팅 UI를 @ant-design/x(Bubble/Sender/ThoughtChain)로 교체한 UI 토대를 볼 때 [antdx,chat,ui,refactor]
- 006 Admin 콘솔 + Playground 이식 — claude.ai/design handoff 번들을 antd 6/antd-x로 재현한 5뷰 콘솔+Playground 이식을 볼 때 [admin,console,handoff,playground]
- 007 실 에이전트 서비스 — mock UI가 기대하는 도메인을 실제 백엔드(FastAPI/SQLAlchemy/LangGraph/Mem0/MLX) 런타임으로 구현한 핵심 토대를 볼 때 [backend,runtime,langgraph,mem0]
- 008 모델 레지스트리 — LLM/임베딩 모델 설정 CRUD와 런타임이 등록 모델로 ChatOpenAI 구성하는 방식을 볼 때 [model,registry,llm,runtime]
- 009 코드 에이전트 원격 실행 — source=code 에이전트를 원격 엔드포인트로 프록시하는 SSE 재전송 계약을 볼 때 [code-agent,remote,proxy,sse]
- 010 비밀값 암호화 저장 — 모델 api_key·코드 에이전트 token을 Fernet at-rest 암호화하고 런타임에서만 복호화하는 방식을 볼 때 [secret,fernet,encryption]
- 011 API 인증 — 모든 도메인 API에 단일 공유 Bearer 토큰(소유자=전체 접근)을 거는 인증을 볼 때 [auth,bearer,token]
- 012 대화 히스토리 윈도우+저장정책 — historyDepth(최근 N개 절단)와 persistHistory(DB 저장 여부) 에이전트별 설정을 볼 때 [history,window,persist,session]
- 013 모델 연결 테스트 — 등록 전/후 모델을 `{base_url}/models`로 도달성·인증·가용성 검증하는 연결 테스트를 볼 때 [model,test,connection,health]
- 014 페르소나 등록/수정 UI — 백엔드 CRUD는 있었으나 빠져 있던 페르소나 작성/편집 UI를 채운 작업을 볼 때 [persona,ui,authoring,form]
- 015 빌딩 블록 작성/편집 폼 — memory/embedding/permission 블록의 작성·편집 폼이 통째로 없던 빈틈을 메운 작업을 볼 때 [blocks,form,authoring,ui]
- 016 메모리 읽기전용 + kind별 모델 테스트 — 메모리=시스템 enum이라 UI 읽기전용으로 잠그고, embedding 모델은 임베딩으로 테스트하도록 정정한 작업을 볼 때 [memory,readonly,enum,embedding-test]
- 017 어드민 콘솔 모바일 반응형 — Tailscale 노출 후 모바일(~390px)에서 깨진 Sider/검색창/패딩/테이블/그리드 반응형 풀패스를 볼 때 [mobile,responsive,admin,layout]
- 018 메모리 유저 스코핑 — mem0를 에이전트 1버킷에서 user_id(유저 장기)·세션 단기로 나눈 스코핑 도입을 볼 때 [memory,mem0,user-scope,session]
- 019 mem0 pgvector 공유 백엔드 — 파일 기반 qdrant를 제거하고 mem0 벡터 스토어를 공유 Postgres/pgvector로 이전한 작업을 볼 때 [mem0,pgvector,postgres,vector-store]
- 020 mem0 다층 스코프 + 카탈로그 재정렬 — memory_type N종 카탈로그가 mem0 설계의도와 어긋남을 확인하고 user/session 다층 스코프로 재정렬한 작업을 볼 때 [mem0,scope,catalog,realign]
- 021 Playground userId UX — Playground 헤더 userId 입력 레이아웃 정리와 과거 userId 선택(세션 영속 출처)을 볼 때 [playground,userid,ux,session]
- 022 모바일 인스펙터 콘텐츠 오버플로 픽스 — 턴 인스펙터가 모바일 전체화면에서 긴 콘텐츠로 오버플로하는 줄바꿈/flexWrap 픽스와 전 페이지 회귀 검증을 볼 때 [mobile,inspector,overflow,regression]
- 023 에이전트 파운데이션 모델 정정 — 등록 에이전트들이 레지스트리에 없는 가상 모델명을 가리켜 화면-실제 불일치를 정정한 작업을 볼 때 [agent,model,realign,foundation]
- 024 Mock LLM 레지스트리 1급 모델 — 라이브 MLX 없이 결정적 실행을 위해 mock chat completions를 레지스트리 ModelConfig로 1급 등록한 작업을 볼 때 [mock,llm,registry,deterministic]
- 025 Playground Proxy 세션 한정 오버라이드 — 에이전트를 실제로 수정·저장하지 않고 세션 한정으로 모델/프롬프트/도구를 덮어 실행해 보는 오버라이드를 볼 때 [playground,proxy,override,session]
- 026 외부 에이전트 A2A 카드 등록 — 남이 만든 A2A 에이전트를 Agent Card(/.well-known/agent.json)로 내 생태계에 편입(등록·표시, 1차)하는 작업을 볼 때 [a2a,external,agent-card,registration]
- 027 어드민 same-origin /api 프록시 — Tailscale 접근 시 localhost 무의미·mixed-content·CORS 연쇄를 same-origin `/api` 프록시로 끊은 작업을 볼 때 [admin,proxy,same-origin,tailscale]
- 028 Playground 모델 배지 정직화 — source별로(ui=진짜 qwen / code=mock 원격) 모델 배지를 정직하게 표시하도록 고친 작업을 볼 때 [playground,badge,model,source]
- 029 에이전트 전용 메모리(agent_id) — agent_id 스코프 메모리의 에이전트 자가 기록 쓰기 채널과 관리자 큐레이션을 볼 때 [memory,agent-id,self-write,curation]
- 030 메모리 관리 화면 — 에이전트(agent_id)·유저(user_id) 메모리를 검색·수정·삭제하는 통합 큐레이션 콘솔을 볼 때 [memory,console,curation,crud]
- 031 멀티유저 인증 + 오픈형 권한 — 단일 토큰에서 fastapi-users+Casbin 차용으로 사용자 등록·로그인·RBAC(pluggable provider seam)로 확장한 작업을 볼 때 [auth,multi-user,fastapi-users,casbin,rbac]
- 032 Playground userId를 인증 주체에서 도출 — 수동 userId 입력을 제거하고 mem0 user_id 축을 로그인 인증 주체에서 고정 도출하는 작업을 볼 때 [playground,userid,auth-principal,mem0]
- 033 기능 로드맵 — 사용자 제기 12개 문제를 코드 전수조사 위에서 의존순(P0~P5)으로 배열한 로드맵·현황 한 줄 요약을 볼 때 [roadmap,planning,phases]
- 034 세션 페이징 — `GET /sessions` 전량 반환을 offset/limit+total 서버 페이징과 서버측 status 필터·배지 카운트로 바꾼 작업을 볼 때 [session,paging,offset-limit] [archived]
- 035 Provider 엔티티 — provider/base_url/api_key가 모델마다 인라인 중복되던 것을 1급 Provider 엔티티(1:N, 모델은 FK)로 승급한 토대를 볼 때 [provider,entity,model,fk] [archived]
- 036 RAG 인제스트 토대 — 죽은 vectorTables를 Collection/Document/Chunk로 재생하고 PDF 청킹·임베딩·pgvector 적재(쓰기 경로)를 볼 때 [rag,ingest,pgvector,chunk] [archived]
- 037 RAG Retrieval — 죽은 vectorTables 필드를 search_documents 도구로 부활시켜 pgvector HNSW cosine 검색을 에이전트에 배선한 읽기 경로를 볼 때 [rag,retrieval,search,tool] [archived]
- 038 배치 토대 + 세션 보존정리 — 시스템과 격리된 별도 배치 서비스(자체 스케줄러)와 첫 작업(오래된 세션 정리)을 볼 때 [batch,scheduler,session-cleanup,retention] [archived]
- 039 유저 메모리 통합·재적재 — 단조 증가하는 mem0 user_id 기억을 임계치 초과 시 LLM으로 통합·스냅샷 백업하며 교체하는 배치 작업을 볼 때 [memory,consolidation,batch,mem0] [archived]
- 040 메모리 백엔드 추상화 — memory.py의 mem0 호출을 Protocol/ABC 뒤로 들어내 그래프DB 등 백엔드 drop-in을 가능케 한 인터페이스 추출을 볼 때 [memory,abstraction,protocol,backend] [archived]
- 041 HIL 승인 게이팅 — 픽션이던 승인을 langgraph checkpoint/interrupt로 위험 도구를 진짜 멈추고 admin 승인/거부로 그래프를 재개/중단하는 게이트로 바꾼 작업을 볼 때 [hil,approval,langgraph,interrupt] [archived]
- 042 A2A 실호출 — 등록된 외부 에이전트를 JSON-RPC 2.0(message/stream) SSE로 실제 호출해 응답을 받는 A2A 런타임 전송 계층(026 2차)을 볼 때 [a2a,external,runtime,jsonrpc,ssrf] [archived]
- 043 컨텍스트 경량화 자산 인덱스 — Context 단계 읽기량이 compaction을 당기는 문제를 인덱스 층(L1~L3)+Context 규칙 교정+완료 스펙 아카이브로 줄인 워크플로 정비를 볼 때 [context,index,archive,workflow]
- 044 UI 피드백 정리 마스터 — 2026-06-28 UI 테스트 14건을 메뉴별 6배치(045~050)로 트리아지·의존순 배열한 지도(질문 답·결정·순서근거)를 볼 때 [feedback,cleanup,master,triage]
- 045 어드민 표시 정직화 — 마스터 044 배치1(#12/#2); 승인 배지 mock상수→실 pending수·목록 pending-only, A2A 무조건online→도달성 probe(SSRF가드+리다이렉트차단), 적대리뷰 7검증 [admin,honesty,badge,a2a,liveness,ssrf,approvals]
- 046 빌딩블록 재료 정리 — 마스터 044 배치2(#4/#5); 코드·인프라 권한5·MCP4 제거+데모 에이전트2 삭제(cascade), 유지 에이전트 dangling JSON참조 strip, seed+라이브DB 양층 dry-run, runtime정책 비범위(041 green) [blocks,cleanup,permission,mcp,seed,dry-run,cascade]
- 047 프로바이더·모델 통합+models.dev — 마스터 044 배치3(#6/#7/#8); 두 메뉴→마스터-디테일 한 뷰, GET /models 실모델 나열·토글, models.dev 번들스냅샷으로 등록시 메타 자동채움(best-effort), Provider+kind/description·ModelConfig+meta(마이그레이션), SSRF는 base_url 관리자입력 신뢰경계 [provider,model,catalog,models.dev,merge,migration,ssrf]
- 048 RAG 임베딩 게이팅+조건부 샘플 적재 — 마스터 044 배치4(#9); 임베딩 모델 존재를 RAG 전제조건으로 명시(메뉴 게이트 배너+생성 disabled), 대표 docs_kb를 mock-embed(1024 결정적)에 바인딩해 실 인제스트 파이프라인으로 샘플 멱등 적재, 빈 컬렉션 정의 2분기(모델있음 대기 vs 모델0 비활성) [rag,collection,embedding,gate,sample,ingest,mock-embed]
- 049 세션 정리 정책 — 마스터 044 배치5(#10); 0턴 세션 소스 미영속(_load_context 보류→첫 _persist에서 lazy-create, #11 정크 뿌리 차단) + BatchConfig.min_session_turns(ge=1) 턴 기준 배치 정리(turns<N AND last_activity<now-IDLE_GUARD 1h, 활성세션 보호), 나이절과 합집합 [session,retention,batch,turns,lazy-create,idle-guard,cleanup]
- 050 파괴적 데이터 정리(dry-run) — 마스터 044 배치6(#1 A2A·#11 세션·#13 유저); 정크 식별 보수규칙(A2A=external+루프백/RFC1918 endpoint 9개, 유저=test_user_email_pattern LIKE−keep-list, 세션=049 재사용)+dry-run→사용자검토→실행, 바닥3겹(패턴NULL→disabled·%거부·마지막super보존·부트스트랩keep), cascade 정합(accesstoken·casbin·session FK), 재생성뿌리=verify/shot 자가정리(self-fixture 전면화)+브라우저샷 전용super생성→삭제 [cleanup,destructive,dry-run,a2a,user,casbin,self-fixture,floor]
- 051 채팅 자가기록 차단·agent_id 메모리는 어드민 저작 전용 — 메모리 누출버그(채팅 LLM이 유저 개인사실을 agent_id 교차사용자 스코프에 자가기록); 두 쓰기채널 중 비신뢰(save_agent_knowledge 도구)만 제거·어드민 CRUD+회상 유지(페르소나 보호), runtime.build_agent_memory_tool 삭제+chat.py 주입 2곳 제거+누출행 DB삭제+패널문구정정, verify_029를 도구 *부재* 단언으로 뒤집음 [memory,agent-scope,leak,write-channel,self-write,admin-curation,spec051]
- 052 유저 메모리 큐레이션 신원 식별 — 큐레이션 드롭다운/패널이 user_id raw UUID로만 라벨링→어떤 유저인지 식별불가; 메모리라우터(general _auth)에 GET /memory/users 신설로 distinct user_id에 email·display_name JOIN 보강(/admin/users users:manage 재사용=비-슈퍼유저 403 권한결합 회피), response_model로 누출-안전 구조화, 드롭다운 이메일·이름 검색+패널헤더 이메일식별(UUID dim 병기), 미등록 graceful fallback [memory,curation,identity,scoped-endpoint,authz-decouple]
- 054 MCP 실연결 런타임(HTTP) — runtime이 _CANNED 하드코딩 문자열만 반환하던 가짜 MCP를 langchain-mcp-adapters MultiServerMCPClient 실연결로; self-host 실 mock MCP(FastMCP /_remote/mcp, spec024 패턴)로 모의6 대체, 라이브 도구 자동탐색(/discover), 트레이스·HIL게이트(041)·graceful 래퍼 보존, net_guard(042) SSRF·auth at-rest(010), 시드 양층 reconcile. stdio는 유예 [mcp,runtime,langchain-mcp-adapters,streamable-http,self-host,discovery,hil,ssrf]
- 053 유저 메모리 역할 기반 스코핑 — 052가 신원 라벨만 붙이고 접근은 무방비였던 프라이버시 구멍 차단; 본인 기본+어드민(memory:manage=머신토큰/is_superuser/casbin admin)은 타인. /memory/users를 capability 객체{can_curate_others,me,users}로 격상(비-어드민=users[me]), per-user GET/PATCH/DELETE에 _assert_principal_may_access(주체×대상) 주입해 기존 _assert_user_owns(대상×행)와 직교 합성, response_model 누출-안전, 프론트는 can_curate_others로 드롭다운/본인패널 분기. _provision_super member모드 추가 [memory,authz,role-scope,privacy,two-axis-guard,capability]
- 055 Playground 세션 이어가기(과거 세션 선택·재개) — 위험도구 승인하러 이탈→복귀하면 Playground가 초기화(convos/sessions React메모리 소실), 백엔드는 이미 영속·재개 지원이라 빠진 건 "불러오는 UI"; GET /sessions에 agent_id필터 추가+DebugChat헤더 에이전트피커 옆 세션피커(수동선택만)+getSessionMessages로 대화복원해 같은 session_id 이어보내기, 사용자피드백("해시만 불편")→preview(첫 사용자메시지 80자) 주라벨·해시 보조, codex적대로 동시성3결함(stale도착·미롤백·레이스) 보강, 검증3rung(통합22단언·E2E RESUME055_OK·타자) [playground,session,resume,agent-filter,preview-label,optimistic-async,adversarial]
- 056 세션 정리: 카운터를 진실로 유지하고 거짓말의 출처(seed)를 고친다 — 사용자 "저턴 세션 지우자"가 안 먹힘=시드 빈 데모세션이 turns=6/14/21로 부풀려져(메시지0행) 저턴 정리에서 고턴으로 오인 보존; 초안은 058 따라 턴절을 실제 user메시지 행세기로 바꿨으나 codex 적대리뷰가 실결함 적발=persistHistory=false(윈도우모드)는 메시지 의도적 미저장인데 turns는 +1→100턴 윈도우세션 행세기면 저턴 오인 삭제(의미보존 실패). 반전: jobs는 카운터 기반 그대로(주석으로 turns≥행수 명문화)·seed만 turns=0 진실화. 검증=verify_056_session_cleanup_counter(11단언, ★ turns=100·메시지0행 보존). learning 059 [session,cleanup,counter,seed,windowed-mode,adversarial,reversal]
- 057 원격/외부 에이전트 등록을 A2A로 단일화(provenance만 구분) — 등록 진입점 둘이 중복으로 느껴짐→사용자 "둘 다 A2A 아닌가?"가 분류축을 프로토콜에서 출처로 바꿈; 자체 SSE `_remote_stream` 폐기·모든 원격은 표준 A2A(`_a2a_stream`) 하나, source=출처만(code=`x-my-agents` 카드 확장 실은 1급 SDK배포 / external=제3자 불투명), `POST /agents/connect`가 fetch_card→extract_my_agents로 자동분류(프론트 날조 제거). codex 적대4건: F1 fetch_card follow_redirects 리다이렉트-SSRF(→False 통일), F2 멀티턴 회귀(폐기 경로는 히스토리 인라인·신규는 마지막만→message.contextId로 세션 맥락 복원), F3 길이 하드닝(_clip 컬럼상한), F4 버전 불변식(active row 실재시만 active_version). 라이브 행 마이그레이션(dry-run·승인·멱등·a2a:true 스모크). 검증=verify_057(C6~C8 43check)·learning 060/retrospect 046 [a2a,unification,provenance,protocol-consolidation,connect-endpoint,redirect-ssrf,contextId,migration,adversarial]
- 058 첫 실행(first-run) 견고성: 부팅 전제와 사람 진입의 갭 메우기 — 외부(테스트불가) 환경서 "처음 받아 실행"(DB미설정·테이블없음·유저0·임베딩/모델없음) 점검·보강; 정적분석이 *이미 견고*(스키마폴백·시드·friendly400·임베딩 graceful)와 *갭*(부팅전제=DB연결·사람진입=첫관리자·셋업문서)을 가름. G1 db.py 프리플라이트(SELECT1→마스킹DSN+docker힌트, 폴백 이중throw 소멸)+pgvector 하드요구 fail-closed(create_all all-or-nothing이라 "RAG만 비활성" 불가·"Vector만 빼고"는 head스탬프 함정→명확 RuntimeError) G2 `api.bootstrap_admin`(신규생성만·기존승격거부 050)+seed_admin 유저0만 시끄럽게 G3 루트README런북(초안·인간검토) G4 chat 연결실패시 'Mock LLM' 전환힌트(기본값 불변·제어흐름 불변). 검증=verify_058(라이브無 정적/단위 G1·G2·G4)·codex 적대(P1 create_all 원자성 적발→fail-closed 정직화). retrospect 047·learning 061 [first-run,db-preflight,bootstrap-admin,pgvector,graceful-degradation,fail-closed,adversarial]
- 059 MLX를 env에서 떼고 Mock LLM을 기본 채팅/임베딩 모델로 — "mlx 설정이 왜 env에 있어? 프로바이더에서 추가하게"가 env 한 블록 삭제가 아니라 *정상 fresh-install이 이미 깨져 있다는 발견*으로 번짐(두 부팅 경로 분기=seed drift). 경로A(정상 alembic)는 f4a5 INSERT+a1b2c3 정규화로 providers 행 존재→seed `_empty(Provider)=False` 스킵→기본 chat·임베딩 없음, 경로B(create_all 폴백)만 seed 실행. 처방=D1 마이그레이션 c9d0e1f2a3b4(경로A: mock-llm을 기본chat 승격·no-clobber·mock-embed 멱등)+D2 seed(경로B: Mock 하나, CHAT_MODEL_NAME=mock-llm)가 *같은 관측 상태*로 수렴, D3 .env/D6 README MLX 제거·Mock안내, D4 agent MLX_*→MODEL_* / local-mlx→mock-llm, D5 chat 힌트 재프레이밍. 검증=verify_059(31단언 정적)·verify_058 무회귀·codex 적대(a/b/c/d 전부 HANDLED, 잔여=손상DB 이름-only 멱등은 수용한계). 통합 rung 실측(2026-06-29): 두 경로가 동일 상태로 수렴+mock-chat/mock-embed 실응답 green(verify_059_integration). retrospect 048·learning 062 [default-establishment,boot-path-convergence,seed-drift,mock-llm,no-clobber,migration,adversarial]
- 060 A2A 에이전트를 플레이그라운드에서 테스트 가능하게(endpoint 정규화+로컬테스트 UX) — 사용자가 A2A 등록 에이전트 채팅 시 "URL은 http(s) 절대 URL이어야" 에러; 코드재현으로 두 실패모드 확정(모드1=validate_card가 서비스url 절대성 미검사→스킴없는 url이 등록은 통과·채팅 guard_url서 늦게 모호하게 거부, 모드2=루프백/사설은 SSRF 기본차단→A2A_ALLOWED_HOSTS 필요). 처방=관대 정규화(net_guard.normalize_http_url: 이미 http(s)→그대로/`/`상대+base→urljoin/스킴없는 host:port→http전치, 결과 scheme·hostname 단언 못하면 ValueError)를 fetch_card·register_code_agent에 적용해 절대화, 보안불변(정규화 url에 guard_url 그대로 도라 사설 모드2 여전히 차단)+로컬테스트 UX(B1 차단메시지 host echo·복붙예시[완료], B2 연결모달 allowlist 힌트, B3 플레이그라운드 에러 가시화). 검증=verify_060(C1~C5 정규화·무회귀)+E2E C6+브라우저 C7/C8, codex 적대(정규화가 SSRF 우회 안하는지·urljoin 엣지) [a2a,playground,url-normalization,ssrf-invariant,lenient-parse,local-test-ux]
- 061 로컬(ui) 에이전트를 실제 A2A로 노출(exposed.a2a 실동작화) — `exposed.a2a` 플래그가 무력(저장만·읽어 서빙하는 엔드포인트 없음, JSON-RPC A2A는 canned mock 하나뿐)→사용자 "임시로 로컬 에이전트 A2A로 열어 등록·테스트"할 대상이 없음(플랫폼 핵심 A2A협업 토대 공백). 처방=새 비인증 라우터 a2a_server.py(전역_auth 뒤면 self-fetch 401이라 mock_remote처럼 무인증 마운트, 인증은 JSON-RPC 라우트 단위): GET …/.well-known/agent-card.json(공개, 게이트 ui+exposed 아니면 404·누출없음, x-my-agents 없음→connect가 external 분류, url=self_base/agents/id/a2a), POST …/agents/id/a2a(current_principal 인증, message/send·stream을 mock_remote 프레임과 동형으로, chat.stream_local_reply가 _load_context+build_agent+astream 재사용·기존 chat() 무변경·v1 persist/HIL/memory 미적용 순수컴퓨트). 실왕복=플그→external사본→_a2a_stream→a2a_client(guard_url loopback→A2A_ALLOWED_HOSTS)→우리 /a2a(머신토큰)→실 LangGraph. self_base=env A2A_SELF_BASE_URL 우선·request.base_url 폴백. UI 노출토글 ON시 카드URL 복사노출. 짝=060. 검증=verify_061(D1~D6 카드게이트·send/stream·인증)+E2E D5 실왕복+스샷 D7, codex 적대 필수(새 인바운드 표면=인증우회·게이트회피·self_base 오염 SSRF·자원캡·자격증명에코) [a2a,exposure,inert-flag,inbound-surface,unauthed-router,self-dogfood,reuse-runtime,adversarial]
- 062 Admin API 에러 detail 가시화 + RAG 차원불일치 조치안내 — 실버그레포트 "RAG 컬렉션 생성 POST /collections가 →409만 보여 원인불명"; probe-deeper 코드재현으로 두 결함=(A 근본·전역) 백엔드는 이미 detail에 안전사유 담는데 프런트 중앙헬퍼 j()가 본문버리고 status만 throw(j() 쓰는 모든화면 공통) (B) 차원불일치 메시지에 조치(어떻게) 누락. D1 httpError.ts 중앙추출(detail 한 문자열필드만·제어문자strip·길이상한600·없으면 METHOD path→status 폴백·raw바이트64KB+시간5s 상한으로 huge-body/SSE-hang 방어)를 j()·uploadDocument·streamChat에 적용(login/getMe 유지). D2 _dim_mismatch에 조치 한줄("{target}차원 모델선택 또는 정책변경 요청"). D3 CollectionsView 무변경(e.message가 D1로 채워짐). 보안불변=detail 한필드만(원문전체/스택/토큰 비노출 원칙2)·백엔드 모든 detail 안전문자열 생산자불변식 codex점검. 검증4rung=verify_062_http_error.mjs(실Response 10단언)·verify_062_live.py(중복409 detail+안전문자열)·shot-collections-062-detail.mjs(토스트 스샷)·codex 적대(1차 Medium huge-body+Low SSE-hang 적발→raw+시간상한 수정·2차 Resolved). retrospect 050·learning 065 [error-surfacing,detail,central-boundary,cap-raw-source,curated-message,rag-dimension,adversarial]
- 063 A2A endpoint 호출 경계 정규화 + 빌더 하드닝 + stale 데이터 마이그레이션 — 사용자 스샷 "URL은 http(s) 절대 URL이어야" 채팅버그; probe-deeper 코드재현으로 확정=060이 정규화를 *등록시점만* 걸어 060이전/우회 경로의 stale endpoint(스킴없음)가 호출경계 guard_url서 거부(learning 065 패턴: 계약이 한홉만 강제되면 다른홉 stale 남음). D1(핵심·자가치유)=a2a_client.a2a_stream가 guard_url 앞 normalize_http_url 적용(모든 outbound A2A의 단일chokepoint), D2(빌더하드닝)=agents.py _norm_endpoint로 _build_external/_build_code_agent_from_card 저장값 청결화(실패시 raw보존→등록500방지), D3(stale)=migrate_063_normalize_endpoints.py 기본dry-run·--apply만·source∈(code,external)·스킴없는행만·compare-and-set 멱등. 보안불변=정규화는 절대화만·사설판정은 guard 그대로(우회 안함). 검증4rung=verify_063_unit(U1~U5+U1b raw-userinfo+U1c encoded-@ 회귀)·verify_063_live(실API 자가치유+마이그레이션 dry→apply→멱등왕복)·060무회귀·codex 적대[P1]SSRF둔갑(mailto colon-form userinfo가 공인host 우회)+[P2]lost-update 적발→수정→"P1 closed". 분리=spec 064(allowlist env→DB, A2A_ALLOWED_HOSTS→ALLOWED_HOSTS, 무재시작, Admin UI+API). retrospect 051·learning 066 [a2a,call-boundary,url-normalization,ssrf-invariant,userinfo-disguise,stale-data-migration,self-healing,adversarial]
- 064 SSRF allowlist를 env에서 DB로(무재시작 관리·Admin UI/API) — 스펙042 allowlist를 env(`A2A_ALLOWED_HOSTS`)→DB 테이블(`allowed_hosts`)로 옮겨 재기동없이 관리, `A2A_`떼고 `ALLOWED_HOSTS`(MCP/카드/probe/blocks 공용), env는 alembic이 1회 읽는 부트스트랩 시드로 강등(DB 진실원). D1 `AllowedHost`모델+마이그 d0e1f2a3b4c5+create_all폴백, D2 env→DB 부트스트랩 alembic단독1회(revision-stamped→재실행없음→049재시드 footgun차단·seed.py무관여·path B 빈 fail-closed), D3 net_guard 모듈 캐시스냅샷(monotonic TTL 기본10s — sync guard_url 유지하며 스냅샷만읽고 async 5호출처가 직전 await refresh_allowed_hosts·콜드/빈=fail-closed), D4 이름일반화+차단메시지 무재시작콘솔안내(063 host echo), D5 Admin CRUD(`require("allowed_hosts","manage")`게이트·정확host검증422·중복409·삭제204·캐시무효화), D6 AllowedHostsView+AdminShell 관리그룹메뉴. 검증4rung=단위(verify_064_unit: normalize거부/수용·캐시시맨틱·_parse_ttl클램프)·라이브통합(verify_064_live: 실DB L1~L5 무재시작반영·≤TTL staleness창)·브라우저(shot-allowed-hosts-064 4샷)·codex 적대(8건→P1 1·P2 3 수정[0.0.0.0/::미지정 거부·과길이253캡·TTL클램프·마이그 미정규화폴백 fail-closed], 4 by-design §5.1[DNS rebinding=allowlist본질·DB-outage stale fail-safe·멀티워커≤TTL·빈시드 fail-closed]). retrospect 052·learning 067 [allowlist,env-to-db,no-restart,ssrf,bootstrap-seed,cache-snapshot,admin-crud,adversarial]
- 065 어드민(슈퍼유저) 메뉴를 그룹 헤더로 구분 — 사용자 제안 "어드민 기능과 아닌 것은 메뉴에서 구분"; AdminShell.tsx 슈퍼유저 항목(유저·배치·허용 호스트)이 일반 작업 메뉴 뒤 평면 append라 경계 없음→`type:'group'` "관리자" 헤더로 묶어 시각 분리(in-file "도구" 그룹 동일패턴·collapsed 라벨숨김). 노출게이트(`user.is_superuser`)·키·views·서버배선 무변경=순수 표현변경. 검증=브라우저 3샷(shot-admin-menu-065: GROUP_TITLES["관리자","도구"]·collapsed 라벨0·그룹항목클릭 뷰전환)·tsc 무에러, 타자적대 스킵(비가역/보안표면 없음). retrospect 053 [admin,menu,group,superuser,antd,presentational]
- 066 유저 self-승인: permission 기반 RBAC 3-way 승인 권한 — HIL 승인(041) resolve가 admin전용+action구분0이던 것을, 이미 카드표시용으로만 쓰이던 `Approval.permission`을 Casbin RBAC 인가에 배선해 3분할: admin/머신=무엇이든·owner(요청주체 본인)=자기것 중 `enforce(user,permission,"self_approve")` 열린것만·그외(민감perm) admin필수. 기본 fail-closed(self_approve 정책0개→data.delete 영구 admin전용). D1 `Approval.user_id`(nullable,NULL=머신/레거시=admin전용)+alembic e1f2a3b4c5d6(백필없음), D2 `_create_approval(user_id)` 단일생성경로 주체기록, D3 `_may_resolve` 3-way+라우트 가시성404게이트(비-admin 비가시행=404 존재은폐·자기행 민감perm=403), D4 `can_self_approve`=enforce(sub,perm,self_approve)·기본 self_approve 0개, D5 `list_approvals` `_own_scope`(본인것만·NULL/타인 숨김), D6 프론트무변경(기존 httpError+message.error가 403 detail 노출). 보안경로(위험도구 실행권한 확대)→4rung=단위 verify_066_resolve_authz(24/24)·라이브 verify_066_live(15/15 실HTTP deny매트릭스·스코핑·fail-closed)·브라우저 shot-user-approval-066(403토스트 도달)·codex 적대 VERDICT PASS(Low#1 404-vs-403 열거오라클→가시성404게이트 수정·재검증). retrospect 054·learning 068 [self-approval,approval,hil,rbac,permission,casbin,self-approve,fail-closed,user-id,enumeration-oracle,adversarial]
- 067 세션 라우터 유저별 스코핑(대화 전사 교차열람 차단) — RBAC 재감사서 도출: `sessions.py`가 `current_principal`을 안 받아 로그인 member가 타인 대화 list/messages/end/배지/`/users`를 교차열람(라이브 실증). 카탈로그(011 단일워크스페이스=공용 config)와 달리 sessions는 개인데이터→memory(052)·approvals(066)의 `_own_scope`+가시성404 미러링. D1 `_is_admin`(머신·superuser·`sessions:read`)/`_own_scope`/`_visible_or_404` 헬퍼, D2 list 본인스코핑(NULL자동제외) D3 배지 counts 본인집계(T6 전역누설차단) D4 item 404게이트(detail·messages·end 타인/NULL/추측→404 존재은폐·403금지 068) D5 `/sessions/users` 본인만 D6 프론트무변경. user_id=서버도출(`chat.py` `str(principal.id)`)이라 위조불가(T7). 4rung=단위 verify_067_scope 17/17·라이브 verify_067_live 23/23(member list=own만·타인messages→404·badge all=1·end타인→404+status무변경)·브라우저 shot-session-scope-067(OWN노출·OTHER숨김·배지`전체(1)`)·codex 적대(067 4점 통과: 라우트순서·배지·404은폐·UUID-str정합). **잔여갭(범위밖 P1)**: chat resume(`POST /agents/{id}/chat`)가 세션을 소유자무검사 바인딩+`_persist` `if user_id: sess.user_id=user_id` 무조건덮어씀→member가 타인세션 resume시 소유권탈취 후 067게이트 우회로 전사읽기+session_id에코 열거오라클(24비트 token_hex(3))→후속스펙 합의필요. retrospect 055·learning 069 [session,rbac,per-user-scoping,enumeration-oracle,list-vs-item,visibility-404,personal-data,chat-resume-bypass,residual-gap,adversarial]
