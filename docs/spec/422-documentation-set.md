# 422 — 공식 문서 3종+α(유저 가이드·개발 가이드·시스템 아키텍처·DB 모델 갱신)

> 상태: **승인됨(2026-07-21)** · 발단: 개발자 "개발은 잠시 중단하고 문서작업. 유저 가이드·개발
> 가이드·시스템 아키텍처. 간결·포멀, 이모지 과다 금지, 마크다운, docs/ 하위." 승인 시 추가:
> ① **db-model.md 갱신**(오래됨 — 2026-07-14 기준) ② **문서별 초고→퇴고→최종검수 3패스 루프**,
> 축적 자산(copy-rubric·learning 016 실물 확인·문구 규칙) 복리 활용. 엔지니어 작가 페르소나.

## 3패스 루프(문서마다)
1. **초고** — 코드/DB/UI **실물 근거**로 작성(상상 금지, learning 016). 서브에이전트 위임 가능.
2. **퇴고** — `tests/copy-rubric.md` 5유형(전문용어 무설명·오탈자·불명확/이중부정·일관성·부정형 문구)
   + 문구 규칙('배선' 금지→'연결', 긍정형) 적용. 간결·포멀 정돈.
3. **최종검수** — 사실 대조(문서 주장 vs 코드/DB 실물)·링크/이미지 양방향 resolve·적대 검토
   (dev-guide는 deep-reasoner "3년차가 이걸로 개발 가능한가").

## 산출물(전부 마크다운, 한국어, 간결·포멀 문체)

| 파일 | 내용 | 독자 |
|---|---|---|
| `docs/architecture.md` | 시스템 아키텍처 — 시스템 컨텍스트, 패키지 구조(api/agent/admin), 요청 흐름(채팅 턴·A2A·승인 재개), 핵심 메커니즘(버전 그래프 캐시·능력 브로커 RBAC·HIL·메모리/RAG 축), 배포 토폴로지 | 개발자·아키텍트 |
| `docs/user-guide.md` | 유저 가이드 — 탑다운: 플랫폼 전체 개요 → **메뉴별 설명**(개요/에이전트/빌딩 블록/노드/RAG 컬렉션/세션/메모리/관리자 5종/Playground/평가) + **실물 스크린샷** | 플랫폼 사용자·관리자 |
| `docs/dev-guide.md` | 개발 가이드 — ① 기본 정보: 아키텍처 요약(architecture.md 링크)·기술 스택·테이블 모델링(db-model.md 요약+링크) ② **SDK(코드) 에이전트**: CustomAgent Protocol·AgentBuildContext·AgentManifest(cacheable/seed_prompt 계약)·신뢰 레지스트리·코드 노드 ③ **주요 클래스 기반 mermaid 클래스 다이어그램** ④ **확장 포인트**(새 impl 등록, 노드 impl, 브로커 능력, MCP/A2A) | **3년차 개발자, LangGraph 가벼운 이해 전제** — 이 문서만으로 개발 가능해야 |
| `docs/db-model.md` **갱신** | 라이브 DB 덤프 재대조(문서 원칙 그대로 "어긋나면 실물이 이김") — 2026-07-14 이후 드리프트 반영, 신규 테이블/컬럼 편입 | 개발자 |

- 스크린샷: `docs/images/` (파일명 `menu-<key>.png`), Playwright+시스템 Chrome으로 실물 캡처
  (기존 `tests/browser/_fixture.mjs` 패턴 재사용). 데스크톱 해상도 기준.
- `docs/guide/`(인앱 가이드 — 별도 파이프라인·build-html.py)는 **건드리지 않음**(별개 용도).

## 실행(오케스트레이션 — 메인=조율·검증·조립, 초안=서브에이전트)

1. **[메인] 스크린샷 일괄 캡처** — 캡처 스크립트 1개 작성(메뉴 순회), `docs/images/` 산출.
2. **[서브 A: general-purpose] architecture.md 초안** — 코드 실물(packages/, chat 경로, broker,
   캐시)을 읽고 작성. 자기완결 브리핑(대상 파일·목차·문체 규칙) 전달.
3. **[서브 B: general-purpose] dev-guide.md 초안** — runtime.py·flows/·nodes·broker 실물 기반,
   클래스 다이어그램은 실제 클래스·시그니처에서 도출(상상 금지).
4. **[메인] user-guide.md 직접 작성** — 메뉴 인벤토리+스크린샷 조립(스샷과 본문의 정합은 메인이 소유).
5. **[메인] 검증** — ① 링크·이미지 resolve 전수([[move-breaks-references-both-directions]])
   ② 사실 검증: 문서의 클래스·경로·계약 주장 vs 코드 실물 대조(서브에이전트 산출물 자가선언 금지)
   ③ deep-reasoner 적대 검토: "3년차가 dev-guide만으로 SDK 에이전트를 만들 수 있는가 — 빠진 전제·
   끊긴 단계"를 짚게 함.
6. **[메인] 커밋**(per-spec).

## 완료 기준
- [x] 3문서+db-model 갱신 — 목차 완결, 간결·포멀(이모지 0).
- [x] user-guide: 전 메뉴 스샷 17장(검증 잔재 61건 청소 후 재촬영) 첨부·실물 대조로 6곳 교정.
- [x] dev-guide: 클래스 다이어그램 실명 일치(deep-reasoner가 코드로 전수 확인), 확장 포인트 절차 포함.
- [x] 링크·이미지 깨짐 0(스크립트 25건 검사).
- [x] deep-reasoner 검토 지적 반영.

## 실행 기록(3패스 루프)
- **초고**: 서브 3기 병렬(architecture 330줄·dev-guide 560줄·db-model 라이브 37테이블 재대조) +
  메인(user-guide — 스샷 10장 실물 확인 후 작성, learning 016).
- **퇴고**: 카피 루브릭 5유형 일괄(3문서 23건 — 용어 첫등장 설명·긍정형·3문서 용어 통일·링크 교정).
- **최종검수**: ① 사실 대조 — 아키텍처 불확실 5건 코드로 해소, db-model 주장 라이브 재측정 일치
  ② 링크·이미지 기계 검사 25건 깨짐 0 ③ deep-reasoner 적대("3년차가 이것만으로 가능한가") —
  **치명 1건 적중**: §4⑥ "admin에서 config.impl 지정"이 **따라 할 수 없는 지시**(커스텀 impl은
  UI 선택 불가·편집 봉인, AgentForm GENERIC_IMPLS 4종) → 실통로(REST 생성·활성화 curl 3단계 +
  verify_099 레시피 참조)로 교체, Playground는 실행 가능(편집만 봉인) 명시. 그 외 기술 서술은
  "줄 단위 일치·실질 오류 0" 판정(§3·§5·§6·§8 전수 대조).

## OUT
- 영문판, docs/guide(인앱) 개편, 문서 사이트 빌드 파이프라인.
