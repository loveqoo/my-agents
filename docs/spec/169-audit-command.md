# 169 — 감사 커맨드화 + 상시 루프 (검증 아크 캡스톤)

## 배경
축1(시나리오·166)·축2(오버플로 165·오버레이 168)·축3(문구·167) 감사를 만들었다. 스폿이 아니라
**한 명령으로 전수 재실행**되어야 "지속적 발견"이 상시화된다(사용자 원 목표). 수치 판정이 서 있으니
사람 없이 반복 가능(numeric→autonomy).

## 딜리버러블
1. **오케스트레이터** `tests/browser/audit-all.mjs`: 기계적 harness 3종(ui-audit·ui-audit-overlays·
   scenario-audit)을 순차 실행, 각 종료코드 수집 → **종합 스코어카드 + 종합 종료코드**(하나라도 FAIL=1).
   프리플라이트(vite 5173·api 8000 도달 확인). 카피 축(축3)은 에이전트 구동이라 기계화 불가 →
   말미에 "deep-reasoner + tests/copy-rubric.md로 별도 실행" 안내.
2. **스킬** `.claude/skills/ui-audit/SKILL.md`: `/ui-audit`로 오케스트레이터 실행 + 결과 트리아지
   절차(도구가 좁히고 육안 확정·과잉억제 금지·soft flag 코드추적) 문서화.

## 상시 루프
- 명령 하나라 언제든 재실행(수동/CI/자율 백로그 루프). 회귀 게이트로도 씀(카피·UI 변경 후 audit-all).
- 자율: 종합 종료코드가 서 있어 Ralph/cron에 얹으면 사람 없이 상시 감사(FAIL 시만 개입).

## 검증
audit-all.mjs 실행 → 3 harness 전부 구동·종합 스코어카드 생성·종료코드 정합(현재 상태=0 FAIL 기대,
batch·카피는 이미 수정됨). 프리플라이트가 서버 down을 정직히 보고.

## OUT
- 카피 축 기계화(LLM judge 자동). CI 배선. 자율 cron 실제 등록. 병렬 실행(현재 순차 — 로그인 3회).
