# 267 — 노드 카드 문구·컴포넌트 간결화 (사용자 정의 문구)

## 배경
사용자 정의(2026-07-09): *"맥락은 이전 노드의 결과를 어떻게 받을 것인가, 출력 형식은 현재 노드가
어떻게 응답을 출력할 것인가. 어려운 단어보다 명확한 문구로, 컴포넌트도 간결하게."* + *"첫 노드도
맥락 설정 필요 — 에이전트끼리 연계하면 앞 에이전트의 결과를 받으므로."*

## 변경 (NodeListEditor, 표시만 — 저장값 carry/clean·text/json 불변)
- "맥락" → **"이전 결과 받기"**, 선택지 "대화 이어가기/깨끗이 받기" → **"대화 전체와 함께"/"이전 결과만"**.
- "출력 형식" → **"응답 형식"**(선택지 자유 텍스트/JSON 유지).
- 두 설정을 모델/도구처럼 **한 줄 그리드**로 나란히 배치(세로 부피 절감), 설명은 각 한 줄로 압축.
  JSON 필수 키 입력은 그 아래 전체 폭.
- 첫 노드에도 "이전 결과 받기" 노출 유지(사용자 확인) — A2A 연계 시 앞 에이전트의 결과가 대화로
  들어오므로 첫 노드의 "이전 결과만"도 의미가 있음(주석으로 근거 기록).

## 검증
- 저작 왕복 하니스(shot-pipeline-259, 새 라벨로 갱신) 17체크 ALL GREEN + tsc 0.
- 상태 캡처(리뷰 하니스) 육안: 한 줄 그리드·한 줄 설명·JSON 키 전개 정상, 모바일 360 오버플로 0.
