# 스펙 247 — 플레이그라운드 집중 모델 (대화 우선·설정 가림)

## 배경 (사용자 설계 대화 2026-07-08)

"컴포넌트 레이아웃은 가장 나중 문제. 먼저 **무엇에 집중**하느냐, 다음 **무엇을 가려서** 집중하게
하느냐." 자료 학습(NN/g progressive disclosure·visual hierarchy — 위계 없는 화면=과업 시간 2배,
Apple HIG 화면당 초점 1개) 후 합의:

- **대화 모드가 가장 중요** — 들어온 이유는 "만든 에이전트를 동작해보고 싶다". 대화→확인→디버깅→
  오버라이드→재대화의 **인간 개선 루프 작업대**.
- **계측(칩·인스펙터)은 상시 유지** — 정밀 디버그 채널(제 숨김 제안 기각 — 확인이 루프의 절반).
- **헤더는 에이전트 설정만 노출** — 나머지 컨트롤은 가림(런처 개편안 기각 — 상단 설정 유지).

## 변경

### 헤더 (집중의 ① ②)
- **노출**: 에이전트 콤보 + 신호 배지(미반영 초안·미리보기 vN·오버라이드 적용 ✓) + 새 대화(대화 중일
  때만 — 루프 되감기 손잡이) + **"대화 설정" 단일 버튼**.
- **가림(대화 설정 팝오버)**: 버전 선택·대화 이어가기(세션)·A2A 경유 토글·시스템 프롬프트·오버라이드
  열기·인스펙터 열기 — 6개 컨트롤이 한 입구 뒤로.
- 계측 진입은 기존대로 턴 칩(상시)·인스펙터 유지.

### 잡음 2건 (③ 신호 대 잡음 — 진단 유효분)
- 추천 명령어 카드: 제목=설명 중복 표시 제거(전문 한 줄만).
- 입력창 힌트: "LangGraph" 내부어 제거.

## 검증
- e2e: 헤더에 에이전트 콤보+대화 설정만(버전·세션 셀렉트 미노출) → 팝오버에서 버전 선택·세션·오버라이드
  진입 동작 → 신호 배지(미리보기·오버라이드 ✓) 헤더 표시 → 칩·인스펙터 무회귀. 모바일 360px 오버플로 0.

## 완료 조건
- 대화 화면의 상시 요소 = 에이전트·대화·입력·계측 칩. 설정류는 한 입구 뒤. 루프(재대화·오버라이드)가
  빨라지고 기능 무손실.

## 검증 결과 (2026-07-08)
- e2e 7/7: 헤더 가림(세션 UUID 미노출·대화 설정 버튼)·추천 카드 중복 제거·내부어 제거·팝오버 6종 진입·
  초안 선택→미리보기 신호 배지·대화+칩 무회귀·모바일 360px 오버플로 0. tsc 0. 육안 확인.
