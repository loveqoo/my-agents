# 스펙 438 — 브라우저 verify 위생 캠페인(러너 자산화 + 전수 triage)

## 발단

`verify-eval-ux-193.mjs` 4건 실패 확증(2026-07-13, UI 개편으로 셀렉터 노후) + 브라우저 verify가
**78개**(총 .mjs 200개)인데 **상시 러너가 없어** 어떤 게 살아 있는지 아무도 모른다. 씨앗 그물
(run_suite)이 파이썬 verify를 살린 것(스펙 353)과 같은 작업의 브라우저판. 최근 매 스펙마다
브라우저 검증을 쓰고 있어(427~437 전부) 이 그물의 위생 가치가 오르는 중.

## 결정 — 파이썬 그물의 궤적(353→384→400)을 브라우저에 이식

1. **러너 자산화** `tests/run_browser_suite.py`: `tests/browser/verify-*.mjs`를 순차 실행
   (playwright/vite/api 전제 — 브라우저는 병렬 시 세션·화면 경합), 스크립트당 타임아웃(기본 180s),
   431 ProgressWriter로 진행 노출, `KNOWN_DRIFT_BROWSER`(사유 필수) 격리 목록, 요약(통과/실패/격리).
   실행 전제 검사(vite 5173·api 8000·playwright 존재) fail-fast.
2. **전수 1회 실행** → 실측 pass/fail 대장.
3. **triage**(스펙 384 결): 실패 각각 = ①셀렉터 드리프트(수리) ②기능 제거·개편(은퇴=삭제)
   ③환경 전제(실모델 등 — 격리+사유) ④실버그(별도 보고). eval-ux-193은 확증된 ①부터 수리.
4. **수리·격리·은퇴 반영** 후 러너 재실행 → 그물(비격리) green으로 종결.

## 완료 조건(측정) — 결과

1. ✅ 러너 `tests/run_browser_suite.py`(+`make test-browser`): 순차 실행·전제 fail-fast·격리 목록·
   진행 파일(431)·실패 로그 보존(/tmp/browser_suite_fails).
2. ✅ 전수 1회: **77개 41분, 통과 28 · 실패 49**. triage 군집:
   | 군집 | 건수 | 판정 |
   |---|---|---|
   | networkidle 대기(SSE 상시연결 335 이후 영영 불성립) | **68파일 77곳** | ①일괄 수리(→domcontentloaded) — 재실행서 5건 즉시 회복, 나머지는 복합 원인 |
   | eval UI 3겹 드리프트(212 kind탭·252 드로어탭·SSE) | 1(193) | ①수리 — 4건→0 green |
   | 사라진 계정·픽스처(7/28 초기화) | 7 | ③격리(_R_ACCT — provisionSuper 재활 후보) |
   | 스펙 370 버전 흐름(활성 null) | 3 | ③격리(_R_370 — activate 호출 추가로 재활) |
   | UI 개편 셀렉터 노후 | 24 | ③격리(_R_UI — 개별 현행화 후보) |
   | 환경 전제(movies-demo)·크래시 꼬리 | 5 | ③격리(_R_ENV/_R_CRASH) |
   | 437 샘플 문구 차이 | 1 | ③격리(_R_437 — 질의/샘플 정합화) |
   | **실버그 의심** | **3** | ④백로그 등재: 312 재인덱싱 이력 0건(**434/436 접점 — 우선 조사**)·344 감사 actor 표시·inspector-matrix 회상 trace |
3. ✅ 수리분 재실행 green(193·274·276·283·284·286 회복). 격리 43건 전부 사유 포함.
4. ✅ 은퇴 1건: verify-artifact-targeting-188.mjs — targeting-catalog 서빙은 **스펙 327에서 기능
   자체 소멸**(seed 주석 근거) — 죽은 기능의 verify.
5. ✅ 최종 확정 실행(전수 76개, 43분): **통과 31 + 격리 43 — 그물 잔여 2건(314·approval-resume)은
   솔로 재실행 green** → 배치 상태 의존 플레이크로 판정(전체 실행 중 앞선 스크립트의 픽스처·타이밍
   영향 — run_suite가 http층을 같은 이유로 상시 그물서 뺀 선례와 동류). 그물에 유지(솔로 기준 green),
   전수 실행 시 이 2건의 붉음은 플레이크 관찰로 해석(재시도 러너 옵션은 후속).

## 예상 소요·운영

- 전수 1회 ≈ **40~80분**(78개 × 평균 30~60초, 실모델 의존 스크립트 포함) — 배경 실행하며 중간
  보고([[long-turns-need-eta-and-progress]]). 실행 중 저장소 편집·dev 서버 사용 금지
  ([[net-runs-are-exclusive]] — 라이브 DB·vite 공유).
- 브라우저 그물은 상시 그물(make test)에 **불포함**(느림+vite/실모델 전제) — 수동/스펙 마감용
  `make test-browser`로.

## OUT(경계)

- demo-*·shot-*·measure-* 등 비-verify 스크립트(데모·일회 측정 — 그물 대상 아님).
- CI 통합·병렬화(단일 브라우저 순차 — 경합 회피 우선).
- audit-all(스펙 169 오케스트레이터)과의 통합(별개 축 유지).
