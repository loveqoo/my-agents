# 스펙 232 — 허용 호스트 새로고침 버튼을 목록 옆으로

## 배경 (사용자 지적 2026-07-08)

"새로고침 버튼도 아래로 내리자." 부제 제거(231) 후 새로고침만 상단 헤더 우측에 홀로 떠 있었다.

## 변경 (admin/src/admin/views/AllowedHostsView.tsx)

- Page `actions`(상단 헤더) 제거 → 새로고침을 **"등록된 허용 호스트" 목록 제목과 같은 줄**(flex
  space-between, 우측)로 이동. 이 버튼이 갱신하는 대상(목록) 곁에 두어 맥락 일치. size="small".

## 검증
- 브라우저: 새로고침 1개, y좌표가 목록 제목과 나란히(±30px)·경고 배너보다 아래. tsc 0.

## 비고
- 액션은 그 대상 곁에(목록 갱신 버튼=목록 옆). 상단 헤더는 제목만 남아 정보 밀도↓.
