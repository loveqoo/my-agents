# 400 — 노후 verify 재활 캠페인(격리 43→5, 랄프 루프)

> 상태: **완료** · 2026-07-19 (랄프 완주 — 스코어보드 43→5, 재활 38건 전부 virgin 그린 후 그물 복귀)
> 발단: 격리 대장 43건 누적(그물 밖 부채). 리팩터 캠페인(392~399)이 신규 드리프트 0을 증명한
> 지금이 재활 적기 — 코드는 안정, 남은 건 낡은 기대치뿐.

## 목표(수치 — 스코어보드)

- **KNOWN_DRIFT 43 → 5**(환경/인프라 5건만 정당 잔류: 054 stdio·057 하네스·068 asyncpg·
  114/143 시드 전제 — 각 사유 재확인 후 존치).
- 재활 38건: virgin 격리(throwaway)에서 그린 확인 후 **그물 복귀**.
- 최종 게이트: make test-all SUITE_OK(그물 +38) + suite 51/51 + metrics-fast.

## 방법(파일당 4단계 — 랄프 반복 단위)

1. **실측**: virgin 서버/DB에서 실행, 실패 지점 실물 확보.
2. **판정**(핵심 — 기대치 재작성은 "현 동작=옳다" 가정 금지): 현 동작을 지배 스펙·코드와
   대조. ①기대치 노후 → 재작성 ②현 동작이 스펙과 어긋난 실버그 의심 → **수선하지 않고
   보고 목록에 적재**(개발자 판정) ③테스트 자체 결함(경로 하드코딩·default 없는 next 등) → 수선.
3. **재작성**: 현 계약 기준으로 단언 갱신(assert-keys-not-exact·명시 fixture·자가정리 보존).
4. **복귀**: virgin 그린 → KNOWN_DRIFT에서 제거(그물 편입).

## 배치(같은 실패 유형 묶음 — 8클래스)

| 배치 | 대상 | 유형 |
|---|---|---|
| A | 139(기통과 — 즉시 복귀) | 격리 해제 후보 |
| B | 137crud·178·193 | eval 라우트 Query 시그니처(직접 호출→값 전달) |
| C | 137runner·140·141·240·242·244 | eval 스키마 드리프트 |
| D | 034·055·098 | 세션(counts·필터·검색 기대치) |
| E | 036·037·048·072·103 | RAG 플로우(생성·검색·브로커) |
| F | 029·115·043 | 경로/자산 하드코딩 |
| G | 040·059·083·122·127·130·131·148·161·191·329 | 개별 표면 드리프트 |
| H | 054auth·063·084·093·235·318 | 개별(다층·전제 포함) |

## 실행 방식(제안 — 랄프 전권)

- 배치 단위 랄프: 실측·판정은 메인, 기계적 재작성은 판단 난도에 따라 메인/서브 혼합.
- 각 배치 완료마다 스코어보드 보고(남은 격리 수). 실버그 의심은 즉시 중간 보고.
- per-spec 커밋 관례: 배치 몇 개 묶음 단위 커밋(대장 갱신 포함), 최종 배터리 후 스펙 마감.
- 예상 소요: 수 시간(38건 — 배치당 20~60분). 중간 보고 유지(장기 턴 규약).

## 결과(실측 — 2026-07-19)

- **스코어보드 43→5 달성**: 재활 38건 전부 virgin 그린 후 그물 복귀. 잔류 5건 = 환경/인프라
  (114·143 시드 전제, 068 asyncpg, 054 stdio 실서버, 057 하네스 이벤트루프) — 사유 재확인 후 존치.
- 최종 게이트: **make test-all SUITE_OK**(통과 185 — 그물 +38, 신규 회귀 0) + **metrics-fast 전판**.
  suite는 49/51 — 실패 2건(pipeline-override-temperature·pipeline-memory-recall)은 dev 기본 채팅
  모델(qwen3.6-35b@localhost:8045)이 API key를 요구하게 바뀐 **외부 환경 이슈**(스펙 무관,
  provider에 키 설정 또는 기본 모델 mock 복귀는 사용자 결정).
- 실버그 의심 1건 보고(수선 안 함 — OUT): 스펙 290 오버라이드 거절 게이트의 None==None 오폭
  (chat_context_models._resolve_model — 모델 미지정 에이전트+model 키 없는 오버라이드에서 400).
- 부수 수확: verify_043이 실자산 드리프트 적발 — learning 146~151·retro 130·131·200·201은
  인덱스 줄만 있고 full 파일이 생성된 적 없음 → 후크에서 복원(복원 노트 명시), spec INDEX 209
  3중복 1줄 병합. 240 플레이크(활성화 spawn 자동회귀와 409 경합)는 드레인 대기가 아니라
  **spawn 산출물 자체 단언**으로 봉합(스펙 399 버전 핀 회귀 핀 겸용).

## OUT

- 환경/인프라 5건의 근본 해결(068 asyncpg 조사·stdio 런타임 등) — 별도 스펙.
- 앱 코드 수정(실버그 발견 시 보고만 — 수선은 별도 승인).
