# 136 — INDEX 재압축 (Scaffolding 정비, 주기 작업)

## 배경 / 왜
측정: 세 INDEX(spec/learning/retrospect) 줄 평균 693~799자, 최대 3,592자 — "한 줄 후크" 원칙 이탈
(서사를 줄에 욱여넣어 인덱스가 통독 대상으로 회귀, CLAUDE.md가 경고한 상태). 원인=최근 스펙들(121~135)
작성 시 회고 내용을 인덱스 줄에 중복 수록.

## 규칙 (CLAUDE.md)
- 인덱스 줄 = `NNN 제목 — 한 줄 후크 [키워드] (→ 참조)`. 후크는 "열지 말지 고르게" 하는 한 줄 —
  서사·표본·처방 상세는 full 파일이 보유.
- 대량 변환은 서브에이전트(fast-worker) 위임, **보존은 측정**: 항목 수·NNN 집합·[키워드] 배열
  문자 그대로·→ 참조 꼬리 4가지를 전후 비교(자가선언 금지).

## 목표
- 300자 초과 줄의 후크를 **220자 이하**로 재작성(번호·제목·키워드·참조 꼬리 불변).
- 전후 측정 보고: 항목 수 동일, 키워드 배열 diff 0, 평균/최대 자수.

## 검증
- 조율자(메인)가 측정 스크립트를 **독립 재실행**해 보존 4종 확인(위임자 자가보고 불신).
