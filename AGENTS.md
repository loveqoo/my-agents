# 작업 방식 (Operating Manual)

이 문서는 이 저장소에서 AI가 작업할 때 따르는 운영 매뉴얼이다. 핵심은 6단계 루프를 반복하면서,
작업 중 축적되는 `.dev/learning/`, `.dev/retrospect/`, `.dev/troubleshooting/`의 교훈을 다음 작업에
상기·적용·검증해 복리로 만드는 것이다.

회고는 기록될 때가 아니라 다음 작업에서 적용될 때 복리가 된다. 따라서 좋은 작업 방식은 "많이 읽기"가
아니라 **필요한 자산을 싸게 고르고, 눈에 보이는 강제물을 남기고, 닫힌 집합을 측정으로 확인하고,
검증 결과를 다시 자산으로 접는 방식**이어야 한다.

---

## 0. 적용 원칙

- 모든 작업은 아래 **6단계 루프**를 따른다. 작업 성격상 특정 단계가 불필요하면 사람과 합의하고 사유를 명시한 뒤 스킵한다.
- 루프는 **작업이 정해질 때** 시작한다. 할 일을 정하는 행위는 Planning이 아니라 Scaffolding이다.
- 모든 응답 첫 줄에는 `[단계 N — 이름]`을 쓴다. 단계 헤더가 없으면 루프 위치 공유가 깨진 것이다.
- 비자명한 작업의 Context 단계에는 반드시 `참고 자산:` 줄을 쓴다. 없으면 Context를 건너뛴 것으로 본다.
- 새 규칙을 만들 때는 반드시 "어떻게 보이게 강제할 것인가"를 같이 설계한다. 규칙만 추가하면 반복 실수는 줄지 않는다.
- 작업자가 바뀌어도 같은 결정을 재현할 수 있게, 판단 근거는 코드·스펙·인덱스·검증 명령 중 하나에 남긴다.

---

## 1. Scaffolding — 루프가 돌 수 있는 구조를 먼저 만든다

Scaffolding의 목적은 일을 시작하기 전에 구조를 과하게 정리하는 것이 아니라, **작업을 잃어버리지
않을 정도로만 판을 고르는 것**이다.

### 해야 할 일

- 작업이 정해졌는지 먼저 확인한다. "다음 뭐 하지?"는 Planning이 아니라 Scaffolding이다.
- 직전 작업이 Verification·Compounding·작업 단위 커밋까지 끝났는지 확인한다. 미진하면 다음 일을
  고르기 전에 루프를 닫는다.
- 산출물이 들어갈 위치를 정한다. 계획 문서, 학습, 회고, 검증 자산은 각자의 역할에 맞는 영역에 둔다.
- 컨텍스트가 무거운 신호가 있으면 이 단계에서 최적화한다. 특히 `INDEX.md`가 길어져 다시 통독 대상이
  되면 작업 전후가 아니라 루프 운영 구조의 문제로 다룬다.
- 역할별 폴더링을 유지한다. 구조 변경은 작업을 가능하게 하거나 반복 실수를 줄일 때만 한다.

### 보완 규칙

- 인덱스는 한 줄 후크다. 서사·표본·처방·판별은 full 파일에 둔다.
- 대량 재압축·rename·보일러플레이트는 서브에이전트나 기계적 도구에 맡기고, 메인은 전후 카운트와 diff를 검증한다.
- 구조를 바꿀 때는 들어오는 참조와 나가는 참조를 모두 본다. 이동은 양방향으로 링크를 깬다.
- `docs/`는 인간 영역이다. 명시 요청 없이 수정하지 않는다.
- `tests/`에는 검증에 필요한 파일을 둔다.
- `.dev/`에는 작업의 흔적을 둔다: 학습, 회고, 문제 해결 과정, 작업 로그, 실험 결과, 스크래치 패드.

### 가시적 강제물

- 현재 단계 헤더.
- 작업 파일 위치 결정.
- 구조를 손댔다면 전후 측정값: 파일 수, 인덱스 줄 수, 참조 수, grep 결과.

---

## 2. Context — 자산을 읽는 단계가 아니라, 필요한 자산을 적용하는 단계

Context의 목적은 과거 기록을 많이 읽는 것이 아니다. 같은 실수를 반복하지 않도록
**상위 규칙, 관련 스펙, 학습, 회고를 싸게 고르고 적용점을 명시하는 것**이다.

### 순서

1. 상위에서 하위로 컨텍스트를 본다: 루트 컨텍스트, 폴더별 컨텍스트, 관련 스펙.
2. 학습, 회고, 스펙의 `INDEX.md`를 먼저 읽는다.
3. 작업 키워드로 관련 항목을 고른다.
4. 한 줄 후크로 충분하면 full 파일을 열지 않는다.
5. 후크가 불충분하거나 판단이 위험하면 그 항목의 full 파일만 연다.
6. 인덱스에 없는 축이 의심되면 `rg`로 보강하되, full 통독을 기본값으로 되돌리지 않는다.

### `참고 자산:` 줄 형식

```
참고 자산: 013 단계 헤더 → 모든 응답 첫 줄 강제, 023 가시적 강제물 → 새 규칙은 출력물로 검출 가능하게 설계
```

관련 자산이 정말 없으면 이렇게 쓴다.

```
참고 자산: 관련 없음(순수 현재 명령 출력 요청이라 저장소 자산 적용점 없음)
```

### 보완 규칙

- 회고는 다음 Context에서 적용될 때만 복리다. 읽고도 적용점이 없으면 복리가 아니다.
- 오래된 기록, 부정확한 인덱스 후크, 깨진 참조를 발견하면 그 자리에서 고친다. Context는 새로 쓰기만
  하는 단계가 아니라 기존 실수와 낡은 기록을 교정하는 단계다.
- "모든 입구를 봤다"는 기억으로 말하지 않는다. 공유 인터페이스, 권한 경계, 설정 통로, 렌더 표면은
  grep·AST·테스트 목록 같은 측정으로 닫힌 집합을 확정한다.
- 하위 컨텍스트가 상위 컨텍스트를 덮어쓴다. 경로가 좁아질수록 더 구체적인 규칙을 우선한다.

### 가시적 강제물

- `참고 자산:` 줄.
- 열어본 full 파일이 있다면 왜 후크만으로 부족했는지 한 문장.
- 닫힌 집합을 주장한다면 그 집합을 만든 명령이나 근거.

---

## 3. Planning — 계획은 승인받기 위한 문서이자, 나중 검증할 계약이다

Planning은 "할 일 목록"이 아니라 나중에 Verification에서 되짚을 계약이다. 계획이 흐리면 완료 조건이
흐리고, 완료 조건이 흐리면 자가만족으로 끝난다.

### 해야 할 일

- 모든 비자명한 작업은 정해진 문서 규칙에 맞춰 계획 산출물로 남긴다.
- 스펙에는 배경, 목표, 비범위, 완료 조건, 검증 방법을 쓴다.
- 모호한 요구는 구현 전에 질문해 의도를 좁힌다.
- 단순하고 산출물과 방향이 이미 명확한 경우에도, 필요한 최소 수준의 실행 근거와 완료 조건은 남긴다.

### 보완 규칙

- 완료 조건은 측정 가능해야 한다. "정리한다"가 아니라 "중복 정의 0, 기존 호출부 통과, 인덱스 한 줄 추가"처럼 쓴다.
- 위험한 작업은 happy-path만 계획하지 않는다. 여집합 공격 질문을 계획에 넣는다: 다른 입구, 다른 방향,
  다른 권한, 다른 데이터 형태, 다른 클라이언트.
- 체크리스트를 만들었다고 안전해진 것이 아니다. 체크리스트 자체도 적대 검증 대상이다.
- 비범위는 회피가 아니라 경계다. 나중에 다시 볼 수 있게 왜 제외했는지 남긴다.
- 계획은 사람이 검토하고 승인한 뒤 실행한다. 단, 사용자가 명확히 실행을 요청했고 범위가 좁으면 필요한 최소 계획을 남기고 진행한다.

### 가시적 강제물

- 정해진 형식의 스펙 또는 그에 준하는 계획 산출물.
- 명시적 완료 조건.
- 검증 사다리 또는 그 생략 사유.

---

## 4. Execution — 구현은 가장 작은 경로로, 하지만 모든 입구를 잊지 않는다

Execution은 계획을 코드나 문서로 옮기는 단계다. 여기서 반복된 실패는 대부분 "한 경로만 고침",
"사본을 고침", "검증이 보는 길과 실제 길이 다름"에서 나왔다.

### 해야 할 일

- 기존 패턴과 가까운 위치에서 고친다. 새 추상화는 실제 중복이나 의미 있는 복잡도를 줄일 때만 만든다.
- shared contract를 바꾸면 모든 생산자와 소비자를 센다.
- 권한·소유권·설정·런타임 경계는 list/item/create/update/delete/resume/lazy-create/external protocol
  같은 입구를 따로 센다.
- UI는 코드상 렌더와 실제 브라우저 렌더를 구분한다. 브라우저가 진실인 결함은 브라우저로 본다.
- 파괴적 정리는 dry-run을 기본값으로 둔다.
- 독립 작업은 병렬 실행을 고려한다. 단, 병렬 결과는 메인이 검증하고 종합한다.

### 보완 규칙

- 가드는 진실원을 검사한다. 사본, 캐시, 표시값, denormalized counter만 검사하면 거짓 초록이 나온다.
- 새 경로는 기존 가드를 자동 상속하지 않는다. provider, broker, endpoint, resume, background job을
  추가하면 기존 본경로의 권한·비용·컨텍스트 축을 다시 배선한다.
- 라이브러리 기본값은 계약이 아니다. 중요한 상한, top_k, timeout, follow_redirects, pagination은 명시한다.
- "삭제"는 행 제거가 아니라 발생원 차단까지 봐야 마감이다.
- 신뢰 못 할 생산자 위의 형식 강제는 보장이 아니라 시도다. 실패하면 꾸며낸 성공을 만들지 말고 정직하게 실패를 표면화한다.

### 가시적 강제물

- 닫힌 집합을 만든 grep/스캔 결과.
- 변경 전후 diff가 계획의 완료 조건에 대응한다는 설명.
- 파괴적 작업이면 dry-run 결과와 적용 조건.

---

## 5. Verification — 초록을 믿지 말고, 초록이 무엇을 보지 못하는지 본다

Verification은 "내가 한 일이 동작한다"를 확인하는 단계가 아니다. **완료 조건이 실제로 닫혔는지,
검증 도구가 실제 클라이언트와 같은 제약을 보는지, 내가 상상하지 못한 범주가 남았는지**를 확인하는 단계다.

### 검증 사다리

가능하면 서로 사각이 다른 런을 2개 이상 둔다.

- 단위 검증: 순수 함수, 경계값, 실패 케이스, 불변식.
- 통합 검증: 실제 DB, 실제 라우트, 실제 런타임 경로, seed/restart/glue.
- UI 검증: Playwright나 브라우저로 실제 렌더와 상호작용 확인.
- 정적 검증: 타입체크, 린트, grep 잔존 0, dead import 0.
- 적대 검증: 보장 목록의 여집합을 타자 모델이나 서브에이전트에 공격시킨다.

### 보완 규칙

- 자가검증은 기본적으로 좁다. 특히 보안·권한·파괴·공유 계약·검증 도구 자체는 타자 검증을 우선한다.
- 타자 검증도 그대로 믿지 않는다. 발견은 코드와 데이터로 재확인하고, 종속 관계와 오탐을 구분한다.
- curl은 브라우저가 아니고, DB 조회는 라우트 직렬화가 아니며, 방금 만든 데이터는 레거시 데이터가 아니다.
- 검증 실패를 코드 버그로 단정하기 전에 하니스와 환경도 의심한다. 반대로 "stale verifier"라며 방치하면 실제 회귀가 숨는다.
- 결정적 신호 도구의 대죄는 조용한 초록이다. 빈 assertion, 삼킨 예외, 조회 실패를 0건으로 둔갑시키지 않는다.
- 사이드이펙트가 예상되면 dry-run을 먼저 둔다.

### 가시적 강제물

- 실행한 검증 명령과 결과.
- 못 돌린 검증과 이유.
- 남은 위험과 비범위.
- 타자 검증을 돌렸다면 발견·수용·기각·수정 내역.

---

## 6. Compounding — 회고, 인덱스, 커밋까지가 작업 단위다

Compounding은 "마지막에 느낀 점 쓰기"가 아니다. 다음 작업자가 같은 실수를 더 싸게 피하도록
**경험을 검색 가능하고, 고를 수 있고, 적용 가능한 자산으로 바꾸는 단계**다.

### 해야 할 일

- 작업 단위 회고를 `.dev/retrospect/NNN-{name}.md`에 남긴다.
- 새로 배운 재사용 가능한 교훈이 있으면 `.dev/learning/NNN-{name}.md`로 분리한다.
- 문제 해결 과정이나 운영 명령은 `.dev/troubleshooting/`에 둔다.
- 새 spec/learning/retrospect를 만들면 같은 턴에 해당 `INDEX.md`에 한 줄을 추가한다.
- 회고를 마치면 작업 단위로 stage하고 커밋까지 한다. push와 merge는 명시 요청이 있을 때만 한다.

### 보완 규칙

- 인덱스 줄은 "언제 열까" 한 문장과 키워드만 담는다. 회고 본문을 인덱스에 욱여넣지 않는다.
- 반복 실수는 "주의하자"로 끝내지 않는다. Scaffolding에서 훅, 체크리스트, 헤더, 검증 스크립트, 폴더
  컨텍스트 같은 강제 장치로 바꾼다.
- 자산의 위치는 강제력이다. 모든 작업에 필요한 규칙은 루트에, 특정 영역 작업에 필요한 체크리스트는
  그 폴더 컨텍스트에, 고르기 위한 항목은 인덱스에 둔다.
- 회고는 성공담보다 다음 실패를 줄이는 분류가 중요하다: 어떤 신호에서 열어야 하는가, 어떤 맹점을
  막는가, 어떤 검증이 그때 유효했는가.
- 같은 작업을 반복하면 스킬이나 커맨드로 도출한다.

### 가시적 강제물

- `.dev/retrospect/NNN-{name}.md` 또는 생략 사유.
- 필요한 경우 `.dev/learning/NNN-{name}.md`.
- 해당 `INDEX.md` 한 줄.
- 작업 단위 커밋.

---

## 에이전트 협업

메인 세션은 오케스트레이터다. 계획·분해·종합·사용자 소통·커밋은 메인이 맡고, 실행은 성격에 따라
서브에이전트나 외부 검증자에게 위임할 수 있다.

- 깊은 추론, 아키텍처 판단, 적대 검토는 읽기 전용 고성능 모델이나 deep-reasoner에 맡긴다.
- 기계적 변경, 반복 치환, 인덱스 재압축, 보일러플레이트는 fast-worker나 스크립트에 맡긴다.
- 위임 결과는 판정이 아니라 가설이다. 메인이 diff, 카운트, 테스트, grep으로 검증한 뒤 받아들인다.
- 위임 과제는 자기완결적으로 작성한다. 필요한 컨텍스트, 완료 기준, 출력 형식, 금지 사항을 포함한다.

---

## 반복해서 적용할 공통 체크

### 닫힌 집합 체크

다음 문장을 쓰려면 측정 근거가 필요하다.

- "모든 입구"
- "모든 소비자"
- "잔존 없음"
- "참조 0"
- "동작 보존"
- "무회귀"

근거는 보통 `rg`, 타입체크, AST 스캔, 오라클 테스트, 브라우저 네트워크 기록, DB 델타 중 하나다.

### 가시적 강제물 체크

새 절차 규칙을 추가할 때마다 묻는다.

- 응답만 보고 스킵을 알아챌 수 있는가?
- 파일이나 인덱스에 남아 다음 세션이 찾을 수 있는가?
- 누락되면 검증이나 카운트로 드러나는가?

대답이 모두 아니면 그 규칙은 곧 잊힌다.

### 검증 충실도 체크

검증 도구가 실제 실패 조건보다 관대한지 본다.

- 브라우저 쿠키·CORS·SameSite 문제를 curl로만 보지 않았는가?
- 라우트 응답 직렬화를 DB 조회로만 보지 않았는가?
- 실제 화면은 레거시·시드·다중 행을 보는데 방금 만든 marker만 봤는가?
- 비동기·resume·background job은 재진입 경로까지 탔는가?
- 타자 검증이 공격한 축이 내 완료 조건과 같은 축에 갇혀 있지 않은가?

---

## 문서 사용법

1. 새 작업을 시작할 때는 이 문서를 기준으로 현재 단계를 잡는다.
2. 절차가 흔들리거나 반복 실수가 의심되면 해당 단계 보완 규칙을 적용한다.
3. 작업 중 새 규칙을 만들었다면 이 문서에 바로 넣지 않는다. 먼저 `.dev/learning/`이나
   `.dev/retrospect/`로 증거를 남기고, 반복되어 범용성이 생겼을 때 승격한다.
4. 이 문서 자체가 길어져 통독 비용이 커지면 Scaffolding에서 재구성한다. 이 문서도 복리 자산이므로
   유지보수 대상이다.
