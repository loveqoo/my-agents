# 201 — 인스펙터 side-by-side 임계값 수정

> **복원 노트**: 이 파일은 인덱스 줄만 있고 full 파일이 생성되지 않았던 항목을 INDEX 후크에서 복원한 것이다(스펙 400 verify_043 실측 — 같은 턴 파일 생성 누락). 원 세션의 상세 서사는 유실됐고, 아래는 후크가 보존한 전부다.

인스펙터 side-by-side 임계값 수정 — overlayInspector 임계 lg(992)가 992~1199서 컬럼 짜부라짐(겨우 맞는 폭≠편한 폭), !screens.xl로 상향 → 브라우저e2e

키워드: responsive-threshold-comfortable-not-barely,comment-vs-constant-drift,reproduce-at-widths
