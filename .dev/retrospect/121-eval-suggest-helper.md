# 121 — 회고: 평가 7탄 — 에이전트 문제집 AI 출제 (스펙 143, 도우미 1탄)

## 무엇을 / 왜
사용자 방향("각 메뉴마다 도우미 에이전트, 우선 평가부터·버튼식"): 에이전트 문제집에 AI가
문제 ~10개를 채우고 사람이 수정. 출제 재료=대상 에이전트 구성(페르소나+RAG 능력), 도우미
가용성=기본 chat이 실모델일 때만(사용자 원칙 — mock이면 정직 비활성+사유).

## 잘된 것
- **능력별 출제 전략**: RAG 능력→컬렉션 골든 질문(142 재사용)+trace_has(도구를 실제로 쓰는가,
  관측 union 축), 페르소나→역할 질문+llm_judge(139 재사용). 기존 스펙 부품의 조립으로 완성.
- 실측이 두 결함을 즉시 노출: ①**조율형은 RAG가 capabilities에 있다**(rag_collections만 보면
  빈 목록 — 스펙 124에서 배운 "조율형은 배선 축이 다르다"의 재림, 이번엔 하루 만에 알아챔),
  ②1건×N회 생성은 중복·이탈로 수율 저조 → **한 호출 N줄**로 교체(모델이 자가 중복 회피).
- codex: description 접미를 상태/게이트로 쓰는 취약성(PATCH로 우회·오작동) → **프로세스 내
  활성 셋 락**(create_task와 수명 일치·재시작 자동 소멸·description은 정보 표시로 강등)+완료
  표기는 현재 값 기준(사용자 편집 보존). mock 판별 오탐 축소(로컬 호스트 결합).
- 자기일관 게이트 재사용: 출제 문제집 실행 → 5/5(사람 문제 포함).

## 잘못한 것 / 배운 것
- **검증 안 된 가설로 e2e에 지침을 보냄**: "antd 비활성 버튼은 툴팁 불가"라 단정하고 완화
  지시 — 실측 결과 틀림(툴팁 잘 뜸, 진짜 원인=antd 6 클래스명 드리프트 ant-tooltip-inner→
  container). fast-worker가 완화를 거부하고 엄격 판정을 유지한 게 옳았다. **막힘 보고를 받기
  전에 원인을 단정해 지침부터 보내지 말 것** — "몇 번째 체크에서 막혔나"를 먼저 물어라.
- e2e 폴링 지연의 원인 후보를 코드가 아니라 **DB 흔적+LLM 부하**로 먼저 삼각측량한 것은 유효
  (120 교훈의 적용) — 다만 결론을 성급히 내린 게 문제.

## 다음 작업 Context에서 상기할 것
- 조율형/직접형 이중 배선 축(capabilities vs 직접 필드)은 **모든 신규 기능에서 양쪽 다** 확인.
- 백그라운드 작업 게이트는 description 같은 사용자 가변 표면이 아니라 메모리 락+startup sweep.
- e2e 셀렉터는 [role=...] 같은 시맨틱 셀렉터 우선(antd 버전 드리프트 내성).
- 참고: 스펙 143, 회고 120(e2e 중 백엔드 동결)·119·124, learning 141.
