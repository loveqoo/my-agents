# 115 — 회고: 평가 하네스 제품화 1탄 (스펙 137)

## 무엇을 / 왜
119 하네스(개발자 코드 전용)를 화면으로 — 문제집 CRUD·필수/금지 도구 채점·시험 실행·성적표.
수치 검증→자율 반복(Ralph)의 제품 표면. 3단계 커밋(①스키마+CRUD ②러너+실행 API ③UI).

## 잘된 것
- **설계 조사(deep-reasoner)가 두 지뢰를 사전 제거**: ①chat() 재사용 불가(플래그로도 세션 lazy-create·
  memory.add 못 끔) → stream_local_reply 골격+관측 이식+브로커 주입이 정답, ②직접형 도구 호출은 그래프
  노드가 아님 → **관측 union**(canonical 토큰) 없으면 조용한 초록. 둘 다 구현에 그대로 반영.
- **오염 제로를 측정으로 보장**: 러너 실행 전후 세션 수·mem0 행 수 불변을 카운트로 검증(자가선언 금지).
- **3층 검증이 서로 다른 결함을 잡음**(사다리 실증): 단위/직접호출(계약·fail-closed) → codex 4건
  (좀비 런·중복 실행·크기 상한·성적표 이름) → **브라우저 e2e가 항상-500을 포착**(성적표 상세 —
  RunDetailOut의 results 필드명이 ORM lazy 관계와 겹쳐 from_attributes가 비동기 밖 lazy load,
  MissingGreenlet). 단위가 못 잡은 이유 = DB를 직접 조회했지 **라우트 함수를 안 불렀다** → C4d 핀 추가.
- fast-worker가 500의 근인(파일:라인·재현 트레이스백·수정 방향)까지 추적해 보고 — 위임 보고 품질이
  조율 비용을 크게 줄임.

## 아쉬운 것 / 리스크
- MissingGreenlet **세 번째 변종**(129 commit-후-직렬화, 이번 응답모델 자동추출×lazy 관계) — 부류는
  알았는데 새 변종을 코드 리뷰에서 못 걸렀다. 응답 모델 필드명이 ORM 관계명과 겹치면 지뢰.
- 검증 중 vite 재기동(사용자 브라우저 멈춤)으로 e2e 1회 유실 — HMR 다변경 후 탭 고착은 재기동+새로고침.
- LLM-judge(3탄)·이력 비교(2탄)·RAG 평가 러너(kind 축만 마련)는 OUT — 백로그에.

## 다음 작업 Context에서 상기할 것
- **모든 신규 엔드포인트는 verify에서 라우트 함수를 직접 호출**(DB만 보면 직렬화 크래시를 놓침).
- **응답 모델 필드명 ≠ ORM lazy 관계명** — 겹치면 from_attributes 자동 추출이 MissingGreenlet.
  안전 패턴: 관계 없는 베이스 모델로 추출 후 명시 구성.
- 실행형 백그라운드 잡 3종 세트: startup 좀비 sweep + 중복 실행 게이트 + 입력 크기 상한.
- 참고: learning 137, 스펙 137, 회고 110(129 MissingGreenlet), learning 119(조용한 초록).
