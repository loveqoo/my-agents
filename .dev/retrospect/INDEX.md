# Retrospect Index  (.dev/retrospect/)

> 각 줄: `NNN 제목 — 언제 열까(후크) [키워드]`. Context 단계에서 이 인덱스를 먼저 읽고,
> 후크로 불충분할 때만 해당 full 파일을 연다.

- 001 프로젝트 초기 셋팅 루프 회고 — 6단계 루프·폴더 구조 첫 수립; User 스코프 오염·회고 위치(docs vs .dev) 혼동을 다룰 때 [scaffolding,scope,claude-md,memory]
- 002 에이전트 서비스 초기 구축 루프 회고 — 스펙 001 시스템 설계 + 미니멀 하드코딩 에이전트; 사전 환경 점검·codex가 untracked 못 봄·재촉 문제 참고 [agent,langgraph,mlx,codex,env-check]
- 003 페르소나 등록 + chat 노출 루프 회고 — 스펙 002 REST 등록·SSE chat; SSE 개행 보간(P1)·Qwen thinking 폭증·세션 조기해제 함정 [persona,sse,fastapi,postgres,thinking]
- 004 Admin Web UI(채팅) 루프 회고 — 스펙 003 React/Antd 채팅 UI; StrictMode 이중 fetch·stream abort·SSE 파서 견고성 함정 참고 [admin,react,antd,sse,cors]
- 005 Admin 콘솔 + Playground 이식 루프 회고 — 스펙 006 design handoff를 진짜 antd 6 + @ant-design/x로 재현; Sider flex 함정·antd-x v2 API 차이 참고 [antd,ant-design-x,design-handoff,playground,sider]
- 006 실 에이전트 서비스 루프 회고 — 스펙 007 mock UI를 실 백엔드+런타임으로(자율 진행); mem0 로컬 구성·create_all이 ALTER 안함·버전 상태머신 가드 [agent-runtime,mem0,langgraph,schema,autonomous]
- 007 서비스 확장 회고(견고화·E2E·모델 레지스트리·원격·env) — 스펙 007/008/009; Alembic 도입·E2E가 숨은 memory 버그 잡음·env 단일소스 강제 참고 [alembic,e2e,model-registry,remote-exec,playwright]
- 008 어드민 콘솔 모바일 반응형 회고 — 스펙 017 실기기(아이폰) 깨짐; 닫힌 Drawer가 가로스크롤 만든 근본원인·표→카드·실측으로 범인 확정 [mobile,responsive,sider,drawer,overflow]
- 009 메모리 유저 스코핑 회고 — 스펙 018 세션 단기↔유저 장기를 user_id 축으로; thread_id와 user_id 직교성을 추측말고 1차출처로 확인한 사례 [memory,mem0,user-scope,thread-id,probe]
- 010 mem0 벡터 스토어 pgvector 이전 회고 — 스펙 019 파일DB→pgvector 공유 백엔드; 이미 도는 PostgreSQL 재사용·DATABASE_URL 재조립 P1을 타자가 잡음 [mem0,pgvector,shared-backend,dimension,verify]
- 011 mem0 다층 스코프 + 카탈로그 재정렬 회고 — 스펙 020; 내 '이점 없음' 결론이 틀린 모델 위였음을 웹리서치로 교정, mem0 진짜 가치=추출+다층스코프+엔티티그래프 [mem0,multi-scope,catalog,web-research,probe]
- 012 Playground userId UX + 피드백 9건 회고 — 스펙 021 userId 세션 영속·AutoComplete·진행중 잠금; 연속 브라우저 피드백 처리 패턴 참고 [playground,userid,ux,feedback,mem0]
- 013 구조 바로잡기: Context 스킵 막는 가시적 장치 — 기능 아닌 루프 자체 구조 결함 수정; Context 상습 스킵→복리 0, "참고 자산:" 줄로 가시화한 메타 교정 [context,process,recall,meta,compounding]
- 014 모바일 인스펙터 콘텐츠 오버플로 회고 — 스펙 022; 시드론 재현 안 돼도 단정 미루고 적대적 픽스처로 7개 오프endер 강제표출, 회고가 행동 바꾼 사례 [mobile,overflow,inspector,probe,regression]
- 015 Mock LLM을 레지스트리 1급 모델로 회고 — 스펙 024; 런타임 분기 대신 데이터(레지스트리행)+계약(엔드포인트)으로, build_agent가 mock 모름·결정적 검증 [mock-llm,registry,runtime,deterministic,single-source]
- 016 Playground Proxy: 세션 한정 설정 오버라이드 회고 — 스펙 025; `if overrides and source!=code` 한 줄 게이트로 무회귀 구조 보장, 기존 경로 안 건드리는 추가 [playground,proxy,override,no-regression,single-source]
- 017 외부 에이전트: A2A Agent Card 등록 1차 회고 — 스펙 026; source 2분기→3분기(+external), 카드 fetch·검증·등록(런타임 호출은 2차), 분기 최소화 [a2a,external,agent-card,source,gate]
- 018 어드민 same-origin API 프록시 회고 — 스펙 027; vite 프록시로 CORS·mixed-content·호스트종속 통째 제거, env 땜질 반복 후 근본 구조로 [proxy,same-origin,cors,vite,tailscale]
- 019 플레이그라운드 모델 배지 정직화 회고 — 스펙 028; code 에이전트가 모델명 거짓 표시→source별 정직 배지, 측정이 사용자 보고와 어긋날 때 양쪽 제시 [playground,model-badge,honest,probe,display]
- 020 에이전트 전용 메모리(agent_id) 자가기록 회고 — 스펙 029; recall_scope/add_scope 분리로 자동누출 차단, 의도적 쓰기채널이 누출 되여는 함정·라이브가 단위테스트 못본 것 잡음 [memory,agent-scope,self-write,leak,curation]
- 021 메모리 관리 콘솔 회고 — 스펙 030; 029 자산(패널·소유권가드)이 그대로 복리, 유저/에이전트 탭 통합, 프론트필터≠백엔드가드 자동반영 [memory,console,reuse,ownership,compounding]
- 022 멀티유저 인증 + 오픈형 권한(fastapi-users + Casbin) 회고 — 스펙 031; 자체구현 결론을 사용자가 밀어 깸→fastapi-users가 세션쿠키+Argon2+확장seam 유일, RBAC 모델 파일 분리 [auth,fastapi-users,casbin,rbac,session-cookie]
- 023 Playground userId를 인증 주체에서 도출 회고 — 스펙 032; 수동 userId 입력 제거→principal에서 도출, 두 번째 호출자 무영향을 타자 2종 수렴으로 확인 [playground,userid,auth,principal,signature]
- 024 세션 페이징: 엔벌로프 전환의 세 교훈 — 스펙 034(P0); list→envelope 시 마지막소비처 단정함정(tsc가 잡음)·공유DB는 불변식/델타로 검증 [pagination,envelope,sessions,invariant,tsc]
- 025 Provider 엔티티: 인라인 필드를 1급 관계로 정규화 — 스펙 035(P1); 마지막소비처 함정을 테스트 *전에* grep으로 잡은 복리 사례, 파이썬엔 tsc 안전망 없음 [provider,entity,normalize,migration,grep]
- 026 RAG 인제스트 회고 — 스펙 036(P2-a); 컬렉션·문서·청크 실저장소+차원 3중가드, 적대 타자가 silent death·drift·OOM 등 5결함 수렴 [rag,ingest,pgvector,embedding,dimension]
- 027 RAG retrieval 회고 — 스펙 037(P2-b); 죽은 vectorTables를 에이전트 검색도구로 부활, 컬렉션 자신의 임베딩 모델·유령버그(반환수 불일치) 끝까지 팜 [rag,retrieval,hnsw,cosine,tool]
- 028 배치 토대 + 세션 보존정리 회고 — 스펙 038(P3-a); 시스템과 격리된 배치 서비스(serve/run, k8s Deployment/CronJob), 배포타깃·격리수준을 먼저 물어야 했던 교훈 [batch,scheduler,k8s,isolation,cleanup]
- 029 유저 메모리 통합·재적재 회고 — 스펙 039(P3-b); LLM 통합 전 MemorySnapshot 박제 후 교체, 038 토대 재사용, 적대검증이 내 안전망 빈칸 찾음 [batch,memory,consolidation,snapshot,llm]
- 030 메모리 백엔드 추상화 회고 — 스펙 040(P4); memory 패키지화+Protocol, drop-in을 주장 아닌 측정으로(두 번째 InMemoryBackend 동일계약 단언) [memory,backend,protocol,drop-in,abstraction]
- 031 HIL 승인 게이팅 회고 — 스펙 041(P5-a); 가짜 큐를 실 langgraph interrupt 게이트로, AsyncPostgresSaver durable, 검증을 사다리 3rung으로 쌓음 [hil,approval,interrupt,langgraph,checkpoint]
- 032 A2A 실호출 회고 — 스펙 042(P5-b·로드맵 마지막); external 안내를 실 A2A JSON-RPC 호출로, 026 유예한 SSRF 빚을 net_guard로 청산(allowlist는 사람과 합의) [a2a,runtime,jsonrpc,ssrf,allowlist]
- 033 컨텍스트 경량화 회고 — 스펙 043; 자산 INDEX.md 3종+Context 규칙을 통독→인덱스우선으로(읽기량 95%↓), 적대리뷰가 아카이브 이동의 나가는 참조 누락 적발 [context,index,archive,refs]
- 034 어드민 표시 정직화 회고 — 스펙 045(마스터 044 배치1·#12/#2); 배지 mock상수→실 pending수·A2A무조건online→도달성probe, 적대리뷰가 리다이렉트SSRF 회귀 적발, Low지적 글자대로 고치다 동시성버그 낼 뻔 [admin,honesty,badge,a2a,liveness,ssrf]
- 035 빌딩블록 재료 정리 회고 — 스펙 046(마스터 044 배치2·#4/#5); 코드/인프라 권한5·MCP4·데모에이전트2 제거, dry-run이 라이브DB 스케일 드러냄, soft-ref strip(FK없음), 적대리뷰가 시드결합 통합rung 상실 적발(→learning045) [cleanup,catalog,seed,dangling,destructive]
- 036 프로바이더·모델 통합+models.dev 카탈로그 회고 — 스펙 047(마스터 044 배치3·#6/#7/#8); 두 메뉴→마스터-디테일, 카탈로그 박제 lookup, 셀프 GREEN 뒤 적대리뷰가 BLOCKER0이면서 결함2(per-read타임아웃≠전체deadline/버전스냅샷 삭제가드 누락) 적발→둘 다 수정+S6·I5b 커버, 거짓주장 안 남김 [merge,catalog,models-dev,timeout,delete-guard,adversarial]
- 037 RAG 임베딩 게이트+샘플 적재 회고 — 스펙 048(마스터 044 배치4·#9); 임베딩 모델 존재를 RAG 전제로 게이트(seed 헬퍼 분리+프론트 loaded가드+서버 400), docs_kb를 mock-embed 결정적 적재, 적대리뷰가 GREEN 뒤 멱등 결함(doc_count>0이 부분실패 오인) 반증→파일명 단위 멱등+라이브 자가치유 실증, learning045 self-fixture 두번째 적용 [rag,gate,seed,idempotency,self-fixture,adversarial]
- 038 세션 정리 정책 회고 — 스펙 049(마스터 044 배치5·#10); 0턴 소스 미영속(_load_context 보류→첫 _persist lazy-create)+턴 기준 배치 정리(min_session_turns, IDLE_GUARD 1h 활성보호, 나이절과 합집합), 생성가드(_create_approval)를 적대리뷰가 배치정리로 깸(결함#1: turns=0 승인세션 1h후 삭제→resume 고아)→cleanup에 pending approval 제외, 라이브 턴매치 126/130건이라 38식 베팅 안통함→적응형 안전, self-fixture 3번째 [session,retention,batch,lazy-create,idle-guard,hil-continuity,adversarial]
- 039 파괴적 데이터 정리+재생성 뿌리 차단 회고 — 스펙 050(마스터 044 배치6·마지막·#1/#11/#13); A2A9+세션9cascade+유저5 dry-run→검토게이트→삭제(마지막super 보호 실작동), Phase3 재생성뿌리(provisioner+shot self-fixture+서버env verify032 재시드를 config층 차단), 적대리뷰2차가 provisioner create EXISTS 실계정super승격(H1)·prefix 도메인오염(H2)·대소문자비대칭(C1) 적발→수정·나머지4 채무수용, self-fixture 4번째 [cleanup,destructive,regeneration-root,self-fixture,escalation,adversarial]
- 040 원격 어드민 로그인 실패: http 위 Secure 쿠키 회고 — 스펙아님·트러블슈팅; 원격 사용자 로그인안됨인데 curl 라운드트립은 green→근본은 Secure쿠키+평문http(브라우저가 silently drop), curl은 Secure무시라 거짓green. "(TLS over TCP)" 라벨을 TLS종료로 오판해 https안내 틀림(serve --json엔 TerminateTLS없는 raw패스스루), AUTH_COOKIE_SECURE=false+재기동으로 해결(.env.example문서화), 교훈→learning051(도구충실성) [login,cookie,secure,http,tailscale,false-green,tooling-fidelity]
- 041 에이전트 메모리 누출: LLM 쓰기 채널을 잘랐다 회고 — 스펙 051; 채팅 자가기록이 유저 개인사실을 agent_id(교차사용자)에 저장→누출, 회상이 축별합집합이라 agent_id는 구조적 교차사용자(user_id 덧태깅 격리 실패, 첫 옵션 철회). "통째 제거" 합의→사용자가 "어드민 수정유지+채팅 차단(페르소나 보호)"로 전환=두 쓰기채널 중 비신뢰(LLM)만 절단(58−/12+), 적대타자가 깨진 verify_029 적발→도구 *부재* 단언 회귀가드로 뒤집음 [memory,leak,agent-scope,write-channel,human-gate,self-fixture,adversarial]
- 042 유저 메모리 신원을 스코프 엔드포인트로 보강 회고 — 스펙 052; 큐레이션 화면이 user_id raw UUID로만 라벨→누구 메모리인지 식별불가, 이메일은 /admin/users(users:manage 슈퍼유저게이트)에 있지만 메모리메뉴는 모든 어드민에 열려 그거 재사용시 비-슈퍼유저 403→권한 조용히 결합. 메모리라우터(general _auth)에 필요컬럼만 JOIN하는 GET /memory/users 신설+response_model로 누출-안전 구조화, 적대타자 6점검 PASS·비블로킹2건(죽은 optionFilterProp·response_model부재) 즉시반영, 브라우저샷 이메일 식별 실증 [memory,identity,scoped-endpoint,authz-coupling,response-model,adversarial]
- 043 유저 메모리 역할 스코핑: 두 직교 축 가드 회고 — 스펙 053; 052는 신원 라벨만 붙였고 접근은 무방비(인증된 누구나 임의 user_id 메모리 열람·수정)→본인기본+어드민(memory:manage=머신/superuser/casbin)은 타인. require() 못쓴 이유=머신토큰 401, current_principal 인라인 판정. 핵심=주체×대상(신규 가드)+대상×행(_assert_user_owns) 두 직교축 합성(주체가드만이면 path 자기위조로 남의 행 삭제 가능, 행가드가 404로 막음), capability는 권고·강제는 per-route 독립. 검증사다리 4rung(단위14·실쿠키통합 타인403·브라우저샷 드롭다운분기·적대8 fail-closed) [memory,authz,role-scope,two-axis-guard,defense-in-depth,verification-ladder]
- 044 MCP 실연결 런타임: 가짜 한 층을 실 프로토콜로 회고 — 스펙 054; runtime._CANNED 하드코딩을 MultiServerMCPClient 실연결로(self-host FastMCP /_remote/mcp), 3페이즈(P1코어·P2탐색UX·P3auth/시드/검증). auth Option B(스킴라벨 안읽혀 암호화 단일컬럼 승격=providers.api_key 대칭, String120→400). 적대리뷰 2적발(둘다 happy초록): H1 리다이렉트-SSRF(guard_url은 최초IP만·새 MCP클라 follow_redirects=True 기본→3xx로 사설+복호Bearer 재전송, mcp_http_client_factory False+두 outbound배선, T8은 설정값 단언), H2 dry-run부정직(리포트 필터/apply 무조건→plan데이터만 소비로 계약복원). 검증4rung(in-process auth18·실인프라T1-8·브라우저샷 Fernet폼·적대2). reconcile 적용+멱등(변경0) [mcp,runtime,real-connection,redirect-ssrf,auth-at-rest,dry-run-honesty,verification-ladder]
- 045 Playground 세션 이어가기: 낙관적 고정과 비동기 쓰기 타이밍 회고 — 스펙 055; 백엔드는 이미 영속·재개 지원→빠진건 "불러오는 UI"(진단이 범위를 필터1+피커1로 못박음). E2E STEP3 실패 근본=비동기쓰기(스트리밍) 끝나기전 3초만에 이탈→턴abort→_persist 전 세션 미박힘(9초대기+헤더칩 createdSid 정확캡처로 해결, 실UX도 답변중 승인이탈시 같은 중단=041빚 재현). codex 2차 재리뷰가 새버그=낙관적 session_id 고정인데 fetch실패시 prevSid 미롤백 적발, 단조토큰(seq)+activeExtRef로 stale도착 폐기. 사용자피드백("해시만 불편")→preview 주라벨. 검증3rung 비겹침(통합=시맨틱·E2E=글루·타자=동시성) [playground,resume,optimistic-async,async-write-timing,rollback,staleness-token,adversarial]
- 046 원격 에이전트를 A2A로 단일화: source를 출처로 강등 회고 — 스펙 057; 자체 SSE `_remote_stream` 폐기·표준 A2A(`_a2a_stream`) 하나로 통합, source=프로토콜아닌 출처만(code 카드확장 / external 불투명). 핵심=통합은 폐기경로 행동 자동승계 안함(codex F2: 폐기경로는 히스토리 인라인·신규경로는 마지막만→멀티턴 단턴화, diff엔 삭제만 찍혀 자가검증 못봄→message.contextId 이식). F1 리다이렉트-SSRF(fetch_card follow_redirects False 통일·055 변종)·F3 길이하드닝·F4 버전불변식. 둘째통찰=사용자 한마디가 분류축을 프로토콜→출처로 바꿔 "중복느낌"의 뿌리가 UI아닌 프로토콜분기였음. 셋째=seed drift(코드옳아도 옛체제 영속데이터는 깨짐, rung2 라이브채팅만 잡음). learning 060 [a2a,unification,provenance,protocol-consolidation,behavior-parity,redirect-ssrf,seed-drift,adversarial]
- 047 첫 실행(first-run) 견고성 회고 — 스펙 058; 외부(테스트불가) 환경서 "처음 받아 실행" 점검·보강, 라이브 인프라 없이 정적/단위+codex 적대만. 정적분석이 *이미 견고*(스키마폴백·시드·모델부재 friendly400·임베딩부재 graceful)와 *진짜 갭*(부팅전제=DB연결·사람진입=첫관리자·셋업문서)을 가름. G1 DB 프리플라이트(SELECT1→마스킹DSN+docker힌트, 폴백 이중throw 구조소멸) G2 `python -m api.bootstrap_admin`(신규생성만·기존승격거부 050)·seed_admin 유저0만 시끄럽게 G3 루트README런북 G4 chat연결실패시 'Mock LLM'힌트(제어흐름불변). 핵심=codex P1: 스펙은 "확장실패해도 RAG만 비활성"이라 적었지만 create_all은 all-or-nothing→확장 catch해도 Vector컬럼서 재throw로 부팅막힘, "Vector테이블만 빼고"는 stamp head와 엮여 *복구해도 영영 안생기는* 더큰함정→부분부팅 버리고 pgvector 하드요구 fail-closed로 코드+스펙 정직화(설치된 PG는 IF NOT EXISTS no-op 통과). happy-path초록이라 자가검증 구조적 못봄→타자필수. learning 061 [first-run,db-preflight,bootstrap-admin,graceful-degradation,create-all-atomicity,fail-closed,adversarial]
- 048 MLX를 env에서 떼고 Mock LLM 기본화 회고 — 스펙 059; "env 제거" 요청이 Context 고고학으로 *정상 fresh-install이 이미 깨짐* 발견(두 부팅 경로 분기). 핵심=기본값은 상수 아니라 *세워지는 모든 경로의 수렴 상태*(seed `_empty(Provider)` 게이트가 alembic 경로서 죽어 그 경로 기본은 마이그레이션만 세움→CHAT_MODEL_NAME만 고치면 정상경로 여전히 깨짐), D1마이그레이션+D2seed를 같은 관측상태(이름/kind/is_default/model_id)로 수렴. no-clobber(기존 실모델 default 보존, 빈곳만 채움=learning046 재적용). 검증=verify_059 정적31·verify_058 무회귀·codex 적대 a/b/c/d 전부 HANDLED+잔여 손상DB 멱등은 수용문서화·통합rung 직접 실측(2026-06-29 던짐pgvector·실init_db 경로A+폴백 경로B): 두 경로 완전 동일 수렴·댕글링0·mock-chat/embed 실응답 green, verify_059_integration 자산화→first-run 준비됨. learning 062 [default-establishment,boot-path-convergence,seed-drift,no-clobber,adversarial,context-archaeology]
- 049 로컬(ui) 에이전트를 A2A로 노출해 플그서 dogfood 회고 — 스펙 060+061(짝); 060=A2A등록 에이전트 채팅 "URL 절대여야" 에러를 코드재현으로 두 실패모드 확정(모드1 validate_card가 서비스url 절대성 미검사→스킴없는url 등록통과·채팅 guard_url서 늦게모호 거부→normalize_http_url 등록시점 절대화·보안불변 정규화url에 guard_url 그대로 / 모드2 루프백사설 SSRF기본차단→A2A_ALLOWED_HOSTS, 로컬테스트UX B1host echo·B2모달힌트). 061=사용자 "임시로 로컬 A2A로 열어 등록·테스트"가 `exposed.a2a` *무력플래그*(저장만·소비코드없음=false-green) 발견→실서빙. 통찰: ①무력플래그는 false-green(토글 happy초록인데 기능부재) ②self-fetch가 새 인증위상 강제(fetch_card 무인증→카드공개·JSON-RPC만 라우트인증, 무인증 라우터 mock_remote패턴) ③dogfood왕복이 클라경로를 자기테스트하니스로 재사용(노출→external사본→_a2a_stream→/a2a머신토큰→stream_local_reply 실LangGraph, 한왕복이 클라+서버 동시운동) ④스펙이 *예상*한 위험도 적대가 *구체화*하면 싸게고침(codex H1 Host-poisoning: Host:attacker→카드url 공격자호스트→프롬프트·Bearer탈취→_self_base를 host_is_private 제약·공인Host 503 fail-closed). 검증4rung 비겹침(단위 verify_060/061+H1·라이브 verify_061_live_e2e 실왕복 실LLM텍스트·codex 적대 no-bypass평결+H1고침+H2문서화·브라우저 D7샷). 메타: 짝스펙은 첫스펙 시작전 커밋했어야(엉킨 net_guard·AgentsView·INDEX 분리곡예=compounding-ends-with-commit 경고). learning 064 [a2a,exposure,inert-flag,self-fetch,dogfood,reuse-runtime,host-poisoning,verification-ladder,adversarial]
- 050 Admin API 에러 detail 가시화 회고 — 스펙 062; 버그레포트 "RAG 컬렉션 생성 POST /collections가 →409만 보임"을 코드재현으로 두 결함 분리: (A)근본·전역=백엔드는 이미 detail에 안전사유 담는데 프런트 중앙헬퍼 j()가 본문 버리고 status만 throw(j() 쓰는 모든 화면 공통, CollectionsView는 e.message 보여줄 의도·주석까지 있었으나 무력화) (B)차원불일치 메시지에 조치누락. 처방=httpError.ts 중앙추출(detail 한 필드만·제어문자·길이상한·없으면 폴백)+_dim_mismatch 조치추가. 통찰①큐레이션 서버메시지는 전송경계가 버리면 무용지물(생산자·소비자 둘 다 옳아도 중앙경계가 body→status로 접으면 가치0, 손실은 invisible=happy초록·메시지존재·표시존재인데 둘이 안만남), 고칠곳은 끝단N개아닌 가치버린 경계 한곳→전역복구 ②codex 1차 Medium이 *내 learning 062(cap-the-raw-source) 안티패턴 그 자체*=httpError가 body 통째 json()파싱 후 600자컷=버퍼위상한="막은척", 적대자 {detail:A×5e8} 구체화→raw바이트상한(64KB)+시간상한(5s, SSE hang=Low도 동시차단)+reader cancel, 재리뷰 clean ③메타=인덱스후크 읽기≠적용(참고자산에 062 안적어 cap-the-raw 미상기→재구현→적대비용으로 재학습, 회고는 상기·적용돼야 복리). 검증4rung(단위 verify_062_http_error 실Response스트림 10단언·라이브 verify_062_live 중복409+안전문자열·브라우저 토스트스샷·codex 적대 1차2적발 2차 Resolved). learning 065 [error-surfacing,detail,central-boundary,cap-raw-source,curated-message,invisible-loss,adversarial]
- 051 A2A endpoint 호출 경계 정규화 회고 — 스펙 063(짝 060/061); 사용자 스샷 "URL은 http(s) 절대 URL이어야" 버그를 probe-deeper 코드재현으로 확정: 060이 정규화를 *등록 시점에만* 걸어 060 이전/우회 경로의 stale endpoint(스킴없음)가 채팅 guard_url서 거부=learning 065 패턴 그 자체(계약이 등록홉선 지켜져도 저장→호출 홉이 stale값 신뢰하면 깸, 픽스위치=소비경계). 처방3겹 D1(핵심·자가치유: a2a_stream가 guard_url 앞 normalize_http_url=단일chokepoint) D2(빌더하드닝: _norm_endpoint로 저장값 청결화·실패시 raw보존) D3(stale 마이그레이션: 기본dry-run·--apply만·안전행만·compare-and-set 멱등). 핵심통찰=가드 앞에 관대 정규화 끼우면 가드의 *입력분포*가 바뀜→codex[P1] mailto:user@example.com이 http전치로 공인host 둔갑→Bearer 누출, "정규화는 절대화만"을 내가 믿어도 절대화가 host 둔갑시키면 guard는 틀린host 봄→정규화서 userinfo/비숫자포트 fail-closed로 guard에 깨끗한host만(learning 066 별도자산화). 잘됨=probe-deeper로 단정회피·비겹침 4rung(통합만이 주입 stale행 실자가치유 실측)·codex가 자가검증 구조적사각(happy초록, 정상입력엔 @없음) 적발. 아쉬움=P1 1차수정 `parsed.port is None and ":" in netloc`가 bare IPv6 false-reject→`try:parsed.port`로 교체(형식휴리스틱은 정상입력변종 먼저 회귀에)·앞단계 060이 "등록시점만" 고친게 씨앗(계약은 경로전체 같은불변으로). 검증=verify_063_unit/live PASS·060무회귀·codex "P1 closed". learning 066 [normalize-before-guard,ssrf,userinfo-disguise,host-confusion,verification-ladder,adversarial]
- 052 SSRF allowlist env→DB(무재시작 관리) 회고 — 스펙 064; 스펙042 allowlist를 env(`A2A_ALLOWED_HOSTS`)→DB(`allowed_hosts`)로 옮겨 재기동없이 Admin UI/API 관리(`A2A_`떼고 ALLOWED_HOSTS 공용·env는 alembic 1회 부트스트랩 시드로 강등·DB 진실원). D1테이블+D2 alembic단독시드(revision-stamped→049재시드 footgun차단·seed.py무관여·path B 빈 fail-closed) D3 net_guard 캐시스냅샷(monotonic TTL 10s, sync guard_url 유지·async 5호출처 직전 await refresh·콜드/빈=fail-closed) D4이름일반화+차단메시지(063 host echo) D5 Admin CRUD(게이트·422/409/204·무효화) D6 AllowedHostsView. 4rung(단위·라이브 L1~L5 무재시작/≤TTL staleness·브라우저 4샷·codex 적대 8건). 통찰①저장위상 이전(env→DB)이 입력표면 넓힘=런타임 CRUD 새경로·env파싱 우연필터/운영자신뢰 소멸→normalize가 유일게이트·DB컬럼제약이 500-vs-422경계(codex 과길이→500 적발) ②정확매칭 allowlist가 IP-class가드보다 먼저 단락→allowlist 정규화가 마지막막, ipaddress 수용≠안전타깃(0.0.0.0/::미지정=와일드카드 canonical통과→로컬리스너 노출 codex[P1]) ③적대가 자가검증 사각 또메움(happy초록, 0.0.0.0/과길이/inf-TTL은 적대자만)→P1 1·P2 3 수정, 4건 by-design §5.1 명시처분 ④by-design을 "안전하니무시"아닌 명시처분(DNS rebinding=allowlist본질·042기존, DB-outage stale=빈스냅샷 self-DoS보다 나은 fail-safe)→근거 남겨야 다음적대가 재제기해도 싸게닫힘. 아쉬움=위협모델에 "수용하는 IP리터럴 중 비-타깃" 축 빠짐. learning 067 [allowlist,env-to-db,no-restart,exact-match-short-circuit,ip-literal,storage-migration,defensive-parse,by-design-disposition,adversarial]
- 053 어드민 메뉴 그룹 구분 회고 — 스펙 065; 사용자 "어드민 기능과 아닌 것은 메뉴에서 구분" 제안을 슈퍼유저 항목 `type:'group'` "관리자" 헤더로 묶어 해결(in-file "도구" 패턴 차용·1블록). 통찰①"노출게이트(보안/권한)"와 "시각구분(표현)"은 별개 축=슈퍼유저 노출은 이미 게이트(서버 064 require 독립강제), 빠진 건 표현뿐→요청을 권한로직으로 안 끌고 presentational임을 먼저 분별→인증/배선 무변경 최소표면 ②in-file 선례패턴이 곧 설계(antd full 통독·learning 미개봉, index 후크 충분 적중). 검증판단=순수 표현·비가역/보안표면 없음→codex 적대 스킵, 브라우저실측+tsc로 충분(064 보안경로의 반대편, rung을 표면으로 고름). learning 미작성(이식가능 실패패턴 없음·선례차용 무버그, 강제작성은 노이즈). [admin,menu,group,presentational,security-vs-presentation,verification-by-surface]
- 054 유저 self-승인 RBAC 3-way 회고 — 스펙 066; HIL resolve(041)를 admin전용→action민감도(`permission`)+Casbin RBAC 3분할(admin/머신=무엇이든·owner=자기것 중 self_approve 열린 perm만·그외 403), 기본 fail-closed(self_approve 0개→data.delete 영구 admin전용). 표시용 permission을 인가에 배선+Approval에 요청주체 user_id 첫 기록. 통찰①표시용필드를 인가축 승격시 "값이 어디서오나"가 보안전부=클라못바꾸는 서버상태(DB permission)라 위조불가(T3/T6), ResolveIn은 decision만받음 ②list 스코핑해도 item 엔드포인트 안스코핑하면 열거오라클(codex Low#1): 초안resolve가 비가시행도 403→404(부재)vs403(있지만내것아님) 갈라 목록이숨긴행 존재누설→_may_resolve앞 가시성게이트(비-admin 비가시행=404통일, 자기행 민감perm거부만 403유지) ③기능핵심값(owner+self_approve→200)이 기본상태선 한번도 안밟힘=인프라완성·기본노출0(정직성, 저위험도구 생기면 정책한줄로 열림). 4rung 비겹침=단위 FakeEnforcer로 owner→True분기 권위·라이브 enforce실소비(member 자기data.read도 403=fail-closed)·브라우저 403detail토스트 도달·codex 적대 열거오라클(happy초록, 적대만). owner+200 라이브양성은 의도적미수행(별프로세스 정책주입=재기동, 단위가덮음). learning 068 [self-approval,rbac,permission-authz,3-way,fail-closed,enumeration-oracle,list-vs-item,adversarial]
