# 118 — 회고: 평가 4탄 — RAG 컬렉션 평가 러너 (스펙 140)

## 무엇을 / 왜
사용자 요청(137 승인 시 예약)의 완성 — 컬렉션 검색 품질을 같은 평가 화면에서 채점.
137이 심어둔 kind='rag' 축이 그대로 살았다: 스키마·UI 골격·추이·비교 전부 재사용, 추가분은
러너 1개 + assert 3종 + 대상 선택 분기뿐.

## 잘된 것
- **확장축 선투자 회수**: 137 설계 때 "RAG 평가는 kind 축만"이라 못박은 것이 4탄을 반나절
  작업으로 만듦. OUT을 명시하되 축은 심어두는 방식의 실증.
- **평행 구현 0**: 해석(resolve_search_collection 추출)·검색(search_collections)·포맷
  (format_rag_hits) 전부 시험 엔드포인트와 공유 — 072 교훈 준수.
- rag 전용 assert는 obs["rag"] 부재=False(fail-closed) — agent 런에 넣으면 정직 실패.
  UI는 종류별 필터로 그 혼란 자체를 예방(codex #3).
- codex 4건(교차 대상 400·NaN/inf 거부·kind별 필터·stale 대상 리셋) 즉시 반영.

## 아쉬운 것 / 배운 것
- **내 수정이 새 표시 버그를 만듦**: kind별 옵션 필터(#3)를 넣으면서 "기준 추가" 기본값
  (trace_has)이 rag에서 숨겨진 옵션이 됨 → Select가 라벨 대신 원시값 표시. e2e(fast-worker)가
  포착. **옵션 목록을 필터할 땐 기본값도 그 목록에서 골라야 한다**(기본값-옵션 정합).
- 다중 컬렉션 동시 평가·인제스트 품질 진단은 OUT — 백로그.

## 다음 작업 Context에서 상기할 것
- 선택지 목록을 조건부 필터하면 **기본값·기존값이 목록 밖으로 밀리는지** 같이 점검.
- 확장축(OUT+축 심기)은 후속을 싸게 만든다 — 스펙 OUT 절에 축 명시 습관 유지.
- 참고: 스펙 140, 회고 115~117(평가 시리즈), learning 137.
