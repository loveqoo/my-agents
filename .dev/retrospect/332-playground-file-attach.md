# 332 — 플레이그라운드 파일첨부(스펙 404): 죽은 클립 버튼에 생명, 그리고 세 번 문 자업자득

## 잘 된 것

- **기존 재료 재사용이 설계의 절반**: extract_text(pypdf)·_sanitize·트레이스 규율·브라우저 픽스처가
  다 있어서 "무상태 2박자(추출→되보내기)+주입 관문" 설계가 하루에 끝남. 주입본을 user 메시지에
  그대로 영속시키니 히스토리 재구성·재개가 공짜(별도 저장소·TTL 0).
- **codex 스팟이 또 밥값**: ①고정 문자열 경계는 본문이 위조 가능 → 요청별 nonce 펜스(115 정본
  재적용)+파일명 소독 ②pypdf 동기 파싱이 이벤트루프 점유(페이지·시간 무제한) → 매직바이트 우선
  판별+스레드 격리+페이지 캡+타임아웃 ③스키마 캡 부재. 공격 픽스처(경계 위조·PDF 위조)로 핀.
- **브라우저 기능 검증의 정석**: "문서에만 있는 임의 토큰을 물어 정확히 답하는가"(외형 아님) —
  실모델이 ORBIT-멜론-9184만 답한 순간이 이 기능의 완성 증명. 390px까지 양 해상도.

## 자업자득 3연타 — "그물 실행 = 배타 구간"

그물(make test)이 도는 동안 ①스펙 파일만 만들고 INDEX 줄을 미루다 043에 물리고(두 번째 반복)
②브라우저 검증으로 dev 서버에 채팅해 db층(라이브 DB 공유)의 카운트 단언을 오염시키고(137runner
"세션 오염 3→4"·049) ③편집 경합까지. **402에서 심은 실패 전문 보존 계측이 이걸 전부 실물로
잡았다**(계측의 첫 수확). 규율로 승격: 그물·배터리 실행 중엔 저장소 편집·dev 서버 사용 금지
(배타 구간), 스펙 파일 생성 시 INDEX 줄은 같은 커맨드에서. → 영속 메모리 등재.

## 덤 — reset-dev가 캔 잠복 테스트 결함(049)

B2 기대치가 B1의 적응형 분기(실삭제 vs dry-run)에 종속인데 dry-run 경로만 하드코딩 —
깨끗한 DB(reset 직후)에서 처음 실삭제 분기가 타지며 적발. 적응형 가드를 쓰는 테스트는
**기대치도 분기-적응형**이어야 한다(고정 기대는 한쪽 경로의 화석).
