# 131 — A2A org 설정+앱 설정 저장소

> **복원 노트**: 이 파일은 인덱스 줄만 있고 full 파일이 생성되지 않았던 항목을 INDEX 후크에서 복원한 것이다(스펙 400 verify_043 실측 — 같은 턴 파일 생성 누락). 원 세션의 상세 서사는 유실됐고, 아래는 후크가 보존한 전부다.

A2A org 설정+앱 설정 저장소 — 스펙 153(자율 루프 4건차); app_settings 닫힌 키 집합+검증기+특권게이트 신설, codex Medium=폴백 의미 역전(값비교로 미설정 추정→기본값 저장 뭉갬), 미설정 판정은 sentinel/found로 → (learning 생략 — 회고 수록)

키워드: app-settings,found-vs-value,read-revalidation
