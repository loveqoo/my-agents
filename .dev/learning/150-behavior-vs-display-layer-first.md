# 150 — 거슬리는 표시는 고치기 전에 계층부터 갈라라(동작 vs 표시)

> **복원 노트**: 이 파일은 인덱스 줄만 있고 full 파일이 생성되지 않았던 항목을 INDEX 후크에서 복원한 것이다(스펙 400 verify_043 실측 — 같은 턴 파일 생성 누락). 원 세션의 상세 서사는 유실됐고, 아래는 후크가 보존한 전부다.

거슬리는 표시는 고치기 전에 계층부터 갈라라(동작 vs 표시) — 모델 프롬프트/상태에 있으면 동작버그, 표시 요약에만 있으면 겉치레(ainvoke 인자 vs 반환 델타로 판별); 표시면 신호는 보존하고 용어만 사람말로

키워드: symptom-layer-first,behavior-vs-display,ainvoke-vs-return-delta,keep-debug-signal,humanize-not-hide,internal-mechanism-leak,precision-debug-channel
