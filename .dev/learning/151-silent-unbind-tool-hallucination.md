# 151 — 도구/능력 미바인딩의 증상은 에러가 아니라 "호출한 척" 환각이다

> **복원 노트**: 이 파일은 인덱스 줄만 있고 full 파일이 생성되지 않았던 항목을 INDEX 후크에서 복원한 것이다(스펙 400 verify_043 실측 — 같은 턴 파일 생성 누락). 원 세션의 상세 서사는 유실됐고, 아래는 후크가 보존한 전부다.

도구/능력 미바인딩의 증상은 에러가 아니라 "호출한 척" 환각이다 — 이름 불일치로 도구가 0개 바인딩돼도 모델은 크래시 대신 그럴듯하게 지어냄; "주장(본문)"과 "호출 기록(트레이스)"을 분리해 단언해야

키워드: silent-unbind,tool-hallucination,claim-vs-record,mandatory-core-assertion,two-stage-probe,name-system-mismatch,suffix-fallback,fixture-fires-axis
