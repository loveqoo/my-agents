# 149 — "모든 입구를 셌다"는 자가선언이 바로 맹점이다

> **복원 노트**: 이 파일은 인덱스 줄만 있고 full 파일이 생성되지 않았던 항목을 INDEX 후크에서 복원한 것이다(스펙 400 verify_043 실측 — 같은 턴 파일 생성 누락). 원 세션의 상세 서사는 유실됐고, 아래는 후크가 보존한 전부다.

"모든 입구를 셌다"는 자가선언이 바로 맹점이다 — 입구 열거는 기억 말고 grep 전수로; 스스로 센 목록을 닫힌 집합으로 믿으면 빠진 입구가 dead-contract로 남음(타자가 3번째를 찾은 사례)

키워드: enumerate-entries-via-grep,self-declared-closed-set-fallacy,dead-contract,third-entry-point,wire-at-every-entry,adversarial-finds-missed-entry,088-sibling
