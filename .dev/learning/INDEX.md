# Learning Index  (.dev/learning/)

> 각 줄: `NNN 제목 — 언제 열까(후크) [키워드]`. Context 단계에서 이 인덱스를 먼저 읽고,
> 후크로 불충분할 때만 해당 full 파일을 연다.

- 001 경로 기반 메모리의 한계와 폴더 리네임 — 폴더 이동·리네임으로 경로 키 메모리가 고아 되니 영속 맥락은 repo에 둘 때 [memory,rename,path]
- 002 User 스코프는 허락받고 접근, 프로젝트 내용으로 오염 금지 — User 스코프(경로 메모리)에 쓰려 할 때·cross-project 아닌 내용을 섞지 말 것 [memory,scope,user]
- 003 재촉하지 말 것 (사람 속도에 맞춰라) — 매 턴 끝을 승인·커밋·다음단계로 몰아가려는 충동이 들 때 [process,review,pacing]
- 004 AskUserQuestion에 CJK는 \u 이스케이프 말고 직접 작성 — 도구 입력 문자열에 한글 넣을 때 JSON 파싱 실패가 나면 [askuser,cjk,json]
- 005 docs 스펙은 AI 초안 + 인간 검토 — docs/spec을 누가 쓰나 헷갈릴 때(AI 초안→인간 검토) [docs,spec,review]
- 006 Qwen(MLX) thinking 비활성으로 콘텐츠 누락 방지 — 로컬 MLX/Qwen 스트림이 빈 응답·추론토큰 폭주일 때 [mlx,qwen,thinking]
- 007 백그라운드 서버 재기동 시 포트 점유를 확실히 정리 — 서버 재기동이 address-in-use거나 옛 코드가 응답할 때 [server,port,restart]
- 008 handoff 디자인을 진짜 antd로 이식하는 플레이북 + antd-x v2 함정 — design handoff 번들을 antd로 이식하거나 antd-x 함정에 막힐 때 [antd,handoff,design]
- 009 Mem0를 로컬 MLX로 구성하기 (LLM·임베딩·벡터스토어) — mem0를 외부 OpenAI 없이 전부 로컬 MLX로 구성할 때 [mem0,mlx,embedding]
- 010 create_react_agent + 회상 주입은 단일 system 프롬프트로 — 회상·동적지시를 별도 system 메시지로 넣어 다중 system 에러 날 때 [langgraph,system,mem0]
- 011 Alembic를 async FastAPI 시작 시 안전하게 돌리기 — async lifespan에서 alembic upgrade가 running-loop로 깨질 때 [alembic,async,fastapi]
- 012 런타임 설정은 단일 소스(레지스트리)에서, env는 부트스트랩만 — LLM/임베딩 설정을 어디서 읽을지·env 폴백을 둘지 정할 때 [config,registry,env]
- 013 매 응답 `[단계 N — 이름]` 표기를 빠뜨리지 말 것 — 자율 실행에 몰입해 단계 헤더를 빠뜨리고 있을 때 [process,header,loop]
- 014 비밀값 at-rest 암호화(Fernet)와 함정 — api_key·토큰을 DB에 암호화 저장하거나 복호화 폴백을 짤 때 [secret,fernet,crypto]
- 015 API Bearer 토큰 인증과 함정 — API에 Bearer 인증 붙이거나 토큰 fail-closed·gitignore 처리할 때 [auth,bearer,token]
- 016 테스트 가이드는 실제 UI를 확인하고 쓴다 — 사용자에게 화면 조작 가이드를 줄 때 코드 안 보고 추정 말 것 [ui,guide,verify]
- 017 자유 문자열을 하드코딩 맵으로 조회하면 렌더가 통째로 죽는다 — 백엔드 자유문자열을 프런트 상태맵으로 조회해 페이지가 백지될 때 [react,status,crash]
- 018 antd 6 어드민 모바일 반응형 플레이북 — antd handoff 이식물이 모바일에서 깨질 때 반응형·Sider 오버레이 패턴 [antd,mobile,responsive]
- 019 mem0 메모리 스코핑: user_id 축, thread_id 직교, entity-id 제약 — mem0 user_id와 LangGraph thread_id를 한 변수로 합치려 할 때 [mem0,scope,thread]
- 020 mem0 공유 백엔드(pgvector) + DSN은 분해 말고 위임 — mem0 벡터스토어를 N-인스턴스 공유·pgvector로 옮기거나 DSN 다룰 때 [mem0,pgvector,dsn]
- 021 mem0의 가치는 memory_type이 아니라 스코프·추출·엔티티그래프 — 부분구현만 보고 "mem0 이점 없음"이라 단정하려 할 때 [mem0,extraction,probe]
- 022 뷰포트 브레이크포인트는 옆 패널 폭을 모른다 — 고정폭 형제 패널이 있는데 반응형 분기를 뷰포트 폭으로 할 때 [responsive,breakpoint,layout]
- 023 프로세스 규칙은 가시적 강제 산출물이 있어야 지켜진다 — 새 절차 규칙을 만드는데 가시적 강제물 없이 텍스트만 남길 때 [process,enforcement,visibility]
- 024 적대적 픽스처로 "특정 콘텐츠 깨짐" 재현 + Playwright antd 오버레이 사이더 네비 함정 — 시드로 재현 안 되는 콘텐츠 깨짐·antd Tag 줄바꿈을 잡을 때 [adversarial,fixture,antd]
- 025 시드/목업이 레지스트리와 어긋나면: 데이터 마이그레이션 + 단일 상수 — 시드된 라이브 DB가 레지스트리와 drift 났을 때(소스만 고치면 안 됨) [seed,migration,drift]
- 026 Mock/스텁은 런타임 분기가 아니라 레지스트리 데이터 + 계약 구현으로 — mock/테스트더블을 if-mock 런타임 분기로 넣으려 할 때 [mock,registry,contract]
- 027 프런트 필터는 백엔드 계약의 가드가 아니다 — 프런트에서 입력 걸렀다고 서버 불변식이 안전하다 믿을 때 [frontend,backend,guard]
- 028 서버측 외부 URL fetch는 "보조도구"여도 보안 표면이다 — 사용자가 준 URL을 서버가 fetch하는 기능 만들 때(크기·타임아웃·SSRF) [ssrf,fetch,security]
- 029 same-origin 프록시는 cross-origin 문제 "클래스"를 통째로 없앤다 — CORS·mixed-content·cert를 env로 땜질하며 원격 fetch 회귀가 날 때 [cors,proxy,origin]
- 030 UI 변경은 진짜 브라우저로 직접 확인한다 (사람 스샷 의존 X) — UI 검증을 사용자 스샷에 의존해 루프가 느려질 때 [ui,playwright,verify]
- 031 의도적 쓰기 채널은 스코프-태깅이 닫은 누출을 되연다 — 에이전트 자가기록 도구를 열어 PII 누출이 재유입될 때(도구 프롬프트≠격리) [mem0,scope,leak]
- 032 antd의 포털 렌더·전체 마운트가 단순 브라우저 셀렉터를 깨뜨린다 — antd Select 옵션·비활성 Tabs 때문에 Playwright 셀렉터가 헛돌 때 [antd,playwright,selector]
- 033 alembic autogenerate는 metadata에 없는 "남의 테이블"을 drop하려 든다 — autogenerate가 mem0 등 외부 런타임 테이블을 drop_table로 끼울 때 [alembic,autogenerate,drop]
- 034 공유·실 DB 검증은 불변식 + 델타로 (절대수치 금지) — 기존 데이터 많은 실 DB를 절대수치로 단언해 검증이 깨질 때 [verification,invariant,delta]
- 035 가드는 진실원을 검사하라, 사본 말고 (+ 초록 verify ≠ 견고) — 차원·불변식 가드를 사본/메타데이터로 검사하거나 초록 verify를 믿을 때 [guard,source,adversarial]
- 036 이상 신호가 인프라를 가리켜도, 의존성보다 내 측정을 먼저 의심하라 — 불일치가 화려한 인프라 버그를 가리킬 때 의존성 만지기 전 계측 확인 [measurement,probe,regex]
- 037 파괴적 노브엔 바닥을 깔아라 (경계값이 곧 delete-all) — retention=0 같은 "없음처럼 보이는 경계값"이 최대 파괴로 매핑될 때 [adversarial,destructive,boundary]
- 038 적대 리뷰는 *네가 상상한 실패*가 아니라 *상상 못 한 범주*를 찾는다 — 파괴적 작업에 자가 불변식+자가 테스트만 깔고 출하하려 할 때 [adversarial,invariant,review]
- 039 "drop-in 가능"은 두 번째 구현을 출하해야 *측정*된다 — 백엔드 추상화가 한 구현에 안 묶였다고 주장만 할 때(Protocol 누수) [abstraction,protocol,dropin]
- 040 실인프라 통합 테스트는 *단위 게이트와 적대 리뷰가 둘 다 못 보는* 결함을 잡는다 — 게이트를 단위+적대로만 검증하고 실 DB·요청간 글루를 안 태울 때 [integration,gate,drift]
- 041 "리밋"은 *원천 바이트*를 막아야 한다 — 선버퍼링·프레임 카운트는 캡이 아니다 — .content/.aiter_lines 위에서 사이즈 캡 걸어 OOM이 안 막힐 때 [limit,bytes,ssrf]
- 042 이동·리네임은 *양방향*으로 참조를 깬다 — 한 방향만 고치면 green이지만 깨져 있다 — 파일 옮기고 들어오는 참조만 갱신할 때(나가는 상대링크·단방향 검증 함정) [move,refs,relative-path,verify]
- 043 "표시가 거짓"인 결함은 코드층·데이터층으로 갈라 고친다 — 화면 수치/상태가 실제와 어긋날 때(코드가 잘못 읽음 vs 데이터가 가짜, 출하 리듬이 다름) [honesty,code-vs-data,batch-split]
- 044 "안전장치를 깔았다"는 "안전하다"의 증거가 아니다 — 적용 범위 경계를 적대자가 짚는다 — 서버측 fetch/probe에 가드 깔고 follow_redirects·재해석을 안 끌 때(가드는 최초URL만, 부수효과는 체인끝) [ssrf,guard,boundary,toctou,redirect]
- 045 통합 픽스처가 데모 시드에 결합하면, 카탈로그를 비울 때 통합 rung이 함께 증발한다 — 시드 데모 데이터(에이전트/MCP/권한)를 지울 때 그걸 픽스처로 빌려쓰던 통합 테스트·프로브·브라우저샷도 같이 죽음(040의 반대편: rung을 잃지 않는 법, self-fixture로 격리) [integration,fixture,seed,self-fixture,rung]
- 046 per-operation 타임아웃은 전체 작업의 deadline이 아니다 — 서버측 스트림 fetch에 httpx timeout(=per-read)만 걸고 "타임아웃 방어"라 주장할 때; slow-trickle이 코루틴 무한홀드 → asyncio.timeout(전체)로 감싸야 참. 041(WHERE 바이트)·044(WHICH URL)와 같은 메타패턴 "서브유닛 방어는 전체를 못 묶는다"의 WHICH-span 축 [timeout,stream,deadline,ssrf,slowloris]
- 047 성공-카운터에 건 멱등성은 부분 실패를 "완료"로 오인한다 — doc_count>0 같은 성공분만 세는 캐시로 멱등 스킵 걸 때(부분 적재 후 재실행이 누락분 안 채움); 완료 단위 집합으로 판단해야. 041/044/046 메타패턴의 WHICH-set(완전성) 축 [idempotency,counter,self-heal,partial,adversarial]
- 048 한 수명 경계(생성)에 건 보장은 수명 전체를 못 덮는다 — 다른 서브시스템이 그 사이 깬다 — 불변식을 생성 시점에만 보장하고 "확보"라 선언할 때(배치/GC/cascade가 후반에 그 행을 지움); 보장은 생성→소멸 전 구간·모든 쓰기주체에 걸쳐 표현하고 검증을 수명 끝까지+대조군으로. 041(WHERE)·044(WHICH)·046(WHICH-span)·047(WHICH-set) 메타패턴의 WHEN(수명) 축 [lifecycle,guard,subsystem,hil,cleanup,adversarial]
- 049 상태를 지우는 청소는 재생성 소스가 살아있으면 무의미하다 — 행이 아니라 발생원을 고쳐라 — 파괴적 정리 후 정크가 재발할 때(부팅시드·크론·다른 프로세스 env·리로더가 DB 밖에서 재생성); 삭제=증상·발생원=원인, ps eww로 실행 env 확인, config/시드층을 끄고 행은 그 뒤에야 영구. 041/044/046/047/048 메타패턴군의 WHENCE(발생원) 축, 045 self-fixture는 예방판 [regeneration,source-vs-state,seed,env,cleanup,whence]
- 050 파괴 헬퍼의 한 연산(delete)에 건 가드는 권한 등가인 형제 연산(create)을 안 덮는다 — 보호대상에 대한 모든 동사를 위협모델에 넣어라; delete막고 create로 실계정 super승격 열어두면 같은등급 피해가 샘(is_superuser=casbin우회=계정탈취), 멱등이 실계정에 작동하면 멱등=에스컬레이션. 044(범위)·048(수명)의 친척=연산(동사) 축, 가드는 한 술어로 모든 입구에 [guard,operation-symmetry,escalation,privilege,idempotency,adversarial]
- 051 실 클라이언트 제약을 무시하는 검증 도구는 거짓 green을 준다 — curl은 브라우저가 아니다 — 인증/쿠키/origin 경로를 curl로 green 받고 안심할 때; curl은 Secure/SameSite/CORS 강제 안 함→서버 라운드트립 통과해도 브라우저는 실패. 코드결함이 아니라 *검증수단*의 충실성 결핍(도구가 실클라보다 관대), 035/045 거짓초록의 발생원 축, 인증경로는 Playwright로 한번 태우거나 헤더레벨로 좁혀 실클라 확인 1회 [verification,tooling-fidelity,cookie,secure,browser,false-green]
- 052 누출은 비신뢰 쓰기 채널을 잘라 막는다 — 데이터도 기능전체도 아니다 — 한 저장소에 신뢰(인간게이트)·비신뢰(LLM자가기록) 채널이 같이 쓸 때; 수리단위는 채널이지 데이터(증상)·기능(과잉)이 아님, 절단면은 "어느 쓰기주체가 신뢰불가"로 그어짐(경계는 데이터 아닌 주체). 031(프롬프트≠격리)의 처방=LLM채널 삭제+권한을 인간게이트로, 절단을 *부재*단언 회귀가드로 못박고 검증자산도 같이 뒤집기(045). 049(소스vs상태)의 채널판 [leak,write-channel,human-gate,trust-boundary,remove-tool,regression-guard]
- 053 신원 보강은 스코프 엔드포인트로, 고권한 엔드포인트 재사용 금지 — 저권한 화면을 다른 곳 신원(email·이름·소유자)으로 보강할 때 그 데이터 있는 고권한 엔드포인트(users:manage) 재사용하면 기능 권한등급이 조용히 끌어올려짐→약한권한 사용자 403로 화면이 빔(happy-path 슈퍼유저만 초록). 절단면은 "소비화면 최소권한"에 긋고 그 등급 라우터에 필요컬럼만 JOIN하는 read 엔드포인트 신설+response_model로 누출 구조화, 약한권한 주체로 200(결합해제)·필드만(누출) 둘 다 단언. 050(연산대칭)/018(가드범위)의 쌍대=over-couple 축 [authz,coupling,scoped-endpoint,least-privilege,response-model,leak,adversarial]
- 054 인가는 모든 직교 축에서, 강제는 서버에서 — 공유저장소 행을 주체가 대상의 행에 접근할 때 가드를 한 축만 걸면 다른 축 path위조로 샘; /{user_id}/{mem_id}처럼 식별자 2개면 축도 2개(주체×대상+대상×행), 주체가드만이면 user_id를 자기로 위조해 남의 mem 삭제→행-소유권 가드와 합성해야 격리. capability(프론트 플래그)는 UX권고일뿐 per-route 독립 재판정(defense in depth), 모든 동사(GET/PATCH/DELETE)에 데이터페치 이전 게이트, 미초기화는 fail-closed 확인, 약한권한 주체로 통합1회. 050(연산축)/018(가드범위)의 친척=대상 좌표화 축 [authz,orthogonal-axis,path-spoof,defense-in-depth,fail-closed,server-enforce]
- 055 새 아웃바운드 경로는 네트워크 가드를 자동 상속하지 않는다(의존성은 위험 기본값을 들고 옴) — 새 외부호출 경로(새 클라/통합)를 추가할 때; "SSRF가드 있다"≠"이 경로에 적용". guard_url은 최초 IP만 보는데 새 HTTP클라가 follow_redirects=True 기본→공인 통과 후 3xx로 사설/메타데이터 재유도+복호 Bearer 재전송으로 표면+토큰 동시 누출. 처방=경로마다 가드 재확인(나가는 모든 지점 배선)·새 의존성 기본값을 주입훅으로 fail-closed 잠금·회귀가드는 "팩토리 존재" 아닌 클라 follow_redirects is False 자체 단언·위협모델에 리다이렉트/리바인딩 홉. installed-guard/move-breaks의 네트워크판, 좌표가 코드위치 아닌 아웃바운드 경로, 거짓green(051) 인접 [ssrf,redirect,outbound-path,dependency-default,follow-redirects,fail-closed,guard-coverage]
- 056 낙관적 비동기 상태: "도착"은 "유효"가 아니다 — set엔 롤백, 도착물엔 단조 토큰 — 비동기로 데이터 불러와 화면상태 바꾸는데 대기중 사용자가 다른선택(전환·재선택·리셋)하거나 요청실패 가능할 때; happy-path 자가테스트가 구조적으로 못보는 2부류=stale도착물(최신선택 났는데 늦은 성공콜백이 덮어씀, catch도 안탐)·실패후 미롤백(fetch전 낙관적 고정한 id를 실패시 안되돌림). 명제="콜백 도착≠결과 유효", 도착시점에 유효성 재판정. 처방=단조토큰 seq로 stale폐기+항상최신 ref로 도착시점 대상일치+낙관적set엔 catch롤백 짝+대상좌표 호출시 캡처. 자가검증이 여집합을 못밟으니 타자(codex) 적대필수(045서 2차재리뷰가 롤백누락 새버그 적발). adversarial/verification-ladder의 비동기판, 051 거짓green 인접(원인은 도구아닌 테스트시나리오) [async,optimistic-update,staleness-token,rollback,race,adversarial,false-green]
- 057 화면 전체 렌더 집합을 검증하라, 대상 행만 보지 말고 — 목록/드롭다운/테이블처럼 여러 행을 한 화면에 렌더하는 UI 검증 시, 특히 E2E가 방금 만든 한 행(marker)만 단언할 때; 스펙055 세션피커가 RESUME055_OK·preview_label=true로 통과했는데 사용자가 "깨진다" 보고→같은 드롭다운 다른 행들이 raw ISO(2026-...+00:00 폭넘침)·시드세션(turns>0·msg0행) "(빈 세션)" 오표기로 깨짐, 그 행들이 E2E 스크린샷에 이미 찍혀있었으나 대상행만 단언해 통과. 근본=데이터 다양성(방금만든 데이터는 정상분기만 밟음, 시드/레거시는 오래된 타임스탬프·메시지없음 폴백 분기). 처방=렌더집합 전체 단언+부정단언(raw ISO 부재 등)·시드/레거시 데이터로도 화면 캡처·스크린샷을 눈으로 읽기·무가공 백엔드값(ISO/UUID/enum)은 표시직전 포맷 경유. verification-ladder/verify-ui-in-browser 보완(단언좌표가 대상행에 갇힘), 051 거짓green 인접하나 원인은 도구아닌 단언좌표 [verification,e2e,assertion-scope,data-diversity,negative-assertion,format,false-green]
- 058 의미가 다른 두 빈 상태는 구분하라·비정규화 카운터는 행 수의 진실이 아니다 — 선택/로드로 채우는 화면이 items.length===0만으로 "비었음" 판정할 때, 특히 메타카운터(turns/count/배지)가 실제 영속행과 어긋날 때(시드/레거시); 스펙055 사용자 "세션 골라도 아무것도 안나온다"→코드는 정상(loadSession이 []받아 setConvos([]))인데 새대화와 똑같은 프롬프트카드 띄움=골랐는데빈 화면=안고른 화면. 근본2겹: Session.turns는 저장카운터지 Message행수 집계아님(turns=14인데 0행→[]), 의미다른 두 빈상태가 픽셀같으면 "선택이 안먹힌다"로 읽힘(화면은 정상동작). 처방=빈상태를 선택맥락유무로 분기(pickedButEmpty=empty&&currentSessionId&&!streaming→"불러올 메시지없음+이어쌓임" 다른UI)·비정규화카운터를 진실로쓰지말고 실제로드행 기준·"코드맞는데 사용자가 깨졌다고느낌"도 버그(loadSession은옳음, 결함은 구분안되는 빈상태UX). 057과 같은스펙·뿌리(데이터다양성), cap-raw-source 계열(파생값≠진실). **단 059에서 정련: 058 후반("실제 행을 세라")은 카운터가 행의 캐시일 때만 옳다** [empty-state,denormalized-counter,ux,data-diversity,playground,probe-deeper]
- 059 "실제 행을 세라"는 카운터가 행의 *캐시*일 때만 옳다·행으로 안남는 사건까지 세면 카운터가 *더 완전한 진실* — 058 후반 처방(비정규화 카운터 불신·실제 행 기준)을 파괴적 정리정책에 이식할 때; 스펙056 시드 빈세션(turns부풀림)을 행세기로 고치려다 codex가 실결함 적발=persistHistory=false(윈도우모드)는 메시지 의도적 미저장(chat.py:330)인데 turns는 +1(335)→100턴 윈도우세션 메시지0행이라 행세기면 저턴 오인 삭제(의미보존 실패·비가역). 통찰: turns는 행의 캐시가 아니라 더완전한 진실(turns≥메시지행수), 058은 "카운터=캐시"일때만 옳음. 판별="카운터가 세는 사건이 전부 행을 남기나?"(윈도우/스트리밍/비저장 경로 있으면 카운터가 더완전). 처방=잘못된 카운터값은 신호를 바꾸지말고 기록자(seed)를 고침(049 발생원의 신호판)·적대테스트 통과해도 내가상상한 drift만밟음 타자(codex)가 상상못한 범주 잡음(038). jobs는 카운터유지·seed만 turns=0 진실화 [counter,rows,denormalized,seed,windowed-mode,destructive,adversarial,whence]
- 060 통합은 폐기 경로의 행동을 자동 승계하지 않는다·살아남는 경로에 폐기 경로의 *행동 집합*을 열거해 이식하라 — 두 코드경로(자체프로토콜vs표준·레거시vs신규)를 하나로 합치며 한쪽 삭제할 때, 특히 "표준으로 통일"처럼 살아남는 쪽이 더 옳아보일 때; 명제=삭제는 폐기경로가 *고유하게* 하던 행동을 함께 지움, "두 경로를 하나로"는 합집합아닌 *교집합*으로 수렴하고 차집합(한쪽만 하던일)은 누락. 빠진 코드는 diff에 안보여 자가검증이 구조적으로 못봄. 표본=스펙057 `_remote_stream`(히스토리 인라인전송) 폐기→`_a2a_stream`(마지막만·contextId없음)으로 합치니 code 에이전트 단턴화, 컴파일·단턴채팅 초록인데 멀티턴 상실, codex(타자)가 잡음→message.contextId 이식·"contextId 실린다" 행동*존재* 단언으로 회귀가드. 처방=삭제전 폐기경로 행동(입력변환·부수효과·기본옵션·에러프레이밍) 열거하고 살아남는 경로가 각각 흡수하는지 명시, 표준이 옳아보여도 *기능패리티*(폐기경로가 풀던 문제를 표준이 푸는지) 별도단언, 회귀가드는 행동 *부재*(051/055 도구부재단언의 쌍대=행동존재단언), 누락은 내 행동모델 여집합이라 타자 적대필수. move-breaks(이동/삭제 양방향)의 행동판·055(새경로 가드 자동상속안함)의 거울상, 공통뿌리=경로위상 바뀌면 보장/행동 자동 안따라옴 [consolidation,retired-path,behavior-parity,deletion,protocol-migration,contextId,regression-guard,adversarial]
- 061 graceful degradation은 그것을 구현하는 연산의 원자성을 넘지 못한다 — "X 없으면 그 부분만 비활성, 나머지 동작"식 부분 열화를 *하나의 원자적 연산*(DDL create_all·단일 트랜잭션·일괄배포·all-or-nothing API) 위에 얹을 때; 명제=선행조건 에러(CREATE EXTENSION)를 try로 잡는 것과 그에 *의존하는* 원자적 연산이 부분성공하는 것은 별개, 의존연산이 원자적이면 catch는 선행에서 멈춰도 다음단계가 같은결함으로 *재*throw해 전체붕괴→"일부만 비활성"은 구현불가 주장. 부분열화 실재조건=(1)구현연산을 실제 부분실행 가능 (2)그 부분실행이 더나쁜 하류함정 안만듦, 둘중 하나깨지면 환상→정직한 fail-closed+또렷한 조치메시지가 옳음. 표본=스펙058 G1 codex P1(init_db 폴백이 확장 catch해도 create_all이 Vector컬럼서 재throw·"Vector만 빼고"는 stamp head와 엮여 복구막는 더큰함정→pgvector 하드요구 fail-closed). happy-path초록이라 자가검증 구조적 못봄→타자적대 필수. 처방=부분열화 적기전 구현연산 원자성 먼저보기·catch위치와 실패발생위치 분리·부분실행 가능해도 하류상태(스탬프/버전) 정합점검. installed-guard(검사≠부수효과지점)·adversarial-review·move-breaks·consolidation-doesnt-inherit 가족 [first-run,db-preflight,graceful-degradation,create-all-atomicity,fail-closed,head-stamp-trap,adversarial]
- 062 기본값은 상수가 아니라 *그것이 세워지는 모든 경로의 수렴 상태*다 — "기본을 X로" 류 요청을 시드·마이그레이션·폴백·env 등 둘 이상 경로로 기본이 세워지는 시스템에서 받을 때; 명제=기본값은 부팅 끝 관측상태이고 세우는 경로가 여럿이면 각 경로가 독립적으로 세움, 한 경로만 손대면 다른(하필 *정상*) 경로는 빈/옛 기본으로 남음. 함정=게이트가 경로마다 다르게 작동(seed per-table `_empty(Provider)`는 마이그레이션이 행 넣은 경로서 False라 *죽음*→그 경로 기본은 seed가 아니라 마이그레이션만 세움). 표본=스펙059 mlx→Mock: 경로A(alembic f4a5+a1b2c3)는 seed 스킵, CHAT_MODEL_NAME만 고치면 정상 fresh-install이 기본chat·임베딩 없이 깨짐→D1마이그레이션+D2seed를 같은 관측상태로 수렴, codex가 일치 단언. 처방=(1)경로 먼저 열거(마이그레이션체인·init흐름 읽기) (2)게이트가 어느 경로서 죽는지·그때 누가 기본 세우나 (3)관측값(이름/플래그/id) 양쪽 명시단언 (4)no-clobber로 기존신호 보존. 가족=046 seed-drift의 기본값establishment축·060 consolidation·061 atomicity와 공통뿌리(경로/위상 바뀌면 보장·행동·기본값 자동 안따라옴, 각 경로 명시이식)·049 발생원신호 [default-establishment,boot-path-convergence,seed-drift,gate-dead-on-path,no-clobber,migration,topology-change]
- 063 fail-closed 가드 메시지는 *위반한 값 + 그 값을 넣은 조치 예시*를 담아라(노브 이름만으론 부족) — SSRF·인증·검증·프리플라이트가 막고 4xx/부팅중단 낼 때 에러문구 쓸 때; 명제=막는 순간 운영자가 알 것 셋=무엇이(위반 구체값)·왜·어떻게(정확한 키 + *그 값 이미 넣은* 예시). 노브만 주면("A2A_ALLOWED_HOSTS로 허용 가능") *무엇을* 넣을지 다시 추론—가드는 이미 답(차단 host)을 쥐고도 안 알려준 것. 표본=net_guard.py 차단문구를 `host={host}` echo + `예: A2A_ALLOWED_HOSTS={host}` 복붙예시 + "재기동 필요"로 보강, `.env.example`에도 같은 키 주석(사전 발견). 처방=(1)검사 중 가진 구체값 echo (2)노브아닌 처방=`KEY=<그값>`+적용조건 (3)에러(사후)와 예제(사전) 같은 키 가리킴 (4)민감값은 마스킹하되 형태는 남김(G1 마스킹DSN). 061 "또렷한 조치메시지"를 값echo+복붙예시로 조작화·044/055 가드커버리지의 관측가능성(observability) 축 [fail-closed,guard,error-message,actionable,observability,echo-offending-value,env-discoverability]
- 064 서버가 *자기 주소를 스스로 광고*할 때 그 주소를 요청 Host에서 끌어오면 공격자 입력이다 — 서버가 자신 가리키는 절대URL을 생성해 내보낼 때(A2A/Agent카드 서비스url·OAuth redirect_uri·웹훅콜백·SSO메타·재설정링크·Location), 특히 self-base를 request.base_url/Host/X-Forwarded-Host로 채울 때; 명제=Host헤더(및 파생 base_url)는 클라가 보낸 임의문자열(가상호스팅용·신뢰출처 아님), 그게 서버가 *되돌려 광고하는* 자기URL로 흐르면 Host:attacker로 광고를 공격자호스트로 돌려 이후 *인증된* 호출(카드url 메시지전송·redirect_uri 토큰전달)의 프롬프트·Bearer·인가코드 탈취→self-base는 신뢰경계. 표본=스펙061 _self_base가 request.base_url 폴백→codex H1(High): Host:attacker→카드url=attacker/agents/id/a2a→클라가 머신토큰+프롬프트 전송→탈취, 스펙§5 예상했지만 폴백 방치, 적대자 실증후 고침(049통찰4). 처방=(1)명시설정 1순위신뢰(env A2A_SELF_BASE_URL 있으면 Host무시, Django ALLOWED_HOSTS·OAuth 사전등록 정확매칭) (2)Host파생 폴백은 로컬/사설만 fail-closed(공인Host 503으로 self-base 명시강제, host_is_private=resolve 전부 비-global일때만) (3)사전등록 화이트리스트가 정규화보다 강함. 판별="응답에 들어가는 내주소 어디서?"(요청파생이면 오염)+"그주소가 이후 인증트래픽 목적지?"(둘다yes=누출경로). 가족=063(fail-closed 위반값+처방)·044/057 redirect-SSRF의 *대칭*(따라가는주소 vs 광고하는주소 오염)·installed-guard 공통뿌리=신뢰못할 입력이 보안결정값으로 흐르는 경로 추적, happy-path초록→타자적대 필수 [self-advertised-address,host-header-poisoning,trust-boundary,ssrf,redirect-uri,fail-closed,whitelist,adversarial]
- 065 큐레이션된 안전 메시지는 *전송/경계 계층이 버리면* 무용지물 — 사유는 중앙 경계에서 surface하라 — 한쪽이 사람읽을 안전사유 만드는데 사용자엔 불투명코드(status/errno/generic)만 닿을 때, 특히 공용 fetch래퍼·RPC클라·에러미들웨어가 본문을 status로 뭉갤 때; 명제=진단가치는 가장약한 중계홉만큼만 살아남음, 생산자·소비자 둘 다 옳아도 사이 한경계가 body→status 접으면 가치0, 손실은 *invisible*(happy초록·메시지존재·표시코드존재, 둘이 안만날뿐→어느 단위테스트도 안빨감, 증상은 사용자보고뿐). 표본=스펙062 j()가 detail버리고 →status throw(쓰는 모든화면 같은병)·CollectionsView는 e.message 보여줄 의도·주석까지 있었는데 무력화→httpError 중앙추출 한곳고쳐 전역복구. 처방=(1)고칠곳은 가치버린 경계(끝단마다 재구현금지·드리프트·새호출부 또빠뜨림) (2)"더노출"과 "원문누출"은 한끗=생산자 큐레이션한 *한필드만* 추출(detail만·원문전체/스택/임의필드 금지, 안전한 유일이유는 생산자 불변식, surface넓힐수록 누출면도 넓어 타자점검) (3)상한은 raw바이트에서(062 cap-the-raw 이식, 통째파싱은 parse전 메모리·시간먹음·codex가 이 안티패턴 재적발) (4)판별=원인없는 실패가 코드 어딘가엔 이미 사유로 존재하나. 판별질문="만든곳과 보여줄곳 사이 status로 뭉개는 공용경계?"+"그경계가 한필드만? 다버림(누락)? 다읽음(누출)?". 가족=063(생산자가 좋은메시지 만드는쪽·이건 전달하는쪽, 둘이만나야 원인+조치)·062(cap-the-raw 이번 재적발)·031/054 안전메시지규율(detail 안전문자열 생산자불변식이 surface 안전전제). 공통뿌리=계약은 경로전체 유지, 한홉깨면 양끝옳아도 깨짐, happy초록→타자적대+실측(브라우저토스트) 필수 [error-surfacing,central-boundary,curated-message,invisible-loss,detail-only,cap-raw-source,producer-invariant,adversarial]
- 066 가드 앞에 관대 정규화를 새로 끼우면, 스킴으로 거부되던 입력이 가드를 통과할 수 있다 — 보안 가드(SSRF/검증) *바로 앞*에 입력을 절대화/정규화하는 단계를 새로 넣을 때; 명제=guard는 받은 URL의 host로 사설/공인 판정하는데, 앞에 관대 정규화를 끼우면 *guard가 보는 host를 정규화가 정함*→정규화가 입력을 다른 host로 *둔갑*시킬 수 있으면 예전에 스킴(형식)으로 거부되던 값이 공인 host로 둔갑한 채 통과(가드는 그대로인데 가드의 *입력 분포*가 바뀜). 표본=스펙063 D1이 guard_url 앞에 normalize_http_url 끼움→codex [P1]: `mailto:user@example.com/a2a`는 `://`없어 "스킴없음"→`http://mailto:user@example.com/a2a`로 전치→host=example.com(공인)·userinfo=mailto:user 둔갑→예전 비-http 거부값이 공인host로 통과해 Bearer토큰 누출(SSRF), `evil.com@127.0.0.1`은 userinfo로 실host를 127.0.0.1로 숨김, happy-path(정상 host:port) 전부초록=적대자만 던짐. 처방=(1)정규화가 host 바꿀 모든 형식 정규화단계서 fail-closed(A2A는 임베디드자격증명 안씀→username/password 있으면 거부·비숫자포트 parsed.port 접근 ValueError) (2)"절대화만"을 *결과*(scheme&hostname존재)로 단언말고 *둔갑벡터*(userinfo/포트)로 단언 (3)방어다중화 유지하되 기대지마라(encoded-@ %40은 normalize 통과하나 guard resolve실패로 2차차단, 그래도 raw는 1차서 막아 guard에 깨끗한host만) (4)raw userinfo(U1b)+encoded-@(U1c) 회귀고정. 판별="이 가드 앞에 입력 *변형* 새로 넣나·변형이 판정대상(host) 바꾸나·변형후 결과만 단언하나 경로도 단언하나". 가족=installed-guard-isnt-covering-guard·044/057 redirect-SSRF의 형제(거긴 검사지점 어긋남·여긴 가드입력 둔갑)·064 self-advertised(어디서 host 오나) vs 066(정규화가 host 어떻게 둔갑)·065 짝결함축(065=경계가 계약값 버림·066=정규화가 가드입력 둔갑, 둘다 경계 한홉이 끼면 양끝불변 깨짐)·happy초록→codex 적대 필수 [normalize-before-guard,ssrf,userinfo-disguise,host-confusion,fail-closed,defense-in-depth,adversarial,regression-pin]
- 067 가드보다 먼저 단락하는 정확매칭 allowlist는 *비-타깃 IP 리터럴*을 스스로 거부해야 한다(canonical 수용≠안전타깃) — SSRF/접근 가드가 `host in allowlist`면 IP-class검사 *전에* 통과시키는 구조서 항목을 `ipaddress.ip_address()`로 "유효IP면 수용"할 때·또는 보안control 저장을 부팅-static(env)→런타임-DB(CRUD)로 옮길 때; 명제=allowlist가 가드보다 먼저 return하면 allowlist 정규화가 *마지막 방어선*, 그런데 파싱성공≠안전한 단일타깃(미지정 0.0.0.0/::·멀티캐스트·예약은 canonical로 깔끔통과하나 전부/없음/그룹 뜻하는 비-타깃, 가드는 allowlist단락 *뒤*에 막지만 allowlist에 들면 그막 안돔→allow-local둔갑). 부수명제=env→DB이전은 런타임CRUD가 새입력경로·env파싱 우연필터/운영자신뢰 소멸→normalize가 유일게이트·DB컬럼제약이 500-vs-422경계(과길이host String(255)→500). 표본=스펙064 guard_url이 host in allowlist면 _ip_is_blocked전 통과·normalize_allowed_host가 0.0.0.0/:: canonical수용→http://0.0.0.0/ 로컬리스너통과 codex[P1]·과길이→500[P2]·ALLOWED_HOSTS_TTL float(env)직파싱이 inf엔 제거영구미반영·오타엔 부팅크래시[P2]→is_unspecified거부+253캡+_parse_ttl [0,300]클램프 수정·회귀고정. 처방=(1)allowlist가 조기단락하면 정규화서 비-타깃IP(is_unspecified 등) 거부·파싱성공을 수용근거로쓰지마 (2)저장컬럼제약과 검증일치(느슨하면 차이가 500으로샘, 422로먼저 조치메시지 063) (3)static→runtime이전시 새진입점서 전부재검증(옛경로 암묵적 좁힘을 열거·명시이식) (4)방어적파싱(env수치 float직파싱금지·비유한/음수/오타/과도→안전기본·상한, 수렴보장이 typo에 안깨지게). 판별="가드가 allowlist매칭서 조기return하나→정규화가 마지막막, 거기서 유효하나비-타깃(미지정/멀티캐스트) 거르나"+"control저장 옮겼나→옛경로 암묵필터를 새진입점이 명시거르나"+"검증이 컬럼제약만큼 엄격한가". 가족=037(노브바닥 */CIDR거부)·066(가드앞 정규화가 입력둔갑·여긴 allowlist항목 정규화가 예외집합 둔갑)·064 self-advertised·063(검증≥저장제약·위반값+조치)·installed-guard-isnt-covering-guard(가드설치≠적용덮음, 여긴 allowlist단락이 IP-class검사 건너뜀). happy초록→적대필수 [allowlist,exact-match,short-circuit,ip-literal,is-unspecified,canonical-not-safe,storage-migration,static-to-runtime,column-constraint,defensive-parse,ssrf,adversarial]
- 068 컬렉션을 스코핑해도 항목 엔드포인트는 안 스코핑된다: 404-vs-403 열거 오라클 — 멀티테넌트/RBAC CRUD서 list는 `WHERE owner=me` 스코핑하면서 항목 엔드포인트(/{id} resolve·delete)는 인가만 걸 때, 특히 "행없음→404"와 "있지만권한없음→403"을 다르게 응답할 때; 명제=컬렉션뷰 스코핑은 항목엔드포인트로 자동전파 안됨, 목록이 타인/숨김행 안보여줘도 item이 404(부재)vs403(존재하나금지) 가르면 id추측으로 *목록이숨긴 그행들의 존재*를 캐냄=열거오라클, "리스트 스코핑됐으니안전"은 거짓(스코핑은 그엔드포인트 하나의 가시성). 핵심=403은 "존재함"을 누설, 목록서숨긴행이면 item서도 부재와똑같이(404) 응답해야 은폐가 경로전체 일관. 표본=스펙066 list_approvals는 _own_scope로 타인·NULL행 숨겼는데 resolve초안은 비가시행도 _may_resolve=False→403, codex[Low#1] approval_id추측 404=없음/403=내것아님→타인 승인행 존재누설→_may_resolve앞 가시성게이트(비-admin 못보는행=404, 자기행 민감perm거부만 403유지). 처방=(1)list에 스코프거는순간 같은스코프 item에도 동시설계(둘은 같은데이터 두입구) (2)응답코드로 가시성갈라=볼수없는행 404·볼수있으나 행위권한만없는행 403, 판별="이행이 그사용자 목록에 떴나" (3)get_or_404후 인가403 기본패턴이 자연히 오라클만듦→비가시행은 404로접기 (4)위협모델에 "열거/존재누설" 축 처음부터(happy초록, 적대rung만 잡음). 가족=installed-guard-isnt-covering-guard(스코핑이 한엔드포인트만덮음)·065/066 경계한홉결함(계약이 경로전체아닌 한홉만→다른홉서깸, list↔item=같은데이터 두홉)·verification-ladder(단위·라이브 happy초록인데 적대codex만 잡음). [enumeration-oracle,list-vs-item-scoping,404-vs-403,existence-disclosure,rbac,multi-tenant,authorization,adversarial]
