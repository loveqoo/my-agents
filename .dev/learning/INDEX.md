# Learning Index  (.dev/learning/)

> 각 줄: `NNN 제목 — 언제 열까(후크) [키워드]`. Context 단계에서 이 인덱스를 먼저 읽고,
> 후크로 불충분할 때만 해당 full 파일을 연다.

- 001 경로 기반 메모리의 한계와 폴더 리네임 — 폴더 이동·리네임으로 경로 키 메모리가 고아 되니 영속 맥락은 repo에 둘 때 [memory,rename,path]
- 002 User 스코프는 허락받고 접근, 프로젝트 내용으로 오염 금지 — User 스코프(경로 메모리)에 쓰려 할 때·cross-project 아닌 내용을 섞지 말 것 [memory,scope,user]
- 003 재촉하지 말 것 (사람 속도에 맞춰라) — 매 턴 끝을 승인·커밋·다음단계로 몰아가려는 충동이 들 때 [process,review,pacing]
- 004 AskUserQuestion에 CJK는 \u 이스케이프 말고 직접 작성 — 도구 입력 문자열에 한글 넣을 때 JSON 파싱 실패가 나면 [askuser,cjk,json]
- 005 docs 스펙은 AI 초안 + 인간 검토 — docs/spec을 누가 쓰나 헷갈릴 때(AI 초안→인간 검토) [docs,spec,review]
- 006 Qwen(MLX) thinking 비활성으로 콘텐츠 누락 방지 — 로컬 MLX/Qwen 스트림이 빈 응답·추론토큰 폭주일 때 [mlx,qwen,thinking]
- 007 백그라운드 서버 재기동 시 포트 점유를 확실히 정리 — 서버 재기동이 address-in-use거나 옛 코드가 응답할 때 [server,port,restart]
- 008 handoff 디자인을 진짜 antd로 이식하는 플레이북 + antd-x v2 함정 — design handoff 번들을 antd로 이식하거나 antd-x 함정에 막힐 때 [antd,handoff,design]
- 009 Mem0를 로컬 MLX로 구성하기 (LLM·임베딩·벡터스토어) — mem0를 외부 OpenAI 없이 전부 로컬 MLX로 구성할 때 [mem0,mlx,embedding]
- 010 create_react_agent + 회상 주입은 단일 system 프롬프트로 — 회상·동적지시를 별도 system 메시지로 넣어 다중 system 에러 날 때 [langgraph,system,mem0]
- 011 Alembic를 async FastAPI 시작 시 안전하게 돌리기 — async lifespan에서 alembic upgrade가 running-loop로 깨질 때 [alembic,async,fastapi]
- 012 런타임 설정은 단일 소스(레지스트리)에서, env는 부트스트랩만 — LLM/임베딩 설정을 어디서 읽을지·env 폴백을 둘지 정할 때 [config,registry,env]
- 013 매 응답 `[단계 N — 이름]` 표기를 빠뜨리지 말 것 — 자율 실행에 몰입해 단계 헤더를 빠뜨리고 있을 때 [process,header,loop]
- 014 비밀값 at-rest 암호화(Fernet)와 함정 — api_key·토큰을 DB에 암호화 저장하거나 복호화 폴백을 짤 때 [secret,fernet,crypto]
- 015 API Bearer 토큰 인증과 함정 — API에 Bearer 인증 붙이거나 토큰 fail-closed·gitignore 처리할 때 [auth,bearer,token]
- 016 테스트 가이드는 실제 UI를 확인하고 쓴다 — 사용자에게 화면 조작 가이드를 줄 때 코드 안 보고 추정 말 것 [ui,guide,verify]
- 017 자유 문자열을 하드코딩 맵으로 조회하면 렌더가 통째로 죽는다 — 백엔드 자유문자열을 프런트 상태맵으로 조회해 페이지가 백지될 때 [react,status,crash]
- 018 antd 6 어드민 모바일 반응형 플레이북 — antd handoff 이식물이 모바일에서 깨질 때 반응형·Sider 오버레이 패턴 [antd,mobile,responsive]
- 019 mem0 메모리 스코핑: user_id 축, thread_id 직교, entity-id 제약 — mem0 user_id와 LangGraph thread_id를 한 변수로 합치려 할 때 [mem0,scope,thread]
- 020 mem0 공유 백엔드(pgvector) + DSN은 분해 말고 위임 — mem0 벡터스토어를 N-인스턴스 공유·pgvector로 옮기거나 DSN 다룰 때 [mem0,pgvector,dsn]
- 021 mem0의 가치는 memory_type이 아니라 스코프·추출·엔티티그래프 — 부분구현만 보고 "mem0 이점 없음"이라 단정하려 할 때 [mem0,extraction,probe]
- 022 뷰포트 브레이크포인트는 옆 패널 폭을 모른다 — 고정폭 형제 패널이 있는데 반응형 분기를 뷰포트 폭으로 할 때 [responsive,breakpoint,layout]
- 023 프로세스 규칙은 가시적 강제 산출물이 있어야 지켜진다 — 새 절차 규칙을 만드는데 가시적 강제물 없이 텍스트만 남길 때 [process,enforcement,visibility]
- 024 적대적 픽스처로 "특정 콘텐츠 깨짐" 재현 + Playwright antd 오버레이 사이더 네비 함정 — 시드로 재현 안 되는 콘텐츠 깨짐·antd Tag 줄바꿈을 잡을 때 [adversarial,fixture,antd]
- 025 시드/목업이 레지스트리와 어긋나면: 데이터 마이그레이션 + 단일 상수 — 시드된 라이브 DB가 레지스트리와 drift 났을 때(소스만 고치면 안 됨) [seed,migration,drift]
- 026 Mock/스텁은 런타임 분기가 아니라 레지스트리 데이터 + 계약 구현으로 — mock/테스트더블을 if-mock 런타임 분기로 넣으려 할 때 [mock,registry,contract]
- 027 프런트 필터는 백엔드 계약의 가드가 아니다 — 프런트에서 입력 걸렀다고 서버 불변식이 안전하다 믿을 때 [frontend,backend,guard]
- 028 서버측 외부 URL fetch는 "보조도구"여도 보안 표면이다 — 사용자가 준 URL을 서버가 fetch하는 기능 만들 때(크기·타임아웃·SSRF) [ssrf,fetch,security]
- 029 same-origin 프록시는 cross-origin 문제 "클래스"를 통째로 없앤다 — CORS·mixed-content·cert를 env로 땜질하며 원격 fetch 회귀가 날 때 [cors,proxy,origin]
- 030 UI 변경은 진짜 브라우저로 직접 확인한다 (사람 스샷 의존 X) — UI 검증을 사용자 스샷에 의존해 루프가 느려질 때 [ui,playwright,verify]
- 031 의도적 쓰기 채널은 스코프-태깅이 닫은 누출을 되연다 — 에이전트 자가기록 도구를 열어 PII 누출이 재유입될 때(도구 프롬프트≠격리) [mem0,scope,leak]
- 032 antd의 포털 렌더·전체 마운트가 단순 브라우저 셀렉터를 깨뜨린다 — antd Select 옵션·비활성 Tabs 때문에 Playwright 셀렉터가 헛돌 때 [antd,playwright,selector]
- 033 alembic autogenerate는 metadata에 없는 "남의 테이블"을 drop하려 든다 — autogenerate가 mem0 등 외부 런타임 테이블을 drop_table로 끼울 때 [alembic,autogenerate,drop]
- 034 공유·실 DB 검증은 불변식 + 델타로 (절대수치 금지) — 기존 데이터 많은 실 DB를 절대수치로 단언해 검증이 깨질 때 [verification,invariant,delta]
- 035 가드는 진실원을 검사하라, 사본 말고 (+ 초록 verify ≠ 견고) — 차원·불변식 가드를 사본/메타데이터로 검사하거나 초록 verify를 믿을 때 [guard,source,adversarial]
- 036 이상 신호가 인프라를 가리켜도, 의존성보다 내 측정을 먼저 의심하라 — 불일치가 화려한 인프라 버그를 가리킬 때 의존성 만지기 전 계측 확인 [measurement,probe,regex]
- 037 파괴적 노브엔 바닥을 깔아라 (경계값이 곧 delete-all) — retention=0 같은 "없음처럼 보이는 경계값"이 최대 파괴로 매핑될 때 [adversarial,destructive,boundary]
- 038 적대 리뷰는 *네가 상상한 실패*가 아니라 *상상 못 한 범주*를 찾는다 — 파괴적 작업에 자가 불변식+자가 테스트만 깔고 출하하려 할 때 [adversarial,invariant,review]
- 039 "drop-in 가능"은 두 번째 구현을 출하해야 *측정*된다 — 백엔드 추상화가 한 구현에 안 묶였다고 주장만 할 때(Protocol 누수) [abstraction,protocol,dropin]
- 040 실인프라 통합 테스트는 *단위 게이트와 적대 리뷰가 둘 다 못 보는* 결함을 잡는다 — 게이트를 단위+적대로만 검증하고 실 DB·요청간 글루를 안 태울 때 [integration,gate,drift]
- 041 "리밋"은 *원천 바이트*를 막아야 한다 — 선버퍼링·프레임 카운트는 캡이 아니다 — .content/.aiter_lines 위에서 사이즈 캡 걸어 OOM이 안 막힐 때 [limit,bytes,ssrf]
- 042 이동·리네임은 *양방향*으로 참조를 깬다 — 한 방향만 고치면 green이지만 깨져 있다 — 파일 옮기고 들어오는 참조만 갱신할 때(나가는 상대링크·단방향 검증 함정) [move,refs,relative-path,verify]
- 043 "표시가 거짓"인 결함은 코드층·데이터층으로 갈라 고친다 — 화면 수치/상태가 실제와 어긋날 때(코드가 잘못 읽음 vs 데이터가 가짜, 출하 리듬이 다름) [honesty,code-vs-data,batch-split]
- 044 "안전장치를 깔았다"는 "안전하다"의 증거가 아니다 — 적용 범위 경계를 적대자가 짚는다 — 서버측 fetch/probe에 가드 깔고 follow_redirects·재해석을 안 끌 때(가드는 최초URL만, 부수효과는 체인끝) [ssrf,guard,boundary,toctou,redirect]
- 045 통합 픽스처가 데모 시드에 결합하면, 카탈로그를 비울 때 통합 rung이 함께 증발한다 — 시드 데모 데이터를 지울 때 그걸 픽스처로 빌려쓰던 통합 테스트·프로브·브라우저샷도 같이 죽을 때(040의 반대편, self-fixture로 격리) [integration,fixture,seed,self-fixture,rung]
- 046 per-operation 타임아웃은 전체 작업의 deadline이 아니다 — httpx per-read timeout만 걸고 "타임아웃 방어"라 주장할 때(slow-trickle이 코루틴 무한홀드→asyncio.timeout 전체로 감싸야); 041(WHERE)·044(WHICH)의 WHICH-span 축 [timeout,stream,deadline,ssrf,slowloris]
- 047 성공-카운터에 건 멱등성은 부분 실패를 "완료"로 오인한다 — doc_count>0 같은 성공분만 세는 캐시로 멱등 스킵 걸 때(부분 적재 후 재실행이 누락분 안 채움); 완료 단위 집합으로 판단할 것 [idempotency,counter,self-heal,partial,adversarial]
- 048 한 수명 경계(생성)에 건 보장은 수명 전체를 못 덮는다 — 불변식을 생성 시점에만 보장하고 "확보"라 선언할 때(배치/GC/cascade가 후반에 그 행을 지움); 보장은 생성→소멸 전 구간·모든 쓰기주체에 걸쳐 표현하고 검증을 수명 끝까지 [lifecycle,guard,subsystem,hil,cleanup,adversarial]
- 049 상태를 지우는 청소는 재생성 소스가 살아있으면 무의미하다 — 행이 아니라 발생원을 고쳐라 — 파괴적 정리 후 정크가 재발할 때(부팅시드·크론·다른 프로세스 env·리로더가 DB 밖에서 재생성); 삭제=증상·발생원=원인 [regeneration,source-vs-state,seed,env,cleanup,whence]
- 050 파괴 헬퍼의 한 연산(delete)에 건 가드는 권한 등가인 형제 연산(create)을 안 덮는다 — 보호대상에 대한 모든 동사를 위협모델에 넣을 때(delete막고 create로 super승격 열어두면 같은등급 피해); 가드는 한 술어로 모든 입구에 [guard,operation-symmetry,escalation,privilege,idempotency,adversarial]
- 051 실 클라이언트 제약을 무시하는 검증 도구는 거짓 green을 준다 — curl은 브라우저가 아니다 — 인증/쿠키/origin 경로를 curl로 green 받고 안심할 때(curl은 Secure/SameSite/CORS 강제 안 함); 인증경로는 Playwright로 한번 태울 것 [verification,tooling-fidelity,cookie,secure,browser,false-green]
- 052 누출은 비신뢰 쓰기 채널을 잘라 막는다 — 데이터도 기능전체도 아니다 — 한 저장소에 신뢰(인간게이트)·비신뢰(LLM자가기록) 채널이 같이 쓸 때; 수리단위는 채널이고 절단면은 "어느 쓰기주체가 신뢰불가"로 그어짐 [leak,write-channel,human-gate,trust-boundary,remove-tool,regression-guard]
- 053 신원 보강은 스코프 엔드포인트로, 고권한 엔드포인트 재사용 금지 — 저권한 화면을 다른 곳 신원(email·소유자)으로 보강할 때 고권한 엔드포인트(users:manage) 재사용하면 권한등급이 조용히 끌어올려짐; 최소권한 read 엔드포인트 신설 [authz,coupling,scoped-endpoint,least-privilege,response-model,leak,adversarial]
- 054 인가는 모든 직교 축에서, 강제는 서버에서 — 공유저장소 행에 주체가 대상의 행으로 접근하는데 가드를 한 축만 걸 때(/{user_id}/{mem_id}면 축도 둘, 주체가드만이면 path위조로 남의 행 삭제); per-route 독립 재판정+fail-closed [authz,orthogonal-axis,path-spoof,defense-in-depth,fail-closed,server-enforce]
- 055 새 아웃바운드 경로는 네트워크 가드를 자동 상속하지 않는다(의존성은 위험 기본값을 들고 옴) — 새 외부호출 경로(새 클라/통합)를 추가할 때("SSRF가드 있다"≠"이 경로에 적용", follow_redirects=True 기본이 사설/메타데이터 재유도); 경로마다 가드 재확인·새 의존성 fail-closed 잠금 [ssrf,redirect,outbound-path,dependency-default,follow-redirects,fail-closed,guard-coverage]
- 056 낙관적 비동기 상태: "도착"은 "유효"가 아니다 — set엔 롤백, 도착물엔 단조 토큰 — 비동기로 데이터 불러와 화면상태 바꾸는데 대기중 다른선택/요청실패 가능할 때(stale도착물이 최신선택 덮음·실패후 미롤백); 단조토큰 seq+catch롤백, 타자 적대 필수 [async,optimistic-update,staleness-token,rollback,race,adversarial,false-green]
- 057 화면 전체 렌더 집합을 검증하라, 대상 행만 보지 말고 — 목록/드롭다운/테이블 검증 시 E2E가 방금 만든 한 행(marker)만 단언할 때(다른 행들이 raw ISO·시드세션 오표기로 깨짐); 렌더집합 전체+부정단언·시드/레거시로도 캡처 [verification,e2e,assertion-scope,data-diversity,negative-assertion,format,false-green]
- 058 의미가 다른 두 빈 상태는 구분하라·비정규화 카운터는 행 수의 진실이 아니다 — 선택/로드로 채우는 화면이 items.length===0만으로 "비었음" 판정할 때(시드/레거시서 메타카운터가 영속행과 어긋남, 골랐는데빈 화면=안고른 화면); 빈상태를 선택맥락유무로 분기 [empty-state,denormalized-counter,ux,data-diversity,playground,probe-deeper]
- 059 "실제 행을 세라"는 카운터가 행의 *캐시*일 때만 옳다·행으로 안남는 사건까지 세면 카운터가 *더 완전한 진실* — 058 행기준 처방을 파괴적 정리에 이식할 때(행 안남기는 사건까지 세면 오인삭제); 판별=카운터가 세는 사건이 전부 행을 남기나·잘못된 값은 신호 말고 기록자를 고침 [counter,rows,denormalized,seed,windowed-mode,destructive,adversarial,whence]
- 060 통합은 폐기 경로의 행동을 자동 승계하지 않는다·살아남는 경로에 폐기 경로의 *행동 집합*을 열거해 이식하라 — 두 코드경로를 하나로 합치며 한쪽 삭제할 때(교집합으로 수렴해 차집합 누락·빠진 코드는 diff에 안보임); 삭제 전 폐기경로 행동 열거·기능패리티 별도단언·타자 적대 [consolidation,retired-path,behavior-parity,deletion,protocol-migration,contextId,regression-guard,adversarial]
- 061 graceful degradation은 그것을 구현하는 연산의 원자성을 넘지 못한다 — 부분 열화("X 없으면 그 부분만 비활성")를 원자적 연산(create_all·단일 트랜잭션·일괄배포) 위에 얹을 때(원자성 탓에 catch 멈춰도 다음단계가 재throw→전체붕괴); 정직한 fail-closed+조치메시지가 옳음 [first-run,db-preflight,graceful-degradation,create-all-atomicity,fail-closed,head-stamp-trap,adversarial]
- 062 기본값은 상수가 아니라 *그것이 세워지는 모든 경로의 수렴 상태*다 — "기본을 X로" 요청을 기본이 둘 이상 경로(시드·마이그레이션·폴백·env)로 세워지는 시스템에서 받을 때(한 경로만 고치면 다른 정상경로는 옛 기본 잔존); 경로 먼저 열거·관측값 양쪽 명시단언·no-clobber [default-establishment,boot-path-convergence,seed-drift,gate-dead-on-path,no-clobber,migration,topology-change]
- 063 fail-closed 가드 메시지는 *위반한 값 + 그 값을 넣은 조치 예시*를 담아라(노브 이름만으론 부족) — 가드가 막고 4xx/부팅중단 내는 에러문구를 쓸 때(노브 이름만 주면 무엇을 넣을지 재추론, 가드는 이미 답을 쥠); 위반 구체값 echo+복붙 예시+에러와 예제 같은 키 [fail-closed,guard,error-message,actionable,observability,echo-offending-value,env-discoverability]
- 064 서버가 *자기 주소를 스스로 광고*할 때 그 주소를 요청 Host에서 끌어오면 공격자 입력이다 — 서버가 자기 가리키는 절대URL을 생성·내보낼 때(A2A카드·redirect_uri·웹훅·SSO·재설정링크), 특히 self-base를 request Host로 채울 때(Host 위조로 인증호출 탈취); 명시설정 1순위·Host파생 폴백은 로컬/사설만 fail-closed [self-advertised-address,host-header-poisoning,trust-boundary,ssrf,redirect-uri,fail-closed,whitelist,adversarial]
- 065 큐레이션된 안전 메시지는 *전송/경계 계층이 버리면* 무용지물 — 사유는 중앙 경계에서 surface하라 — 사람읽을 안전사유를 만드는데 사용자엔 불투명코드만 닿을 때(전송계층이 body→status로 뭉개 손실이 invisible); 중앙 경계 한곳서 한 필드만 추출·상한은 raw바이트에서 [error-surfacing,central-boundary,curated-message,invisible-loss,detail-only,cap-raw-source,producer-invariant,adversarial]
- 066 가드 앞에 관대 정규화를 새로 끼우면, 스킴으로 거부되던 입력이 가드를 통과할 수 있다 — 보안 가드(SSRF/검증) 바로 앞에 절대화/정규화 단계를 새로 넣을 때(정규화가 guard가 보는 host를 둔갑시킴·userinfo로 실host 숨김); 둔갑벡터로 단언·결과만 단언 금지 [normalize-before-guard,ssrf,userinfo-disguise,host-confusion,fail-closed,defense-in-depth,adversarial,regression-pin]
- 067 가드보다 먼저 단락하는 정확매칭 allowlist는 *비-타깃 IP 리터럴*을 스스로 거부해야 한다(canonical 수용≠안전타깃) — allowlist가 가드보다 먼저 return하는 구조서 항목을 "유효IP면 수용"할 때(0.0.0.0/멀티캐스트가 canonical로 통과→allow-local 둔갑), 또는 보안control 저장을 env→DB로 옮길 때; is_unspecified 거부·저장제약과 검증일치·방어적 파싱 [allowlist,exact-match,short-circuit,ip-literal,is-unspecified,canonical-not-safe,storage-migration,static-to-runtime,column-constraint,defensive-parse,ssrf,adversarial]
- 068 컬렉션을 스코핑해도 항목 엔드포인트는 안 스코핑된다: 404-vs-403 열거 오라클 — 멀티테넌트/RBAC CRUD서 list는 owner 스코핑하나 항목 엔드포인트(/{id})는 인가만 걸 때, 특히 404 vs 403을 다르게 응답할 때(403이 존재 누설→id추측으로 숨긴 행 캐냄); 볼수없는행은 404로 접어 경로 일관 [enumeration-oracle,list-vs-item-scoping,404-vs-403,existence-disclosure,rbac,multi-tenant,authorization,adversarial]
- 069 읽기 경로를 스코핑해도 같은 데이터의 쓰기/resume 입구가 소유권 안 걸면 스코핑 무력화 — 읽기엔드포인트에 유저별 스코핑 새로 걸 때, 같은 행의 생성·변경·resume 입구가 소유자 덮어쓰거나 무검사 바인딩하면(읽기 완벽해도 쓰기입구가 탈취·존재오라클); 모든 입구에 소유권 처리·소유권이전 기본금지 [scope-read-not-write,ownership-takeover,enumeration-oracle,covering-guard,write-entry-bypass,multi-tenant,rbac,session,adversarial]
- 070 읽기/resume 게이트를 조이면 *소유자 미스탬프* 행이 고아가 된다(069의 거울상=자가-잠금) — 같은 데이터에 owner 스코프 게이트를 새로 걸 때, 모든 생성 입구가 소유자를 박는지 점검 안 하면(lazy-create 빠뜨리면 NULL-owned로 남아 본인조차 못이음); 생성입구 전부 열거·같은 헬퍼·자가잠금 별도핀 [tighten-gate,unstamped-row,self-lockout,creation-entry,owner-stamp,covering-guard,single-source,session,rbac,adversarial]
- 071 인덱스도 비대해진다: 한 줄 후크 규약을 어기면 인덱스가 다시 통독 대상으로 회귀 — 통독 절감용 인덱스 줄에 서사를 욱여넣으면 그 자체가 통독 대상이 됨; 추가 시 줄 짧게·주기적 재압축(Scaffolding 점검)·대량변환은 서브에이전트·보존은 git대조 측정 [index-bloat,recompress,one-line-hook,context-optimization,scaffolding,subagent-delegation,measure-not-self-verify,compounding]
- 072 방어 규칙/체크리스트 자체가 적대검증 대상: "메타 가드의 열거는 닫힌 집합이 아니다" — "모든 X를 다뤄라"식 가드/정책/체크리스트를 만들 때(만든 직후 완료감이 듦); 그 자산의 열거가 빠뜨린 여집합을 적대검증·실코드 grep으로 대조, 닫힌집합 취급 금지, 트리거도 자가판정이면 맹점이 거기로 이동 [meta-guard,closed-set-fallacy,adversarial,complement,enumeration,defense-asset-verify,happy-path-blindspot,trigger-self-judgment,installed-guard]
- 073 타이밍 오라클은 계층마다 따로 봉합된다: 앱계층 단일화 ≠ 저장계층 동일성 — fetch-then-check를 SELECT-WHERE 융합으로 앱경로 단일화해도 비밀키 단독 인덱스가 남아 B-tree 프로브가 존재 vs 부재를 다르게 처리할 때(조임=제거 아닌 축소); 복합인덱스+EXPLAIN 봉합·"제거"는 계층 숨김 [timing-oracle,layer-specific,app-vs-storage,select-where,composite-index,query-planner,btree-probe,explain,measure-not-claim,verification-ladder,probe-deeper,adversarial]
- 074 RFC-정확 ≠ 배포-정확: 프록시 path-prefix 뒤 루트상대 URL은 origin으로 풀려 prefix를 잃는다 — 제3자가 광고한 루트상대 URL을 urljoin으로 풀 때(RFC상 origin루트로 가나 프록시 prefix 의도와 어긋나 404); 제공위치 상대 resolve·urljoin으로(concat은 `..` 탈출)·liveness는 route vs host 도달 구분 [url-resolution,rfc-vs-deployment,proxy-path-prefix,root-relative,urljoin,dot-segment,liveness-probe,route-vs-host,probe-deeper,verification-ladder,adversarial,third-party-url]
- 075 내부 도구를 직접 엔드포인트로 승격하면 입력 신뢰경계가 리셋된다 — 내부 함수/도구를 외부 POST 엔드포인트로 올릴 때; 상류(LLM·연결코드)가 암묵 보장하던 입력 제약이 raw로 도착하니 새 입구가 *직접* 재검증해야(안 하면 502로 뭉개지거나 자원고갈 입구)·같은 코어라도 입구 바뀌면 경계 바뀜 [trust-boundary,promote-tool-to-endpoint,input-validation,raw-input,length-cap,kind-guard,field-validator-layer,resource-exhaustion,adversarial,cap-the-raw-source]
- 076 완화를 검증할 땐 실제 비용지표 + 반증(counterfactual)으로: "선택≠강제" 구분 — 완화책을 측정으로 검증한다며 happy-path 초록 받았을 때; 프록시 아닌 실제 비용지표 단언·완화책 끈 반증으로 인과+민감도 입증·비용기반 선택자 의존은 감축이지 강제 아님(잔존 정직기록) [mitigation-verify,cost-metric-not-proxy,counterfactual,causation-sensitivity,explain-buffers,planner-selection-not-forcing,honest-residual,adversarial,verification-ladder,probe-deeper]
- 077 "잘려 안 보임"은 추측 말고 scrollWidth로 *범인 컬럼*을 측정해 특정 — UI가 잘린다/안 보인다 보고를 받을 때; 잘린 칸이 아니라 비대한 칸을 줄여야 풀리는데 어느 칸인지는 측정만 앎(scrollWidth/boundingRect)·계산→적용→재측정으로 확인(2회 추측<1회 측정) [table-overflow,max-content,measure-not-guess,culprit-column,scrollwidth,boundingrect,layout-debug,verify-ui-browser,probe-deeper]
- 078 "체크박스+설명"은 설명을 Checkbox 형제로 두지 말고 라벨 안에 세로 스택 — 체크박스 옆 설명문 UI를 만들 때; 설명을 형제 flex 아이템으로 두면 폭 다퉈 라벨 shrink→한글 글자단위 세로래핑, Checkbox children에 flex-column 스택으로 해소·antd6 모달본문은 .ant-modal-container(구 명 아님) [admin-ui,antd6,checkbox,flex-shrink,nested-label,modal-container-class,verify-ui-browser,measure-not-guess]
- 079 deprecated API 마이그레이션은 추측 말고 실측·문서로 대체 API 잡고 가장 강한 기존 회귀로 계약 보존 증명 — deprecated 심볼을 후속으로 갈 때; 설치본 @deprecated 본문+공식문서로 시그니처 확정(추측 금지)·기존 실인프라 통합회귀가 1순위 증거·전 사용처 grep 쓸기·no-deprecation을 warnings.catch로 핀 [deprecation,migration,call-contract,verification-ladder,strongest-regression,probe-deeper,no-deprecation-assert]
- 080 antd 6는 내부 DOM 클래스를 광범위 개명: Playwright 셀렉터는 antd5 이름 가정 말고 probe로 실측 — antd6 화면을 Playwright로 잡다 `.ant-*` 셀렉터가 타임아웃날 때; 개명된 것·안 바뀐 것 섞여 개별 실측 필요·getByText로 렌더 먼저 확인(렌더O+클래스 타임아웃=개명 의심)·라벨/역할 스코프+폴백체인·0개=측정실패 [antd6,dom-class-rename,playwright,selector,modal-container,select-content,probe-dont-assume,false-green,verify-ui-browser,probe-deeper]
- 081 스테이징(draft) 상태는 편집 지점만 아니라 *모든 소비 지점*에 표시하라 — 쓰기가 스테이징 레이어로 가는 구조에서 "저장했는데 안 바뀜" 혼란을 막을 때; 사용자는 편집 직후 다른 소비 표면으로 이동해 미반영을 거기서 마주침→증상 화면에 신호를 진실원으로 구동·양성+음성 둘 다 검증 [staging-state,draft-publish,consumer-surface,signal-reaches,truth-source,no-backend-change,positive-negative-verify,probe-deeper]
- 082 관측은 "조회 행위"를 계측하라, 결과만 싣지 말고 — 검색·회상·필터가 조건부로 0건/빈 채 돌 수 있는데 결과만 직렬화돼 "이력 안 보임"이 날 때; 조회의 쿼리·대상·시도를 결과와 *독립*으로 실어 "0건/미호출"과 "조회 안 함"을 구분·쿼리 에코는 표시전용+길이상한 [observability,instrument-the-act,query-not-result,0-result-still-happened,trace,display-only,probe-deeper,positive-negative-verify]
- 083 소비 표면 신호는 *신선하게 유지*하라: 마운트 1회 페치는 탭 밖 변경에 stale — 진실원을 스냅샷해 표시하는 표면이 그 표면 밖(다른 탭·액터·크론)에서 바뀔 수 있을 때(081=공간축, 083=시간축); 교차 탭 staleness는 교차 컨텍스트 전파로만 닿음(BroadcastChannel은 두 페이지 테스트 필수)·즉시전파+포커스 백스톱 2층 [consumer-surface,stale-snapshot,cross-tab,broadcastchannel,event-propagation,focus-backstop,two-layer,preserve-user-state,false-green,probe-deeper,verify-ui-browser]
- 084 파생값을 저장하면 그 *출처*도 저장하고 refresh는 타임스탬프가 아니라 *재파생*하게 — 다른 데이터에서 계산한 값(resolve된 URL·요약·캐시)을 컬럼에 영속할 때(시점 스냅샷이라 규칙·원천 변경에 stale; 083=소비축, 084=영속축); 출처도 같이 저장·refresh는 재fetch→재파생까지·재파생 입력은 guard통과 저장값만 [derived-value,stale-snapshot,store-the-source,re-derive-not-timestamp,self-heal,in-place-update,offline-honesty,trust-boundary,migration-vs-ondemand]
- 085 맥락상대 경로 resolution은 꼬리에 멱등 — base가 이미 그 세그먼트로 끝나면 또 붙이지 마라; prefix-mount·urljoin로 상대 합성할 때(base가 꼬리를 이미 포함하면 중복 /a2a/a2a); 정확 꼬리매치 멱등 collapse나 probe-disambiguate·버그+정상 토폴로지 매트릭스 검증 [path-resolution,idempotent-on-tail,prefix-relative,ambiguous-heuristic,probe-disambiguate,exact-tail-match,trust-boundary,regression-matrix]
- 086 소비층 fail-closed만으론 "죽은 상태"가 샌다·술어를 *모든 쓰기/렌더 경계*에 정렬 — capability 플래그가 소비측에서만 fail-closed인데 "되는 것처럼 보이는데 안 됨"을 사용자가 보고할 때(보안 아닌 데이터정합/UX, happy-path엔 안 잡힘); flag⟹predicate를 모든 입구 계약으로·입구 닫힌집합·기존데이터도 입구 [capability-flag,fail-closed-consumer,dead-state,align-predicate-every-writer,enumerate-write-entries,idempotent-clear,no-frontend-only-guard,data-is-an-entry,jsonb-sibling-key]
- 087 상태 표시값은 프록시가 아니라 *실제 가용성*에 묶어라("구성됨≠작동함") — API가 하위기능 가용여부를 플래그로 응답에 실을 때 특히 리졸버가 초기화 실패를 흡수할 때(enabled=config!=None은 프록시라 백엔드 구성 실패도 True→"고장"을 "가용0건"으로 위장, 086 동형); 플래그를 리졸버 결과로 판정·None≠[] 3값 구분·얇은 facade [status-flag,proxy-vs-actual,configured-not-working,resolver-absorbs-failure,three-valued-none-vs-empty,thin-facade,defensive-output-bound,086-sibling-axis]
- 088 인터페이스에 새 필드를 더하면 *모든 입구*에서 다뤄질 때까지 죽은 것이다 — 공유 인터페이스(config/dataclass/Protocol)에 새 필드를 더하고 읽기·쓰기·빌드 입구가 여럿일 때(빠뜨린 입구가 초록인 채 떨어뜨림: full-replace silent drop·빌드 미배선 dead contract); 입구 닫힌집합·model_fields_set 보존·통합 라운드트립 [new-interface-field,wire-at-every-entry,full-replace-silent-drop,model-fields-set,dead-contract,enumerate-entries,roundtrip-through-all-entries,adversarial-codex,probe-deeper,086-sibling-axis]
- 089 사람에게 보일 임의 데이터는 키 blocklist가 아니라 값 allowlist로(노출 deny-by-default) — 신뢰못할 임의 구조를 인스펙터/로그/trace에 요약·표시하고 비밀누출0이 불변식일 때(키 마스킹은 필요조건일 뿐·평범/비영문 키의 비밀 값이 샘); 노출 뒤집어 최소 값 allowlist·형제 표면 같은 정책·fail-closed는 예외까지 [redaction,value-secret,key-blocklist-insufficient,allowlist-failclosed,minimal-allowlist,sibling-surface,enumerate-surfaces,failclosed-on-exception,budgeted-cap,double-cap-honesty,adversarial-codex]
- 090 분리된 표면에선 위협 모델을 *실측*하라(상속한 심각도 의심)·표시 redactor가 영속 경로를 깰 수 있다 — 선행 리뷰가 P0로 짚어 분리한 보안항목을 실행할 때, 그리고 표시용 정화 함수 출력이 영속 저장소(JSONB)에도 실릴 때; 심각도는 단독 위협모델로 실측(상속하면 과투자)·표면 목적이 polarity 결정·redactor 출력은 유효 직렬화 보장(NaN→commit 깸) [inherited-severity,reframe-on-isolated-surface,threat-model-measure-not-assume,defense-in-depth-vs-p0,display-redactor-breaks-persistence,jsonb-nan-infinity,failclosed-serialization,assert-serializable,purpose-sets-polarity,probe-deeper]
- 091 비싼 연산을 막는 가드는 그 연산 *앞*에 둬라(검사지점=비용지점 앞)·정직한 타입 생산자 없으면 메타 파이프라인 말고 렌더시점 휴리스틱 — 거대/적대 입력의 비싼 동기연산을 가드로 막을 때·"타입 받아 렌더러 고르고 싶은데" 정직한 타입 생산자가 없을 때(가드를 비용지점 앞에·모든 비싼경로 덮음, 메타파이프라인 대신 렌더시점 휴리스틱+완료게이트) [covering-guard,guard-ordering,check-before-cost,non-covering-time-axis,render-time-heuristic,no-honest-type-producer,format-inference,streaming-gate,cover-all-cost-paths,proportional-fix,adversarial-codex,probe-deeper]
- 092 깨진 설정은 *크게 거부하되 좁게 공개*하라(폴백이 실패 가림·에러가 입력값 새게 함) — 선언했으나 미해결인 설정(레지스트리키·플러그인id·impl)을 런타임이 만나 무엇으로 응답·통보할지 정할 때; 미선언(default 정답) vs 선언-but-broken(거부) 가르고 후자는 서빙 거부로 표면화(폴백이 설정실패 가림), 게이트는 정책 모르게·호출측이 키선언여부로 판정(단일게이트 공유), 에러에 입력값 반영=그 값을 에러청중에 노출이라 권한비대칭(청중<값소유자)이면 쪼개라(서버로그=상세·클라=일반) [broken-config,no-fallback-masking,declared-vs-unset,refuse-loud-disclose-narrow,error-leaks-input,audience-privilege,split-log-vs-client,single-gate-policy-split,fail-closed,adversarial-codex,probe-deeper,089-sibling]
- 093 값에 의존하는 effect는 값이 안 변하면 안 돈다 / 비동기 재계산 리스트에 보존한 인덱스는 경합 — 명령형 DOM 부수효과(caret·포커스)를 useEffect로 걸었는데 의존이 결과값일 때(no-op setState=리렌더 없음=effect 미발화→보조 ref 플래그까지 미소비로 남아 다음 사이클 오염; 단조 카운터에 의존시켜라), 그리고 useMemo로 비동기 재계산되는 리스트에 외부 보존 인덱스로 접근할 때(탐색중 소스 축소→stale idx→history[음수]=undefined가 controlled draft로 샘; 접근부서 Math.min(idx,len-1) 양방향 클램프); 공통뿌리 "이벤트 발생≠관찰값 변경"이라 값동일성 단락(effect deps·onChange·Playwright fill 동일값)이 happy-path 초록에 안 보이는 구멍 [value-dependent-effect,noop-setstate-no-fire,monotonic-counter,ref-flag-leak,stale-index-race,async-recomputed-list,clamp-both-directions,event-vs-observable-value,value-equality-short-circuit,adversarial-codex,probe-deeper]
- 094 스트림 메시지 판별은 `.type` 문자열 말고 isinstance / 본문 content는 항상 str 정규화 — LangGraph messages 스트림서 메시지종류 분기·본문 누적할 때; `.type`은 청크서 바뀜(ToolMessageChunk.type=="ToolMessageChunk"≠"tool", AIMessageChunk=="AIMessageChunk"≠"ai")이라 isinstance(서브클래스 포함)로 가야 안정, AIMessageChunk.content는 str아닌 content-block 리스트일 수 있어 `"".join(acc)`가 try밖 TypeError(빈검사는 빈리스트만 거름)→정규화기 한곳(_content_text) 도구반환·본문 공유; 공통뿌리 "스트리밍은 비청크 happy-path와 타입/모양이 다름"이라 단발probe·비청크단언에 안보이는 구멍, rung①에 *MessageChunk·list content 명시케이스 넣어라, 선재잠복은 만진 줄서 적대리뷰가 표면화(변경의 결함만 아니라 변경이 밟은 코드 결함도)→같은줄·기존정규화기면 함께봉합 [stream-message,type-string-unstable,isinstance-not-type,messagechunk-subclass,content-block-list,join-typeerror,_content_text-shared-normalizer,empty-check-insufficient,streaming-vs-settled,rung1-chunk-cases,adjacent-latent-bug,boil-the-ocean,adversarial-codex,probe-deeper]
- 095 name 기반(비-FK) 참조 가드는 *링크 끊는 모든 연산* × *활성화 가능한 모든 상태* 덮고 런타임과 normalizer 공유 — 자원을 config가 name 문자열로 참조(FK아님)해 DB가 무결성 안 지킬 때; 세 축 동시 덮기(①연산: 삭제뿐 아니라 rename도 dangling 남김 operation-symmetry→닫힌집합 열거+동일헬퍼, ②상태: "archived는 dead"는 미검증 가정일 수 있음→상태전이코드 측정, activate가 archived 롤백 허용하면 archived도 live 참조라 전 버전 스캔, ③정합: 가드 판정기준=런타임 `name.in_` 해석기준 단일 normalizer 공유 안 하면 drift), 공통뿌리 "참조는 한 곳·한 연산·한 시점"이란 암묵 축소가 happy-path 초록 뒤 구멍, 적대 타자에 "보장의 여집합" 시켜라·잔존 TOCTOU 정직 호명 [name-reference-not-fk,operation-symmetry,delete-and-rename,covering-guard,archived-rollbackable-live,unverified-lifecycle-assumption,measure-state-transitions,guard-runtime-drift,single-normalizer,config-names,self-green-blindspot,verification-ladder-3rung,adversarial-codex,toctou-honest-defer,probe-deeper]
- 096 불투명 오버레이는 밑 요소가 *그 프로퍼티로* 전하던 상태를 끊는다 / 공용 렌더=레이아웃 불변식 단일 레버 — 스크롤셀 안비치게 sticky td에 불투명 background 덮으면 tr이 background로 전하던 hover(및 selected/zebra) 상태와의 연결이 끊겨 그 셀만 하이라이트 안 됨→오버레이가 그 상태 스스로 재유도해야(data-sticky 마커+hover핸들러로 배경 동시갱신, antd fixed컬럼도 CSS로 같은 재유도); 일반화="A를 B로 안비치게 덮으면 A가 B와 같은 프로퍼티로 신호하던 상태목록 세서 오버레이에 재배선" 안하면 가장자주보는 상태(hover)만 조용히 빠진 초록; overflow원천이 공용 DataTable minWidth:max-content 한줄이라 sticky+숨김을 공용에 두면 전표면(안넘치는것까지) 상속=covering-guard 프런트판; 반응형 숨김 임계는 자가판정말고 폭별 scrollWidth-clientWidth 측정으로 measure-then-tune [opaque-overlay-severs-inherited-state,sticky-cell-rehover,data-sticky,background-channel-hijack,re-derive-state,covering-guard-frontend,shared-render-single-lever,minWidth-max-content,responsive-hide-threshold,measure-then-tune,happy-path-silent-gap,adversarial-codex]
- 097 중복을 없애면 버그 수정도 covering-guard가 된다 / 비동기 결과 setState엔 요청-시퀀스 가드 — 90% 동일한 두 드로어(072·084)를 공유 셸로 통합할 때; codex가 짚은 P1(await onSearch 뒤 무가드 setOut→스코프 A 검색중 B전환시 늦은 A결과가 B화면 덮음=stale-async 스코프유출)이 통합회귀 아니라 원본 두 드로어에 *각각* 잠복하던 공통결함(git show로 확인)이라, 공유셸 reqSeq ref 가드 한 번으로 양쪽 봉합→"중복을 단일컴포넌트로 접으면 미래 모든수정(기능·버그·성능)이 전사용처 자동적용, 리팩터 배당=수정지점1"; await뒤 setState는 항상 "이 응답이 최신요청인가"(seq!==reqSeq.current→폐기) 물어야=learning093과 동근"이벤트발생≠관찰값확정", happy-path(한스코프 1회검색) 안보이고 전환·연타서만 샘; antd6 검증 셀렉터 실측함정2(아이콘+텍스트버튼 accessible name≠"검색"이라 getByRole exact=0→loose, 드로어본문=.ant-drawer-content아닌 .ant-drawer-section)—실패가 조용한 타임아웃이라 추측말고 DOM덤프 실측 [dedup-as-covering-guard,bugfix-single-lever,refactor-dividend-one-fix-site,stale-async-scope-leak,request-sequence-guard,reqSeq-ref,await-then-setstate,event-vs-observable-value,latent-common-defect,git-show-preexisting,antd6-selector-pitfall,icon-button-exact-fails-loose,drawer-section-class,silent-timeout-measure-dom,adversarial-codex,probe-deeper]
- 098 로컬필터 vs 서버검색은 "데이터 지역성"이 가른다 / 사용자 LIKE 입력엔 메타문자 이스케이프 — 같은 정직성 계약(097)이라도 판정은 반대: 097(컬렉션·메모리)은 전량 클라 로드라 로컬필터 정당, 098(세션)은 서버 페이징이라 로컬필터 부정직(현재 20행만 걸러 다른페이지 매치를 "없음" 오인)→서버 파라미터 q로 내려 페이징 이전 전체 스코프서 매칭해야 total·빈결과 정직; 가르는 축=UI아닌 데이터지역성(전량로드→로컬OK/페이징→서버필수), 새 검색UI 첫질문="목록이 전부 여기있나 페이지조각인가"; RBAC 걸린 목록엔 predicate를 스코프 WHERE **뒤 AND**로(AND는 좁히기만→스코프 못넓힘, codex도 이 위치 첫 확인)·통합검증은 *타인과 공유되는 검색어*(같은 agent_name)로 "스코프 못넘음" 실증; ilike(f"%{q}%")는 사용자 %_ 가 와일드카드로 살아 과매칭/열거오라클→escape="\\"+`\`먼저 치환, 검증은 리터럴 메타문자행+임의문자행 나란히 심어 "escape 안하면 둘다 하면 하나" 수치로 가름(happy-path 못잡음=보장의 여집합) [local-filter-vs-server-search,data-locality-decides,honesty-contract-opposite-verdict,server-paged-local-filter-dishonest,scope-where-then-and,and-only-narrows,shared-search-term-integration,ilike-metachar-escape,escape-backslash-first,adversarial-seed-literal-vs-any,happy-path-blind,adversarial-codex]
- 099 확장점을 "런타임"이 아니라 "저작시점"에 두면 신뢰경계가 보존된다 — 같은 기능("사용자가 새 에이전트 플로우를 만들어 재사용")도 확장점 위치가 보안 속성을 가름: 시각 그래프빌더는 런타임에 사용자 산출물(그래프JSON)을 로드→결국 문자열→클래스 해석 경로 생겨 "임의 eval/import 없음"(085 U5) 불변식과 충돌, 반면 저작시점 코드젠(스킬이 모듈 파일로 쓰고 레지스트리에 정적 import 2줄+재기동)은 커밋·리뷰 거쳐 신뢰경계 그대로·dict조회 레지스트리 안 건드림; 설계 첫질문="런타임에 사용자입력을 코드로 해석하는 경로가 생기나"→생기면 저작시점으로 밀어 신규 보안표면 0("저마찰 생성"은 동적로딩 아닌 저작자동화로도 달성); 무거운 UI기능(시각빌더)은 폐기가 답일 수 있다=목적만 남겨 기존 검증된 시임(Protocol+신뢰레지스트리+conformance) 위에 저작스킬로 재구현하면 표면 급감·수용게이트는 발명말고 기존 089/085 재사용(생성→자가수용 루프 닫힘)·데모 1종이 스펙 자체 실증; 코드젠 산출물의 분기/판정 로직은 노드 클로저밖 모듈 순수함수로(classify_route(text)->a/b) 빼야 단위가 실LLM없이 결정성 단언(클로저에 묻으면 값싼 단이 빔); codex엔 스코프금지경로(.claude/skills) 빼고 코드파일만(문서는 보안검토 대상 아님) [extension-point-location-decides-security,authoring-time-vs-runtime,trusted-registry-no-eval,codegen-skill,discard-heavy-ui-reuse-seam,reuse-acceptance-gate-089-085,generate-then-self-accept,pure-function-branch-testable,conditional-branch-overfit-remeasure,codex-scope-isolation]
- 100 신뢰불가 데이터는 데이터 채널에 넣어라(system 아님) / 인가 입도의 커버범위를 단언하라 — untrusted 외부출력(A2A 위임결과)을 프롬프트에 담을 때 *어느 채널*이 방어 하한을 정함: SystemMessage(최고 신뢰)에 "지시 따르지말라\n{데이터}"로 넣으면 방어지침과 데이터 속 인젝션이 *같은 채널서 경쟁*(격리 아님)→모델이 데이터쪽 집으면 무너짐; 고침=system엔 지침만·데이터는 라벨붙은 별도 Human블록(데이터채널)으로 분리, 채널경계 자체가 하한("trust=untrusted 태그"만으론 부족—태그가 데이터 *배치*를 바꿔야), happy-path엔 안보이고 적대페이로드로만 드러남; 검증도 채널경계로=조립을 순수함수(build_synthesis_messages)로 빼 "페이로드가 system엔 안들고 Human에만"을 모델없이 단언(099 순수함수규약 재활용); 교훈2=인가 "장치있음"≠"커버범위"—codex가 우회는 못찾고 입도한계(경계=`에이전트별 allowlist ∩ kind단위 RBAC`, Agent엔 owner없어 공유카탈로그라 per-cap·per-user 못막음) 짚음, 이런건 우회로 고칠게 아니라 커버범위를 docstring·스펙 비목표에 명시("설치≠덮음" 연장), codex의 "설계한계"는 코드결함과 동급으로—기각도수용도아닌 *명시화*가 답인 부류; 부수=페이크는 사용처 필드 다 채워(_FakeAgent token 누락 죽음)·신규untracked 적대리뷰는 diff말고 파일직독 [untrusted-data-channel-not-system,injection-defense-channel-boundary,dont-follow-competes-in-system,tag-must-change-placement,pure-function-assert-channel-isolation,authorization-granularity-limit,installed-guard-vs-covering-scope,document-scope-not-fix,kind-level-rbac,shared-catalog-no-owner,codex-design-limit-is-third-verdict,fake-fill-usage-fields,untracked-file-review-direct-path,adversarial-codex]
- 101 스토어 직접 시드는 write-schema 드리프트를 못 잡는다 / 스테일 --reload split-brain / 리뷰어도 검증하라 — ①지속(persistence) 검증은 실 write-schema/create 경로를 타야 함: allowlist가 `AgentConfig.config["capabilities"]`에서 오는데 그 필드가 Pydantic모델에 없어 model_dump가 조용히 드롭→저장config에 없음→브로커 발견0; 단위·그래프는 allowlist 직접주입(lambda/시드)이라 초록, verify_100 HTTP하네스마저 Agent행을 DB직접시드해 create-엔드포인트(write-schema)를 안탐→직접시드가 필드드롭을 가림; 통합rung 고유가치는 "실인프라"만으론 부족·"실 입력스키마"까지 타야 완성, 새검증 첫질문="이 픽스처는 프로덕션 write경로로 넣나 스토어에 밀어넣어 그층을 건너뛰나" ②--reload가 이전편집 안물면 in-process=신코드·HTTP=구코드 split-brain; 진단은 추측아닌 로그의 *부재*(chat중 /_remote/mcp/ 호출0=서버가 discovery 시도조차안함=구코드)→재기동; 규칙=HTTP테스트가 같은로직 in-process와 불일치하면 로직전에 스테일서버 의심·로그활동으로 확인후 재기동 ③타자검증 우선하되 타자도 무오류아님: codex가 이미 단언된 불변식(채널격리)을 뒤집는 P1을 라인 104-117(=describe())로 오인용→실제 81-100 Human채널 격리 present, 소스직독 기각; 알려진/단언된 불변식 뒤집는 P1은 수용전 소스라인 직접대조("probe deeper"의 리뷰어판, 라인인용 오프바이 가능) [seed-bypasses-write-schema,persistence-needs-real-create-path,direct-seed-hides-field-drop,integration-rung-needs-real-input-schema,stale-reload-split-brain,diagnose-by-absence-of-log-activity,http-vs-inprocess-mismatch-suspect-stale-server,verify-the-reviewer,false-positive-line-citation,probe-deeper-reviewer-variant,codex-design-limit-is-third-verdict]
- 102 전략은 impl로 갈라 공통 조상(ABC 템플릿)에 불변식 소유 / 둘째구현이 추상을 *측정* / 여집합공격은 "정직한 경계"가 답 — 교체가능한 방식(오케스트레이션 전략 등)은 **변주점 위치**가 불변식보존을 가름: 플로우 안 if로 가르지 말고(분기폭증·불변식 분기마다 재작성) **전략=각각의 impl**로 갈라 기존 선택메커니즘(config["impl"]) 재사용(새 config필드=088함정), **불변식은 공통조상 ABC가 소유**(골격·채널격리·HIL·정책재검증을 build_graph에 확정, 자식 유일구멍=`@abstractmethod` 하나=select)—구조적 Protocol 아닌 *구체 ABC 템플릿*이라 골격이 실코드로 강제, describe/build_graph 구현으로 CustomAgent 자동적합(089무회귀); 드리프트0은 주장말고 get_graph().nodes 실측; **둘째구현이 추상무누수를 측정**(039/085)=추상경계 안샌다는건 둘째구현 나올때까지 주장일뿐→첫날 2전략(①행위보존리팩터로 조상에태움 ②진짜다른전략)이 select한줄만 다르면 실증(하나면 조상이 그하나에 과적합됐는지 모름); **codex 여집합공격 성공=항상 코드결함 아님, 미문서 경계일수있다**=다중순차위임중 뒤cap interrupt→LangGraph 노드 처음부터 재실행→앞 read-only cap 재호출은 안전위반 아님(gated 부수효과는 exactly-once, interrupt-before-sideeffect가 그cap엔 그대로)이나 침묵하면 "정직히 안다룸"→답은 셋 함께(주석경계명시+안전불변식 테스트실측[H10 pre0/post1 순서무관]+스펙OUT기록)=100의 "설계한계=제3판정(명시화)" 확장; Python은 override 못막음→`@final`(타입체커·리뷰)+스킬 수용게이트(저작시점)+조상 delegate가 chosen⊆candidates 교집합(고르는자≠만드는자, 하류 broker재검증에만 기대지않고 한겹더) 겹쳐막기; 부수=discover(substring)vs rank(토큰) 마찰→query공백으로 전량매치 후 select직접검증·순환import는 runtime먼저 [abc-template-method,strategy-as-impl,common-ancestor-owns-invariant,only-hole-abstractmethod,drift-zero-measured,second-impl-measures-abstraction,behavior-preserving-refactor,complement-attack-is-honest-boundary,gated-side-effect-exactly-once,document-boundary-not-silent,final-static-enforcement,python-cant-block-override,chosen-subset-candidates-guard,chooser-not-maker,codex-design-limit-third-verdict,discover-substring-vs-rank-token,adversarial-codex,probe-deeper]
- 103 읽기전용 능력은 정책은 받고 승인은 안 받는다 / 교차입구 불변식은 공유 코어에 — ①브로커 게이트는 둘이고 별개다: **정책**(allowlist∩RBAC·deny-by-default·존재비노출)은 모든 provider에 적용(읽기·쓰기무관), **승인**(`approval_for`→HIL interrupt)은 부수효과 있을때만; 둘을 처음부터 분리(101 per-provider approval_for)해뒀기에 읽기전용 rag는 `approval_for=None` 한줄로 HIL기계 전부우회하되 정책은 완전적용→"읽기전용이니 게이트생략"으로 합쳤으면 정책까지새어 무인가검색; 시임설계시 정책·승인을 **한판정에 합치지마라**(합치면 읽기전용kind 등장순간 정책끄면 구멍·승인켜면 읽기에HIL), 분리하면 확장이조합=rag(정책O/승인X)·mcp(정책O/위험툴승인)·memory read(O/X)·memory write(O/O) ②N개입구가 공유코어 부르면 **입력 자원경계(길이·크기·top_k상한)도 코어에** 둔다: RAG검색코어를 엔드포인트·인챗도구·브로커 세입구가 부르는데 엔드포인트만 스키마4000자상한·나머지무제한→거대질의1건 임베딩provider장시간점유; 고칠자리는 세입구아닌 코어한곳(search_collections에 4000자1회→엔드포인트≤4000무영향·도구/브로커함께경계), 입구마다막으면 드리프트+새입구마다재발; 스키마검증은 그입구국소방어일뿐 공유코어 타는 다른입구엔 안미침([[cap-the-raw-source-not-the-buffer]] "누적지점에걸어라"의 "공유지점에걸어라"판·072 코어추출 drift0연장); happy-path초록은 무제한입력·빈id 여집합 못봄→적대타자가잡음 [read-only-capability,two-gates-policy-vs-approval,dont-merge-policy-and-approval,approval-for-none,extension-as-combination,shared-core-holds-input-invariant,cap-at-shared-point-not-per-entrance,schema-validation-is-local-only,drift-zero,adversarial-codex-catches-complement]
- 104 per-user 소유 자원은 능력 이름 아닌 실행 주체에 묶어라 / 새 상태 축은 모든 배선 지점을 센다 — ①공유카탈로그(agent·mcp·rag=주인없음)와 per-user 자원(메모리)은 능력 식별방식이 반대: 공유는 cap_id에 대상 박아도 됨(누구에게 노출되든 같은 데이터, 정책=allowlist∩RBAC 한겹, 100 입도한계=우회아님)이나 per-user는 치명적(`memory:앨리스`를 밥이 실행→앨리스 기억 유출, 이름이 소유자·실행자 분리)→**정공법=능력이름에 대상 안박고 소유자를 런타임 principal서 도출**: 능력 `memory:user` 하나, user_id=build_broker가 `str(principal.id)` 뽑아 주입, 검색스코프 **오직** `{"user_id":self._user_id}`(cap_id·args 어떤필드도 지정·덮어쓰기 불가)→이름으로 남 가리킬 방법 자체가 없어 교차유출 *구조적* 불가(런타임검사 아닌 표현불가능성으로 막음)+부수효과 어드민에스컬레이션 자동차단(superuser도 자기id→자기것만, 브로커능력은 대상인자 애초에 안받음·타인큐레이션은 URL대상받는 별도경로 053); 적용=per-user/테넌트 자원을 능력·도구·엔드포인트로 노출시 첫질문 "대상소유자가 식별자에 담기나 실행주체서 도출되나"—담기면 A토큰으로 B자원 가리킬 여지, 도출되면 그 여지 소멸 ②새 상태축(user_id)을 브로커에 흘리며 request-time 팩토리(build_broker)는 고치고 **resume-time 팩토리(_build_resume_broker) 놓침**→승인 interrupt 멀티턴 재개시 user_id 미주입→memory:user 사라짐(fail-closed지만 자기기억접근 재개서 깨지는 회귀); happy-path(단일턴) 안보임, 같은능력 만드는 팩토리 둘인데 하나만 새인자 받으면 그인자 필요한 흐름(resume+memory)서만 터짐→RBAC체크리스트 §1(입구열거)의 *배선판*: 새 생성자인자 더하면 그객체 만드는 모든지점 닫힌집합으로(build+resume+fixture), grep로 모든 호출처 세고 각각 새인자 옳게넘기나 확인("하나 고쳤다≠다 고쳤다", 070 "주경로 하나만 보면 샌다"의 배선판·069·070 "모든입구 소유권"의 생성입구판); 부수=자원경계(limit)는 공유지점에(103, recall_probe가 084이래 limit방어점 정수강제+clamp)·codex 여집합=cap_id·args·principal 세각도 anti-leak 전부 공격실패(오탐)+배선누락1건(실결함) [per-user-capability,bind-to-principal-not-cap-id,ownership-derived-not-named,structural-anti-leak-impossibility,shared-catalog-vs-per-user,no-admin-escalation,identifier-vs-runtime-subject,new-state-axis-all-factories,request-vs-resume-symmetry,entry-enumeration-wiring,one-fix-isnt-all-fix,limit-coerce-clamp-shared-point,complement-attack-honest-boundary,adversarial-codex]
- 105 부수효과 능력은 승인 게이트가 "구조" / 게이트는 최소 하네스로 검증 / privacy finding은 권한-델타로 판정 — ①부수효과(쓰기·삭제·전송) 능력의 안전은 프롬프트("PII 쓰지마") 아니라 **승인 게이트라는 구조**(031 처방: LLM은 "기억해줘"를 프롬프트금지보다 우선→프롬프트는 격리경계 아님): 부수효과 provider는 `approval_for` **항상 non-None**(읽기의 항상-None과 정반대), 브로커가 부수효과 이전 interrupt로 멈추고 사람승인돼야 실행, 승인 payload는 실행내용을 **마스킹없이** 노출(승인하려면 봐야—MCP 비밀마스킹과 반대); 두게이트분리(103)의 완성=읽기 정책만·쓰기 정책+승인, 확장이 조합(새 kind는 부수효과 유무만 선언) ②부수효과 게이트(reject→무부수효과/approve→정확히1회)는 **게이트를 부르는 최소 하네스**로 결정검증=전체플로우(orchestrate+실LLM) 불요, `broker.invoke`를 부르는 최소 1노드 StateGraph+MemorySaver+FakeMem-add면 interrupt/resume 실발화·부수효과 카운트로 실측(LLM·dev서버 비의존, 101 풀 HIL보다 싸고 결정적)→"테스트는 불변식을 부르는 가장 작은 실행맥락에서"(interrupt는 그래프런타임 필요하니 그래프는 쓰되 플로우로직은 뺌) ③privacy/authz finding 판정 핵심질문="이 경로가 그 주체가 *이미 다른경로로 가진* 권한/가시성을 넓히나"—안넓히면 새결함 아닌 기존경계(admin이 memory-write 승인 열람=결함처럼 보이나 admin은 이미 053 memory:manage로 임의유저 기억 전체접근→권한델타 0→명시화), 안전불변식(저장스코프=승인자무관 요청자user_id, 교차유저쓰기0) 유지되면 나머지는 권한델타로 가름(여집합 공격성공이 항상 결함아님의 authz판, 특히 권한이 이미 존재하면); 부수=자원상한(text길이)은 승인·실행 **공유헬퍼**로(승인한것==실행되는것 드리프트0, 103)·정책 민감도등급(self승인 vs admin)은 도메인판단→자가결정말고 사용자확인(outcome 제시) [side-effect-capability,approval-gate-is-structure,prompt-not-boundary,approval-for-always-non-none,unmask-for-approval-visibility,two-gates-complete,minimal-harness-gate-test,langgraph-1node-memorysaver,smallest-execution-context,privacy-finding-permission-delta,already-has-via-other-path,safety-invariant-holds,shared-helper-approved-equals-executed,sensitivity-grade-domain-decision,ask-user-outcome,complement-attack-honest-boundary,adversarial-codex]
- 106 config 필드 왕복 완전성 = 4지점 교집합 / 좋은 ID 네임스페이스는 저작 UI를 순수 포매팅으로 만든다 — ①에이전트 config 새 필드를 폼이 편집하게 하려면 **4지점 전부** 있어야 왕복이 닫힘: (1)쓰기스키마(AgentCreate 수용) (2)읽기직렬화(AgentOut+agent_to_out 방출) (3)폼초기화(configOf/initial 로드) (4)폼저장(save 조립); 스펙 106서 capabilities는 (1)만 있고(101 추가) 직렬화가 impl은 방출하나 capabilities는 **말없이 빠짐**(비대칭 누락, 형제필드만 있음)→저장은 성공하나 **편집 재열기때 사라짐**(2누락→조회응답 없음→3 빈값로드), happy-path(방금저장) 안보이고 *재열기* 다른흐름서만 터짐→처방=필드명을 schema+serializer+form 파일에 grep해 4지점 닫힌집합 확인("쓰기되니 됐다"=절반), 직렬화 비대칭누락은 형제필드 옆줄 대조로; 이건 **learning 104 "새 상태축→모든 팩토리"의 config-필드판**(104=생성자인자 build/resume, 106=config필드 write/read/form, 같은형태 "한곳고침≠다고침 닫힌집합으로") ②능력 피커에 **새 백엔드 카탈로그 엔드포인트 불요**=cap id `<kind>:<name>` 규약(100–105)이라 폼이 이미로드한 목록서 문자열포매팅 조립(memory:user/memwrite:user 고정+rag:<collection>+mcp:<server>+agt_<id> 원격만)→브로커설계때 "이름에 대상 안박고 규약으로 kind표현"(103·104)이 **저작계층서 배당**(강제 백엔드broker·표시 UI피커가 같은 ID문법 공유→UI는 포맷만); 처방=능력/권한/리소스 식별은 `<kind>:<name>` **파생가능 규약** 택하라(관리UI가 전용카탈로그 조회대신 이미가진 데이터서 조립→백엔드표면 안늚), opaque id면 저작UI마다 전용엔드포인트 필요; 부수함정=`/agents/{uuid}` 충돌(목록엔드포인트는 별도 top-level router)·vite IPv6-only(localhost=::1 접속)·antd Tag중첩 getByText 정규식 [config-roundtrip-4-points,write-read-form-intersection,asymmetric-serialize-gap,sibling-field-contrast,new-state-axis-config-field-variant,one-fix-isnt-all-fix,kind-namespace-formatting-layer,derivable-convention,authoring-layer-dividend,opaque-id-needs-catalog,uuid-path-collision,vite-ipv6-only,antd-tag-nesting]
- 107 성장하는 카탈로그를 폼에 넣을 땐: 모드 조건부 + 종류별 접이식 + 카운트 배지 — 개수가 무한정 늘 수 있는 항목(컬렉션·도구·태그·멤버)을 flat 체크박스로 나열하면 폼 높이가 카탈로그 크기에 비례(O(항목수))해 커짐→사용자 "복잡·어렵다"; **폼 높이를 카탈로그 크기와 분리**하는 세 장치: ①**모드 조건부 표시**=그 항목이 특정 모드에서만 의미있으면 그때만 렌더(능력은 오케스트레이터 impl에서만→일반 에이전트엔 아예 0픽셀=가장 강한 단순화 "안쓰는이에겐 0픽셀") ②**종류별 접이식 기본접힘**=kind마다 패널로 접어, 종류 4·각 100개여도 기본 헤더 4줄(선택있는 패널만 기본펼침) ③**접힌 헤더에 카운트 배지**(선택/전체 `1/2`·`0/12`)=펼치지 않아도 무엇이 얼마나 켜졌는지 한눈에, 배지없는 accordion은 매번펼쳐 확인해 오히려불편→배지가 접힘의 정보숨김 상쇄; 핵심=폼높이⟂카탈로그크기(flat=O(항목수), 셋=O(종류수)+펼친것); 부수=선택상태·토글·저장 배선 안건드리고 **렌더링 계층만** 교체(UX 리워크가 저장경로 안건드리니 회귀위험이 표현계층에 갇힘); 함정=antd Checkbox Playwright 토글은 라벨 텍스트span 클릭 불안정→input `.check()`(`label.ant-checkbox-wrapper … locator('input').check()`)·vite IPv6-only(106, localhost=::1); 적용=관리폼 "늘수있는 목록"은 flat 대신 모드조건부→접이식→카운트배지, UX 피드백은 AskUserQuestion 미리보기(ASCII목업)로 방식 좁혀 추측없이 즉결 [growing-catalog-in-form,conditional-display,accordion-by-kind,count-badge,form-height-vs-catalog-size,zero-pixels-when-irrelevant,pure-presentation-swap,regression-confined-to-view,antd-checkbox-check-input,askuserquestion-preview,vite-ipv6-only]
- 108 내부 아키텍처를 UI 단어로 노출하지 마라 / 같은 자원이 두 곳에 나오면 모드로 갈라라 — 능력 UI(106·107)를 사용자 세번 지적(impl 단어·MCP 두곳·"첫매치위임" 어려움) 후 뿌리; ①**시스템 분해≠유저 멘탈모델**: impl(레지스트리키)·broker·capability·kind는 구현이 세상 나눈 축, 유저는 "이 에이전트가 뭘 하나"(직접답/남에게넘김)로 생각→UI단어는 유저 축으로("실행방식(impl)"→에이전트 종류[직접응답/조율형], "능력(브로커위임)"→무엇에 맡길까요?, cap id는 화면엔 사람이름만·id는 값으로만), 내부어 새면 유저가 문서읽어야 씀→**innerText서 금지어(impl·브로커·위임)+기술id 자동단언**으로 잔여노출 측정(자가검증 회피) ②**같은 자원이 두 설정면=두 배타 런타임경로를 한화면에 올린 증상**(표면버그 아님): 코드진단하니 직접응답(DefaultUiAgent) ctx.tools(직접mcps/RAG)만·조율형(orchestrate) ctx.broker(capabilities)만→런타임은 종류따라 하나만 쓰는데 폼은 둘다 보여줘 중복필연→해법=중복 예쁘게정리 아니라 **모드(종류)가 설정면 가름**(직접응답=직접자원칸만, 조율형=위임칸만→자원 정확히 한곳); 처방=폼서 같은대상 두번 나오면 "두 런타임경로 한화면?" 코드확인→맞으면 모드로 배타렌더(배타경로 한화면=유저 "왜 두번?" 영영 이해못함) ③**반복 UX지적은 표면신호**: 세번 지적받고서야 뿌리팜, 첫수정(접이식)·둘째(카운트)는 표면만→같은종류 불만 두번=틀 잘못잡음 신호(더다듬어라 아님), 표면 또손대기전 아키텍처↔유저멘탈모델 간극 의심; 부수=UX명명은 AskUserQuestion 미리보기(ASCII목업)로 유저가 기준단어 직접선택→추측제거후 폼재구성·표현계층만 교체(회귀 뷰에 갇힘 107) [no-internal-jargon-in-ui,system-decomposition-vs-user-model,ui-words-follow-user-axis,measure-jargon-absence-innertext,duplicate-resource-two-exclusive-paths,mode-splits-config-surface,diagnose-duplication-in-code,repeated-feedback-is-framing-signal,probe-root-on-second-complaint,askuserquestion-preview-naming,pure-presentation-swap]
- 109 cross-surface UI 일관성은 재사용 컴포넌트로 / 어댑터로 이질적 상태를 하나로 흡수 — 사용자가 "같은 UX를 두 곳(등록 폼·오버라이드)에"를 원할 때; ①**"여기도 저기도 같게"=복붙아닌 컴포넌트 추출**: 각 화면 복붙은 즉시 drift(한쪽만 고침, 106 비대칭배선 UI판)→컴포넌트 뽑으면 렌더/검색/카운트 한곳=두 소비처 항상 동일·세번째도 공짜(`PickerGroups`=접이식+카운트+검색 추출, "오버라이드에도 반영"=`<PickerGroups/>` 한줄로 끝), **"일관되게"=컴포넌트 추출 신호** ②**어댑터(prefix+toggle 라우팅)로 이질 저장 흡수**: 같은 UI인데 뒤 저장구조 다름(등록 직접자원 4배열·오버라이드 2배열·능력 1배열)→storage 통일 불요, id를 카테고리 prefix 네임스페이스(mem:/col:/perm:/tool:)+컴포넌트엔 `selected:string[]`(prefix합집합)·`onToggle(id)`만, 소비처 onToggle이 prefix 떼어 올바른 배열로 라우팅→컴포넌트는 **storage-agnostic**(단일 계약), 소비처가 얇은 어댑터로 연결, 저장스키마·왕복 무변경(표현계층만=회귀 뷰에 갇힘 107) ③**폼 위계=필수/공통→종류별→선택(접힘)**: "뭘 꼭 정하고 뭘 나중에"가 안보이면 평평, 세켜=기본(매번 정하는 소수 항상보임)·종류별(모드가 가르는 핵심 108 접이식)·세부(튜닝 기본접힘 평소감춤); 함정=antd v6 Collapse 헤더텍스트 `.ant-collapse-header`(‑text아님)·체크박스 `label.ant-checkbox-wrapper input.check()`·플그 오버라이드 폼은 ui에이전트만(code/external read-only); 적용=같은 UX 2곳+면 컴포넌트 추출(복붙금지), 저장 달라도 prefix+어댑터로 흡수·storage 그대로, 폼은 필수→종류별→선택(접힘) [reusable-component-for-consistency,cross-surface,copy-paste-causes-drift,extract-on-consistency-signal,adapter-prefix-routing,storage-agnostic-component,single-selected-ontoggle-contract,pure-presentation-swap,form-tiers-required-typespecific-optional,antd-v6-collapse-header,ui-agent-only-override]
- 110 "각 고리 증명" ≠ "체인 증명" / E2E는 실제 principal(인증 컨텍스트)을 재현해야 — 브로커 전 체인 각 고리 따로 검증됐으나 채팅 EP 관통은 미실측(스펙 110); ①**링크별 초록≠체인 증명**: A·B·C 각각 초록이어도 A→B→C 한 경로 실행 전엔 이음매(글루·컨텍스트 전달·전제) 미검증, 첫 E2E가 곧바로 진짜 이음매 드러냄=**위임은 유저 세션만·머신토큰 deny-by-default**(단위/그래프 102엔 principal 안 나와 미검출)—엔드포인트 관통만이 "누가 호출했나"를 경로에 넣음(verification-ladder 통합 rung의 브로커판) ②**E2E는 실제 인증 컨텍스트 재현 안 하면 거짓 음성 오진**: 처음 dev 머신토큰 Bearer로 짜서 위임 공집합(delegated:<0자>)→"위임 안 되나" 오진 직전, 원인은 코드결함 아닌 인증컨텍스트(build_broker rbac_allows가 string principal 전부 deny)=**인증 주체 타입이 계약의 일부**(같은 요청도 principal 다르면 결과 다름), 프로덕션과 다른 principal E2E는 초록이든 빨강이든 거짓신호→해결 `app.dependency_overrides[current_principal]=lambda:슈퍼유저스텁`(채팅만 유저세션 재현·다른 EP Bearer 유지·is_superuser 우회로 capability:rag 허용) ③**시연=결정적 테스트+시각 두겹**: 위임(delegate 노드)은 LLM 독립→mock-llm 결정적(synthesize만 LLM), verify_110(broker_invoke 존재+무위임 대조=회귀가드)+인스펙터 스샷(사용자 납득); 함정=broker.discover lexical 부분일치(`q in "name id hook"`)→문장질의 발견실패, 질의는 cap id 부분문자열(컬렉션명)로·플그 선택기 커스텀(헤더 클릭 후 이름); 적용=체인 만들면 관통 E2E 한번은 돌려라(링크별 초록 대체 금지)·E2E는 프로덕션과 같은 인증컨텍스트(다르면 dependency_overrides로 실경로와 같게)·LLM무관 경로는 mock 결정화+시각증거 [links-proven-not-chain-proven,run-the-whole-path-once,seams-only-show-on-traversal,auth-context-is-part-of-contract,principal-type-changes-result,e2e-must-reproduce-real-principal,false-negative-from-wrong-auth,dependency-overrides-current-principal,mock-deterministic-for-llm-independent-path,test-plus-visual-evidence,lexical-discover-substring,custom-agent-selector]
- 111 가드가 초록이어도 그 가드가 안전한 *이유*(하중 가정)는 따로 검증해야 — 브로커 memedit 소유권 검사 후 삭제, 교차유저 거부 테스트 초록이나 codex가 한 겹 더; ①**가드 초록≠전제 검증**: user_owns 확인 후 delete(mem_id 전역 호출)가 원자적 아님, 교차유저 거부는 초록("남의 id 거부")이나 "확인한 행==삭제되는 행"의 *전제*는 테스트가 안 봄→그 하중가정(mem_id 전역 UUID+소유자 불변)이 어디에도 안 적혀 있었음, 자가검증이면 초록에 만족하고 넘어갔을 것; 방어 짤 때 "이 방어는 뭘 가정? 깨지면?"을 명시, 특히 check-then-act는 "확인한 것==실행되는 것" 전제를 적어라 ②**정직화=가정명시+안전불변식+OUT 셋 다**(complement-attack-can-be-honest-boundary): codex [P1]은 새 안전위반 아님(기존 HTTP도 같은 check-then-act, mem0 API에 scope 원자 mutation 없어 재구조화 불가)=정직한 경계→가정 코드주석 명시(왜 안전+깨는 백엔드는 fail-closed로 scope 원자 제공)+안전불변식 테스트(교차유저 거부)+OUT 기록, 셋 중 하나만이면 부족(주석만=회귀 못잡음, 테스트만=왜 안전한지 안 남음, OUT없음=덜짠 것과 의도경계 구분 안됨) ③**codex exec 출력 라우팅 함정**: 최종 agent_message가 stdout에 안 실림(--json도 불안정)→포그라운드+`2>&1` 병합+넉넉한 타임아웃(540s, Bash 570000)으로야 verbatim, 백그라운드 nohup+stdout 리다이렉트는 빈파일; 적용=방어(check-then-act·소유권 가드) 짜면 하중가정 명시+깨지는 조건과 fail-closed 동작 기록, 적대 타자에 "가드가 안전한 *이유*를 무너뜨려봐" 시켜라(가드 자체 아니라), 여집합 성공해도 안전위반 아니면 정직화 3종 [verify-load-bearing-assumption,guard-green-not-premise-verified,check-then-act-premise,honest-boundary-triad,comment-plus-invariant-test-plus-out,adversarial-attacks-the-why,codex-exec-stdout-routing,foreground-2>&1-long-timeout]
- 112 소유권은 "관리"를 막지 "사용"을 막지 않는다 / 게이트 추가가 기존 열린 문을 드러낸다 — 공유 카탈로그에 owner 붙일 때; ①**사용≠관리 축**: 소유권으로 브로커 invoke 막으면 admin이 만든 공유 에이전트를 member가 못 써 깨짐→사용은 per-cap RBAC(공유 가능)·소유권은 편집/삭제 게이트에만, 틀린 축에 도구 대면 멀쩡한 공유 깨짐(codex가 짚은 invoke 빚은 사용 축이라 per-cap이 갚음, 소유권과 무관) ②**게이트 추가=모든 변경 입구 재열거**: 관리 게이트 달다 카탈로그 변경 라우트가 인증만(아무 member나 남의 것 수정/삭제)인 현존 구멍 드러남→닫음, 주 CRUD만 보면 샘(codex가 빠뜨린 메모리 변조 3라우트 P1로 잡음=066→068 재판) ③**404-fold는 status 아니라 body**: 미존재 "agent not found" vs 비소유 "not found"면 body 차이로 존재 샘(068), `not_found_detail`로 라우트 존재-404와 일치, "404 반환"만 보는 테스트는 통과하고 누출→body 단언 ④**인자 즉시평가가 지연 단락 무력화**: `get_enforcer()`를 인자로 넘기면 특권이라 불필요해도 평가→ASGI 테스트(lifespan 미가동) RuntimeError, 헬퍼가 지연 로드(특권 먼저 단락·미초기화 비특권 폴백), 비싼/실패가능 값은 분기 안 지연 획득 ⑤**머신토큰 특권은 맥락마다 다름**: 오케스트레이션 deny vs 관리 특권 둘 다 정상(같은 principal도 "무엇에 대한 권한"이냐로 갈림); 적용=공유자원 소유권은 사용은 RBAC·관리만·게이트 추가 시 모든 변경 입구 적대 타자로 재열거·404-fold status+body 둘 다·비싼 값 인자 즉시평가 말고 분기 내 지연 [ownership-gates-management-not-use,use-vs-manage-axis,gate-reveals-open-door,reenumerate-all-mutation-entries,404-fold-body-not-just-status,not-found-detail-match,eager-arg-defeats-lazy-shortcut,lazy-enforcer-load,machine-token-context-dependent,adversarial-finds-missed-entries]
- 113 confused deputy는 "누가 그 참조를 넣었나"로 인가 / 신원은 권한조회 키로 쓰기 전 형식 검증 — 이름/id 참조로 특권 자원(크레덴셜) 쓰는 경로(채팅 런타임 tool 배선, 112 P0); ①**참조 넣은 주체로 인가**: config mcps/vectorTables 이름으로 MCP/컬렉션 로드해 복호 크레덴셜 배선하던 무인가 경로→주체는 그 이름을 넣은 사람=에이전트 **작성자**(owner_id), 채팅 사용자로 막으면 공유 에이전트 깨짐(112 갈래 재적용, 사용자 축은 브로커 per-cap) ②**호출자 입력이 작성자 권한 빌리는 2차 confused-deputy**: 작성자 권한 게이트를 override(세션 덮어쓰기)가 우회—호출자가 overrides.mcps에 남의 비공개 MCP 주입하면 작성자 권한으로 통과(codex P0)→**"config에 이미 있던 것=작성자·이번 요청이 넣은 것=호출자(own)"로 분리 인가**(override 병합 전 저장본 포착), 캘러 제어 입력이 저장상태에 섞여 downstream 권한에 태워지는 지점 의심 ③**신원 문자열은 권한조회 전 형식 검증**: owner_id="admin"(비정상 스탬프)이 casbin enforce에서 admin **역할 이름**과 우연 매칭→특권 오판(codex P2), UUID 파싱을 casbin보다 **먼저**(식별자/역할 이름 공간 충돌 차단, owner_of는 UUID/None만) ④**같은 RBAC 레버가 두 경로 커버**: `_rbac_check`(112 A) 런타임 배선서 재사용→admin per-cap grant가 브로커·배선 양쪽 동일하게 먹힘, 술어 단일화(drift0)라 새 경로도 같은 레버로 덮임·헬퍼는 enforcer 지연로드(미초기화 fail-closed); 적용=이름/id로 특권 자원 쓰는 경로는 참조 넣은 주체로 인가(저장본=작성자·요청 주입분=호출자)·캘러 입력이 저장상태 병합돼 권한에 태워지는 지점 분리 재인가·신원은 권한조회 전 형식검증·인가 술어 단일 헬퍼(새 경로 자동 커버) [authorize-by-who-supplied-the-reference,confused-deputy,author-not-caller-for-stored,caller-authority-for-injected,override-injection,split-stored-vs-request-refs,uuid-validate-before-authz-key,identifier-vs-role-namespace-collision,rbac-lever-covers-both-paths,single-authz-predicate,lazy-enforcer-fail-closed]
- 114 게이트된 동작은 *모든* 렌더 지점을 막아야 / 세션서 못 만드는 상태는 응답 인터셉트로 검증 — 백엔드 게이트(112)를 UI에 반영(소유 표시+비소유 버튼 숨김); ①**백엔드가 판정 소유·프론트는 필드만**: can_manage를 프론트가 재계산하면 casbin admin 역할 모름(is_superuser만)→오판, 백엔드가 each Out에 can_manage 실어 내리고 프론트는 `item.can_manage`만 읽음(may_manage=assert_may_manage의 불리언 형제, 단일 술어, 새 프롭 스레딩 불요) ②**게이트된 동작은 모든 렌더 지점 가드**(112 입구열거의 UI판): can_manage=false로 버튼 숨길 때 한 곳만 막으면 샌다—상세 드로어는 막고 **목록 행 액션 컬럼 삭제 아이콘을 놓침**(백엔드 게이트로 눌러도 404지만 "할 수 있는 척"=거짓 affordance), 같은 동작이 여러 표면(리스트행·상세·버전패널·문서패널)에 렌더되면 전부 열거해 각각 가드 ③**세션서 자연발생 안 하는 상태는 응답 인터셉트로 결정적 검증**: admin=superuser라 can_manage=false가 안 나옴→Playwright `page.route`로 /agents 실응답 받아(route.fetch) 한 항목에 owner_id=타인·can_manage=false 주입해 렌더 경로 실측(전부 목킹은 취약, 한 필드 변형이 견고) ④**브라우저 눈검증이 자가검토 누락 잡음**: 스샷서 비소유 행 휴지통 남은 걸 보고서야 리스트 액션 누락 발견(코드만 보면 "드로어 막았으니 됐다"고 넘어감), 렌더 지점 누락은 tsc/grep 안 잡힘; 함정=BlocksView 목록 소스는 `/mcp-servers` 아닌 `/blocks`→can_manage를 `/blocks` mcp 매핑에도·OwnerTag는 owner 개념 있는 카테고리(mcp)만; 적용=백엔드 불리언 each 객체에 실어 프론트 표시만·게이트 동작 모든 렌더 지점 전수 가드·도달 어려운 상태는 응답 인터셉트 한 필드 변형·UI 게이팅 브라우저 눈검증 [guard-every-render-site,reenumerate-ui-render-sites,backend-owns-predicate-frontend-displays,may-manage-boolean-sibling,route-intercept-for-unreachable-state,mock-one-field-not-everything,browser-catches-what-static-checks-miss,false-affordance,blocks-endpoint-is-list-source]
- 115 위조 불가 경계는 "비밀 구분자"로 만들되 *경계의 신뢰 구성요소도* 정규화해야 — 신뢰/비신뢰를 텍스트로 섞을 때(위임 결과 fold 출처 라벨); ①**위조 불가 구분자=untrusted가 못 보는 요청별 랜덤 nonce**: 신뢰 표식을 데이터와 섞으면 데이터가 스푸핑(가짜 `## 능력:` 헤더), 각 데이터를 `⟦BEGIN nonce⟧…⟦END nonce⟧`로 감싸고 nonce를 요청별 랜덤+untrusted 미노출→데이터가 nonce 몰라 펜스 못 깨고 새 구획 못 만듦(진짜 경계 위조 불가), 정적 구분자는 흉내 가능해 무의미=핵심은 비밀성+요청별 유일성(LLM 완벽준수는 별개 모델 품질, 이건 경계 자체를 위조 불가로 만든 구조적 하한) ②**경계는 가장 약한 구성부분만큼만 강함**(codex P1): 콘텐츠를 펜스로 가둬도 펜스 밖 라벨을 cap.name으로 raw 보간하면 헛일—name이 자원명(MCP/RAG/agent, 소유자 제어)이라 개행+`## 능력:` 심어 라벨 줄 위조, 데이터 가두기와 별개로 신뢰 측 구조 이루는 모든 값(라벨 name/id)을 한 줄로 접고 펜스 브래킷 제거, "신뢰 측"이라 믿은 값이 실은 attacker-영향 자원명이었음→무엇이 진짜 신뢰 소스인지 끝까지 따져라 ③**방어 지침은 실제 데이터 모양과 일치**(codex P2): "출처는 ⟦BEGIN⟧ 앞 라벨"이라 지시하며 단일은 fence 없이 raw로 흘리면 악의적 단일이 심은 가짜 ⟦BEGIN⟧을 출처로 오인, 지침이 설명하는 구조가 실제 있을 때만 그 지침을 내고(펜스 있을 때만) 단일도 펜스해 항상 일치—방어 설명과 데이터 형식 어긋나면 그 틈이 새 공격면 ④**랜덤은 순수 함수에 넣지 말고 주입**: fold_results 순수 유지(결정성)—nonce는 인자로 받고 생성은 노드(부수효과 경계) 담당, 조작적 검증=고정 nonce로 세그먼트 파싱해 "주입 콘텐츠가 진짜 경계 못 만듦" 단언; 적용=신뢰/비신뢰 텍스트 혼합은 요청별 랜덤·비노출 nonce 펜스로·경계의 신뢰 측 구조값도 정규화(attacker-영향인지 확인)·방어 지침은 데이터 형식과 일치+형식 균일화·랜덤은 순수함수 밖 주입 [unforgeable-boundary-secret-per-request-nonce,static-delimiter-is-useless,nonce-not-exposed-to-untrusted,boundary-only-as-strong-as-weakest-part,sanitize-the-trust-side-structure,label-name-is-attacker-influenced,defense-instruction-must-match-data-shape,uniform-format-single-too,random-injected-not-in-pure-function,operational-definition-of-unforgeable]
- 117 "고리가 돌았다"와 "협업이 성공했다"는 다르다 / opt-in 정책값도 write-schema를 관통해야 — 에이전트→에이전트(A2A) 위임 실증·부수효과 게이트 추가할 때; ①**노드 실행(invocation)≠협업 성공**: broker.invoke는 원격 에러도 프레임으로 기록→노드 타임라인은 성공/실패 무관하게 남음→"노드 존재"만 단언하면 **실패한 협업도 초록**(verify_100 H4는 원격 도달불가라 위임만 봄), 진짜 실증=원격을 결정적 성공시켜(a2a_stream 패치) 대상 kind 노드+**정확히 1회 호출**+**질의가 원격에 도달(이음매)**+종합 발화 넷을 함께, "링크별 초록≠체인 증명"(110)의 한 겹 더=체인이 돌았음≠성공했음(happy-path 초록이 실은 error-path 초록일 수) ②**정책 결정 config 값은 write-schema(스키마) 관통 보존 필수**: 게이트를 대상 config.requires_approval opt-in으로 뒀는데 AgentConfig 스키마에 필드 안 넣으면 create/update의 model_dump가 **조용히 드롭**→게이트 영구 비활성(브로커 항상 None), 테스트가 DB 직시드하면 못 잡음(초록, learning 101 seed-bypasses-write-schema 재발)→정책 소스 config 값은 **API 라운드트립(create→저장→read) 보존 실측**+스키마 필드 명시(브로커가 read하는 모든 config 키는 write-schema에 대응 필드=읽기·쓰기 스키마 정합) ③**opt-in 기본값이 무회귀의 열쇠**: 승인 게이트를 "항상"으로 두면 조율형 상시 위임 매번 막혀 흐름 깨짐→대상별 opt-in 불리언(기본 False)=명시적으로 켠 것만 게이트·나머지 현동작 보존, MCP `_APPROVAL_ACTIONS`(툴 단위 opt-in)의 에이전트 단위 형제("부수효과 게이트는 대상이 스스로 켠다") ④**시그니처 넓힐 땐 이미 있는 값을 넘겨라(재조회 말고)**: approval_for가 대상 config 읽어야 하나 sync라 DB 재조회 불가→캐시/재조회 아니라 invoke가 이미 받는 row를 approval_for에도 넘기는 시그니처 통일((row,cap_id,args)), 호출부가 바로 앞 resolve한 값 재활용·정규화 헬퍼(_a2a_text) 공유로 "승인한 것==전송되는 것"(현재 args 한정) ⑤**"승인==전송" 등식은 재실행 간 args 안정 전제→성립 범위로 좁혀 문서화**: 헬퍼 공유는 현재 args 드리프트만 닫음, 재개마다 args 새로 만드는 호출자엔 승인 payload(X)vs전송(Y) 갈림(codex), 기본 orchestrate는 state["query"](116 pending 커밋)라 안정→보장을 그 경로로 좁혀 문서화·일반보장은 payload에 args 스냅샷/해시 바인딩(후속), 넓게 주장 말고 실제 성립범위로(116 ④ 계열); 적용=체인 실증은 "노드 존재" 아니라 성공 경로 결정적 재현(원격 성공·호출횟수·이음매·산출)·런타임이 read하는 config 키는 write-schema 필드+라운드트립 보존 실측(seed 우회 금지)·부수효과 게이트는 대상별 opt-in 기본off·sync 콜백엔 resolve된 값 시그니처로·"승인==실행"은 성립범위로 좁혀 [invocation-vs-collaboration-success,node-present-includes-failed,deterministic-success-path,run-success-not-just-traversal,policy-config-must-survive-write-schema,api-roundtrip-persistence-assert,seed-bypasses-write-schema,read-write-schema-parity,side-effect-gate-optin-default-off,mcp-approval-actions-sibling,pass-resolved-value-not-refetch,signature-unify-row,approved-equals-sent-shared-normalizer,equation-holds-only-for-stable-args,narrow-the-guarantee-scope]
- 118 부가 계층은 inert-until-configured / 헬퍼 보장은 받아들이는 타입 계약 전체를 덮어야 — 관측·계측 등 부가물을 핵심 경로에 배선할 때; ①**부가물은 설정될 때만 켜지고 실패해도 핵심 경로 안 깸**: 관측은 부가물이지 하중 아님→세 겹으로 접음=설정 게이트(필요 env 키 둘 다 있을 때만 활성·없으면 no-op [])·graceful(패키지 미설치·핸들러 생성 실패도 예외없이 no-op, import·생성 둘 다 try/except)·병합 비파괴(config에 얹되 기존 callbacks/metadata/configurable 안 덮고 확장·미설정 원본그대로), "키가 스위치"(패키지는 설치·키 넣는 순간 활성, 외부 비밀은 env만 받고 값은 코드/로그/응답 절대 미노출) ②**헬퍼의 "보장"은 받아들이는 타입 계약 전체에 성립**(codex P2): with_trace가 "callbacks 보존·확장·무예외" 보장하며 `[*cfg.get("callbacks",[]),*cbs]`로 짰으나 list일 때만 안전, 받는 타입(RunnableConfig.callbacks)은 list|CallbackManager|None→None·비-iterable이면 splat TypeError로 보장 파탄, **현 호출처가 그 값을 안 넘긴다고 안전한 게 아님**=헬퍼 보장 범위는 호출처의 현재 습관 아니라 파라미터의 타입 계약→타입별로 접어(None→[]·list→extend·그외→wrap) 어떤 입력에도 무예외, "지금 안 터진다"≠"보장한다"(117 ⑤ 성립범위 계열) ③**config에 얹을 땐 프레임워크 예약 키(configurable.thread_id) 보존 실측**: 관측 콜백 병합하며 configurable(재개 키) 드롭하면 HIL 재개 깨짐→dict 확장으로 보존 보장+thread_id가 콜백 metadata로 실제 전달되는지 최소 그래프 실측(LangGraph가 자동 forward), 얹기는 항상 "기존 보존+내 것 추가"이고 그 보존을 조작적 검증 ④**외부 계측은 콜백이 실제 실행에 꽂히는지까지 관통**(리스트 반환 아님): trace_callbacks가 리스트 돌려주는 것과 콜백이 실 그래프 실행서 이벤트 받는 것은 다름→Recorder 콜백을 with_trace로 주입해 astream 돌리고 on_chain_start 수신 단언(실 서버 불요)+미설정 실행은 미수신·정상완주로 무회귀; 적용=부가 계층(관측·계측·플래그)은 설정게이트+graceful+비파괴병합 3겹으로 상시 배선해도 안전·헬퍼 무예외 보장은 파라미터 타입계약 전체(현 호출습관 아님)로 회귀가드·config 병합은 예약키 보존 조작적 검증·외부계측은 콜백이 실행에 꽂혀 이벤트 받는지까지 [inert-until-configured,addon-not-load-bearing,config-gate-graceful-nondestructive,keys-are-the-switch,secret-env-only-never-logged,guarantee-covers-full-type-contract,not-safe-just-because-callers-dont,now-doesnt-throw-not-equal-guaranteed,preserve-framework-reserved-keys,thread-id-forwarded-to-metadata,callback-delivery-passthrough,record-callback-events-not-list-return]
- 119 평가/신호 도구의 대죄는 "조용한 초록" — 모든 경로가 fail-closed여야 — 평가 하네스·게이트·검증 신호 도구를 만들 때; ①**신호 도구는 거짓 초록이 신호 없음보다 나쁨**: 하네스 수치는 자율 루프(Ralph)가 최적화하는 대상→실수/실패가 점수를 올려 보이는 "조용한 초록" 경로는 치명(루프가 거짓 향해 최적화, 없느니만 못함)→모든 경로 fail-closed: 빈 asserts→자동통과 금지(all([])=True라 무조건 pass→명시 실패)·숨은 실행실패→error 표면화(러너가 HTTP 401/500 status 확인 없이 error=False+빈출력 돌려주면 negative case trace_lacks+no_error 거짓통과)·scorer 예외→그 assert 실패(malformed obs/커스텀 scorer 버그가 run_eval 통째 raise 금지, 개별 예외는 그 assert만 실패로 접고 계속)·run_fn 예외→error 관측으로 접고 뒤 case 계속 ②**codex는 신호 도구에 특히 값짐**(조용한 초록은 happy-path 테스트를 정의상 통과→통과 case만 있는 verify로 안 드러남): 판별력을 실측(틀린 assertion·실패 관측이 점수를 *내리는가*)+적대타자에 "실수가 점수 올리는 경로" 맡김, 판별 테스트=통과 case+의도적 실패 case 함께 둬 숫자 움직임 확인 ③**결정적 수치의 하한은 행위·구조 신호(LLM 생성텍스트 아님)**: LLM 출력 비결정→그대로 채점하면 수치 흔들려 자율 신호 못 됨→행위(trace 노드=어느 능력 호출/위임, 085/100 관측 재사용)·구조·error 신호를 하한(110 "LLM 무관 경로 mock 결정화" 연장), LLM-judge는 비결정 별도축 후속, 결정성이 곧 자율 전제 ④**재사용 규약=데이터셋 × 실행함수 분리**: run_eval(cases,run_fn)로 scorer/집계(generic)와 실행어댑터(run_fn) 분리→어떤 실행경로(HTTP·그래프직접·미래표면)든 run_fn만 갈아끼워 같은 수치틀, 단 어댑터가 실패 숨기지 않게(규약의 약한고리=어댑터); 적용=평가/게이트/신호 도구는 모든 경로 fail-closed(빈조건·숨은실패·예외 전부 점수 내림)·판별력 실측+조용한초록을 적대타자에·결정수치는 행위구조 하한(LLM텍스트 채점 지양)·재사용은 데이터셋×실행함수 분리하되 어댑터가 실패 안 숨김 [signal-tool-fail-closed,silent-green-worse-than-no-signal,empty-condition-must-fail,hidden-failure-must-surface,scorer-exception-is-assert-fail-not-abort,codex-for-signal-tools,discrimination-measured-intentional-fail-case,deterministic-behavior-signal-not-llm-text,determinism-is-autonomy-prerequisite,dataset-times-runfn-adapter,adapter-is-weak-link]
- 120 복제는 use/manage 축의 새 동사 — 권한·렌더를 그 축으로 맞춰라 — 기존 자원에 새 동사(복제·공유·import) 붙일 때; ①**새 동사는 "사용이냐 관리냐" 먼저 판정**: 스펙 112 축=사용(공유가능·RBAC) vs 관리(소유자/특권만), 복제=읽기+새생성이지 원본 변경 아님→원본 **관리 권한 불요·가시하면 복제**→(a) 백엔드는 assert_may_manage를 원본에 안 걸고 존재만 404-fold (b) 프론트 복제 버튼은 can_manage 게이트 **밖**(비소유 공유 에이전트도 복제=재사용 핵심), 새 동사를 기존 게이트(삭제·편집 옆이라 can_manage)에 습관적으로 묶으면 재사용 막음→**동사마다 축 재판정** ②**생성형 동사는 소유권 새로 스탬프(승계 금지)**: 복제본은 복제자 소유(069 no-takeover), 복제·포크·import 등 "기존 것에서 새 것 만드는" 동사는 생성시점 소유자 스탬프, 원본 owner 물려주면 "내가 만든 내 복제본을 내가 관리 못 함" 모순 ③**복사는 "행위 설정"만 — 등록·비밀 스냅샷 떼어냄**: 원본 config 복사하되 등록특화 키(card 등) 제거·비밀(endpoint/token)은 애초 별도 컬럼이라 config 복사에 안 딸려옴→복제본은 안전한 ui 행위 에이전트, "복사" 대상은 무엇을 하는가(페르소나·모델·능력)이지 어디 등록됐나/무슨 크레덴셜 아님 ④**같은 버튼이 여러 렌더 변형에 있으면 전부 배선(하나만 하면 놓침)**: 상세 드로어가 소스별(ui/code/external) 3 변형이라 복제 버튼을 ui에만 달면 원격 재사용 빠짐→세 변형 모두 배선(114 "브라우저가 놓친 렌더 지점" 사전적용), 새 액션은 "이 UI가 몇 개 렌더 경로냐" 먼저 세고 전부 덮되 브라우저로 실렌더 확인; 적용=새 동사는 use/manage 축에 먼저 놓고 권한·UI 게이트를 그 축으로·생성형은 소유권 새 스탬프(승계금지)·복사범위는 행위설정로 좁히고 등록/비밀 떼어냄·같은 액션 렌더변형 세어 전부 배선+브라우저 확인 [new-verb-judge-use-or-manage-first,clone-is-read-plus-create,needs-visibility-not-manage,button-outside-can-manage-gate,reassess-axis-per-verb,generative-verb-restamps-ownership,creator-owns-not-inherit-069,copy-behavior-not-registration,strip-card-secrets-separate-column,wire-all-render-variants,count-render-paths-then-cover,browser-verify-actual-render]
- 116 StateGraph에서 노드 경계 = 커밋 단위 = 재개 멱등 단위 — LangGraph 순차 작업 중 interrupt(승인 대기) 가능할 때; ①**노드는 완주(return) 시만 state 커밋**: 노드 안에서 interrupt하면 그 실행의 부분 반환 통째 폐기·재개 시 노드 처음부터 재실행→한 노드 loop로 N작업 돌리면 뒤 작업 interrupt가 앞 작업 커밋 되돌림(앞 작업 재실행), "재개돼도 유지될 것"은 interrupt하는 노드보다 **앞선 완주 노드에 커밋**하거나 작업 쪼개 **작업 하나=노드 실행 하나** ②**재개 멱등=각 작업 단위를 체크포인트 경계로**(self-loop): 노드를 자기 자신으로 되돌리는 self-loop로 매 반복을 노드 경계화→완료 단위는 state(리듀서 누적)에 커밋돼 재개 시 재실행 안 됨, pending(대기)/done(operator.add 누적) 두 필드+conditional edge(pending 남으면 자기 루프·비면 다음), interrupt하는 단위 자신만 재실행·부수효과는 interrupt-before-sideeffect로 1회 ③**interrupt 이전 미확정 "선택"은 재개 시 재계산돼 뒤바뀜**: 커밋 안 된 결정(discover/select)이 재개 시 재실행되면 그 사이 환경(카탈로그·정책·권한) 변화로 선택 달라짐(승인한 A 대신 B 실행·A 소멸로 승인만 남고 실행 누락), 방어=선택을 부수효과/interrupt 노드보다 **앞선 노드에서 확정·커밋**(plan 노드가 discover/select→pending을 delegate 이전 커밋, 재개는 interrupt된 노드서 이어져 앞 노드 재실행 0)→"결정 코드"와 "부수효과 코드"를 노드로 가름 ④**fail-closed(안전)≠완전성**: 다중 승인 미지원(2번째 gated cap 승인 row 안 뜸=고아)은 완전성 결함이지 안전 결함 아님(interrupt-before-sideeffect가 미승인 부수효과 차단→무단 실행 0), 경계 OUT 문서화 시 "안전 유지·완전성만 미지원"을 분리 검증(verify H5가 두번째 gated 부수효과 0 실측)→안전 불변식만 지켜지면 완전성 갭은 정직하게 후속; 적용=재개 유지값은 앞선 완주 노드/self-loop로 커밋·순차 중단가능 작업은 pending/done 리듀서+conditional self-loop·선택은 부수효과 노드 이전 확정·경계 OUT는 안전(fail-closed)과 완전성 갈라 안전 불변식 별도 테스트 [node-boundary-is-commit-unit,interrupt-discards-partial-return,self-loop-per-work-unit-checkpoint,pending-done-reducer-conditional-edge,selection-must-commit-before-sideeffect,decide-code-vs-sideeffect-code-split,fail-closed-not-equal-completeness,safety-invariant-separate-test]
- 121 가드 완화는 형제 가드와 재정렬 + 라우트 게이트 추가는 직접호출 테스트를 조용히 깬다 — 같은 "참조 중" 개념을 답하는 가드가 둘 이상이거나 라우트에 횡단 게이트(principal/RBAC) 추가 시; ①**형제 가드는 같은 스코프로 답해야**(삭제=전버전 vs 배지=활성 어긋남이 UX 벽), 판정을 단일 함수/스코프로; ②**라우트에 게이트 추가하면 직접호출 *기존* 테스트가 시그니처·게이트로 깨져 조용히 red**(verify_093이 112 assert_may_manage로 404-fold), 게이트 스펙은 그 라우트 모든 호출처(테스트 포함) 같은 턴에 쓸기 [sibling-guard-realign,active-only,route-gate-breaks-direct-test,silent-red,sweep-all-callers,rerun-full-verify-suite]
- 122 kind별로 표면을 가르면 모든 소비 표면에 전파하라(안 하면 다른 kind 데이터 조용히 누락) — 자원을 에이전트 kind별 다른 필드에 담기 시작하고 그 자원 만지는 표면이 둘 이상일 때; **표면 분기 결정은 편집·오버라이드·요약 등 *모든* 표면에 퍼뜨려야**(108이 편집폼만 갈라 109 오버라이드가 조율형 capabilities 누락→"동작 정상·화면만 틈"=happy/백엔드 테스트 안 잡힘, 브라우저가 잡음), split도 move처럼 모든 미러에 전파([[move-breaks-references-both-directions]] 사촌); 판정헬퍼 단일소스 승격이 드리프트 예방; 보안=오버라이드 자원마다 "최종 권한 어디서 닫히나"로 게이트 필요 갈림(capabilities=브로커 호출자스코프라 불필요 vs mcps=자격증명 배선이라 필요) [fork-surface-by-kind,propagate-to-all-consumers,split-like-move,single-source-helper,ui-only-gap-needs-browser,where-does-authority-close,nul-byte-breaks-diff]
- 123 `<label>`은 컨트롤 하나만 감싼다(그룹 감싸면 클릭이 첫 컨트롤로 전달) — 공용 필드 래퍼가 자식을 `<label>`로 감싸는데 그 자식에 컨트롤이 2개+(체크박스 목록·Switch+Slider·Collapse 입력들)일 때; **HTML label은 컨트롤 하나에만** — 여러 개 감싸면 label 아무 곳 클릭이 첫 labelable 하위 컨트롤로 forwarding돼 엉뚱한 항목 토글(id/htmlFor 무관, 래핑 자체가 원인), 래퍼는 단일=label·그룹=div role=group 구분; **디버깅**=실패한 수정이 근인 가리킴(id부여 반증→forceRender로 DOM순서 흔드니 토글대상이 "첫 요소" 따라 이동→label→첫하위 확정), UI 크로스토크는 document capture 트레이스+DOM순서 흔들기 [label-one-control,click-forwards-first-descendant,field-group-vs-label,wrapping-not-htmlFor,failed-fix-redirects,dom-reorder-probe,capture-trace,ui-verify-adjacent-interaction]
- 124 게이트 뒤 하드 필터가 기능을 조용히 무력화 + GREEN 테스트가 버그를 굳힌다 — 권한 게이트(allowlist∩RBAC) 뒤에 후처리 필터(어휘·관련성)를 두고 그 결과를 오케스트레이션이 소비할 때; ①**게이트 뒤 하드필터는 관련성 도우미인 척 기능을 통째 죽임**(discover가 `쿼리⊆능력`으로 자연어서 허가 능력 전부 떨궈 조율형이 아무것도 못 씀)→작은 카탈로그 discover는 **필터 아니라 랭킹**(허가된 걸 버리지 말고 순서만); ②**GREEN 테스트가 버그 동작을 단언**할 수 있음(`discover(비매칭)==[]`가 버그 필터 부수효과)→수정이 그 테스트 깨면 불변식vs부수효과 판정, 후자면 코드 안 되돌리고 테스트를 진짜 불변식(미허가 부재)으로; ③디버깅=빈쿼리vs실쿼리로 후보-비었나vs필터-떨구나 격리, 네트워크 없는 provider로 연결실패 격리 [hard-filter-after-gate,rank-not-filter,inverted-subset-match,green-test-encodes-bug,invariant-vs-sideeffect,layer-isolation,no-net-provider-isolate]
- 125 관측성으로 재현불가 버그를 옮기고 "실패≠0건"을 UI로 구분 — 검색/시험 도구가 "결과 안 나옴"인데 사유가 뭉개지거나(None/빈결과) 재현불가 환경(다른 배포·기기)에서만 나거나 예외가 사라지는 토스트일 때; ①**로컬서 못 고치는 환경버그는 "왜 실패인지"를 그 화면에 띄워 사용자에 옮김**(다른 노트북 임베딩 설정 못 고쳐도 진단이 "미설정/초기화실패/검색실패"를 명시), 설정고치기=비목표·관측성만 출하, 로컬 스샷으로 "정상·0건"=진짜없음 증명; ②진단이 예외를 **삼켜 구조화**(200+error)하면 `enabled=True·results=[]·error`가 기존 "0건" 표시경로 타 **실패가 정상0건 위장**→상태분기 추가 시 기존 표시경로(토스트·emptyMessage) 전부 억제([[installed-guard-isnt-covering-guard]]); ③비밀정제=**실제 비밀값 정확치환 우선**·정규식은 백스톱(추측 단독은 샌다, hex·공백라벨 놓침); ④진단이 프로덕션 가용성 관문(resolve_backend) 우회하면 계약 갈림→단일권위 항상 통과 [observability-moves-unreproducible,show-why-on-screen,failure-not-zero,swallowed-exception-not-empty,exact-secret-redaction-first,regex-backstop,single-authority-gate]
