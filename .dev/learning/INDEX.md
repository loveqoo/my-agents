# Learning Index  (.dev/learning/)

> 각 줄: `NNN 제목 — 언제 열까(후크) [키워드]`. Context 단계에서 이 인덱스를 먼저 읽고,
> 후크로 불충분할 때만 해당 full 파일을 연다.

- 001 경로 기반 메모리의 한계와 폴더 리네임 — 폴더 이동·리네임으로 경로 키 메모리가 고아 되니 영속 맥락은 repo에 둘 때 [memory,rename,path]
- 002 User 스코프는 허락받고 접근, 프로젝트 내용으로 오염 금지 — User 스코프(경로 메모리)에 쓰려 할 때·cross-project 아닌 내용을 섞지 말 것 [memory,scope,user]
- 003 재촉하지 말 것 (사람 속도에 맞춰라) — 매 턴 끝을 승인·커밋·다음단계로 몰아가려는 충동이 들 때 [process,review,pacing]
- 004 AskUserQuestion에 CJK는 \u 이스케이프 말고 직접 작성 — 도구 입력 문자열에 한글 넣을 때 JSON 파싱 실패가 나면 [askuser,cjk,json]
- 005 docs 스펙은 AI 초안 + 인간 검토 — docs/spec을 누가 쓰나 헷갈릴 때(AI 초안→인간 검토) [docs,spec,review]
- 006 Qwen(MLX) thinking 비활성으로 콘텐츠 누락 방지 — 로컬 MLX/Qwen 스트림이 빈 응답·추론토큰 폭주일 때 [mlx,qwen,thinking]
- 007 백그라운드 서버 재기동 시 포트 점유를 확실히 정리 — 서버 재기동이 address-in-use거나 옛 코드가 응답할 때 [server,port,restart]
- 008 handoff 디자인을 진짜 antd로 이식하는 플레이북 + antd-x v2 함정 — design handoff 번들을 antd로 이식하거나 antd-x 함정에 막힐 때 [antd,handoff,design]
- 009 Mem0를 로컬 MLX로 구성하기 (LLM·임베딩·벡터스토어) — mem0를 외부 OpenAI 없이 전부 로컬 MLX로 구성할 때 [mem0,mlx,embedding]
- 010 create_react_agent + 회상 주입은 단일 system 프롬프트로 — 회상·동적지시를 별도 system 메시지로 넣어 다중 system 에러 날 때 [langgraph,system,mem0]
- 011 Alembic를 async FastAPI 시작 시 안전하게 돌리기 — async lifespan에서 alembic upgrade가 running-loop로 깨질 때 [alembic,async,fastapi]
- 012 런타임 설정은 단일 소스(레지스트리)에서, env는 부트스트랩만 — LLM/임베딩 설정을 어디서 읽을지·env 폴백을 둘지 정할 때 [config,registry,env]
- 013 매 응답 `[단계 N — 이름]` 표기를 빠뜨리지 말 것 — 자율 실행에 몰입해 단계 헤더를 빠뜨리고 있을 때 [process,header,loop]
- 014 비밀값 at-rest 암호화(Fernet)와 함정 — api_key·토큰을 DB에 암호화 저장하거나 복호화 폴백을 짤 때 [secret,fernet,crypto]
- 015 API Bearer 토큰 인증과 함정 — API에 Bearer 인증 붙이거나 토큰 fail-closed·gitignore 처리할 때 [auth,bearer,token]
- 016 테스트 가이드는 실제 UI를 확인하고 쓴다 — 사용자에게 화면 조작 가이드를 줄 때 코드 안 보고 추정 말 것 [ui,guide,verify]
- 017 자유 문자열을 하드코딩 맵으로 조회하면 렌더가 통째로 죽는다 — 백엔드 자유문자열을 프런트 상태맵으로 조회해 페이지가 백지될 때 [react,status,crash]
- 018 antd 6 어드민 모바일 반응형 플레이북 — antd handoff 이식물이 모바일에서 깨질 때 반응형·Sider 오버레이 패턴 [antd,mobile,responsive]
- 019 mem0 메모리 스코핑: user_id 축, thread_id 직교, entity-id 제약 — mem0 user_id와 LangGraph thread_id를 한 변수로 합치려 할 때 [mem0,scope,thread]
- 020 mem0 공유 백엔드(pgvector) + DSN은 분해 말고 위임 — mem0 벡터스토어를 N-인스턴스 공유·pgvector로 옮기거나 DSN 다룰 때 [mem0,pgvector,dsn]
- 021 mem0의 가치는 memory_type이 아니라 스코프·추출·엔티티그래프 — 부분구현만 보고 "mem0 이점 없음"이라 단정하려 할 때 [mem0,extraction,probe]
- 022 뷰포트 브레이크포인트는 옆 패널 폭을 모른다 — 고정폭 형제 패널이 있는데 반응형 분기를 뷰포트 폭으로 할 때 [responsive,breakpoint,layout]
- 023 프로세스 규칙은 가시적 강제 산출물이 있어야 지켜진다 — 새 절차 규칙을 만드는데 가시적 강제물 없이 텍스트만 남길 때 [process,enforcement,visibility]
- 024 적대적 픽스처로 "특정 콘텐츠 깨짐" 재현 + Playwright antd 오버레이 사이더 네비 함정 — 시드로 재현 안 되는 콘텐츠 깨짐·antd Tag 줄바꿈을 잡을 때 [adversarial,fixture,antd]
- 025 시드/목업이 레지스트리와 어긋나면: 데이터 마이그레이션 + 단일 상수 — 시드된 라이브 DB가 레지스트리와 drift 났을 때(소스만 고치면 안 됨) [seed,migration,drift]
- 026 Mock/스텁은 런타임 분기가 아니라 레지스트리 데이터 + 계약 구현으로 — mock/테스트더블을 if-mock 런타임 분기로 넣으려 할 때 [mock,registry,contract]
- 027 프런트 필터는 백엔드 계약의 가드가 아니다 — 프런트에서 입력 걸렀다고 서버 불변식이 안전하다 믿을 때 [frontend,backend,guard]
- 028 서버측 외부 URL fetch는 "보조도구"여도 보안 표면이다 — 사용자가 준 URL을 서버가 fetch하는 기능 만들 때(크기·타임아웃·SSRF) [ssrf,fetch,security]
- 029 same-origin 프록시는 cross-origin 문제 "클래스"를 통째로 없앤다 — CORS·mixed-content·cert를 env로 땜질하며 원격 fetch 회귀가 날 때 [cors,proxy,origin]
- 030 UI 변경은 진짜 브라우저로 직접 확인한다 (사람 스샷 의존 X) — UI 검증을 사용자 스샷에 의존해 루프가 느려질 때 [ui,playwright,verify]
- 031 의도적 쓰기 채널은 스코프-태깅이 닫은 누출을 되연다 — 에이전트 자가기록 도구를 열어 PII 누출이 재유입될 때(도구 프롬프트≠격리) [mem0,scope,leak]
- 032 antd의 포털 렌더·전체 마운트가 단순 브라우저 셀렉터를 깨뜨린다 — antd Select 옵션·비활성 Tabs 때문에 Playwright 셀렉터가 헛돌 때 [antd,playwright,selector]
- 033 alembic autogenerate는 metadata에 없는 "남의 테이블"을 drop하려 든다 — autogenerate가 mem0 등 외부 런타임 테이블을 drop_table로 끼울 때 [alembic,autogenerate,drop]
- 034 공유·실 DB 검증은 불변식 + 델타로 (절대수치 금지) — 기존 데이터 많은 실 DB를 절대수치로 단언해 검증이 깨질 때 [verification,invariant,delta]
- 035 가드는 진실원을 검사하라, 사본 말고 (+ 초록 verify ≠ 견고) — 차원·불변식 가드를 사본/메타데이터로 검사하거나 초록 verify를 믿을 때 [guard,source,adversarial]
- 036 이상 신호가 인프라를 가리켜도, 의존성보다 내 측정을 먼저 의심하라 — 불일치가 화려한 인프라 버그를 가리킬 때 의존성 만지기 전 계측 확인 [measurement,probe,regex]
- 037 파괴적 노브엔 바닥을 깔아라 (경계값이 곧 delete-all) — retention=0 같은 "없음처럼 보이는 경계값"이 최대 파괴로 매핑될 때 [adversarial,destructive,boundary]
- 038 적대 리뷰는 *네가 상상한 실패*가 아니라 *상상 못 한 범주*를 찾는다 — 파괴적 작업에 자가 불변식+자가 테스트만 깔고 출하하려 할 때 [adversarial,invariant,review]
- 039 "drop-in 가능"은 두 번째 구현을 출하해야 *측정*된다 — 백엔드 추상화가 한 구현에 안 묶였다고 주장만 할 때(Protocol 누수) [abstraction,protocol,dropin]
- 040 실인프라 통합 테스트는 *단위 게이트와 적대 리뷰가 둘 다 못 보는* 결함을 잡는다 — 게이트를 단위+적대로만 검증하고 실 DB·요청간 글루를 안 태울 때 [integration,gate,drift]
- 041 "리밋"은 *원천 바이트*를 막아야 한다 — 선버퍼링·프레임 카운트는 캡이 아니다 — .content/.aiter_lines 위에서 사이즈 캡 걸어 OOM이 안 막힐 때 [limit,bytes,ssrf]
- 042 이동·리네임은 *양방향*으로 참조를 깬다 — 한 방향만 고치면 green이지만 깨져 있다 — 파일 옮기고 들어오는 참조만 갱신할 때(나가는 상대링크·단방향 검증 함정) [move,refs,relative-path,verify]
- 043 "표시가 거짓"인 결함은 코드층·데이터층으로 갈라 고친다 — 화면 수치/상태가 실제와 어긋날 때(코드가 잘못 읽음 vs 데이터가 가짜, 출하 리듬이 다름) [honesty,code-vs-data,batch-split]
- 044 "안전장치를 깔았다"는 "안전하다"의 증거가 아니다 — 적용 범위 경계를 적대자가 짚는다 — 서버측 fetch/probe에 가드 깔고 follow_redirects·재해석을 안 끌 때(가드는 최초URL만, 부수효과는 체인끝) [ssrf,guard,boundary,toctou,redirect]
- 045 통합 픽스처가 데모 시드에 결합하면, 카탈로그를 비울 때 통합 rung이 함께 증발한다 — 시드 데모 데이터를 지울 때 그걸 픽스처로 빌려쓰던 통합 테스트·프로브·브라우저샷도 같이 죽을 때(040의 반대편, self-fixture로 격리) [integration,fixture,seed,self-fixture,rung]
- 046 per-operation 타임아웃은 전체 작업의 deadline이 아니다 — httpx per-read timeout만 걸고 "타임아웃 방어"라 주장할 때(slow-trickle이 코루틴 무한홀드→asyncio.timeout 전체로 감싸야); 041(WHERE)·044(WHICH)의 WHICH-span 축 [timeout,stream,deadline,ssrf,slowloris]
- 047 성공-카운터에 건 멱등성은 부분 실패를 "완료"로 오인한다 — doc_count>0 같은 성공분만 세는 캐시로 멱등 스킵 걸 때(부분 적재 후 재실행이 누락분 안 채움); 완료 단위 집합으로 판단할 것 [idempotency,counter,self-heal,partial,adversarial]
- 048 한 수명 경계(생성)에 건 보장은 수명 전체를 못 덮는다 — 불변식을 생성 시점에만 보장하고 "확보"라 선언할 때(배치/GC/cascade가 후반에 그 행을 지움); 보장은 생성→소멸 전 구간·모든 쓰기주체에 걸쳐 표현하고 검증을 수명 끝까지 [lifecycle,guard,subsystem,hil,cleanup,adversarial]
- 049 상태를 지우는 청소는 재생성 소스가 살아있으면 무의미하다 — 행이 아니라 발생원을 고쳐라 — 파괴적 정리 후 정크가 재발할 때(부팅시드·크론·다른 프로세스 env·리로더가 DB 밖에서 재생성); 삭제=증상·발생원=원인 [regeneration,source-vs-state,seed,env,cleanup,whence]
- 050 파괴 헬퍼의 한 연산(delete)에 건 가드는 권한 등가인 형제 연산(create)을 안 덮는다 — 보호대상에 대한 모든 동사를 위협모델에 넣을 때(delete막고 create로 super승격 열어두면 같은등급 피해); 가드는 한 술어로 모든 입구에 [guard,operation-symmetry,escalation,privilege,idempotency,adversarial]
- 051 실 클라이언트 제약을 무시하는 검증 도구는 거짓 green을 준다 — curl은 브라우저가 아니다 — 인증/쿠키/origin 경로를 curl로 green 받고 안심할 때(curl은 Secure/SameSite/CORS 강제 안 함); 인증경로는 Playwright로 한번 태울 것 [verification,tooling-fidelity,cookie,secure,browser,false-green]
- 052 누출은 비신뢰 쓰기 채널을 잘라 막는다 — 데이터도 기능전체도 아니다 — 한 저장소에 신뢰(인간게이트)·비신뢰(LLM자가기록) 채널이 같이 쓸 때; 수리단위는 채널이고 절단면은 "어느 쓰기주체가 신뢰불가"로 그어짐 [leak,write-channel,human-gate,trust-boundary,remove-tool,regression-guard]
- 053 신원 보강은 스코프 엔드포인트로, 고권한 엔드포인트 재사용 금지 — 저권한 화면을 다른 곳 신원(email·소유자)으로 보강할 때 고권한 엔드포인트(users:manage) 재사용하면 권한등급이 조용히 끌어올려짐; 최소권한 read 엔드포인트 신설 [authz,coupling,scoped-endpoint,least-privilege,response-model,leak,adversarial]
- 054 인가는 모든 직교 축에서, 강제는 서버에서 — 공유저장소 행에 주체가 대상의 행으로 접근하는데 가드를 한 축만 걸 때(/{user_id}/{mem_id}면 축도 둘, 주체가드만이면 path위조로 남의 행 삭제); per-route 독립 재판정+fail-closed [authz,orthogonal-axis,path-spoof,defense-in-depth,fail-closed,server-enforce]
- 055 새 아웃바운드 경로는 네트워크 가드를 자동 상속하지 않는다(의존성은 위험 기본값을 들고 옴) — 새 외부호출 경로(새 클라/통합)를 추가할 때("SSRF가드 있다"≠"이 경로에 적용", follow_redirects=True 기본이 사설/메타데이터 재유도); 경로마다 가드 재확인·새 의존성 fail-closed 잠금 [ssrf,redirect,outbound-path,dependency-default,follow-redirects,fail-closed,guard-coverage]
- 056 낙관적 비동기 상태: "도착"은 "유효"가 아니다 — set엔 롤백, 도착물엔 단조 토큰 — 비동기로 데이터 불러와 화면상태 바꾸는데 대기중 다른선택/요청실패 가능할 때(stale도착물이 최신선택 덮음·실패후 미롤백); 단조토큰 seq+catch롤백, 타자 적대 필수 [async,optimistic-update,staleness-token,rollback,race,adversarial,false-green]
- 057 화면 전체 렌더 집합을 검증하라, 대상 행만 보지 말고 — 목록/드롭다운/테이블 검증 시 E2E가 방금 만든 한 행(marker)만 단언할 때(다른 행들이 raw ISO·시드세션 오표기로 깨짐); 렌더집합 전체+부정단언·시드/레거시로도 캡처 [verification,e2e,assertion-scope,data-diversity,negative-assertion,format,false-green]
- 058 의미가 다른 두 빈 상태는 구분하라·비정규화 카운터는 행 수의 진실이 아니다 — 선택/로드로 채우는 화면이 items.length===0만으로 "비었음" 판정할 때(시드/레거시서 메타카운터가 영속행과 어긋남, 골랐는데빈 화면=안고른 화면); 빈상태를 선택맥락유무로 분기 [empty-state,denormalized-counter,ux,data-diversity,playground,probe-deeper]
- 059 "실제 행을 세라"는 카운터가 행의 *캐시*일 때만 옳다·행으로 안남는 사건까지 세면 카운터가 *더 완전한 진실* — 058 처방(행 기준)을 파괴적 정리에 이식할 때(persistHistory=false 윈도우모드는 메시지 미저장이나 turns는 +1→행세기면 오인삭제); 판별="카운터가 세는 사건이 전부 행을 남기나?", 잘못된 값은 신호말고 기록자(seed)를 고침 [counter,rows,denormalized,seed,windowed-mode,destructive,adversarial,whence]
- 060 통합은 폐기 경로의 행동을 자동 승계하지 않는다·살아남는 경로에 폐기 경로의 *행동 집합*을 열거해 이식하라 — 두 코드경로를 하나로 합치며 한쪽 삭제할 때("두 경로를 하나로"는 교집합으로 수렴, 차집합은 누락, 빠진 코드는 diff에 안보임); 삭제전 폐기경로 행동 열거·기능패리티 별도단언, 타자 적대 필수 [consolidation,retired-path,behavior-parity,deletion,protocol-migration,contextId,regression-guard,adversarial]
- 061 graceful degradation은 그것을 구현하는 연산의 원자성을 넘지 못한다 — "X 없으면 그 부분만 비활성"식 부분 열화를 원자적 연산(DDL create_all·단일 트랜잭션·일괄배포) 위에 얹을 때(의존연산이 원자적이면 catch가 선행서 멈춰도 다음단계가 재throw해 전체붕괴); 정직한 fail-closed+또렷한 조치메시지가 옳음 [first-run,db-preflight,graceful-degradation,create-all-atomicity,fail-closed,head-stamp-trap,adversarial]
- 062 기본값은 상수가 아니라 *그것이 세워지는 모든 경로의 수렴 상태*다 — "기본을 X로" 요청을 시드·마이그레이션·폴백·env 등 둘 이상 경로로 기본이 세워지는 시스템에서 받을 때(한 경로만 손대면 다른 정상경로는 옛 기본으로 남음, 게이트가 경로마다 다르게 작동); 경로 먼저 열거·관측값 양쪽 명시단언·no-clobber [default-establishment,boot-path-convergence,seed-drift,gate-dead-on-path,no-clobber,migration,topology-change]
- 063 fail-closed 가드 메시지는 *위반한 값 + 그 값을 넣은 조치 예시*를 담아라(노브 이름만으론 부족) — SSRF·인증·검증·프리플라이트가 막고 4xx/부팅중단 낼 때 에러문구 쓸 때(노브만 주면 무엇을 넣을지 다시 추론, 가드는 이미 답을 쥠); 위반 구체값 echo+`KEY=<그값>` 복붙예시+에러와 예제 같은 키 [fail-closed,guard,error-message,actionable,observability,echo-offending-value,env-discoverability]
- 064 서버가 *자기 주소를 스스로 광고*할 때 그 주소를 요청 Host에서 끌어오면 공격자 입력이다 — 서버가 자신 가리키는 절대URL을 생성·내보낼 때(A2A카드 url·OAuth redirect_uri·웹훅·SSO·재설정링크), 특히 self-base를 request.base_url/Host로 채울 때(Host:attacker로 광고를 돌려 인증호출 탈취); 명시설정 1순위·Host파생 폴백은 로컬/사설만 fail-closed [self-advertised-address,host-header-poisoning,trust-boundary,ssrf,redirect-uri,fail-closed,whitelist,adversarial]
- 065 큐레이션된 안전 메시지는 *전송/경계 계층이 버리면* 무용지물 — 사유는 중앙 경계에서 surface하라 — 한쪽이 사람읽을 안전사유 만드는데 사용자엔 불투명코드만 닿을 때(공용 fetch래퍼·RPC클라·에러미들웨어가 body→status로 뭉갬, 손실이 invisible); 중앙 경계 한곳서 한 필드만 추출, 상한은 raw바이트에서 [error-surfacing,central-boundary,curated-message,invisible-loss,detail-only,cap-raw-source,producer-invariant,adversarial]
- 066 가드 앞에 관대 정규화를 새로 끼우면, 스킴으로 거부되던 입력이 가드를 통과할 수 있다 — 보안 가드(SSRF/검증) 바로 앞에 절대화/정규화 단계를 새로 넣을 때(정규화가 guard가 보는 host를 정함→입력을 다른 host로 둔갑, mailto:→http://mailto 전치·userinfo로 실host 숨김); 둔갑벡터(userinfo/포트)로 단언, 결과만 단언 금지 [normalize-before-guard,ssrf,userinfo-disguise,host-confusion,fail-closed,defense-in-depth,adversarial,regression-pin]
- 067 가드보다 먼저 단락하는 정확매칭 allowlist는 *비-타깃 IP 리터럴*을 스스로 거부해야 한다(canonical 수용≠안전타깃) — allowlist가 가드보다 먼저 return하는 구조서 항목을 "유효IP면 수용"할 때(0.0.0.0/::·멀티캐스트는 canonical로 통과하나 전부/없음 뜻하는 비-타깃→allow-local둔갑), 또는 보안control 저장을 env→DB로 옮길 때; is_unspecified 거부·저장제약과 검증일치·방어적 파싱 [allowlist,exact-match,short-circuit,ip-literal,is-unspecified,canonical-not-safe,storage-migration,static-to-runtime,column-constraint,defensive-parse,ssrf,adversarial]
- 068 컬렉션을 스코핑해도 항목 엔드포인트는 안 스코핑된다: 404-vs-403 열거 오라클 — 멀티테넌트/RBAC CRUD서 list는 WHERE owner=me 스코핑하면서 항목 엔드포인트(/{id})는 인가만 걸 때, 특히 404(부재)vs403(존재하나금지)을 다르게 응답할 때(403이 존재를 누설→id추측으로 숨긴 행 캐냄); 볼수없는행은 404로 접어 경로 일관 [enumeration-oracle,list-vs-item-scoping,404-vs-403,existence-disclosure,rbac,multi-tenant,authorization,adversarial]
- 069 읽기 경로를 스코핑해도 같은 데이터의 쓰기/resume 입구가 소유권 안 걸면 스코핑 무력화 — 자원 읽기엔드포인트에 유저별 스코핑 새로 걸 때, 같은 행을 생성·변경·resume하는 입구가 소유자 덮어쓰거나 무검사 바인딩할 때(읽기게이트 완벽해도 쓰기입구가 탈취·존재오라클); 같은 데이터의 모든 입구에 소유권 처리·소유권이전 기본금지 [scope-read-not-write,ownership-takeover,enumeration-oracle,covering-guard,write-entry-bypass,multi-tenant,rbac,session,adversarial]
- 070 읽기/resume 게이트를 조이면 *소유자 미스탬프* 행이 고아가 된다(069의 거울상=자가-잠금) — 같은 데이터에 owner 스코프 게이트를 새로 걸 때, 그 데이터를 만드는 모든 입구가 소유자를 박는지 점검 안 하면(주영속경로만 owner박고 lazy-create 빠뜨리면 NULL-owned로 남아 본인조차 못이음); 생성입구 전부 열거·같은 헬퍼 사용·자가잠금 별도핀 [tighten-gate,unstamped-row,self-lockout,creation-entry,owner-stamp,covering-guard,single-source,session,rbac,adversarial]
- 071 인덱스도 비대해진다: 한 줄 후크 규약을 어기면 인덱스가 다시 통독 대상으로 회귀 — 통독 절감용 인덱스 줄에 서사를 욱여넣으면 그 자체가 통독 대상이 됨; 추가 시 줄 짧게·주기적 재압축(Scaffolding 점검)·대량변환은 서브에이전트·보존은 git대조 측정 [index-bloat,recompress,one-line-hook,context-optimization,scaffolding,subagent-delegation,measure-not-self-verify,compounding]
- 072 방어 규칙/체크리스트 자체가 적대검증 대상: "메타 가드의 열거는 닫힌 집합이 아니다" — "모든 X를 다뤄라"식 가드/정책/체크리스트를 만들 때(만든 직후 완료감이 듦); 그 자산의 열거가 빠뜨린 여집합을 적대검증·실코드 grep으로 대조, 닫힌집합 취급 금지, 트리거도 자가판정이면 맹점이 거기로 이동 [meta-guard,closed-set-fallacy,adversarial,complement,enumeration,defense-asset-verify,happy-path-blindspot,trigger-self-judgment,installed-guard]
- 073 타이밍 오라클은 계층마다 따로 봉합된다: 앱계층 단일화 ≠ 저장계층 동일성 — fetch-then-check를 SELECT-WHERE 융합으로 앱경로 단일화해도 비밀키 컬럼 단독 인덱스가 남으면 플래너·B-tree 프로브가 타인-존재 vs 부재를 다르게 처리(조임=제거 아닌 축소)·복합(owner,key) 인덱스+EXPLAIN로 봉합·"제거"는 계층 숨김(probe-deeper)·적대rung이 단위·라이브 사각을 잡음을 볼 때 [timing-oracle,layer-specific,app-vs-storage,select-where,composite-index,query-planner,btree-probe,explain,measure-not-claim,verification-ladder,probe-deeper,adversarial]
- 074 RFC-정확 ≠ 배포-정확: 프록시 path-prefix 뒤 루트상대 URL은 origin으로 풀려 prefix를 잃는다 — 제3자가 광고한 루트상대 `/a2a`를 urljoin(base,"/a2a")로 풀면 RFC상 옳게 origin루트行(표준)이나 프록시 prefix 발행자 의도와 어긋나 404·구제는 제공위치(prefix) 상대 resolve·URL은 concat 아닌 urljoin으로(concat은 literal `..` 남겨 prefix탈출)·liveness는 route존재 vs host도달 구분·적대rung이 clean테스트 입력공간 사각을 짚음을 볼 때 [url-resolution,rfc-vs-deployment,proxy-path-prefix,root-relative,urljoin,dot-segment,liveness-probe,route-vs-host,probe-deeper,verification-ladder,adversarial,third-party-url]
- 075 내부 도구를 직접 엔드포인트로 승격하면 입력 신뢰경계가 리셋된다 — 내부 함수/도구를 외부 POST 엔드포인트(공개 API)로 올릴 때; 상류(LLM·연결코드)가 암묵 보장하던 입력 제약(길이상한·참조대상 kind/상태·비공백)이 raw로 도착하니 새 입구가 *직접* 재검증해야·안 하면 설정오류가 하류 502로 뭉개지거나 자원고갈 입구가 됨·같은 코어라도 입구 바뀌면 신뢰경계 바뀜 [trust-boundary,promote-tool-to-endpoint,input-validation,raw-input,length-cap,kind-guard,field-validator-layer,resource-exhaustion,adversarial,cap-the-raw-source]
- 076 완화를 검증할 땐 실제 비용지표 + 반증(counterfactual)으로: "선택≠강제" 구분 — 완화책을 측정으로 검증한다며 happy-path 초록 받았을 때; (a)프록시 아닌 실제 비용지표 단언(타이밍이면 EXPLAIN buffer 총블록, rows_removed·인덱스명은 정황), (b)완화책 끈 반증으로 델타 재출현→인과+민감도 입증(트랜잭션 DROP/rollback), (c)비용기반 선택자(플래너) 의존 완화는 감축이지 강제 아님→잔존 정직기록(완료선언 금지)·계산만 하고 안 쓴 지표는 없는 것 [mitigation-verify,cost-metric-not-proxy,counterfactual,causation-sensitivity,explain-buffers,planner-selection-not-forcing,honest-residual,adversarial,verification-ladder,probe-deeper]
- 077 "잘려 안 보임"은 추측 말고 scrollWidth로 *범인 컬럼*을 측정해 특정 — UI가 잘린다/안 보인다 보고를 받을 때; max-content 테이블에서 잘린 칸(우측 액션)을 줄여도 안 풀리고 *비대한 칸*(이름 392px)을 줄여야 풀림·어느 칸인지는 측정만 앎(scrollWidth−clientWidth=오버플로우, 칸별 boundingRect, 버튼 inView)·줄일양 계산→적용→재측정으로 0 확인(2회 추측<1회 측정)·표면(발견성=검색 primary)과 구조(오버플로우)를 같이 고침 [table-overflow,max-content,measure-not-guess,culprit-column,scrollwidth,boundingrect,layout-debug,verify-ui-browser,probe-deeper]
- 078 "체크박스+설명"은 설명을 Checkbox 형제로 두지 말고 라벨 안에 세로 스택 — 체크박스 옆 설명문 UI를 만들 때; 설명을 Checkbox의 형제 flex 아이템으로 두면 둘이 폭 다퉈 라벨 shrink→한글 이름 글자단위 세로래핑, 제목·설명을 Checkbox children에 flex-column 스택+래퍼 alignItems flex-start로 해소(덤으로 외부 label 제거→중첩 label invalid HTML도 제거)·antd 6 모달본문 클래스는 .ant-modal-container(구 .ant-modal-content 아님)→셀렉터 안 맞으면 빈결과=거짓초록·이름 노드만 재야 한 줄(≈20px) 검증 정확 [admin-ui,antd6,checkbox,flex-shrink,nested-label,modal-container-class,verify-ui-browser,measure-not-guess]
- 079 deprecated API 마이그레이션은 추측 말고 실측·문서로 대체 API 잡고 가장 강한 기존 회귀로 계약 보존 증명 — deprecated 심볼을 후속으로 갈 때; (1)설치본 @deprecated 본문 실측+공식문서로 시그니처·breaking 확정(추측 금지, prompt→system_prompt·콜러블만 제거), (2)메타패키지 설치 필요성 점검(langchain 별도), (3)호출계약 열거→실 인프라 통합회귀(verify_041 G1-G7)가 1순위 증거·신규 스모크는 안 덮는 계약만 보완(invoke/astream messages), (4)테스트헬퍼·주석까지 grep으로 전 사용처 쓸기, (5)no-deprecation을 warnings.catch로 단언 고정 [deprecation,migration,call-contract,verification-ladder,strongest-regression,probe-deeper,no-deprecation-assert]
- 080 antd 6는 내부 DOM 클래스를 광범위 개명: Playwright 셀렉터는 antd5 이름 가정 말고 probe로 실측 — antd6 화면을 Playwright로 잡다 `.ant-*` 셀렉터가 타임아웃날 때; 개명 확인(.ant-modal-content→.ant-modal-container[075], .ant-select-selector→.ant-select-content[077])·안 바뀐 것도 있음(.ant-drawer/.ant-select-item-option/.ant-checkbox-wrapper/.ant-slider/.ant-switch)→개별 실측·getByText로 렌더는 됐는지 먼저 확인(렌더O+클래스 타임아웃=개명 의심)·[class*=]evaluate로 실클래스 probe·라벨/역할 스코프+루트클래스(.ant-select)+폴백체인 우선·0개 잡힘=통과 아닌 측정실패 [antd6,dom-class-rename,playwright,selector,modal-container,select-content,probe-dont-assume,false-green,verify-ui-browser,probe-deeper]
- 081 스테이징(draft) 상태는 편집 지점만 아니라 *모든 소비 지점*에 표시하라 — draft/publish·staging·flag로 쓰기가 스테이징 레이어로 가는 구조에서 "저장했는데 안 바뀜" 혼란을 막을 때; 편집화면 안내가 아무리 충실해도 사용자는 편집 직후 *다른 소비 표면*(미리보기·실행기·목록)으로 이동해 "미반영"을 거기서 마주침→증상 화면에 신호 없으면 안내 미도달, 신호는 진실원으로 구동(versions.some(draft), mock 아님)·이미 직렬화돼 오면 백엔드 0줄로 표시층만 추가 가능·양성(스테이징有→배지)+음성(無→배지없음) 둘 다 검증해 거짓배지 배제 [staging-state,draft-publish,consumer-surface,signal-reaches,truth-source,no-backend-change,positive-negative-verify,probe-deeper]
- 082 관측은 "조회 행위"를 계측하라, 결과만 싣지 말고 — 검색·회상·필터가 조건부로 0건/빈 채 돌 수 있는데 결과 컨테이너만 직렬화돼 "이력 안 보임"이 날 때; 조회의 쿼리·대상·시도를 결과와 *독립*으로 싣어 "0건/미호출"(도구 돌았는데 빔)과 "조회 안 함"을 구분(둘은 운영상 완전 다름)·회상=used_memory면 쿼리 에코(0건도 "조회→0건")·RAG=호출과 별개로 연결/미해석 컬렉션 노출(안 불러도 가용했는지·조용히 비는 footgun)·쿼리 에코는 표시전용+길이상한이라 새 입구 0·호출결과와 분리렌더해 중복 방지·0개=측정실패·양성+음성 [observability,instrument-the-act,query-not-result,0-result-still-happened,trace,display-only,probe-deeper,positive-negative-verify]
- 083 소비 표면 신호는 *신선하게 유지*하라: 마운트 1회 페치는 탭 밖 변경에 stale — 진실원을 스냅샷해 표시하는 표면(배지·카운트·목록)이 그 표면 밖(다른 탭·액터·크론)에서 바뀔 수 있을 때; 081이 신호의 공간축(어느 화면)이면 083은 시간축(언제 다시 읽나)·in-app 이벤트/공유 store는 탭 경계 못 넘음→교차 탭 staleness는 교차 컨텍스트 전파(BroadcastChannel/storage/서버푸시)로만 닿음(BroadcastChannel은 자가 채널 미수신→두 페이지 테스트 필수, 단일 페이지=거짓초록)·즉시전파+포커스/가시성 백스톱 2층·재페치는 표시 메타만(선택·대화 보존)·stale 재현 후 신호 끼워 해소 측정 [consumer-surface,stale-snapshot,cross-tab,broadcastchannel,event-propagation,focus-backstop,two-layer,preserve-user-state,false-green,probe-deeper,verify-ui-browser]
- 084 파생값을 저장하면 그 *출처*도 저장하고 refresh는 타임스탬프가 아니라 *재파생*하게 — 컬럼에 다른 데이터에서 계산한 값(resolve된 URL·요약·정규화·캐시)을 영속할 때; 저장된 파생값은 시점 스냅샷이라 규칙 변경(우리 배포)·원천 변경(원격)에 stale해짐(083=소비 표면 시간축, 084=영속 저장 시간축)→(1)출처를 재계산 가능하게 같이 저장(cardUrl) 안 하면 삭제·재등록 수작업만이 답, (2)refresh/resync가 last_sync만 찍으면 *리프레시처럼 보이는 no-op*이라 재fetch→재resolve→파생·상태 갱신까지 해야 진짜 자가치유·"보정 로직 고침"≠"굳은 재고 고침"(유입만 막음→마이그레이션/on-demand 자가치유로 재고 청소)·재파생은 in-place(id·소유·버전 보존) 출처 도달 실패는 status만 offline 정직(삭제 금지)·재파생 입력은 신뢰경계 안 guard통과 저장값만(외부 파생=host-poisoning) [derived-value,stale-snapshot,store-the-source,re-derive-not-timestamp,self-heal,in-place-update,offline-honesty,trust-boundary,migration-vs-ondemand]
- 085 맥락상대 경로 resolution은 꼬리에 멱등 — base가 이미 그 세그먼트로 끝나면 또 붙이지 마라; prefix-mount·urljoin·concat·네임스페이스 접두로 상대 합성할 때(071이 루트상대 /a2a를 prefix 하위로 붙여 prefix가 이미 /a2a면 /a2a/a2a 중복낸 사례); "상대를 base에 붙임"은 base가 그 꼬리를 이미 포함하면 중복생성→멱등이어야(붙일 세그먼트로 base가 끝나면 base 그대로)·같은 입력이 토폴로지마다 다른 정답인 heuristic은 본질 모호(루트상대 /a2a=origin기준 vs prefix기준)→한쪽 고정하면 반대 토폴로지 깨짐, 멱등 collapse나 probe-disambiguate(도달하는 해석 채택, 045/084 진실=도달)로 닫기·collapse는 정확 꼬리매치만(부분겹침 a2a/rpc 건드리면 정상경로 파손)·결과는 base 파생값만(host·canonical path, 상대입력이 host/.. 주입 못하게+normalize 후행)이라 신뢰경계 안 넓힘·검증은 버그+정상 토폴로지 같은 매트릭스로(고친 것만 보면 무회귀 못잡음) [path-resolution,idempotent-on-tail,prefix-relative,ambiguous-heuristic,probe-disambiguate,exact-tail-match,trust-boundary,regression-matrix]
- 086 소비층 fail-closed만으론 "죽은 상태"가 샌다·술어를 *모든 쓰기/렌더 경계*에 정렬 — capability 플래그(노출/활성/공개/허용)가 한 층(소비측)에서만 fail-closed인데 사용자가 보안결함 아닌 "되는 것처럼 보이는데 안 됨"을 보고할 때; 소비측 닫기는 동작/보안만 지키고 데이터정합/UX는 못 지킴→플래그 쓰는·그리는 입구가 같은 술어 공유 안 하면 "겉도는 죽은 상태"(플래그 켜졌으나 소비층이 무시·UI는 켤 수 있다 말함) 누적·happy-path/보안 테스트엔 안 잡혀 사용자가 "이상함"으로 먼저 짚음→처방=(1)불변식 한 줄 flag==True⟹predicate를 모든 입구 계약으로, (2)입구를 닫힌집합으로 셈(write-API/seed/기존데이터/mock/렌더/배지/중앙헬퍼 주경로만 보면 샘), (3)끄기(False)는 술어무관 항상허용(stale 멱등청소), (4)프런트 숨김 의존금지(입구 400+렌더숨김은 추가층), (5)기존데이터도 입구라 마이그레이션으로 stale청소(정상값 보존)·곁가지 JSONB 통째교체는 형제키 파괴라 merge/jsonb_set+보존단언 [capability-flag,fail-closed-consumer,dead-state,align-predicate-every-writer,enumerate-write-entries,idempotent-clear,no-frontend-only-guard,data-is-an-entry,jsonb-sibling-key]
