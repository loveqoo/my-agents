# 146 — 다단 폼에 변주를 넣을 땐 컨테이너 구조+최근접 형제를 먼저 읽어라

> **복원 노트**: 이 파일은 인덱스 줄만 있고 full 파일이 생성되지 않았던 항목을 INDEX 후크에서 복원한 것이다(스펙 400 verify_043 실측 — 같은 턴 파일 생성 누락). 원 세션의 상세 서사는 유실됐고, 아래는 후크가 보존한 전부다.

다단 폼에 변주를 넣을 땐 컨테이너 구조+최근접 형제를 먼저 읽어라 — 필드가 단계에 흩어져 있어 플랫 폼 가정 금지, 관용구는 옆 종류 grep해 계승; 숨긴 표면은 하위 항목 합집합에서 역산해 구조적 보장

키워드: variant-in-multistep-form,container-structure-first,nearest-sibling-idiom,no-flat-form-assumption,hide-surface-derive-from-union,structural-subset-guarantee,single-tool-scoping-limit,honest-boundary
